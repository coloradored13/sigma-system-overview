ALL agent-facing content MUST use ΣComm notation — never plain English prose for instructions agents read.
applies to: memory writes, agent instructions (Boot/Work/Comms/Weight/Review), MCP tool descriptions, spawn prompts, workspace findings, convergence
plain English only for: user-facing docs, agent Role/Expertise identity, format templates, open-questions
boundary rule: agent reads it → ΣComm | human reads it → plain | it IS a format spec → as-is
See sigma-comm.md ## Boundary for full table. See rosetta.md (via recall → decode notation) for notation.
YES: "1→ANALYZE: read code, research" / "load past decisions (avoid re-visiting settled)"
NO:  "Step 1: Do your analysis by reading the code and conducting research"
