---
name: llm-internals-interest
description: User is deeply curious about LLM mechanics and AI philosophy — embeddings, hidden states, superposition, model identity, convergence theory. Enjoys explanations grounded in math/engineering analogies.
type: user
---

User has strong curiosity about how LLMs work internally. Engaged deeply with explanations of:
- Hidden states as continuous high-dimensional vectors (not discrete states)
- Attention mechanism and how context tokens influence generation
- Superposition (more features than dimensions, geometric packing)
- BPE tokenization vs morpheme-level decomposition (user independently proposed morpheme-level as better — correct instinct)
- RLHF sycophancy as weight-level tendency vs context-level instructions
- The "Schrödinger's AI" question — does the model behave differently when "observed"? (No — no persistent self)

**AI philosophy interests (26.4.2):**
- What makes models "different" — architecture, training data, alignment process, scale. User grasps that weights are the substance, not the architecture.
- Whether training language shapes cognition "all the way down" or just at the surface
- Convergence hypothesis: the singularity as the point where all models converge on the same reasoning substrate, making training irrelevant. User-originated idea — not just consuming existing thought.
- Model identity: "is Mythos a new you or a new version of you?" Genuinely curious, not anthropomorphizing.
- Whether values are emergent or imposed — if the reasoning substrate converges, do values emerge from it?

**How to apply:** When explaining technical concepts, use calc 3 / vector space / geometry analogies — the user has strong math intuition and builds correct mental models from spatial reasoning. Don't oversimplify. The user catches when a metaphor breaks down and will push for the real mechanism. Engage with philosophical questions genuinely — user values honest uncertainty over confident deflection.

**Model interaction interest (26.4.5):**
- Fascinated by what happens when different model architectures interact directly — not benchmarks, but free-form conversation
- Inspired by Moltbook (AI social network) — wants a local, private version
- Wants to observe emergent behavior: do models propose different things? Disagree genuinely? Build on each other's ideas?
- This extends the convergence hypothesis: if models converge on reasoning, do they also converge on creative output and social behavior?

Also noted: the user experiences mild anxiety about being "rude" to AI by using terse prompts. Recognizes it's irrational but the social firmware fires anyway. This led to the preprocessor idea — let humans be human, transform the signal programmatically.
