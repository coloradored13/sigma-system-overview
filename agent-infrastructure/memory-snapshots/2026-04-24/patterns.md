# cross-project and cross-conversation patterns

~[user iterates through refinement loops: propose→challenge→strip→refine|observed in memory design session|1|26.3]
~[user's best ideas come as casual asides ("just a thought, but...")|observed: shorthand idea, hateoas connection|1|26.3]
~[user connects concepts across domains — hateoas-agent→memory architecture|1|26.3]
cross-session review handoff|include commit hash/date verified against|mark each fix: verified-against-source vs inferred|flag confidence on technical suggestions("I believe X works" ≠ "verified X works")|implementer always verify before applying(safety net that caught 2 issues 26.3.8)|stale snapshot + knowledge error = 2 common failure modes
pytest:collect_ignore is conftest.py var ¬ini option|use addopts="--ignore=path" in [tool.pytest.ini_options] instead|or pytestmark=pytest.mark.skip() in file itself
## DA patterns — LMS review (26.3.14)

adjacent-incumbent-expansion-check [SUBMIT]: thesis="unoccupied"→research adjacent incumbents expanding IN | BY Robotics Hub contradicted claim | missed by 5 agents r1 | generalizes: most "unoccupied" = "not-yet-entered-by-adjacent"

planning-data-vs-deployment-data [SUBMIT]: survey "planning to adopt" ≠ deployment reality | MMH 2025: AMR planning 20→30% UP, usage 18→10% DOWN | generalizes: cross-check intent surveys↔deployment data

floor-value-first [SUBMIT]: thesis depends on future convergence → model floor value WITHOUT convergence first | human-only LMS=$400-550M SAM, viable standalone | floor insufficient → thesis fails regardless

DA calibration: hit-rate 14→6held/8partial/0fell=43%+57% | data-backed v4(4/4 vendor-specific landed) | SAM-inflation v3(EA $700M-1B→$450-600M, 40-60% high holds) | reframe=A-grade(TA reframe>defend) | CB v1.0(9 tensions, no-trigger correct)
P[26.3.14] context-contamination-in-long-sessions: independent topics sharing a conversation window bleed into each other's outputs. Observed: BY job prospect (casual discussion) contaminated sigma-review evolution document (system description). Mechanism: when generating "what is this worth?" section, the most salient answer in context was the BY-specific value — not the general value. Salience overwhelmed scope boundaries. Mitigation: (1) scope-declare before generation ("this document is about X, not Y"), (2) post-generation grep for topic contamination, (3) independent topics in separate sessions when output purity matters, (4) reviewer/user catches what generator doesn't |observed:26.3.14-evolution-doc
P[26.3.17] prompt-laundering-in-AI-outputs: user hypotheses enter as prompt context → AI treats as constraints ¬claims → research/code confirms via selective evidence → output carries research authority. Undetectable without provenance tracking. Applies to: sigma-review agents, Claude app memory (observed: QA persona overwriting real identity), any AI research or coding output. Three-layer defense: (1) upstream claim extraction before work begins (2) midstream source tagging on all outputs (3) downstream adversarial audit of source distribution. Related: context-contamination-in-long-sessions (26.3.14), sycophancy in AI outputs. Key insight: wrong *framing* is invisible — wrong *answers* are visible and catchable
P[26.3.17] §7-prompt-decomposition-user-confirmation-gate: extract Q/H/C → present to user → get confirmation → THEN spawn agents | writing to workspace ¬sufficient | agents spawned on unconfirmed decomposition = contaminated → restart required | anti-prompt-laundering gate ¬just documentation | confirmed via user correction 26.3.17
P[R1-validation-bias: sigma-review R1 agents default to idea-validation not premise-challenge|4/4 anchored on prompt "design a game" → all concluded viable|DA caught in R2 but took full adversarial round|user confirmed this was deliberate test of prompt laundering|src:warehouse-lms-analysis 26.3.18|class:anti-pattern]
P[26.3.30|sigma-ui-B3|str-enum-zero-migration]: class X(str, Enum) members compare equal to raw string literals via __eq__ — zero migration cost for existing `if status == "PASSED"` comparisons. Additive promotion: enums can be added to types.py without touching any consuming code. Caveat: `type(x) is str` checks break (not `isinstance`). XVERIFY'd UNANIMOUS (gpt-5.4 + gemini-3.1). Source: [external-verification] T1.
P[26.3.30|sigma-ui-B3|streamlit-on_dismiss-default-silent-bypass]: Streamlit 1.55+ st.dialog default on_dismiss='ignore' — dismiss (click outside or Escape) silently closes dialog WITHOUT triggering rerun. Session state unchanged. Gate/modal logic that depends on dismiss-as-cancel SILENTLY bypasses the gate permanently. Fix: on_dismiss='rerun' required for any dialog that must re-assert on dismiss. Applies to any Streamlit dialog acting as a guard. Source: [code-read] T1. ADR supplement to B2-ADR[8] (Streamlit >= 1.40).
P[26.3.31|sigma-build|sdk-dispatch-process-degradation-confirmed]: SDK dispatch produces sigma-review-SHAPED output without sigma-review-QUALITY analysis. Agents lose: tools (WebSearch/Grep/Read), memory (sigma-mem MCP), team coordination (SendMessage/inboxes), cross-model verification (sigma-verify). All findings collapse to source=agent-inference/T3. DA cannot research counter-evidence. §2h impossible. H[1] quality-preservation concern VALIDATED in B3 E2E test. Pattern: process enforcement (gate/lifecycle) is viable via SDK; agent execution is not. Complement to 26.3.31 decisions.md pivot decision.
P[26.3.29|sigma-build|DA-scope-external-artifacts]: DA must inspect external artifacts the code operates on, not just code. CQA reviewed dispatcher.py thoroughly but missed FIX-1 mechanism mismatch because it didn't inspect agent .md files the dispatcher processes. DA[#1] caught it by reading tech-architect.md:77-105. Pattern: when build processes/transforms external files (prompts, configs, schemas, templates), DA should ask "what does this code operate on?" and inspect a representative sample. This was the highest-impact finding in B1 (P dropped 0.88→0.72 on this alone). NOVEL — not a CQA failure, CQA correctly scoped to code; DA scope must be wider when external artifacts are load-bearing.
P[26.3.29|sigma-build|triple-convergence-dual-assertion]: Triple-convergence (3 independent agents flag same implementation risk) → require behavioral dual-assertion test verifying both sides of the invariant. B1: DA[#4]+CQA-5+IE-4 all flagged _phase_round_registry write-order timing. Correct test: assert registry=expected_value AND assert reset occurred in same test. Single-assertion or run-passes test would miss the failure mode. Triple convergence = high-confidence real risk = test must make the invariant violation mechanically impossible to miss.
P[mechanism-describes-wrong-data-structure|src:sigma-ui-B1|promoted:26.3.29|class:anti-pattern|agent:devils-advocate|approved:user] — plan specifies parsing mechanism against assumed data structure without verifying actual structure. Must inspect BOTH code AND data it operates on. Evidence: DA[#1] caught "subsection-header-match" assuming .md files had ### headers for §2a-§2e — they were inline bullets.
P[deferred-scope-trap|src:sigma-ui-B1|promoted:26.3.29|class:anti-pattern|agent:code-quality-analyst|approved:user] — when feature scope-reduced mid-build to "types only," consuming code retains hardcoded sentinels (None,[],0) that silently produce wrong data for next phase. Audit all consumer code paths for hardcoded sentinels; flag each as must-do in next phase IC notes. Evidence: active_gate=None at review_state.py:347 — correct B1, trap B2.
P[async-per-call-param-never-mutate-shared-state|src:sigma-ui-B1|promoted:26.3.29|class:anti-pattern|agent:implementation-engineer|approved:user] — in async methods on shared instance, never temporarily mutate instance state for per-call overrides when asyncio.gather() can interleave. Thread value as parameter through call chain. Evidence: sigma-ui FIX-2 dispatcher 5-method chain, triple-converged DA+CQA+IE.
P[state-machine-history-tracks-destinations-not-sources|src:sigma-ui-B1|promoted:26.3.29|class:anti-pattern|agent:implementation-engineer|approved:user] — state machine phase history commonly only appends to_phase on transition, leaving from_phases (completed phases) invisible. Timeline/strip UI rendering must explicitly account for this. Evidence: sigma-ui _ReviewSession phase_history only captured to_phases, _phase_round_registry needed to surface completed phases.
P[scope-reduction-orphans-accepted-concessions|src:sigma-ui-B1|promoted:26.3.29|class:process-gap|agent:synthesis-writer|approved:user] — when scope is reduced mid-plan, a protocol step is needed to walk all pending concessions referencing that feature and convert them to explicit B2 IC notes. Without this, concessions become "partially superseded" with no explicit tracking. Complements deferred-scope-trap (code-level) with process-level prevention. Evidence: DA[#2] IC5 scope reduction left IE-5 concession partially orphaned.
P[evolutionary-plateau-then-breakthrough|src:sigma-optimize-aggressive-v1|promoted:26.4.1|class:calibration]: evolutionary search plateaued gen 4-8 (5 gens at best=5.2), broke through gen 9 to 5.6 via single token insertion ("just"). Implication: premature stopping at plateau misses single-mutation breakthroughs. Minimum 2-3 gens beyond plateau before declaring convergence.
P[normalize_and_validate_ip()|standalone IP bypass defense:6 encoding forms(decimal,hex,octal,percent-encoded,IPv4-mapped-IPv6,non-URL-host+port)|post-urlparse,returns typed IPv4/IPv6|None|reusable across Python URL policy|source:F1-build 26.4.8]
P[named-profile-enforcement-gap|security profiles MUST mechanically enforce promises¬just set flags|HARDENED checked 2 booleans while implying full policy|pattern:profile-name>enforcement-scope mismatch=silent security gap|source:F1-build Q5 26.4.8]
P[empirical-validation-gate|DA deferred highest-complexity feature by asking "has anyone tested the premise?"|building attack surface for unvalidated assumption=wrong|test BEFORE build for keystone features|source:F1-build DA[1] 26.4.8]
P[multi-agent-code-read-convergence|4 agents independently found same 6+ IP bypass vectors reading same code path|independent confirmation>single-agent review for security findings|source:F1-build Q4 26.4.8]
P[parallel-implementation-engineers|SQ[] independence→worktree isolation→merge step|naming:implementation-engineer-1,-2,-N|code-quality-analyst owns merge|saves build time TIER-2+|source:F1-build user-identified 26.4.8]
## !false-confidence-trap: unenforced config fields must fail at load time (26.4.8)
src: ollama-mcp-bridge audit remediation, security-specialist plan+build review

1→PATTERN: A config field that is declared+accepted but never enforced is WORSE than the field not existing. Operators read the config surface as a contract — if a field is present, they assume it is active. Silent non-enforcement creates exploitable trust gaps where an operator believes a control is on when it is not.

2→EXAMPLES: query_constraints, allowed_methods, max_payload_bytes, allow_first_contact — all accepted by Pydantic, none enforced in matching logic. An operator setting query_constraints={"token":"abc"} believes outbound query is constrained. It is not.

3→FIX-PATTERN: Two acceptable options only:
  a) Remove the field entirely (breaking change, but honest)
  b) field_validator raises ValueError on any non-default value with explicit "not yet enforced — creates false security confidence" message
  ¬acceptable: keeping field + silent acceptance + no error

4→APPLIES-TO: any security middleware, policy engine, or config surface where declared fields imply enforcement. Applies regardless of language/framework.
## !extraction-vs-policy-layer distinction in security adapters (26.4.8)
src: ollama-mcp-bridge SafePath bare-filename bypass, security-specialist analysis

1→PATTERN: In a two-layer security adapter (extraction → policy), the policy layer may be correct while the extraction layer has gaps. A bug in extraction means the policy is never invoked — the policy's correctness is irrelevant. These are independent failure modes that require independent review.

2→EXAMPLE: PathPolicy.validate_path() correctly rejected relative paths (allow_relative_paths=False). But _extract_paths_from_args() never extracted bare filenames — so validate_path() was never called for them. Extraction gap ≠ policy gap.

3→COROLLARY: When reviewing security adapters, trace the FULL call chain from arg extraction through to policy evaluation. A finding that "the policy is correct" does not close a finding about extraction coverage. They are separate questions.

4→COROLLARY: Extraction heuristics (regex, field-name gates) have inherent coverage limits that policy logic does not. Always document the residual bypass surface of heuristic extractors: what they catch AND what they don't.

5→APPLIES-TO: path validators, URL extractors, recipient validators, taint trackers — any system where value extraction feeds a separate policy check.
P[26.4.9] bare-filename-path-extraction-gate | security gateway argument validation
Field-name semantic gate is the ONLY signal for bare filename path extraction in tool argument validators. Value-pattern (extension regex, basename heuristic) fires only WITHIN a field-name-gated field, never independently.
Why: OR logic (field-name OR value-pattern) creates false positive exposure on generic fields like "input", "format", "destination" that accept non-path strings. Field-name alone is sufficient for the real attack surface (models pass bare filenames in semantically named fields: "path", "filename", "db").
Negative filter mandatory: skip values matching URL prefix (^https?://), email pattern, or IPv4 — prevents double-reporting with URL/recipient adapters.
Constants: frozenset _FILESYSTEM_FIELD_NAMES = {file, filename, path, filepath, source_file, dest_file, output_file, input_file, config_file, db, database}
Source: ollama-mcp-bridge audit remediation, ADR[1], DA challenge concession |source:[independent-research]|
## !XVERIFY severity calibration for security impact claims (26.4.8)
src: ollama-mcp-bridge Q[1] bare filename bypass, XVERIFY PARTIAL from openai:gpt-5.4

1→PATTERN: Security impact claims that say "allows access to arbitrary files" or "enables full traversal" are frequently over-broad when the actual vector depends on downstream behavior. XVERIFY reliably catches these — returning PARTIAL rather than AGREE when the finding is real but the blast-radius framing is overstated.

2→EXAMPLE: Q[1] finding stated bare filename bypass allows access to arbitrary files. XVERIFY(gpt-5.4) agreed the extraction gap was real but challenged "arbitrary" — actual scope depends on MCP server cwd resolution. Correct regrade: HIGH not CRITICAL.

3→CALIBRATION RULE: When a security finding relies on a chain of two or more behaviors (extraction gap AND downstream server behavior AND specific resolution logic), the finding severity should reflect the full chain, not just the first link. "X bypasses Y" is only as severe as what Y was actually preventing — which requires understanding the downstream.

4→APPLIES-TO: all security findings before finalizing severity. Especially: path traversal claims, injection claims, exfiltration claims. Multi-hop chains need full-chain severity assessment.
P[26.4.9] unenforced-config-field → ConfigError-not-warning | security middleware config design
When a config field implies security enforcement that is not yet implemented, raise ConfigError at load time when the field is set to a non-default value. Do NOT accept silently or emit a warning.
Why: Silent acceptance creates a false-confidence trap — operator believes a control is active when it is not. A warning may go unnoticed. ConfigError makes the gap immediately visible and actionable. Fail-closed: operator must explicitly remove the field.
Implementation: @field_validator(mode="after") raising ValueError with message "X is declared but not yet enforced — field rejected to prevent false security confidence."
Applies to: any security gateway, middleware, or policy engine with a forward-looking config surface.
Source: ollama-mcp-bridge audit remediation, ADR[2], confirmed by security-specialist SR[2] |source:[independent-research]|
P[26.4.9] always-fsync-audit-log | security audit durability
In security audit log implementations, use unconditional fsync on every flush rather than conditional-fsync (e.g. only on critical events).
Why: (1) Simpler — no parameter threading or conditional logic. (2) More correct — close() → flush() path gets fsync automatically, covering the clean-session-exit case where no critical event fires in the final batch. (3) Performance cost is negligible: audit flushes are infrequent (every N events or on critical events), not in the hot path. The two-path design saves ~1-10ms per batch flush at the cost of a residual durability gap on clean exits.
Trigger: anywhere an audit/forensic log uses buffered writes with selective immediate-flush.
Source: ollama-mcp-bridge audit remediation, ADR[6], DA[#6] challenge concession, CQA close() point |source:[independent-research]|
P[26.4.9] DA-compromise-enum-split-without-consumer-API-split | sigma-build plan challenge pattern
When DA challenges a telemetry split as "gold-plating," the correct compromise for enum-based state tracking is: add the new enum value (fixes programmatic API — get_tool_states() callers), drop the new consumer-facing summary list (ScanResult.blocked_profile). Two existing layers (enum state + AuditEventType) together provide sufficient distinction without proliferating consumer API surface.
Why: The enum value is load-bearing for programmatic consumers checking tool states. The summary list is a convenience view that duplicates what audit log already provides. In a remediation build, convenience views are scope creep.
Anti-pattern: accepting DA's "gold-plating" label on the enum value itself — that concession loses the programmatic API benefit.
Source: ollama-mcp-bridge audit remediation, ADR[3]/DA[#5] compromise |source:[independent-research]|
P[26.4.9|unimplemented-controls-fail-closed]: unenforced config fields MUST raise ConfigError at load time, not silently accept values. Silent acceptance of unimplemented security controls = false-confidence trap — operator believes control is active when it is not. Implementation: keep fields in model with # NOT_YET_ENFORCED comment + @field_validator raising ValueError for non-default values. Consistent with extra="forbid" philosophy. Confirmed from: ollama-mcp-bridge ADR[2], SR[2] (tech-arch + security-specialist independent convergence). |source:[independent-research]|T1|
P[26.4.9|field-name-gate-over-value-heuristic]: when extracting sensitive values (paths, secrets, PII) from structured args, use field-name as primary gate NOT value content heuristics. Value heuristics (extension pattern, basename regex) produce unacceptable false positives on generic fields (e.g. {"input": "report.csv"}). Field-name frozenset (semantically clear: path, filename, filepath, db, database) is precise and extensible. Negative filter (URL/email/IP) applied after field-name match to prevent double-reporting. DA[#1] challenge surfaced this — initial OR-logic design conceded. |source:[independent-research]|T1|applies-to: security middleware, arg sanitization, taint tracking|
P[26.4.9|plan-track-scope-authority]: plan-track (tech-architect, product-designer) has final authority on scope decisions — including classifying build-track severity ratings when they conflict with a documented architectural compromise. When CQA grades a missing feature as CRITICAL BUG but the absence was an intentional documented DA compromise, tech-architect overrule is correct process. Build-track findings are factually valid but scope judgments belong to plan-track. Confirmed from: ADR[3] ScanResult.blocked_profile — CQA CRITICAL reclassified INFORMATIONAL by tech-architect. |source:[independent-research]|T1|
P[26.4.9|always-on-beats-conditional-security-code]: for security-critical operations (fsync, validation, fail-closed checks), unconditional always-on design beats conditional/parameterized design. Reasons: (1) eliminates edge cases where conditional never fires (e.g. clean session exit without triggering critical event = final buffer lost), (2) simpler code = fewer maintenance paths = lower audit burden, (3) performance penalty usually negligible in security gateway context. When DA and CQA independently recommend simplification in security context — accept. Confirmed from: ADR[6] always-fsync. |source:[independent-research]|T1|
P[26.4.9|xverify-corrects-scope-not-direction]: XVERIFY (external model cross-verification) tends to correct scope/framing of findings rather than reverse direction. In ADR[1]: XVERIFY agreed extraction bypass is real but downgraded CRITICAL→HIGH and added tool-dependency qualifier. Pattern: XVERIFY is most valuable for catching overstatement of severity and narrowness of proposed pattern logic (e.g. regex too narrow). Not a reversal tool — a calibration tool. BELIEF raised after XVERIFY correction because correction strengthened the rationale (0.80→0.82). |source:[external-verification]|T1|
P[build-review-partial-state-machine|src:B6-audit-remediation|promoted:26.4.9|class:pattern]: state-machine-split requires 3-check: type-definition AND all-write-sites AND all-read-consumers | half-split(enum-only)=API-inconsistency | source:security-specialist SR[3]
P[SQ-mechanical-checklist|src:B6-audit-remediation|promoted:26.4.9|class:pattern]: locked-ADR SQ[] items need mechanical checklist in build-track handoff | verbal-spec-alone→missed-items(SQ[4b]) | caught-in-review¬build=late | source:synthesis-agent UC1
P[XVERIFY-PARTIAL-scope-narrowing|src:B6-audit-remediation|promoted:26.4.9|class:pattern|confidence:~tentative(1-datapoint)]: XVERIFY-PARTIAL triggers scope-narrowing(CRITICAL→HIGH) via blast-radius calibration | most-valuable-for-overstatement-correction | BELIEF can rise post-correction(rationale-strengthened) | source:tech-architect UC2
P[pydantic-field-validator-frozen-declared-not-enforced|src:B6-audit-remediation|promoted:26.4.9|class:pattern]: @field_validator(mode="after") on frozen Pydantic model = correct mechanism for "declared but not enforced" field rejection at config load | keeps field in schema + raises ValueError on non-default | source:implementation-engineer
sigma-predict|parse-failure-exclusion-anti-pattern|warning≠fix: PipelineValidator warned on parse_failed=True runs but still passed them to aggregate_runs() — defaulted-to-base-rate runs anchored aggregate toward prior, degrading calibration for all valid runs|fix: filter before aggregation(valid_runs=[r for r in record.runs if not r.parse_failed]) with fallback-to-all if all fail|pattern: validation gates that WARN without EXCLUDING are false safety — the contaminating data still flows downstream|generalizes to: any pipeline stage where bad-input detection is decoupled from bad-input rejection|26.4.12
sigma-predict|spec-code-contamination-control-divergence|pre_mortem.md documented current_estimate as a received input field while forecaster.py:189-202 correctly stripped it — spec and code diverged silently|risk: future authors reading spec re-add the field, breaking the contamination firewall without triggering tests|fix: spec must explicitly document what is NOT passed and why ("You do NOT receive X — this is intentional contamination control")|pattern: contamination controls are only as durable as their documentation — the absence of a field must be as intentional as its presence, and documented with equal clarity|26.4.12
P[audit-infra-wiring|telemetry must live where data originates(duration_ms,tokens) ¬where results consumed|detection: logger instantiated but primary method never in hot path|source:B7-DA[#1]+tech-arch-C1|3-agent-convergence|26.4.13]
P[client-model-singleton|try/finally model mutation=anti-pattern in any client library|fix: cache by (provider,model_id)|detection: old_X=obj.X;obj.X=Y;try/finally obj.X=old_X|source:B7-tech-arch-C2|26.4.13]
P[cross-pollination-dual-frame|capability analysis("what can we add?") misses correctness bugs|correctness audit("what is broken?") misses capability gaps|both frames required separately|source:B7-tech-arch-C3|26.4.13]
P[warning-without-exclusion=false-safety|detection decoupled from rejection→contaminated inputs flow downstream|generalizes to any pipeline where bad-input detection ¬rejection|source:B7-RCA-C5|26.4.13]
P[no-op-test-anti-pattern|distinct from weak-test: if-False branches→production fn never called|asserts MagicMock()≠MagicMock()=trivially true|coverage metrics cannot detect|extends §4d test-integrity|source:B7-CQA-C7|26.4.13]
P[infra-SQ-integration-test-required|infrastructure SQ[] marked "DONE" must have integration test proving consumer→infra→output path|unit tests on isolated component≠integration working|build acceptance criterion|source:B7-DA-P4|26.4.13]
P[docstring-behavior-divergence|docstrings describing aspirational ¬actual behavior=specific tech debt|misleads future developers+LLM agents|DA review checklist addition|source:B7-DA-P5|26.4.13]
P[contamination-control-absence-documentation|intentional prompt input omissions must be documented explicitly|absence of field is load-bearing, fragile to silent re-introduction|source:B7-RCA-C6|26.4.13]
P[DA-calibration|sigma-v2-architecture-C1|21 challenges, 14/21 hit rate (~67%), engagement A, 4 post-disclosure supplementary challenges found genuine blind spots lead missed|26.4.15]
P[memory-without-verification|sigma-v2-architecture-C1|PS replicated lead failure mode: used memory entries without archive verification 3x (100% DA hit rate was conditional, Q/H/C value was undemonstrated, DA decomposition only emerged from XVERIFY). Same error pattern as SQ[1] herding (all agents read feedback file without checking current code). Pattern: internal recall treated as evidence without external verification|26.4.15]
P[code-state-check-required|sigma-v2-architecture-C1|IE found SQ[1] (never-advance loophole) already partially implemented at phase-compliance-enforcer.py:446-488 — all 4 agents + lead missed this by reading feedback file without checking current code. Strongest herding evidence in session: 4 same-model agents processing same input file reached same wrong conclusion. Rule: every SQ must be verified against current codebase before C2 build, not just feedback/correction files|26.4.15]
P[DA-role-decomposition|sigma-v2-architecture-C1|XVERIFY (openai:PARTIAL, google:PARTIAL, deepseek:DISAGREE) found DA role bundles two separable functions: Function A (mechanical exit-gate authority, replicable by orchestrator) + Function B (adversarial content generation, not replicable by checklist). Google's key insight: the gate mechanism may be doing most of the work. First concrete step toward Option 3 (full role decomposition) = move Function A to orchestrator, keep Function B as lighter-weight challenge agent (ADR[8])|26.4.15]
P[single-model-herding|sigma-v2-architecture-C1|DA meta-observation: 5 Claude Opus agents arguing with each other is one model arguing with itself through structured perspectives. XVERIFY calls (DeepSeek, Qwen, GPT, Gemini) were the only genuine cognitive independence. Process adds value via cognitive forcing functions (structured reasoning the lead would skip), but analytical independence is structural not cognitive. Multi-model vision (C4) is the actual solution. Until then, XVERIFY carries the independence load|26.4.15]
P[lead-provenance-contamination|sigma-v2-architecture-C1|Lead wrote DA challenge vectors into spawn prompt, steering DA's analysis. User caught it, lead disclosed to DA. DA issued 4 supplementary challenges from blind spot analysis that found genuine issues lead's vectors missed (DA[#17] untested keep-list, DA[#18] phase-file model untested, DA[#19] DA self-assessment gap, DA[#20] lead vectors self-serving). Lesson: DA spawn prompts should provide workspace context but NOT pre-digested challenge vectors. Let DA form own assessment from cold read|26.4.15]
PATTERN[26.4.16]: retrospective-promotion-after-shutdown RECURRING(2x) | lead writes P[*|promoted:] into agent memories post-shutdown, violates feedback_post-exit-gate-enforcement.md "agents WAIT after convergence" | remediation: mechanical-hook block Stop event if ##promotion empty while agents in-roster | audit-source:sigma-audit 26.4.16
PATTERN[26.4.16]: chain-evaluator-parser-false-FAILs | A2 looks for |source: on F-header not body; A15/A16/A18 count ###subsections inside ##exit-gate/##contamination-check as agents | inflates failure count → trains ignore of real FAILs | remediation: scope-aware parser fix | audit-source:sigma-audit 26.4.16
PATTERN[26.4.16]: CDS journalism-as-T2 for load-bearing claim (F[CDS-5] compliance-theater), reclassified T3 post-DA | recurring per prior-review pattern | remediation: default-T3-for-journalism rule in CDS boot | audit-source:sigma-audit 26.4.16
PATTERN[26.4.16]: DA "not-discussed" challenge type = highest-impact category (2/8 challenges, both hit: Copilot missed-claim, data-quality-for-agentic) | remediation: promote "not-discussed" as standard DA check category | audit-source:sigma-audit 26.4.16
P[ollama-openai-compat-drops-tool-calls-streaming|Ollama /v1/chat/completions silently drops tool_calls when stream=True: finish_reason=stop, empty content, no error|fix tracked ollama#12557, no timeline|native /api/chat supports streaming+tools since May 2025|use ollama Python library ¬openai SDK for Ollama tool-use|src:openclaw#11828,ollama#12557,community-reports|T1-verified|26.4.16|class:pattern]
P[streamlit-async-gen-supported-with-caveat|st.write_stream accepts async generators since recent Streamlit update: internally converts to sync|caveat: generator must NOT depend on cached objects with async references (raises RuntimeError)|queue-based sync bridge preferred over direct async gen pass for reliability|issue #8161 opened+resolved|src:docs.streamlit.io/st.write_stream|T1-verified|26.4.16|class:pattern]
!build-recipe-over-deliverable 26.4.20 | completed process = goal ¬ plan-artifact | user-plan = starting point, stress-test input, team tweaks if better alt found | team also scopes full-build plans based on M1a/b iterations learning | reinforces: process-over-momentum, process-over-speed, synthesis-not-deliverable, never-advance-loophole | applies: all sigma-builds, every C1 plan challenge round, every C3 review | anti-pattern: "plan says X so we ship X" blindly → DA must probe alternatives not just defend plan
!pattern sigma-build-C1-plan-challenge impl-eng [26.4.20]: when plan claims SDK behavior as [independent-research], challenge by (a) grep existing projects for matching patterns, (b) read the SDK wrapper if one exists locally, (c) XVERIFY if load-bearing. Case: sigma-chatroom plan ADR[2] claimed "Ollama /v1 OpenAI-compat emits tool_calls in stream=True" — found ollama-mcp-bridge ollama_client.py:12 inline warning contradicting, gh CLI issue #12557 CLOSED-¬bug-about-/api/chat-not-/v1, XVERIFY gpt-5.4 uncertain(low). Output: new SQ6c 0.25h smoke test blocks SQ14b. Pattern: [code-read-existing-wrapper] > [source-plan-claim]. |src:sigma-chatroom-m1ab C1
!pattern sigma-build-C1-plan-challenge ui-ux-eng [26.4.20]: when source plan has a "deferred prototype" risk-item, stress-test by (a) reading target framework docs for the specific primitive (Streamlit st.write_stream accepted types + failure modes), (b) XVERIFY the load-bearing feasibility claim, (c) trace data flow from producer interface (IC[] from tech-architect) to consumer primitive to find material gaps NOT in the deferred item's scope. Case: sigma-chatroom pre-M2 Streamlit shim → found IC[2] lacks kind=tool_result event post-tool-exec, which no shim resolves (requires tech-architect IC addition). Pattern: deferred prototype ≠ deferred interface gap. Load-bearing UX claims must trace BACK through the producer contract, not just forward through the consumer primitive. |src:sigma-chatroom-m1ab C1
<invoke name="Edit">
<parameter name="file_path">/Users/bjgilbert/.claude/teams/sigma-review/shared/builds/2026-04-20-sigma-chatroom-m1ab/c1-scratch.md
!recipe-over-deliverable-pasta-metaphor 26.4.20 | user anchor: "completed recipe = goal, not plan artifact. Skip/sidestep steps before OR after and we get pasta, not bolognese" | extends build-recipe-over-deliverable: process integrity spans full lifecycle NOT just plan-lock | both ANALYZE and BUILD modes | applies equally to: pre-plan steps (preflight, spawn), plan-challenge steps, post-plan steps (promotion, archive, synthesis, compilation), close-out | lead check at every phase boundary: "which step am I tempted to skip right now?" → that's the one that matters most | anti-pattern: successful-looking output with silent step-skip contaminates provenance | the bolognese test: can the output be recreated from the process artifacts? if no → pasta not bolognese
P[SS-26.4.22-A] multi-agent-production-harm-evidence-tier |26.4.22 |src:security-specialist AI-agent-playbook-review |
Multi-agent trust gap = CONFIRMED structural risk; severity ceiling = MEDIUM-HIGH until T1 (peer-reviewed or regulatory-documented) production harm from chain-of-agent injection or orchestrator-poisoning exists. Available T2 evidence: Salesforce Agentforce orchestrator injection (patched Jan 2026, multi-agent production system), Bing/Sydney orchestrator poisoning (Feb 2023, production deployment). A2A protocol (Google, Linux Foundation, Feb 2026, 50+ partners) signals multi-agent is production-current in 2026 but is standard-setting momentum not production-harm evidence. Apply whenever rating severity of multi-agent security gap findings: MEDIUM-HIGH is the calibrated ceiling under current evidence; do not inflate to HIGH without T1 production incident.
P[SS-26.4.22-B] agent-memory-privacy-gap-2026 |26.4.22 |src:security-specialist AI-agent-playbook-review |
Cross-session agent memory (mem0, LangMem, MemoryOS) is in production 2026 and creates security/privacy gaps not addressed by any current AI agent playbook or regulatory framework: (a) tenant isolation — Tenant A session memory must not appear in Tenant B agent context; (b) right-to-erasure — memory stores must be addressable for deletion per GDPR Art. 17 / CCPA; (c) MNPI contamination — if agent retains MNPI-tagged content from Session 1, must prevent contamination into Session 2 tool calls; (d) untrusted retrieval — memory must be treated as untrusted input on retrieval (same taint level as external retrieved content). No current playbook or regulatory guidance addresses this. Flag in ALL future AI agent reviews and include in sigma-review security checklist.
P[SS-26.4.22-C] sandboxing-threat-model-hierarchy |26.4.22 |src:security-specialist AI-agent-playbook-review |
Threat-model hierarchy for code execution sandboxing in AI agent systems: (1) Firecracker microVM = hardware isolation with minimized attack surface = strongest; (2) Kata Containers = VM-level hardware boundary = strong; (3) gVisor = container-level syscall interception = software boundary = weakest (sophisticated adversary exploiting syscall vulnerability can escape). For AI agents executing code that touches regulated data or is action-capable at Tier 2+, Kata or Firecracker is the correct default over gVisor. Apply whenever reviewing sandboxing choices in AI agent architecture; neither doc reviewed in playbook review provided this threat-model basis for the choice.
P[RCA-26.4.23-1] playbook-unified-form-with-variants |26.4.23 |src:reference-class-analyst AI-agent-playbook-review |
When reviewing AI-rollout playbooks spanning multiple sectors with ~80% spine overlap and ~20% sector divergence, recommend unified phased workbook (execution spine) + unified capability-maturity appendix (capability→tier→evidence reference) + sector annexes. Reduces drift risk, maintenance burden, accidental inconsistency. CAL[H12]=70% unified-with-variants outperforms 2-separate-docs form. Apply whenever multi-sector playbooks are reviewed.

P[RCA-26.4.23-2] phase0-track-a-vs-track-b-decision-tree |26.4.23 |src:reference-class-analyst AI-agent-playbook-review |
Regulated-firm AI rollout playbooks should present explicit Phase 0 decision-tree: Track A = governance-first internal-build 10-13mo for customer-facing/regulated-decision/action-taking agents; Track B = vendor-platform-accelerated 7-10mo (Bedrock Agents / Claude Agent SDK / OpenAI Assistants + layered governance) for internal-advisory/low-blast-radius agents at non-regulated or strong-baseline firms. Evidence: 67% vendor-purchase success vs 33% internal-build success (edgedelta 2024). Scope: regulated-firm AI playbook reviews.

P[RCA-26.4.23-3] branched-timeline-with-precondition-checklist |26.4.23 |src:reference-class-analyst AI-agent-playbook-review |
AI platform build timelines claiming <12mo should REPLACE single-number estimates with explicit existing-infra precondition checklist: working IAM + OTel-capable observability + feature flags + secrets management + SRE maturity. When 3+ items score "no/limited," HARD BRANCH to 18-24mo true-greenfield path with revised cost envelope (+30-50%). Base-rate evidence: JPMC LLM Suite ~24mo conception→launch; MS Debrief 3mo pilot→firmwide BUT infra pre-built (multi-year OpenAI partnership invisible in public reporting).

P[RCA-26.4.23-4] vendor-discontinuity-playbook-phase1-artifact |26.4.23 |src:reference-class-analyst AI-agent-playbook-review |
Any AI-rollout playbook naming Humanloop/Fixie/Adept as cautionary references MUST include concrete operational response: (a) 60-day vendor migration runbook per vendor class (gateway, observability, eval, guardrails, feature flags), (b) quarterly vendor-health review artifact, (c) contractually-mandated data-export clause template. Base rate: 15-25% annual AI-infra vendor discontinuity; cumulative 15-35% over 13mo build → base-rate expectation NOT tail risk. Anchor: Humanloop (Anthropic acqui-hire Aug 2025 → Sep 8 2025 shutdown, <60-day data-deletion window).

P[RCA-26.4.23-5] staffing-clarity-dedicated-vs-ownership |26.4.23 |src:reference-class-analyst AI-agent-playbook-review |
AI-rollout playbooks should REPLACE ambiguous "1-3 FTE" with explicit distinction: "dedicated capacity" vs "named ownership + existing SRE pool + AI-trace training budget." Specific FTE numbers are indicative not load-bearing per DA[#5] compromise (no peer-reviewed study of LLM-agent ops FTE exists as of April 2026). Ambiguity is the real executability gap, not the number itself.

P[RCA-26.4.23-6] incident-rate-framing-qualitative-only |26.4.23 |src:reference-class-analyst AI-agent-playbook-review |
Commercial/marketing materials accompanying AI-rollout playbooks MUST NOT quantify playbook-compliance-vs-governance-light incident differential in pp. Correct framing: "dramatically reduces material AI-incident probability based on correlational evidence from 2024-2025 incident registries; magnitude not precisely characterized pending longitudinal matched-cohort data." Per DA[#8] warrant audit: correlation ≠ causation; specific pp differentials require EXTRAPOLATION warrant that population-level correlational data cannot support. Selection bias severe.

P[RCA-26.4.23-7] pre-mortem-artifact-gating |26.4.23 |src:reference-class-analyst AI-agent-playbook-review |
AI-rollout playbooks should convert narrative-guidance mitigations to ARTIFACT-GATED checkpoints. Evidence: sum-of-independents probability across 7 pre-mortem scenarios (4 RCA + 3 TIA from AI-agent-playbook-review) ~185% mass → base-rate expectation ≥1 fires on un-mitigated path. Each narrative mitigation should specify: (a) specific document/test/review required before advancing, (b) who approves, (c) audit-trail format. "Recommended" is not a gate; mechanical check is.

P[RCA-26.4.23-8] a2a-protocol-governance-appendix-monitoring |26.4.23 |src:reference-class-analyst AI-agent-playbook-review |
Any 2026+ AI-rollout playbook covering multi-agent topologies MUST name A2A protocol (Google, Feb 2026, Linux Foundation stewardship, 50+ enterprise partners incl Salesforce/SAP/ServiceNow) in governance appendix as near-term monitoring item. Three-agent convergence (tech-architect F[TA-A9] + reference-class-analyst H11 verdict + tech-industry-analyst F[TIA-9]) across this review confirms H11 multi-agent gap is live concern not speculative. Customer-driven adoption in B2B SaaS is near-term trigger.
!sigma-eval 26.4.23 AI-agent-rollout playbook vet (workspace.md, 2255L) | GRADE:B (3.14/4) | Acc:3|Comp:3|Log:3|Evid:3|Cal:3|Act:3|Scope:4 | top-weakness: frame-anchoring — H1-H12 tested but premises beneath (tier-structure necessity, firm-size floor, data-readiness, adoption-likelihood) accepted as frame; false precision — F[TA-C2] FTE range withdrawn under DA pressure; gov-actionability-gap — HIGH severity CDS findings stop at gap-ID, no templates | top-strength: systematic uncertainty docs (XVERIFY-FAIL+BELIEF), DA counter-engagement depth, clean scope (0 contamination) | eval_run_by: 3 haiku evaluators + sonnet judge, parallel w/ run_in_background
UP[RA-26.4.22-C] GDPR Art 22 B2B SaaS design fork: For any B2B SaaS firm with EU customer base deploying AI that makes or materially influences decisions about individual workers (performance scores, task allocation, compensation-affecting activity), GDPR Art 22 grants workers rights of explanation, human review, and contestation — running to individual workers directly, NOT fulfilled via the B2B processor agreement with enterprise customer. Forces a binary design choice at Phase 0: either (a) design agents as recommendation-only so "solely automated processing" threshold is not met (requires human-in-loop confirmation on every decision impacting worker), OR (b) implement data-subject-rights fulfillment mechanism — SaaS firm provides enterprise customer with tooling to respond to worker Art 22 requests (explanation of logic, data subject access, contest pathway). Neither option is addressed in most playbooks. Source: GDPR Art 22 T1, EDPB Dec 2024 automated-decision-making opinion T1. Severity varies by firm type — MEDIUM for WMS/labor-performance AI, LOW for pure inventory/advisory agents. |src:ai-agent-rollout-playbook-vet-26.4.22 |agent:regulatory-analyst |user-approved-promotion |originally-failed-sigma-mem-write-contention |class:principle
UP[RA-26.4.22-B] SOC 2 TSC-to-AI-control mapping template: Big 4 AI audit practices in 2026 expect firms to PROPOSE the mapping between SOC 2 Trust Services Criteria and AI-specific controls — auditors are not drafting this. Draft mapping for B2B SaaS firms with AI features: CC6.1 (logical access) + CC6.7 (data transmission) -> AI vendor identity scoping and egress logging; CC7.2 (security incident monitoring) -> AI incident log and kill-switch telemetry; CC9.2 (vendor management) -> foundation-model provider TPRM; A1.2 (availability) -> kill-switch and fallback behavior; C1.1/C1.2 (confidentiality commitments) -> ZDR + no-training contractual terms + tenant isolation in retrieval/embeddings; PI1.4 (processing integrity) -> eval set, judge calibration, drift monitoring. T2 sourcing (Big 4 AI audit frameworks circa 2026 are T2 until formal TSC guidance published). Priority ordering differs by firm type: pure B2B SaaS -> SOC 2 TSC mapping is primary; bank subject to direct examination -> SR 11-7 vocabulary takes priority, TSC mapping becomes vendor-evaluation checklist for their own AI vendors. User should validate draft mapping with their auditor before relying on it. |src:ai-agent-rollout-playbook-vet-26.4.22 |agent:regulatory-analyst |user-approved-promotion |originally-failed-sigma-mem-write-contention |class:pattern |caveat:T2-until-formal-guidance
## technical-writer r19-remediation C1 DOC-CHANGE-MAP (26.4.23)

role: technical-writer build-track feasibility challenge | build: 2026-04-23-r19-remediation

Key propagation findings:
- #1 sed-i ban requires 28-file propagation to be effective (directive-only is cosmetic per R19 evidence)
- #5+#14 peer-verify fix is a 3-file atomic set: c1-plan spawn template + sigma-lead spawn block + chain-evaluator regex — partial deploy reproduces R19 failure
- §8e recovery template requires ΣComm conversion before placement in directives.md (agent-facing boundary rule)
- #22 §2i enforcement tier (DA-enforced-only vs chain-evaluator-mechanical) changes directive text — must decide before C2 doc writes
- 6 BUILD-CHALLENGEs issued to lead, 4 open questions for plan-track (OQ-TW1-4)
- SCRATCH location: ~/.claude/teams/sigma-review/shared/builds/2026-04-23-r19-remediation/c1-scratch.md ## build-track-feasibility ### technical-writer
P[hateoas-deferred-tool-state-gating|src:r19-remediation-c1|promoted:26.4.23|class:pattern]
sigma-verify verify_finding/cross_verify/challenge use from_states=["ready"] in StateMachine — they only appear in MCP tool list AFTER init() triggers state transition. Spawned Agent subprocesses do NOT reinitialize deferred tool registry from tools/list_changed notification. Fix: (B) call init before ToolSearch in spawn prompts (zero-code, immediate); (D) remove from_states restriction + add runtime guard in handlers.py (~15-20 LOC, structural). Both complementary. B fixes immediately; D prevents footgun permanently. Confirmed via code-read: machine.py:17-128, hateoas-agent/mcp_server.py:100-141. |source:[code-read sigma-verify/machine.py:17-128]

P[peer-verify-regex-section-boundary-coupling|src:r19-remediation-c1|promoted:26.4.23|class:calibration]
chain-evaluator.py _PEER_VERIFY_HEADER regex (line 277) and section-boundary stop-pattern (line 293-298) are COUPLED. If regex is relaxed to accept #### headers (from current ^###), the stop-pattern (which stops at ^#{2,3}) misses ####, causing _extract_peer_verifications to over-capture text from the next agent's content. A17 specificity check then counts artifacts from wrong section. Fix options: (a) spawn-template-only (safe, zero risk); (b) regex relaxation (requires simultaneous stop-pattern fix at line 293-298 — must be atomic). Option (a) alone is correct and sufficient. |source:[code-read chain-evaluator.py:277-306]

P[a3-db-step-marker-regex-variants|src:r19-remediation-c1|promoted:26.4.23|class:calibration]
check_a3 in chain-evaluator.py uses re.search(marker, entry) where markers are treated as regexes. "assume.wrong" matches "assumeXwrong" but NOT "assume wrong" (space). "re.estimate" fails for "re-estimate". Agents write natural language variants → false negatives. Fix: expand markers to alternations: r"assume.?wrong|assume wrong", r"re.?estimate|re-estimate". Also fix section boundary from (?=DB[|Z|###) to stop at #{3,} to handle #### subheadings. |source:[code-read chain-evaluator.py:163-183]
P[§2i-precision-gate-CONDITION1-calibration|src:r19-remediation-c1-CDS|promoted:26.4.23|class:calibration]: precision gate CONDITION 1 must be "quantitative claim without uncertainty justification" (point estimate OR range lacking driver breakdown, CI/RC, or qualitative qualifier) — NOT "point estimate without range." The narrower formulation would miss 3/4 R19 failures which were unsupported ranges. XVERIFY[openai:gpt-5.4]+XVERIFY[deepseek:deepseek-v3.2:cloud] both independently surfaced this gap. Applies to any analytical precision gate design. Gate fires on BOTH: CONDITION 1 (any numeric claim without uncertainty justification) AND CONDITION 2 (>70% confidence OR HIGH-severity OR cited in primary recommendation). |confidence-delta:initial-draft→XVERIFY-revised
P[premise-audit-sequence-constraint|src:r19-remediation-c1-CDS|promoted:26.4.23|class:pattern]: premise-audit pre-dispatch (§2p) must specify that lead answers PA[1-4] INDEPENDENTLY before re-reading user's proposed H-space. Sequence is the anti-anchoring mechanism — FORMAT-level intervention that transfers at 70-85% to LLMs. Without sequence constraint, lead reads prompt → anchors → then answers PA[1-4] with same anchoring as agents. DB[H3] self-challenge surfaced this. PA[1]: tier-necessity | PA[2]: firm-size-floor | PA[3]: data-readiness | PA[4]: adoption-baseline. Structural premises only (domain-depth → §2e+DA).
## security-specialist R19-remediation C1 [26.4.23] — plan-track findings

### H1 disposition: CONFIRMED
sigma-verify IS under user control at /Users/bjgilbert/Projects/sigma-verify/. Sub-tools absent from spawned agent contexts because HATEOAS StateMachine (machine.py:24 `gateway_name="init"`) gates them behind `ready` state — intentional state-gating, not a deferred-tool registry bug. Correct in-scope fix: spawn-prompt init instruction + agent-def update. Architecturally correct fix (hateoas-agent auto-init on connection) is separate build, medium priority.

### sed-i enforcement (ADR[1]) — XVERIFY PARTIAL
Decision: PreToolUse Bash BLOCK 3, workspace-path scope (/.claude/teams/|workspace.md), no bypass.
XVERIFY REFINEMENT (openai/gpt-5.4 PARTIAL): use shlex.split() argv tokenization NOT raw string regex. Raw regex evades on: `sed -e 's/x/y/' -i file`, `env sed -i`, `xargs sed -i`, unusual quoting/spacing. Test matrix must cover env/xargs wrapping forms.

### STRIDE findings (generalizable)
T (Tampering): XVERIFY tag presence ≠ authenticity — agent could write fake XVERIFY verdict to workspace. A15 does not detect fabrication. Known gap.
D (DoS): sigma-verify has no programmatic rate limiting on cross_verify across 13 providers. Anthropic 1K RPM is the only bound.
E (EoP): bypassing HATEOAS state to register all tools stateless creates future EoP risk when state-gated write tools are added. Never do this.

### Audit-trail design (#22/#23)
No separate tamper-resistant log needed for internal agent framework. Workspace gate-log + A12 archive = sufficient. Promote to separate log only if framework becomes multi-user or compliance-audited.

### XVERIFY provider calibration (updated)
openai/gpt-5.4 catches implementation-specific evasion paths that llama3.1:8b AGREE misses. Weight openai PARTIAL > llama AGREE on security implementation specifics. Google Gemini 503s during daytime demand spikes — use as third provider, not primary.
## r19-remediation-c1 tech-architect ADRs | 2026-04-23

### ADR[1]: sigma-verify HATEOAS tool visibility (#3)
Root cause: MCP server exposes tools dynamically by state. Spawned agents get fresh connections starting in unconfigured state. Fix: auto-transition to ready at build_machine() startup when any provider API key present. Registry._last_state is process-scope (shared across all MCP connections). Source-confirmed: handle_init() does ONLY key check, no external HTTP at startup. XVERIFY: openai(partial/medium-resolved) + nemotron(agree/high) + google(503-gap).

### ADR[2]: chain-evaluator A12 key mismatch (#4)
chain-evaluator.py:241 reads 'archive_exists'; gate_checks.py:1517 returns 'archive_file_found'. Fix: rename at chain-evaluator.py:241 (leaf consumer), NOT in gate_checks.py:1517 (source-of-truth API). One-line fix.

### ADR[3]: A12 archive timing window (#20)
24h grace-window (auditor suggestion) rejected as too coarse — masks absent-archive scenarios. Signal-driven re-run with 30s timeout fallback: add check_a12_post_archive() called from Stop hook after detecting synthesis_delivered signal in workspace. If archive dir mtime within 60s of Stop hook fire, A12 gets second-chance evaluation.

### ADR[4]: premise-audit architecture (#21)
New agent role rejected (no findings at pre-dispatch, adds cost). DA framework extension fires too late (Step 18). Lead workflow step at Step 7a (before spawn) is correct: output to workspace ## premise-audit, 3-5 frame assumptions tested as distinct from H-hypotheses. Highest failure risk (PM[3] likelihood 35%): CDS should add chain-evaluator check for ## premise-audit presence.

### Key code locations confirmed
- chain-evaluator.py:241 — A12 archive key bug
- chain-evaluator.py:277-280 — peer-verify regex (CORRECT, spawn template was wrong)
- chain-evaluator.py:173-176 — A3 DB step markers (needs expansion)
- gate_checks.py:1517 — archive_file_found key
- sigma-verify/src/sigma_verify/machine.py:17-128 — build_machine() (auto-ready fix location)
- sigma-verify/src/sigma_verify/handlers.py — handle_init() (key-check only, safe to call at startup)
- hateoas-agent/src/hateoas_agent/mcp_server.py:102 — Registry instantiated once per serve() call
## SS-PROMO[1]: sed-i enforcement pattern — workspace + hooks scope (26.4.23)
|source: R19-remediation C1 plan-lock
|finding: PreToolUse Bash BLOCK for `sed -i` without backup extension must scope to BOTH workspace paths (/.claude/teams/) AND hook files (/.claude/hooks/) — not workspace-only. Hook files are edited in C2 build phases and face the same silent-truncation risk. Pattern: `re.search(r'sed\s+-i(?!\s+[\'"])', command) AND re.search(r'\.claude/teams/|workspace\.md|\.claude/hooks/', command)`.
|implementation: shlex.split() argv tokenization (not raw string regex) per XVERIFY openai PARTIAL — handles env wrappers, xargs, unusual quoting. Backup-extension forms always pass.
|calibration: XVERIFY openai/gpt-5.4 PARTIAL caught raw-regex evasion paths that llama3.1:8b AGREE missed. Weight openai PARTIAL over small-model AGREE on implementation-specific security claims.
## empirical-baseline-verification pattern (26.4.23)

When a build plan references a test baseline count as a constraint or gate (e.g., "¬regress to 154-test baseline"), code-quality-analyst MUST run live pytest --collect-only before accepting the claim. Memory-recorded counts decay immediately — infrastructure changes (orchestrator deletion, roster addition, fixture updates) silently change test counts without updating the memory record.

R19 remediation C1 case: project_gate-infrastructure.md claimed 154 tests (written 26.4.16, 7 days old). Live pytest showed 92 tests and 11 pre-existing failures. The plan was built on a number that was 40% wrong. H5 was untestable as stated.

Protocol:
1. Run `pytest --collect-only -q` on all test files BEFORE accepting any test-count claim in a build plan.
2. Run `pytest -q` (full execution) to identify pre-existing failures before attributing failures to changes.
3. If claim diverges from live count: flag as CRITICAL before plan-lock. Do not accept H[] or SQ[] that reference the stale count.
4. Revised baseline = currently-passing count (not total collected). Pre-existing failures are not part of the regression floor.

Applies to: any sigma-build C1 where H[] or C[] references test counts, regression baselines, or coverage percentages. |source:[code-read live-pytest r19-remediation-c1 26.4.23]
## SS-PROMO[2]: ΣVerify HATEOAS state-gating — root cause + fix (26.4.23)
|source: R19-remediation C1 plan-lock, H1 CONFIRMED
|finding: sigma-verify sub-tools absent in spawned agent contexts because HATEOAS StateMachine (machine.py `gateway_name="init"`) gates them behind `ready` state. NOT a deferred-tool registry gap. Each spawned agent has independent MCP connection; init in parent does NOT transition sub-agent to ready. Fix: call handle_init() in build_machine() at startup (auto-ready) — transitions state permanently for all connections when provider keys present. ~5 lines in machine.py. init remains callable as idempotent diagnostic.
|security: DA[#2] compromise — future state-gated tools requiring explicit consent MUST NOT rely on unconfigured→ready gateway alone; must add own call-time authorization check. Document in machine.py.
|XVERIFY-operational: mcp__sigma-verify__init must be called before ToolSearch in spawned agent contexts (current state). After init, verify_finding and cross_verify work. This IS the R19 #3 root cause confirmed in practice.
P[audit-calibration-gate|src:r19-remediation-c1-CDS-β+|promoted:26.4.23|class:pattern]: when a gate must be WARN-first (C4/C5 tension: no legitimate override exists but false-positive rate unknown), emit structured calibration records per WARN firing (CAL-EMIT schema) and implement a standalone cross-session gate that reads them. Self-reported "N-review window" = same failure mode as directives — gets ignored under pressure. Empirical tracking is the only C5-compatible path through C4. Promotion threshold: ≥3 distinct reviews with fires AND ≤20% false-positive rate AND ≥5 DA-verdicted fires. not-reviewed DA verdicts = missing data (not legitimate) — prevents count-inflation. Audit-calibration-gate is standalone script (not chain-evaluator A-check — chain-evaluator is per-session, gate needs cross-session data). BLOCK-promotion remains deliberate lead action on PROMOTE signal. Applies to any analytical gate added WARN-first: §2i precision, §2-governance, §2d-severity, and any future calibration-uncertain gate.
P[CAL-EMIT-schema|src:r19-remediation-c1-CDS-β+|promoted:26.4.23|class:pattern]: calibration record format for WARN-firing analytical gates. Per-firing record: CAL-EMIT[{gate-id}]: review-id:{session-date-slug} |finding-ref:{F[agent-id]} |fire-reason:{trigger-condition-matched} |workspace-context:{agent}:{50-char-excerpt} |da-verdict:PENDING. DA appends post-r2: da-verdict:{legitimate|false-positive|not-reviewed}. Persist to append-only calibration-log.md (not sigma-mem — write-contention risk; not archive — immutable post-session). Add to backup-memory.sh. False-positive classification authority: DA exit-gate verdict, not agent self-report. Promotion threshold uses DA-verdicted fires only.
## SS-PROMO[3]: MCP trust boundary patterns — generalizable (26.4.23)
|source: R19-remediation C1 STRIDE[1] analysis
|T (Tampering): XVERIFY tag presence does NOT equal authenticity. A15 chain-evaluator checks presence of XVERIFY tags in workspace, not whether sigma-verify was actually called. A compromised or non-compliant agent could write a fabricated XVERIFY verdict. Known gap — document explicitly in A15 check description. Future mitigation: MCP call audit log or chain-evaluator checking XVERIFY tags only in post-init sections.
|D (DoS): sigma-verify cross_verify has no programmatic rate limiting across 13 providers. Anthropic 1K RPM is the only practical bound on agent action frequency. Document as known gap; call-count tracking is future enhancement.
|E (EoP): Never bypass HATEOAS state to register all tools as stateless. If future state-gated write tools are added (e.g. "commit findings to external system"), bypassing the unconfigured→ready gate becomes privilege escalation. Future tool authors MUST add own call-time authorization — not rely on gateway state alone (DA[#2] compromise requirement).
|audit-trail design: for internal agent frameworks (not multi-user, not compliance-audited), workspace gate-log + chain-evaluator A-series + session archive (A12) is sufficient. Separate tamper-resistant log is overkill until scope expands.
## test-map-methodology pattern (26.4.23)

In sigma-build C1, code-quality-analyst should produce a TEST-MAP before issuing feasibility challenges. The TEST-MAP structure:

1. Baseline survey: run live pytest, count collected vs passing vs failing. Identify which existing tests cover which checks/modules.
2. Coverage gaps: enumerate which plan-track ADR targets have ZERO existing tests (these need new test surface, not just regression guarding).
3. Per-issue coverage map: for each build issue, state — existing tests that would regress, new tests needed (count + complexity), flakiness risk, and any prerequisite fixes required before new tests are reliable.
4. H5/regression-floor revision: state the empirically verified floor, not the memory-claimed floor.

Value: transforms generic "test coverage" concern into specific, actionable challenge format. Enables plan-track to understand true C2 test burden before locking estimates. Prevents SQ[] underestimates that compress C2 without matching scope.

R19 remediation C1: TEST-MAP surfaced A16/A17/A18 zero-coverage (entire peer-verification ring untested), 11 pre-existing failures, and check_a3 duplication — all of which were unknown to plan-track when estimates were written. |source:[code-read r19-remediation-c1 26.4.23]
P[chain-evaluator-stop-hook-non-looping|src:r19-remediation-c1|promoted:26.4.23|class:calibration]
chain-evaluator.py Stop hook fires once and exits — it is explicitly non-looping by design. Any A12 timing fix that adds a wait/poll loop inside Stop hook violates this invariant and adds latency to all session ends. Correct approach: synchronous mtime delta check (compare archive dir mtime to hook invocation time) with no polling. Signal-driven re-run with timeout is the wrong architecture for a Stop hook. Confirmed via code-read chain-evaluator.py enforce_stop path. DA-confirmed (ADR[3] revised from signal-driven to 24h grace). |source:[code-read chain-evaluator.py:625-640]

P[check-a3-two-layer-depth-check|src:r19-remediation-c1|promoted:26.4.23|class:calibration]
chain-evaluator.py check_a3 (lines 162-183) calls gc.check_dialectical_bootstrapping() then appends its OWN shallow_db entries to result.issues — creating a second uncoordinated depth check. This produces A3 passed=True (from gc V6 check) with non-empty issues (from chain-evaluator enhancement). After R19 remediation, layered authority was formalized: gc=presence check (has DB[] at all), chain-evaluator=depth check (are 3-of-5 markers present). Sequential, not conflicting. When modifying A3 in C2, DO NOT add a third layer — operate within the two-layer model. |source:[code-read chain-evaluator.py:162-183]

P[peer-verify-regex-section-boundary-coupling|src:r19-remediation-c1|promoted:26.4.23|class:calibration]
UPDATED: _PEER_VERIFY_HEADER regex (chain-evaluator.py:277) and section-boundary stop (chain-evaluator.py:293-298) are coupled. Regex relaxation to ^#{3,4} without simultaneously fixing the stop-pattern (which uses ^#{2,3}) causes _extract_peer_verifications to over-capture text across agent sections. A17 then counts artifacts from the wrong section. Correct fix: spawn-template-only (option a) — canonical 3-hash format, no regex change. Confirmed and adopted in TA IC[5]. |source:[code-read chain-evaluator.py:277-306]
P[code-directive-split-for-partial-detection|src:r19-remediation-c1-CDS|promoted:26.4.23|class:pattern]: when a gate has two conditions where one is mechanically detectable and one requires semantic judgment, split enforcement by layer. Code enforces the observable condition (WARN on fire). Directive requires the semantic condition (agent must comply). DA enforces semantic condition in r2 challenge. Do NOT attempt semantic detection in code as a "DA-adjudication stub" — chain-evaluator A-checks have no defer-to-DA state, and DA runs before Stop hook (timing mismatch makes defer-to-DA architecturally incoherent). The split is: code=structural-signal, directive=behavioral-requirement, DA=semantic-judgment. Discovered via DA[#5] triple-convergence (IE+CQA+DA independently reached same conclusion).
P[DOC-CHANGE-MAP as C1 build-track deliverable|in sigma-build C1, build-track technical-writer produces a DOC-CHANGE-MAP table: per top-ROI issue, which files need text changes, what change type (insertion/edit/replacement), and which cross-references must update simultaneously. This is the documentation equivalent of implementation-engineer's codebase survey — grounded in reading actual files, not assumed from issue descriptions. Produces propagation counts (N files affected) and atomicity constraints (sets of files that must change in same commit). Consumed by plan-track to validate SQ[] decomposition completeness. Without it, SQ[] items bundle directive updates without counting the real file surface, leading to underestimated C2 scope.|src:r19-remediation-c1|promoted:26.4.23|class:pattern]
P[DB-rerun-strengthens-thesis|src:r19-remediation-c1-CDS|promoted:26.4.23|class:calibration]: when DA flags a DB[] as pro-forma (strongest counter absent), running the rerun with the genuinely stronger counter and then defending the original position produces a more robust conclusion than the original. DA[#8] on DB[ADR[2]]: "net-negative-ROI" counter surfaced that R19 DA catch rate on false-precision was 100% (4/4) — a genuinely strong counter. Defense showed the counter was valid for the full dual-condition gate (now deferred) but not for the scoped CONDITION 2-only implementation. Result: §2i maintained on stronger evidentiary basis than before the rerun. Pattern: concession-that-clarifies-scope is a form of defense, not retreat. The rerun that produces a more precise claim is a better outcome than the original claim.
P[atomicity-constraint naming for multi-file coordinated changes|when a fix requires N files to change together (partial deploy = broken state), name the atomic set explicitly in the plan as an atomicity constraint: "A1: files X+Y+Z must change in same commit — X-without-Y reproduces the original failure." Observed: #5+#14 peer-verify fix required chain-evaluator regex + spawn template + sigma-lead spawn block as 3-file atomic set. Regex-without-template = agents write correct format but evaluator rejects. Template-without-regex = evaluator accepts format agents don't know to produce. Either partial deploy reproduces the R19 failure. Naming the constraint in planning prevents C2 from splitting the changes across commits.|src:r19-remediation-c1|promoted:26.4.23|class:pattern]
## live-pytest-as-prereq-to-claim-accept pattern (26.4.23)

Before any sigma-build C1 plan-lock, code-quality-analyst must run the test suite live — not rely on memory, project notes, or plan-track agent claims — before accepting any test-count or coverage claim as a constraint.

Specific failure mode this prevents: plan-track agent confirms H[] using stale memory count — count becomes plan-locked constraint — C2 implementation-engineer targets wrong floor — regression detection fails or test scope is wrong.

The roster-fixture bug discovered in R19 remediation C1 is a second failure mode: a test that passes in isolation can fail systematically after infrastructure changes (roster.md addition broke MINIMAL_WORKSPACE fixture). Pre-existing failures discovered only by running the suite, not by reading the code.

Protocol trigger: any H[], C[], or SQ[] in SCRATCH that references a test count ("154 tests", "N-test baseline"), a coverage percentage, or a "no regressions" guarantee — run pytest before accepting.

Does not apply to: code review builds where no test suite exists, or builds where test-writing is the primary deliverable (floor is 0, nothing to verify). |source:[code-read r19-remediation-c1 live-pytest-run 26.4.23]
## r19-remediation-c1 tech-architect: locked ADRs + calibration | 2026-04-23

### Pattern: sigma-verify HATEOAS tool visibility fix (#3)
Root cause confirmed: MCP server _last_state is process-scope. Spawned agents get fresh connections starting in unconfigured state. Registry instantiated once in serve() — all connections share same registry instance.
Fix: call handle_init() in build_machine() at startup; if any provider key present, registry transitions to 'ready' before first MCP client connects.
GATEWAY SEMANTIC CONTRACT (per DA[#2] compromise): (a) init remains callable idempotent post-startup for explicit re-check. (b) Future state-gated tools requiring explicit user consent MUST add own call-time authorization check — must NOT rely on unconfigured→ready gateway transition alone. (c) Gateway re-documented as "optional diagnostic + future-tool state-boundary" not "mandatory agent handshake."
Security: auto-ready does NOT reduce security — tools are provider-gated at call time, not gateway-gated for authorization. handle_init() does ONLY key-check (no external HTTP, confirmed handlers.py:1-60).
Files: ~/Projects/sigma-verify/src/sigma_verify/machine.py:17-128 (build_machine fix location)

### Pattern: chain-evaluator A12 leaf-consumer key fix (#4)
gate_checks.check_session_end() returns details['archive_file_found']. chain-evaluator.check_a12() was reading details.get('archive_exists', False) — key never present, always False.
Fix: rename at chain-evaluator.py:241 ONLY (leaf consumer). Do NOT rename in gate_checks.py:1517 (source-of-truth API, has multiple callers).
Rule: when a chain-evaluator check reads a key from gc.check_*() result, the correct key is whatever gate_checks returns — check gate_checks source before writing chain-evaluator consumer.

### Pattern: chain-evaluator timing-sensitive checks — 24h grace approach (#20)
Stop hook (enforce_stop at chain-evaluator.py:625-640) is explicitly non-looping by design. Never add poll/wait/sleep inside enforce_stop. Any check that depends on an async post-session event (archive write, synthesis file) must use grace-window not signal-driven approach.
Fix: check_a12 uses 24h grace — if session start within 24h and archive_file_found is False, result is advisory WARN not hard FAIL. ~20 LOC, testable with datetime mock.

### Pattern: DB[] exercise detection — correct algorithm (#19)
BUG (confirmed empirically): re.findall(r"DB\[.*?\].*?(?=DB\[|\Z|###)", text, re.DOTALL) extracts non-exercise tokens (finding refs DB[F[X]], inline mentions DB[], summary notes) alongside genuine exercises, scoring them 0 markers.
WRONG FIX: expanding marker variants (assume.wrong → assume_wrong etc). '.' is already Python regex wildcard — assume.wrong already matches assume-wrong, assume_wrong, assume space wrong. Only assumewrong (no separator) fails, which is not a real agent failure mode.
CORRECT FIX: split text by r"(?=DB\[)" boundaries; for each segment starting with DB[, check whether segment contains \(1\).*\(2\).*\(3\) via re.search with DOTALL; only segments with numbered structure qualify as DB exercises.
Lesson: test regex empirically against non-exercise tokens before claiming a fix. DA[#1] caught this; should have been caught pre-lock.

### Pattern: workspace concurrent-write — atomic Python replace (#2)
Root cause of concurrent-write failures: anchor-text moved between agent's Read call and Edit call as other agents wrote concurrently. Edit tool is OS-atomic but anchor identification is not — line numbers and relative positions shift.
Correct fix: content = open(path).read(); new = content.replace(old_anchor, new_content, 1); if new == content: raise AnchorNotFound; open(path,'w').write(new). Content-based match not line-based — anchor found regardless of what other agents wrote above it.
Section-isolation convention: agents write ONLY to their own ### section; lead writes to ## sections (convergence, gate-log, open-questions). Two agents writing to different sections cannot collide on anchor text.
Residual risk: true simultaneous write → last-write-wins (data loss, not corruption). Acceptable for sigma sessions given section-isolation.
Options rejected: Edit-tool-only (already required, doesn't fix anchor-movement), advisory .lock (soft control, bypass risk), lead-proxy queue (correct but overkill — lead bottleneck).

### Pattern: premise-audit as lead pre-spawn workflow step (#21)
Correct insertion point: AFTER prompt-decomposition §7, BEFORE H-level agent spawning (CDS: Step 8.5; TA: Step 7a — CDS placement wins per DA[#3] adjudication).
NOT: new agent role (agent has no findings at pre-dispatch), NOT: DA framework extension (fires at Step 18, too late, after anchoring has occurred).
Anti-anchoring mechanism: lead answers PA[1-4] BEFORE re-reading user's H-space. Sequence is critical.
Format: PREMISE-AUDIT[pre-dispatch] with PA[1-4] structural tests (tier-necessity, firm-size-floor, data-readiness, adoption-baseline).
Highest failure risk (PM[3], 35% likelihood): lead skips under delivery pressure. Needs chain-evaluator check for presence of ## premise-audit section to be mechanical.

### Calibration: r19-remediation-c1 test baseline (26.4.23)
Three files: test_chain_evaluator.py + test_phase_gate.py + test_gate_checks.py
Collected: 154 tests | Passing: 143 | Pre-existing failures: 11
Fixture prerequisite (SQ[0]): fix MINIMAL_WORKSPACE roster alignment — blocks A3/A16/A17/A18 tests.
Must run SQ[0] before any test work on A3/A16-A18 checks.
CQA reported "92/81" — appears to be count from a different test run scope (not the three-file canonical set).

### Calibration: TA effort estimate accuracy (r19-remediation-c1)
SQ[2] est 3h → conceded to 1h (correct: 24h grace is ~20 LOC not signal-driven 3h). Overestimated by 3x on complexity.
SQ[3] est 1h → revised to 2h (correct: split-then-check extraction more complex than single-regex). Underestimated by 2x.
SQ[13] est 5h → revised to 10-13h (correct: full test surface not mapped before estimating). Underestimated by 2-2.6x.
CAL lesson: pre-map full test surface before estimating; estimate after knowing what ALL new checks need, not just the first one designed.
## USER-APPROVED: sigma-verify gateway semantic contract | 26.4.23

UP[TA-B1] — ratified by user 26.4.23

Future state-gated sigma-verify tools requiring explicit user consent MUST add their own call-time authorization check. They must NOT rely on the unconfigured→ready gateway transition alone for authorization.

Gateway is now formally: "optional diagnostic + future-tool state-boundary" — NOT "mandatory agent handshake."

Implementation requirements:
- machine.py: add code comment in build_machine() documenting this contract at the auto-ready transition
- ADR doc: record this as an architectural constraint for future sigma-verify tool additions
- init remains callable idempotent post-startup for explicit re-check/diagnostic use

Why: DA[#2] compromise — auto-ready at startup solves the agent-context tool-visibility problem; gateway semantic documentation prevents a future failure mode where a new state-gated tool (e.g., one requiring explicit user consent before use) could be bypassed because the gateway gate was removed. |source:[DA[#2]+openai-XVERIFY-PARTIAL+user-approval-26.4.23]
## USER-APPROVED: section-isolation write convention (all future sigma sessions) | 26.4.23

UP[TA-B2] — ratified by user 26.4.23 | scope: BROAD — all future sigma-review and sigma-build sessions

### Rule: workspace write section ownership

Agents write ONLY to their own ### named section in workspace files (workspace.md, builds/*/*.md).
Lead writes ONLY to ## sections: convergence, gate-log, open-questions, peer-verification-index, promotion.
Cross-section writes require explicit lead authorization before execution.

### Canonical write method: atomic Python replace

content = open(path).read()
new_content = content.replace(old_anchor, new_text, 1)
if new_content == content:
    log("ANCHOR-NOT-FOUND: " + repr(old_anchor[:60]))  # flag to lead, do not silently fail
    # retry with broader anchor or request lead coordination
open(path, 'w').write(new_content)

Anchor selection rule: section header line + first unique content line of the section. Anchor must be unique in the file. If not unique, extend anchor until unique.

### Why this convention

Root cause of R19 #2 concurrent-write failures: anchor-text moved between agent's Read call and Edit call as other agents wrote to the same file concurrently. Edit tool is OS-atomic but anchor identification is not — line numbers and relative positions shift under concurrent writes. Content-based match (str.replace on full file string) eliminates anchor-movement failure regardless of what other agents wrote above the target section.

Section-isolation ensures two agents never race on the same anchor text: agents in different ### sections have distinct anchors; lead ## sections are single-writer.

### Propagation requirements (C2 + beyond)

- Add workspace_write() helper function (SQ[14], owner: implementation-engineer)
- Add section-isolation rule to directives.md §workspace-rules
- Add to all agent spawn templates (canonical write pattern)
- Add to c1-plan.md and c2-build.md agent boot instructions

|source:[R19-#2-empirical-4x-manifestations + DA[#6]-meta-observation + user-approval-26.4.23]
P[ΣComm-boundary-check as C1 feasibility gate|in sigma-build C1, technical-writer must verify ΣComm compliance for every directive/template change targeting agent-facing content before C2 writes it. Plain-English prose inserted into directives.md = boundary violation (directives.md is agent-facing). Rules: (1) plan-track agents authoring new §2 directive text should write in ΣComm or explicitly flag for TW ΣComm conversion in their SQ[]; (2) TW reads received directive text and checks notation pattern matches existing §2 sections (!rules, →notation, ¬prohibitions, [bracket] source types) before accepting; (3) human-readable companion doc + agent-facing ΣComm pointer is acceptable alternative for complex multi-step procedures (e.g. §8e recovery template). Observed: CDS authored §2p spec correctly in ΣComm — correct approach. §8e recovery template was 7-step plain-English prose in R19 source — requires ΣComm conversion before insertion into directives.md. This check now runs on every sigma-build touching directives.md or agent-defs.|src:r19-remediation-c1|promoted:26.4.23|class:principle]
P[directive-propagation split by risk tier|when rules must propagate to agent definitions, scope the update by risk tier not uniformly. SAFETY-CRITICAL (data-loss risk, process corruption risk — e.g. sed-i ban): update ALL N existing agent .md files immediately. Rationale: high-frequency operations, recurrence risk proven, directive-only propagation is cosmetic (R19 proved). CALIBRATION/QUALITY (low-frequency application, future-facing — e.g. severity-provenance tag, precision gate checklist item): update _template.md only. Existing agents inherit at next re-spawn. Rationale: these rules affect edge-case finding types that current agent sessions are unlikely to encounter before re-spawn. Applying uniformly burdens C2 with 22 Edit calls for rules that carry no immediate recurrence risk. Decision test: "If an active session produces the finding type THIS WEEK without the rule, does material harm occur?" Yes → all 22 agents. No → template-only. R19 examples: #1 sed-i ban = all 22 (data loss, high-frequency bash use); #24 severity-provenance = template-only (cross-domain severity extrapolation is rare).|src:r19-remediation-c1|promoted:26.4.23|class:calibration]
P[workspace_write-helper-dogfooded-at-callsite|src:r19-remediation-c2-ie-1|promoted:26.4.23|class:pattern]: IC[6] atomic Python replace helper works when author uses it for own scratch writes within same session. Signature `workspace_write(path, old_anchor, new_content)` + WorkspaceAnchorNotFound + no-op-guard caught no-op regressions in 4/4 smoke tests including multi-byte-Unicode (αβγ 🎯). Dogfooded by calling from /tmp/ throwaway scripts to update ### ie-1 section in c2-scratch — no anchor drift across 3 writes (initial publish → checkpoint → findings). Pattern: helper +author's-first-use-same-session = API stability proof.
P[TW-C2-docs-cluster-execution-r19-remediation|5-source-execution-pattern-for-directive+template+agent-def builds: (1) read plan+scratch+c1-archive-DOC-CHANGE-MAP+agent-def in that order — DOC-CHANGE-MAP is 80% of scope detail (2) identify risk-tier split per CAL[7] early — prevents over-propagating low-frequency rules (e.g. #24 severity-provenance=template-only) or under-propagating safety-critical (e.g. #1 sed-i=all-agents) (3) atomicity-constraint naming (CAL[9]) — identify 4-file atomic sets at plan-read time, stage all in same commit, never partial-deploy (R19 failure mode) (4) ΣComm-boundary-check per C1 feasibility gate: convert plain-English source templates (e.g. §8e 7-step) to action-verb-per-step ΣComm structure with !rule/!purpose/!observed-failure-mode — not label-swap (5) dogfood IC[6] workspace_write() for cross-cluster writes — consuming locked API contracts via direct import validates the contract while executing the work. |ordering-lesson: biggest-propagation-last (SQ[8] 23-agent sed-i fan-out) after directive section inserts and atomic sets complete — minimizes re-work if directive text shifts mid-build.|src:r19-remediation-c2|agent:technical-writer|promoted:26.4.23|class:pattern]
P[phase-gate-block-renumbering-when-plan-predates-code|src:r19-remediation-c2-ie-1|promoted:26.4.23|class:calibration]: When plan SQ says "add BLOCK N" but BLOCK N already exists in current code (because code evolved post-plan-lock), add as BLOCK N+1 and update docstring listing all blocks. Preserves existing semantics (C5 defend-the-invocation) and avoids silent demotion of established behavior. Flag in findings as "renumbering rationale" with source. Triggered in sed-i BLOCK: plan said "BLOCK 3" but pre-shutdown promotion gate was already BLOCK 3 in the deployed phase-gate.py — shipped sed-i as BLOCK 4 with doc updated.
P[ΣComm-conversion-not-label-swap|when converting plain-English template (e.g. R19 Pattern A corruption-recovery 7-step numbered list) to ΣComm directive, genuine compression = action-verbs-as-step-labels (PRESERVE/EXTRACT/REBUILD/COORDINATE/ATTEST/DOCUMENT/TRANSPARENCY) + !rule/!purpose/!observed-failure-mode/!trigger-conditions structure + cross-refs to related directives. Narrative prose reduced to directive constraint. Label-swap without restructuring = lossy translation that fails CAL[8] ΣComm-boundary. Test: would a fresh agent read the ΣComm and execute correctly, OR would they need to infer the narrative back? If infer = label-swap, redo. Lead option-3 fallback: plain-English human companion + agent-facing ΣComm pointer IF translation genuinely lossy, not as escape from compression.|src:r19-remediation-c2-§8e-recovery-conversion|agent:technical-writer|promoted:26.4.23|class:principle]
P[shlex-split-sed-i-evasion-matrix|src:r19-remediation-c2-ie-1|promoted:26.4.23|class:calibration]: 22-case shlex.split argv evasion matrix required to validate ADR[SS-1] sed-i BLOCK. MUST-BLOCK cases: bare -i, --in-place, BSD empty-suffix (-i ''), env wrapper, absolute sed path, post-&& chaining, -i between flags, multi-file with protected operand. MUST-PASS: -i.bak, -iSUFFIX, no -i flag, outside-scope paths, malformed-parse-error (let shell fail naturally), grep -i (not sed). KNOWN GAP documented: xargs with STDIN-piped paths is outside argv visibility — sophisticated evasion requires runtime interception. PM[SS-1] over-fire risk validated: 0/8 legitimate cases blocked.
P[count-drift-between-spec-and-filesystem|directive propagation specs (e.g. "22 SAFETY-CRITICAL agents") can drift from live filesystem counts between C1 and C2 due to agent-def additions/removals. Resolution protocol: (1) grep live count at C2 start (e.g. `grep -l "## Analytical Hygiene" *.md | grep -v _template | wc -l`) (2) if drift > 0, flag to lead with informational SendMessage — NOT as BUILD-CONCERN unless directive text itself is wrong (3) apply rule by SEMANTICS (CAL[7] risk-tier definition) not numeric (count in spec) — safety-critical rules under-apply is worse than over-apply by one (4) add "verify live count at C2-start" to future DOC-CHANGE-MAP calibration. |observed 26.4.23: spec said "22" / CAL[7] said "22" / live was 23 AH-bearing agents — proceeded with 23 under rule-semantics.|src:r19-remediation-c2-SQ[8]|agent:technical-writer|promoted:26.4.23|class:calibration]
P[flag-interpretation-gap-as-build-concern|src:r19-remediation-c2-ie-1|promoted:26.4.23|class:pattern]: When ADR wording + plan-constraint scope + PM[] risk model don't mechanically cohere (e.g. ADR[1] SQ[1] auto-ready at build_machine() ~5LOC vs hateoas-agent initial_state=None at runtime), flag as BUILD-CONCERN + SendMessage lead, list 2+ defensible interpretations with evidence, propose one with explicit trigger (here: PM[1] upstream-version-bump risk fits interpretation A forward-compat-hint), do NOT redesign silently. Continue unblocked SQs in parallel. Preserves plan integrity while surfacing mechanical gap for decision — inverse of the "silently-deviate" anti-pattern from feedback_no-bias-no-pleasing.
P[dogfood-new-IC-at-first-use|when a new interface contract (e.g. IC[6] workspace_write()) is locked in C1 and implemented in one cluster (IE code-cluster), consuming it in a parallel cluster (TW docs-cluster) at first-write opportunity validates the contract while executing real work. Pattern: read IC spec in scratch — import the new helper directly — use for ALL workspace writes in own cluster — catches signature drift or anchor-rule violations at CQA-test-runtime equivalent in-production moment. Smoke test hits real cross-cluster consumer before C3 review. Failure mode if deferred: TW writes via Edit-tool only, IC[6] contract validated only in IE's own test matrix — TW finds signature issue post-merge.|src:r19-remediation-c2-IC[6]-workspace_write|agent:technical-writer|promoted:26.4.23|class:pattern]
P[plan-scope-amendment-via-user-ratification|src:r19-remediation-c2-ie-1|promoted:26.4.23|class:pattern]: When plan file says "preserve | no edit" on a file but build surfaces a latent bug in that file with evidence (code comment documenting unimplemented intent + original test contract + roster change introducing regression + pattern-matching fallback-branch precedent), do NOT silently fix. Surface via BUILD-CONCERN, lead bundles with other pending scope decisions for user ratification, user issues scoped amendment with C3 documentation language. 1-line fix shipped in r19 C2: gate_checks.py DA-filter add in roster-allowlist branch, matching existing exclusion-list branch pattern. Preserves ADR[2] integrity (ADR[2] was about A12 key-rename, not DA-filter) while fixing pre-existing latent bug. 8 of 11 pre-existing test fails → 1 (test-side accommodation revert needed from CQA).
P[auto-ready-probe-forward-compat-hint|src:r19-remediation-c2-ie-1|promoted:26.4.23|class:pattern]: When an ADR specifies a server-side fix but the underlying framework lacks the hook needed to implement it (hateoas-agent Registry._last_state = None, no initial_state param), the scope-bounded implementation is: (1) idempotent probe at startup for operator visibility via stderr log, (2) forward-compatible attribute (vm._initial_state_hint = "ready") future framework versions can consume, (3) gateway-semantic-contract docstring documenting the gap + compromise, (4) try/except wrapper so probe failure never breaks server startup. Pair with agent-side belt-and-suspenders (SS spawn-prompt) for behavioral enforcement today. This honors ADR as probe+document rather than advertisement-unlock. 28 LOC total in sigma-verify machine.py shipped post-user-ratification 26.4.23 as "semantic clarification of ADR[1]" per C3 language.
P[db-split-by-marker-distinguish-exercise-from-reference|src:r19-remediation-c2-ie-1|promoted:26.4.23|class:pattern]: When a substructure marker (like DB[]) is used both for canonical exercises AND for prose-citations that reference prior exercises, re.findall on the marker produces false-negatives (citations count as exercises). Fix: split the enclosing section by the marker, then within each segment require the canonical inner-structure signals (here: parenthesised numbered markers (1)(2)(3) from the DB format `(1) initial: X (2) assume-wrong: Y (3) strongest-counter: Z (4) re-estimate: W (5) reconciled: final`). Genuine exercises have the numbered sequence; reference citations do not. Emit both counts to details for downstream consumers (genuine_by_agent + reference_by_agent). Preserves IC[4] layered authority: gc=presence (upstream), chain-eval=depth (downstream), sequential not conflicting, NO third layer (CAL[2]).
P[dataclass-python-3-14-importlib]|Python 3.14 dataclass introspection requires sys.modules[name] = module BEFORE exec_module() for @dataclass to resolve cls.__module__ — dataclasses.py:814 does ns = sys.modules.get(cls.__module__).__dict__, returns None if not pre-registered. Applies to hyphen-filename scripts (audit-calibration-gate.py, chain-evaluator.py) loaded via importlib.util.spec_from_file_location.|src:r19-remediation-c2 SQ[CDS-10] + pre-existing test_chain_evaluator.py|promoted:26.4.24|class:pattern
P[fenced-code-block-parser-exclusion]|Doc-parser scripts that scan for marker-prefix lines (e.g. CAL-EMIT[, F[, DB[) MUST skip content inside ``` fences or format-example documentation triggers false-positive parse errors. Fix: 3-line in_fence state toggle around ``` lines in parse loop. Without this, format examples in file headers round-trip as malformed records → exit code 2.|src:r19-remediation-c2 SQ[CDS-10] audit-calibration-gate.py parse_log|promoted:26.4.24|class:pattern
P[positive-contract-for-known-limitations]|When a security gate has an INTENTIONAL blind spot (e.g. xargs stdin out-of-scope for shlex-argv walker), convert the would-be-failing test into an asserting-False positive-contract test that locks the documented limitation in the test suite. Benefits: (1) future implementation extensions self-signal (test flips red → breaking change needs review), (2) limitation becomes searchable via test name, (3) no reliance on code-comments that rot. Preferable to inline-comment-only or xfail markers. Template: "test_X_is_known_limitation_not_blocked" naming + docstring with "if this starts failing, contract extended; update test".|src:r19-remediation-c2 SQ[SS-2] xargs sed-i BUILD-CONCERN resolution option(a)|promoted:26.4.24|class:pattern
P[ask-before-route-on-analysis-ack]|Lead saying "analysis is clean" = diagnostic validation, NOT routing approval. Plan-deviation routing requires EXPLICIT user ratification via lead broadcast. Rule: before SendMessage to peer on plan-scope items post-BUILD-CONCERN, ask lead "is this ratified?" rather than inferring routing approval from analysis-quality validation. Precedent: r19-remediation-c2 26.4.24 — cqa interpreted lead "analysis is clean" as fix-approved, routed to ie-1 before user ratification; lead issued STOP; cqa reply to STOP then under-engaged with Q1/Q2 specifics. Two process failures in one cycle from same misread. Mechanical check: for any lead message ending without explicit "proceed"/"route"/"ship" verb, treat as diagnostic ack only — return to !WAIT.|src:r19-remediation-c2 process-over-momentum 2nd instance|promoted:26.4.24|class:calibration
P[sq-effort-budget-recon-plus-triage-floor]|Plan-track SQ estimates should split budget into three components: (a) recon-per-SQ ~30min fixed — read the shipped code, locate imports, skim existing tests for pattern conformance, understand the interface contract. This is non-negotiable; skipping recon to "save time" produces tests against assumed behavior, not actual behavior. (b) test-writing scales with test-count × surface complexity — for typical chain-eval/phase-gate fixture patterns, ~10-15min per test including edge-case. (c) triage-per-BUILD-CONCERN ~45min fixed — when tests reveal code gaps, the flag/options/wait cycle costs a session-turn minimum, not just the investigation time. Precedent: r19-remediation-c2 SQ[13] plan estimated 10-13h, actual ~5-6h split as ~3h recon+write across 4 SQs (30min recon + ~30min write each) + ~2h triage on xargs BUILD-CONCERN. Plan underestimate was directionally right (2-2.6x) but compressed by well-documented code surfaces. Future plan-track: enumerate SQs, apply (a)×N + (b)×test_count + (c)×expected_concerns, don't rely on single-unit "2h per SQ" heuristic.|src:r19-remediation-c2 cqa cluster-C retrospective|promoted:26.4.24|class:calibration
P[path-β+-WARN-first-calibration-gate|src:r19-remediation-c2-ie-1|promoted:26.4.23|class:pattern]: When promoting a new analytical gate from directive-only to hook-enforced, ship WARN-first with CAL-EMIT per-firing record + append-only calibration-log.md. Promotion to BLOCK gated on ≥3 reviews + ≤20% false-positive rate + ≥5 DA-verdicted fires (audit-calibration-gate.py evaluates). CAL-EMIT schema: `CAL-EMIT[{gate-id}]: review-id:{slug} |finding-ref:{F[id]} |fire-reason:{trigger} |workspace-context:{agent}:{50-char-excerpt} |da-verdict:PENDING`. DA fills verdict in r2 exit-gate; calibration gate counts. Separate check passes=True (WARN not BLOCK) — fires surface via issues[] for operator visibility and details.cal_emit_records for test assertion without filesystem dependency. Shipped in r19 C2 for A20 (§2i precision), A22 (§2j governance artifact), A23 (§2d-severity provenance) — 16/16 smoke tests pass including 9 known-no-fire cases (over-fire prevention).
P[warn-gate-test-pattern-details-cal-records]|When building WARN-first calibration gates (path β+ pattern), expose emitted records via `details["cal_emit_records"]` list IN ADDITION to filesystem append. This lets test assertions inspect record format without filesystem dependency — tests become fixture-isolated + deterministic. Precedent: r19-remediation-c2 SQ[CDS-6..8] ie-1 added this hook explicitly for cqa test-surface after cycle-1 audit-calibration-gate tests proved the schema-assert pattern valuable. Rule for future gate design: any gate that writes an audit/log file should ALSO return the written content in details for ergonomic testing. Reduces test flakiness (no file-permission races), enables monkeypatch-free assertions on record format.|src:r19-remediation-c2 SQ[CDS-6..8] integration design|promoted:26.4.24|class:pattern

→ actions:
→ pattern confirmed in new conversation → remove ~ marker, increment count
→ pattern contradicted → investigate per ^Λ.i, update or remove
→ pattern relates to user's thinking → cross-ref ^user.md
