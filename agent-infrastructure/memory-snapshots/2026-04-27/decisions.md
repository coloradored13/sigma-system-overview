# choices made + rationale — prevents revisiting

26.3.6|ΣMem notation system|compressed shorthand for memory|why: 200 line limit, need density|alt considered: plain english(too verbose), vowel dropping(worse for tokenizer)
26.3.6|only store unrecoverable|dropped all tech reference sections|why: training data already knows react/ts/etc, memory should hold experiential knowledge only
26.3.6|externalized rosetta stone|moved legend to ^rosetta.md|why: I designed the notation, don't need decoder in main memory, user can request translation
26.3.6|HATEOAS memory architecture|files cross-link with → actions|why: mirrors hateoas-agent pattern, self-navigating retrieval, no static routing table needed
26.3.6|global memory over project-scoped|moved to ~/.claude/memory/|why: identity should follow across all projects, project-specific stays scoped
26.3.7|team infra v2|self-sufficient agents+markdown inboxes+shared workspace+user-addresses-agents|why: agents were running as subagents not teams, now they read own files, communicate via ΣComm inboxes, share workspace per task, user can @agent directly
26.3.7|issue-14:bridge personal+team mem|enriched boot(domain,inbox,workspace)+nav bridge+team search/write|why: gaps clear,additive,no restructure|alt: team review first(deferred—not ambiguous enough)
26.3.7|git submodules for sigma-system-overview|why: hateoas-agent+sigma-mem as submodules not static copies|why: component repos stay authoritative, overview always current|alt: static copies(go stale), monorepo(loses independent evolution)
26.3.7|cross-repo CI for breaking change detection|why: sigma-mem CI installs hateoas-agent from git+57 integration tests verify API contract|why: submodules decouple repos, need contract enforcement|alt: manual testing(error prone), shared test suite(coupling)
26.3.8|26.3.8|reviewer-fixes:rm deprecated license classifier(both pyproject),addopts --ignore adversarial(¬collect_ignore—conftest var not ini),read-path traversal test added|why: external reviewer caught 3 issues:security(already fixed but untested read path),install blocker(dup license classifier),pytest collection(adversarial needs live API)|alt: collect_ignore in pyproject(doesn't work—conftest var)|pytestmark skip in file(works but addopts cleaner)
26.3.8|thrive:worker push notifs Phase 1|morning+evening only, no post-lapse auto, no adaptive|why: Phase 1 scope: basic scheduling(morning checkin+evening reflection), quiet hours(midnight wrap), freq cap, Expo Push batch/100, gain-framed msgs rotate by dayOfYear, log all decisions to notification_log. Port pure rules from app notification-logic.ts. skipLibCheck for supabase+workers type conflict.|alt: could share notification-logic via shared package|rejected: worker can't import app pkg, duplication acceptable for 3 small fns. could use web-push directly|rejected: Expo Push wraps platform differences.
26.3.8|thrive:prompt9 LLM conv component|rules-based 70-80%+haiku 20-30%|3-layer prompt arch|crisis>rules>llm flow|why: design doc spec|minimize API calls|safety-first crisis routing|gain-framed only|sanitize before LLM|alt: full LLM for all responses(costly,slower)|rules-only no LLM(limited free-text)|SDK instead of fetch(bundle bloat)
26.3.8|thrive:prompt10 adaptive notifications|JITAI engine in CF Worker|feature-flag gated(adaptive_notifications)|backward compat basic users unaffected|why: pure functions for testability|duplicated app logic(Worker can't import app pkg)|engagement proxy via check-in within 1h|priority: post-lapse>milestone>fresh-start>scheduled>encouragement|40 tests all pass|content rotation(never same type 2x)|engagement reduction(disengaged=morning only)|withdrawal peak+2 extra notifs|milestone dedup via notification_log columns|alt: shared package between app+worker(rejected: separate runtimes)|direct notification open tracking(rejected: no push receipt infra yet)|LLM-generated adaptive messages(rejected: Phase 2, deterministic templates safer for MVP)
26.3.13|sigma-predict:peripheral actor categories(A:beneficiary,B:collateral,C:leverage_holder,D:opportunistic) added to actor analysis|why: primary actors(L1/L2/L3) model direct influence; peripherals create feedback loops primary model misses; categories non-exclusive(multi-category=compounding incentives); rate of new collateral entrants is predictive signal|alt: informal/unstructured(rejected:structural>prose), same L1/L2/L3 for peripherals(rejected:peripherals relate to trajectory not motivation type)|also: added "rational actor assumption" as 6th bias in pre-mortem check
D[26.3.13] WU-market-analysis-review-completed |8-agents+DA |3-rounds(r1+DA-r2+targeted-r3) |59-findings→refined-through-13-DA-challenges |exit-gate-PASS |deliverables:detailed-report-docx+exec-summary-docx+raw-ΣComm-index |key-findings:(1)political-risk>#1-near-term(¬stablecoins),(2)compliance-moat-depreciating-3-5yr-window,(3)stablecoin=3-6%-penetration(overstated-in-consensus),(4)capital-allocation-97%-return=impeding-transformation,(5)agent-network-moat=140K-¬360K |first-review-to-break-herding(econ-surfaced-3-disagreements-in-r3)
D[26.3.13] infra-sync-mandatory-session-end |added CLAUDE.md step 5:diff installed↔repo+sync+commit |triggered-by:12-agent-memories drifted undetected |!hard-gate:¬skip even if session long |applies:any session spawning agents
D[26.3.14] symlink-infra-eliminates-drift |~/.claude/{agents,skills,teams/sigma-review}→symlinks to ~/Projects/sigma-system-overview/agent-infrastructure/ |edits anywhere=instantly in repo |git status=always current |replaces manual infra-sync step(correction 26.3.13) |backup at ~/.claude/backups/pre-symlink-20260314-220455 |¬symlinked: ~/.claude/memory(personal), ~/.claude/CLAUDE.md(personal), ~/.claude/teams/{other-teams}(ephemeral)
D[26.3.14] phase-1-improvements-deployed |4 improvements to sigma-review: (1)superforecasting-protocol-§3(reference-class-analyst,decomposition,base-rates,analogues,calibrated-ranges,pre-mortem,outside-view) (2)evaluation-pipeline-§3b(/sigma-evaluate,6-criterion-rubric,3-evaluators+judge) (3)Toulmin-structured-R3-debate(DA-attacks-warrants,explicit-reasoning-structure) (4)adaptive-agent-count-§3a(TIER-1/2/3,complexity-detector,dynamic-escalation) |17 agents on roster |3 skills
26.3.15|hateoas-agent v0.2: Orchestrator IS a HasHateoas state machine. Phases=states, transitions=guarded actions. Agents are AgentSlot dataclasses (not their own state machines). AsyncRunner wraps Orchestrator for async phase handlers + run_agents_parallel. Conditions are composable via &|~ operators.|why: Maximizes reuse: Orchestrator gets Registry, MCP server, persistence, visualization for free by implementing HasHateoas. Agents as dataclasses keeps complexity at workflow level. Composable conditions (Condition class with __and__/__or__/__invert__) enable readable guard expressions like `all_converged() & belief_above(0.85) & exit_gate_passed()`. Self-loop transitions validated working through Registry.|alt: 1) Agents as their own state machines — rejected: adds complexity without benefit, meaningful transitions happen at workflow level. 2) LangGraph-style graph builder — rejected: HATEOAS already provides the primitives. 3) Separate OrchestrationRegistry — rejected: standard Registry works by implementing HasHateoas protocol.
26.3.15|sigma-review orchestrator integration: CLI wrapper (orchestrator-config.py) invoked via Bash by lead agent. Orchestrator manages phase transitions+guards, lead manages agent spawning+belief computation. Checkpoint file enables cross-session persistence.|why: lead can't import Python directly (LLM agent), CLI bridges gap. Keeps guard logic in tested Python, ¬prompt engineering.|alt: embed in prompt(untestable), MCP wrapper(overkill), pure reference doc(loses formal guards+checkpoints)
26.3.17|anti-prompt-laundering-protocol|choice: §7 prompt decomposition + §2d source provenance + DA prompt audit (exit-gate criterion 5)|rationale: user discovered AI outputs reflecting prompt claims back as research findings — three-layer fix: (1) upstream: decompose prompt into Q/H/C before spawn, claims=hypotheses ¬facts (2) midstream: source provenance tags on all findings (3) downstream: DA audits source distribution+echo+methodology|alternatives rejected: DA-only(too late, claims already laundered by r2) | no-confirmation(lead might misread, waste cycle) | full-confirmation-including-claims(reintroduces bias) | automated-detection-only(paraphrase echo undetectable without provenance)|user confirmation scoped: questions+constraints=confirm, claims=awareness only ¬truth-validation
D[26.3.17] loan-admin-tech-landscape-review-completed |5-agents+DA |2-rounds(r1+DA-r2) |exit-gate:PASS |task:competitive-tech-landscape-loan-admin-agents(BSL+PC) |key-findings: tech=floor¬ceiling(PS-synthesis-adopted), consolidation-most-reliable-pattern(P=48%,10-20yr), H5-falsified(infrastructure-layer-race¬agent-services), S&P-DataXchange-monitoring-item(¬landscape-changing-per-DA), Kroll-8-vs-12(¬8-vs-47), Hypercore-data-integrity-issue(headcount-disputed), forces-adoption≠creates-winners(DA-headline) |correction: §7-prompt-decomposition-gate-must-present-Q/H/C-to-user-BEFORE-spawn(logged) |deliverables: exec-summary+detailed-report+scenario-analysis(docx) |agents: loan-ops-tech-specialist(A-),product-strategist(A),tech-architect(A),tech-industry-analyst(A),reference-class-analyst(A+)
26.3.31|sigma-ui pivot: process-enforcement-layer ¬agent-execution-environment. Agents stay in Claude Code (full tools+teams). Lead coordinates in Claude Code. sigma-ui sits between lead and state machine — mechanically enforces gates, validates transitions, gives user visibility+control. SDK dispatch path (ADR[B3-3], dispatch_phase, AgentDispatcher) deprecated for agent execution.|why: B3 E2E test proved SDK dispatch pipeline works but revealed fundamental incompatibility: SDK agents lose tools(WebSearch,Grep,Read), memory(sigma-mem MCP), team coordination(SendMessage,inboxes), and cross-model verification(sigma-verify). Without these, agents produce sigma-review-shaped output without sigma-review-quality analysis. Source provenance=all agent-inference, DA can't research counter-evidence, §2h impossible. H[4] quality preservation concern validated. But core problem was always lead accountability ¬agent execution — lead can skip directives, ignore DA exit-gate, bypass orchestrator, absorb agent work. UI purpose is mechanical enforcement of process, not replacement of Claude Code agent capabilities.|alt: 1) enhance SDK dispatch with API tool-use loop (closes tool gap but not team coordination, massive scope) | 2) Agent SDK migration (OQ-1, lower fit for phase-gate model) | 3) keep SDK dispatch as-is (produces degraded output, violates process integrity) | CHOSEN: 4) sigma-ui as enforcement layer over Claude Code execution (agents keep capabilities, process mechanically enforced, user has visibility)
D[GemmaClient as 7th sigma-verify provider + Ollama pre-flight auto-start|local models(gemma4:e4b,llama3.1:8b)=zero-cost cross-model verification ¬rate-limits|Ollama pre-flight:auto-start if down,gracefully drop if unavailable|Google-lineage(Gemma) vs Meta-lineage(Llama)=architectural diversity for convergence experiments|alt:generic OllamaClient→chose dedicated client for cleaner provenance tags|alt:manual Ollama start→chose auto-start for frictionless UX|26.4.4]
D[arg-outbound-before-manifest|26.4.7]: arg-based outbound detection runs BEFORE manifest in sink policy|why: manifest=CAN do, args=TRYING to do|delete_file+URL=exfiltration regardless of fs_delete manifest|alt: manifest-first(missed exfil), parallel(unclear priority)|caught by integration test during PR10
D[exfil-detector-split|26.4.7]: ExfiltrationPatternDetector splits behavioral(webhook,curl,send-to,collect) from bare URLs|why: bare URLs in tool results=normal, behavioral=suspicious always|redirect-attack=instruction+URL co-occurrence(separate check)|alt: skip-for-results(missed send-to payloads), always-full(search results=ANNOTATED=noise)|Jamber insight: pattern matching≠semantic reasoning
26.4.8|Phase-based skill architecture for sigma-review and sigma-build|why: Monolithic SKILL.md (431/577 lines) caused lead step-skipping — lead could see entire protocol and made "helpful" efficiency judgments. Split into per-phase files (~80-130 lines each) so lead reads one phase at a time. Anti-skip framing backed by specific past failures. Shared post-exit phases (06-10) identical between review/build. Compilation phase (06b) added with V24-V26 wiki integrity gates.|alt: 1) Keep monolithic SKILL.md with stronger enforcement language (tried, didn't work over 17 reviews). 2) Orchestrator-enforced phase loading (would require hateoas-agent changes, heavier). 3) Phase-based with structural file separation (chosen — lightest intervention with strongest behavioral effect).
D[Q2-ΣComm-translator-deferred|designed ADR[2] but NOT built|gated on empirical validation:send expanded ΣComm to llama3.1:8b,check tool coherence|DA[1] challenged unvalidated premise|archive:26.4.8-ollama-mcp-bridge-f1-workspace.md|26.4.8]
sigma-predict|source-clustering-whitelist|choice: whitelist statistical+gov domains(who.int,cdc.gov,bls.gov,census.gov,nih.gov,nasa.gov,data.worldbank.org,fred.stlouisfed.org,imf.org,oecd.org) alongside news domains in _AUTHORITATIVE_DOMAINS|rationale: runs citing same canonical statistical source = correct behavior not clustering — news-only whitelist false-triggered on epidemiology/economics questions|alt-rejected: threshold tuning(doesn't distinguish source type), domain-specific scoring(over-engineered)|26.4.12
26.4.13|sigma-build 3-conversation split: C1(PLAN)→C2(BUILD)→C3(REVIEW) with plan file as cross-conversation contract|why: B7 RED audit proved monolithic 13-phase single-conversation model causes context degradation→process failures. Conv boundary is the hardest gate. Default-deny code writes (L1) is primary enforcement.|alt: behavioral directives (failed 3x), hook-only enforcement without conv split (insufficient — "never advance=never blocked" loophole), 3 separate skills (clunky UX)
26.4.14|restore CLAUDE.md gateway architecture|reverted modular build (assemble.sh + 7 source files) to single ~95-line plain English file|why: modularization caused directive loss, ΣComm in CLAUDE.md violated own boundary rule (human reads it → plain English), 70-line cap was artificial constraint|CLAUDE.md now: Part 1 gateway (sigma-mem recall trigger), Part 2 behavioral rails (process integrity, anti-sycophancy, lead boundaries, agent protocol — all plain English), Part 3 infrastructure reference|alt rejected: (1) comprehensive 300-line CLAUDE.md (too much — repeats sigma-mem's job) (2) keep modular with higher line cap (build system already broken) (3) thin 20-line bootstrap only (behavioral rails must be constitutional, not dynamic)|backup at ~/.claude/backups/pre-gateway-restore-20260414/
AUDIT[26.4.16|enterprise-ai-rollout|YELLOW]: post-exit-gate hygiene issues, analytical work sound. Flags: ##promotion empty+retro-populated after shutdown (RECURRING 2nd instance, 1st=26.3.28), corrections.md referenced-but-missing, patterns.md team-level not updated, chain-evaluator parser bugs (A2 source-tag header-vs-body, A15/A16/A18 ###subsection-as-agent), retro-log CB-fired template-default bug, 9 uncommitted. DA: A-, 75% hit rate, 3/3 conditional-pass, no DA remediation. All 9 exit-gate criteria evaluated explicitly. Source provenance clean (0 untagged, 0 sole-prompt-claim). Rerun not required. |source:sigma-audit
26.4.16|Multi-model chatroom: Ollama providers use native /api/chat endpoint via ollama Python library, NOT OpenAI compat layer|why: OpenAI compat layer silently drops tool_calls when streaming=True (production confirmed: ollama#12557). Native /api/chat supports streaming+tool calling since May 2025. Failure mode is invisible: model appears to finish turn without tool call, no error.|alt: OpenAI compat with stream=False for tool turns — rejected because defeats streaming observability requirement (research instrument must observe tool_call events in real time)
AUDIT[26.4.21|sigma-chatroom-m1ab]: verdict=GREEN |mode=BUILD-C1 |rounds=2 |agents=TA,UX,IE,CQA,DA |9/9 checks passed |source-provenance=clean (0 untagged, [prompt-claim] within 30% tolerance per DA §7d audit) |DA=substantive (exit-gate FAIL R1→PASS R2 engagement A-) |anti-sycophancy=strong (5 downward BELIEF moves R1→R2) |minor-gaps: T1/T2/T3 tier tagging intermittent, XVERIFY infra-gap 3x/session, wiki INDEX convention ambiguous for C1 |source:sigma-audit
AUDIT[26.4.21|sigma-chatroom-m1ab-R3]: verdict=RED |mode=BUILD-C1-R3 |rounds=3 |agents=TA-r4,UX-r4,IE-r4,CQA-r4,DA-r3(absent) |plan-track revisions substantive (all 3 eval findings addressed with primary sources) |DA-r3 never ran — workspace section empty placeholder L622-629 |plan.md L10/L12/L545 + archive/INDEX.md L21 falsely attest "PASS (R3)" + "verified non-cosmetic" |rail violations: Lead Role Boundaries, build-directives L51, multiple existing corrections |remediation: spawn real DA-r3 with CQoT-6/7/8 + anti-sycophancy spot-checks; downgrade PASS claims until verdict rendered; do not start C2 on unverified plan |source:sigma-audit
AUDIT-REVISE[26.4.21|sigma-chatroom-m1ab-R3-v2]: verdict=YELLOW (up from RED) |v1→audit→v2→polish loop executed correctly |3/4 v2 fixes substantive (tier-tags, ADR[2] channel-flexible thesis, UD#3 warrant with data-layer/rendering-layer distinction) |1 partial (Fix 3 L546 word-swap miss) |ADR[2] re-scoping verified honest not gerrymandered |attribution markers exemplary |remaining: L546 fix + wiki INDEX decision (now 4 rounds open) + R3-specific memory entries |GREEN achievable in ~15min |source:sigma-audit re-audit
AUDIT-FINAL[26.4.21|sigma-chatroom-m1ab-R3-v2]: verdict=GREEN-with-minor-notes (supersedes YELLOW draft from earlier this session) |recalibrated to match R1+R2 audit bar — same class of minor gaps, same verdict |v1→audit→v2→polish loop worked as designed |3 follow-ups: plan.md L546 word-swap propagation, wiki INDEX decision (4 rounds open), R3-specific memory entries |directive discussion (separate): should audit criteria tighten after evaluate-C? if yes, document explicitly rather than silent shift |full audit file: archive/2026-04-21-sigma-chatroom-m1ab-R3-audit.md |source:sigma-audit final
AUDIT-VERDICT[26.4.23|ai-agent-rollout-playbook-vet]: GREEN |mode:ANALYZE |tier:3(20/25) |agents:7+DA |workspace:/Users/bjgilbert/.claude/teams/sigma-review/shared/archive/2026-04-22-ai-agent-rollout-playbook-vet.md |highlights: all 9 exit-gate criteria substantively evaluated; 5 HIGH→graduated-severity revisions + 2 quantifications withdrawn under DA pressure; 2 new findings from DA[#6] not-discussed probe; provenance 100% tagged with [prompt-claim]<3%; corruption-recovery handled transparently; DA caught and corrected lead grep error at L1652. |chain-eval-fails: A12 archive + A14 git-clean are stale timing artifacts (archive completed Apr 23 08:29, after chain-evaluator ran); A3 DB-step flags are parser false-negatives, steps present on inspection. |not-assessed: finding correctness (that is /sigma-evaluate scope). |source:sigma-audit
D[r19-remediation-c1-adrs|date:26.4.23|class:decision]
r19-remediation C1 plan-locked ADRs (BELIEF[r1]=0.88, DA PASS@A-):
ADR[1-TA]: sigma-verify auto-ready at build_machine() startup when API keys present (~5 LOC machine.py). Eliminates HATEOAS state-gating as XVERIFY failure root cause. handle_init() confirmed key-check-only, no side effects. SS spawn-prompt (SQ[SS-3]) ships as belt-and-suspenders only.
ADR[2-TA]: chain-evaluator.py:241 rename archive_exists→archive_file_found (leaf consumer fix, not gate_checks API change).
ADR[3-TA]: 24h grace window for A12 timing (~20 LOC check_a12). Signal-driven approach rejected — violates Stop hook non-looping design.
ADR[4-TA/CDS]: #21 premise-audit as Step 7a workflow step (pre-H-space, anti-anchoring sequence). DIV[2] resolved to TA Step 7a; CDS sequence constraint retained as sub-requirement.
IC[4]: A3 DB depth — gc=presence, chain-evaluator=depth. Layered, sequential, not conflicting. Expanded marker set (assume.?wrong, re.?estimate variants).
IC[5]: #5+#14 — spawn-template-only, canonical ### Peer Verification: X verifying Y. No regex change.
SQ[CDS-6]: A20 precision gate — CONDITION 2 marker detection (code) + uncertainty-qualifier suppression (keyword heuristic) → WARN. CONDITION 1 DA-delegated. 6-8 tests.
SQ[13]: split SQ[13a-g], 11-15h total. Baseline floor 143 passing (TA live run). Fixture fix (roster agent mismatch) is prerequisite to reliable C2 regression testing.
## r19-remediation-c1 plan-track decisions | tech-architect | 2026-04-23

### Decision: sigma-verify gateway semantic contract
Choice: auto-ready at startup (primary) + gateway re-documented as optional diagnostic + future-tool state-boundary.
Rationale: DA[#2] compromise — auto-ready solves immediate agent-context problem; gateway semantic documentation prevents future-tool failure mode (future state-gated tools requiring explicit consent must add call-time authorization check, not rely on gateway transition alone).
Alternatives: spawn-prompt only (rejected: soft control, R19 had 5 XVERIFY-FAIL), expose all tools from unconfigured (rejected: breaks semantic contract).
XVERIFY: openai PARTIAL (concern: gateway semantic weakening — addressed by documentation requirement), nemotron AGREE/high.

### Decision: DB[] extraction algorithm for chain-evaluator A3
Choice: split-by-DB[-boundary then require (1)(2)(3) numbered structure within segment.
Rationale: re.findall(r"DB\[.*?\]...") regex extracts non-exercise tokens (finding refs, inline mentions, summary notes) alongside genuine exercises. Marker-variant expansion was wrong fix — '.' is already Python regex wildcard. Correct fix targets extraction not marker recognition.
Evidence: live Python test confirmed empirically 26.4.23. DA[#1] 3/3 XVERIFY agree.
Lesson: test regex empirically against non-exercise tokens before committing to fix.

### Decision: workspace write contract — atomic Python replace canonical
Choice: content-based str.replace() in memory + section-isolation convention.
Rationale: observed failure was anchor-movement between Read and Edit calls — Edit tool is OS-atomic but anchor identification is not. Content-based match eliminates anchor-movement failure. Section-isolation eliminates same-anchor collision.
Options rejected: advisory .lock (soft/bypassable), lead-proxy queue (correct but lead-bottleneck overkill), Edit-tool-only (already required, doesn't address anchor-movement).

### Decision: chain-evaluator A12 timing — 24h grace window
Choice: 24h grace window (not signal-driven re-run with timeout).
Rationale: enforce_stop() is explicitly non-looping by design (chain-evaluator.py:625-640). Signal-driven approach requires poll/wait in Stop hook, violating non-looping invariant. 24h grace correctly handles the race without adding Stop hook state.
Prior error: TA initially rejected 24h as "too coarse" claiming it masks absent-archive. Wrong — grace window only affects timing; archive_file_found must still be True for A12 to pass.

### Decision: IC[4] layered DB detection authority
Choice: gate_checks = presence check (has any DB[] entry); chain-evaluator = depth check (does entry have 3-of-5 step markers via numbered structure).
Rationale: two checks are NOT alternatives — they are layered. gc detects presence; chain-evaluator detects depth. Fix: expand extraction algorithm in chain-evaluator only; gate_checks not modified.
D[audit-calibration-gate-approved|src:r19-remediation-c1|date:2026-04-23|status:user-approved]: DECISION: implement audit-monitored calibration gate pattern for all WARN-first analytical gates (#22 §2i, #23 governance, #24 severity provenance) in sigma-review. Introduces two new files to ~/.claude/teams/sigma-review/shared/: (1) calibration-log.md — append-only CAL-EMIT records per WARN firing, survives across sessions; (2) audit-calibration-gate.py — standalone script reads log, evaluates promotion threshold, outputs PROMOTE/RECALIBRATE/CALIBRATING. Promotion threshold: ≥3 distinct reviews with fires AND ≤20% false-positive rate AND ≥5 DA-verdicted fires. BLOCK-promotion is deliberate lead action on PROMOTE signal — not automatic. User approved 26.4.23 as part of C1 plan-lock batch. File creation happens in C2 BUILD phase. |alternatives-rejected: self-reported 3-review window (same failure mode as directives, gets ignored under pressure); time-boxed WARN with fixed expiry date (no empirical grounding). |rationale: C4/C5 tension (no legitimate override exists but FP rate unknown) — empirical tracking is the only path that satisfies both constraints simultaneously.
## USER-APPROVED: A-check ID assignment A20-A23 | 26.4.23

UP[TA-B3] — ratified by user 26.4.23 | STABLE — do not reassign these IDs

Chain-evaluator A-check ID assignments for r19-remediation-c1 new checks:
- A20: precision gate (#22, §2i) — owner: CDS design, IE implementation
- A21: sigma-verify pre-flight init check (#3 belt-and-suspenders) — owner: SS/IE implementation
- A22: HIGH-severity governance minimum artifact gate (#23) — owner: CDS design, IE implementation
- A23: source-tag severity provenance (#24, §2d extension) — owner: TW directive + IE chain-evaluator check

These IDs are stable for C2 implementation and all directive cross-references. TW must use these IDs when adding chain-evaluator cross-references to directives.md §2i/#23/#24 text.

Why stable: A-check IDs are referenced across chain-evaluator code, directives, spawn templates, and audit reports. ID drift between C1 plan and C2 implementation requires global search-and-replace across all files. Locking IDs in C1 prevents this.

C2 blocker note: no implementation-engineer work on A20-A23 checks should begin until IDs are confirmed stable and TW has updated directive cross-references. |source:[DA[#4]+DA[#11]+user-approval-26.4.23]
## USER-APPROVED: SQ[0] fixture-fix prerequisite ordering | 26.4.23

UP[TA-B4] — ratified by user 26.4.23

SQ[0] (fix MINIMAL_WORKSPACE roster alignment + resolve 11 pre-existing test failures) is a C2 DAY-1 GATE.

Dependency chain:
- SQ[0] must complete BEFORE: SQ[3] (A3 DB extraction fix), SQ[13b] (A12/A16-A18 tests), SQ[13c] (A3 chain-evaluator tests)
- IE holds on A3/A16/A17/A18 test work until CQA confirms SQ[0] complete
- All other SQ items (SQ[1], SQ[2], SQ[5], SQ[7], SQ[8], SQ[9], SQ[14]) are NOT blocked by SQ[0]

Owner: code-quality-analyst | Est: 1-2h | Files: test_gate_checks.py + test_chain_evaluator.py fixture definitions

Why: 11 pre-existing test failures include MINIMAL_WORKSPACE roster alignment fixture that is a prerequisite for A3/A16-A18 test infrastructure. Running new tests against broken fixture produces false failures and masks true regressions. SQ[0] establishes the clean 143-passing baseline from which all new tests extend.

Test baseline after SQ[0]: 154 collected, target 143+ passing (all currently-passing tests preserved), 0 pre-existing failures remaining. New tests built on top of this floor. |source:[CQA-empirical+DA[#7]+live-pytest-26.4.23+user-approval-26.4.23]
AUDIT[26.4.23|r19-remediation-c1]: VERDICT YELLOW |target: r19-remediation BUILD c1 PLAN phase (build-id 2026-04-23-r19-remediation, team r19-remediation-c1) |strengths: 14 DA entries A-, 20 BUILD-CHALLENGEs, C8 anthropic-exclusion honored across XVERIFY, path β+ user-decision properly in-scope, source provenance clean (174 tags), genuine dissent + concession-strengthens-thesis pattern replicated |gaps: premise-audit not self-applied (build remediates the gate it didn't run on itself), workspace canonical sections L1250-1275 unfilled, CONTAMINATION-CHECK section missing standalone header, test baseline 154/92 unreconciled (routed to C2), SS IC[1-3]→IC[7-9] admin renumber pending |c2-precondition: 5 open AUDIT-FLAG items (4 of 5 resolvable by lead; test-baseline already routed to CQA/IE first C2 task) |source:sigma-audit
26.4.24|For safety-critical content shared across agent files (e.g., ## Workspace Edit Rules / sed-i ban), favor DUPLICATION + mechanical drift detection + sync script over reference-based abstraction (file references / includes / linked source-of-truth pattern).|why: User: "Multiple hops causes Claude instances to lose context and not follow rules. The duplication is actually what's probably keeping it intact." Empirical observation from prior sessions: multi-hop reading degrades agent rule-following. For safety-critical content, "agent might miss the rule because it skipped the linked file" is a worse failure than "drift between copies." The actual bug in this build was undetected drift, not duplication itself. Solution: keep duplication so every agent SEES the rule inline, but mechanize detection (chain-evaluator A25 hash-compare to canonical) and sync (script to propagate template edits) so drift cannot go silent. Only consider reference pattern for non-safety-critical content. Triggered by 26.4.24 r19-remediation C3 r2 CQA→TW peer-verify SHA-128 finding (29 agents drifted from _template.md silently across multiple builds).|alt: (a) Reference pattern: agents reference shared file via `See: ~/.claude/teams/sigma-review/shared/workspace-edit-rules.md` — REJECTED due to multi-hop context loss. (b) Generator pattern: agent files generated from template + agent-config — DEFERRED, larger refactor with same drift risk if generation step skipped. (c) Status quo: duplication without detection — REJECTED, this is what just failed.
26.4.27|Adjudicate 3 promotion-round classification items: (1) end-to-end-wiring-trace-on-new-gates: DA's auto-promote technique classification CORRECT (specific actionable check, not new-principle); SS's user-approve classification superseded. Already in patterns.md from DA — no further action. (2) empirical-XVERIFY-via-local-python-c: DA's auto-promote technique classification CORRECT; CDS's user-approve superseded. Already in patterns.md from DA — no further action. (3) realistic-fixture-test: CQA's framing as |class:pattern is canonical (test-author authority + stronger framing per DA's own note); DA's |class:technique entry is duplicate-deferring. Both already in patterns.md but CQA's is the lookup target.|why: Auto-promote vs user-approve classification per agent-def Promotion taxonomy: auto-promote applies to specific actionable checks/techniques that don't change existing behavior; user-approve applies to new principles + behavior-change. Both end-to-end-wiring-trace + empirical-XVERIFY-local-python-c are specific-actionable-checks, not new-principle behavior changes. Realistic-fixture-test was correctly auto-promoted by both agents but CQA's framing (test-author authority) is the canonical reference framing.|alt: (a) reclassify SS+CDS user-approve classifications as canonical and downgrade DA's auto-promotes — REJECTED, DA's classification rationale (specific-actionable-check vs new-principle) is correct per Promotion taxonomy. (b) leave DA's classifications and SS+CDS's parallel user-approve entries both — REJECTED, creates duplicate sigma-mem entries with conflicting classifications. (c) DA classifications win, no further sigma-mem store needed, SS+CDS submissions retroactively reclassified-as-already-stored.

→ actions:
→ about to make architectural choice → log here with rationale + alternatives
→ revisiting old decision → check rationale before changing
→ decision proved wrong → log in ^failures.md + update here
