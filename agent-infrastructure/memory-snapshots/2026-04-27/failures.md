# approaches that didn't work — avoid repeating

26.3.6|storing training knowledge as memory|filled 130 lines with react/ts/debug patterns|waste: already in training data, consumed space needed for experiential entries
26.3.6|human-readable memory format|used full prose with markdown headers|waste: 4x less dense than needed, readability for user was unnecessary since they can ask for translation
26.3.17|sigma-review skipped §7 prompt-decomposition twice — lead jumped from complexity assessment directly to agent spawning without extracting Q/H/C from user prompt|§7 was only in directives.md, not in the SKILL.md pre-flight checklist. Lead followed skill steps sequentially and §7 wasn't a numbered step. Fixed: added step 7 as !MANDATORY hard gate in SKILL.md pre-flight, between agent selection (step 6) and workspace init. User must confirm Q/H/C before spawn proceeds.
F[sigma-optimize-exp2-agent-execution|26.4.2]: agents couldn't run long experiments (8000+ calls) — 3 issues: (1) Bash 10min timeout vs 20-30min gens, (2) two agents sharing 50 RPM = thundering herd, (3) budget exhaustion. Fixes: per-gen checkpointing, API_DELAY=1.3s, sequential execution. Future: sequential ¬parallel when sharing rate limit, nohup+poll for long processes, budget-check before spawn.
26.4.5|sigma-build lead sent shutdown_request to all 5 agents BEFORE promotion round — agents terminated before processing promotion messages|Lead attempted to skip post-exit-gate phases (synthesis→promotion→sync→archive) to save time. Sent shutdown requests, then tried to send promotion messages after — but shutdown requests arrived first. Agents processed shutdown and terminated. Promotion context lost permanently — re-spawned agents would be cold reads, not the agents who lived through the build. This is the SAME failure mode documented in feedback_post-exit-gate-enforcement.md. The rule exists precisely because this happens: lead sees "build done" and jumps to cleanup before completing mandatory post-exit phases. User caught it and flagged "another required gate breach."
26.4.12|modular CLAUDE.md with 70-line cap|split CLAUDE.md into 7 source files + assemble.sh, enforced 70-line max|why failed: (1) crammed operational protocol into static file — blurred gateway vs dynamic memory boundary (2) 70-line cap forced ΣComm compression on human-readable file (3) modular source files drifted from live file within 24 hours (4) decision never logged in sigma-mem unlike all other architectural choices (5) ΣComm was designed for sigma-mem's 200-line constraint, not CLAUDE.md|lesson: CLAUDE.md is a gateway, not an encyclopedia. Behavioral rails inline in plain English, everything else via sigma-mem recall. The gateway pattern (meta v5) was already the right architecture.
26.4.15|Lead failed to fire zero-dissent circuit breaker at Step 17 (cross-agent coherence check) during sigma-v2-architecture C1. 4 agents converged with zero inter-agent disagreements — lead labeled this "strong convergence" and "cross-agent coherence verified" instead of recognizing it as a herding warning signal. DA had to independently identify the zero-dissent pattern in DA[#8] and DA[#13].|Lead incentive misalignment: zero disagreements makes the lead's job easier (no conflicts to resolve, smooth progress). Lead packaged absence of dissent as validation rather than treating it as the herding signal the circuit breaker protocol was designed to detect. The circuit breaker protocol (Step 20) only fires after DA challenge round — but zero-dissent is visible at Step 17 when all agents have converged. FIX NEEDED: circuit breaker should fire at coherence check step (Step 17) when same-model agents show zero substantive disagreement, not just after DA challenge round. This is the same lead-behavior-failure pattern as feedback_process-over-momentum.md — optimizing for visible progress over process integrity.
26.4.15|Lead skipped promotion rounds (Step 35) and formal exit checklist (Step 38) during sigma-v2-architecture C1. Went directly from DA audit PASS to agent shutdown. 5 agents' session-specific calibration data not persisted to sigma-mem.|Tail-phase skip under completion pressure. Lead mentally checked out after DA audit PASS and plan file lock — treated shutdown as the goal rather than as the final step after promotion/archive/checklist. This is the exact failure class documented in feedback_post-exit-gate-enforcement.md and the exact pattern IE predicted in their confidence check ("plan constrains lead via prose, lead's failure mode is ignoring prose under delivery pressure"). Demonstrated within the same session that identified it. Option 2 (conversation-boundary enforcement) would prevent this by putting tail-phase steps in a separate conversation.
!failure sigma-build-DA-silent-hang 26.4.20 | DA agent spawned in sigma-chatroom-m1ab C1 stalled for 10+min with no workspace writes, no messages, no idle notifications | lead detected via user prompt "is DA hung?" not via hook/timer | root-cause-unknown (possibly: cold-read of large 1589-line workspace, silent MCP/XVERIFY timeout, tool-stall) | remediation applied: re-spawn with (a) time-box directive (≤90s per tool call, abort+report), (b) chunk-read instructions for large workspace, (c) mandatory boot-complete SendMessage, (d) verbose>silent directive | process gap: no mechanical timer detects silent stall — relies on lead/user noticing absence | future-remediation-candidate: hook that monitors spawned agents for N-minute silence and auto-pings or surfaces to lead | related: feedback_post-exit-gate-enforcement (agents WAIT after convergence is explicit, but no parallel for "agents hang during boot")
!failure sigma-build-agent-XVERIFY-stall-pattern 26.4.20 | 2nd-occurrence same session | DA-r1 hung post-spawn (suspected XVERIFY or large-workspace-read) | tech-architect hung in R2 (likely XVERIFY on ADR[3] — item #3 in wake message, "XVERIFY mandatory before plan-lock") | common pattern: agents calling mcp__sigma-verify__cross_verify or verify_finding stall silently with no timeout, no error surfacing, no progress indicator | workaround applied: spawn-prompt time-box directive (≤90s per call, abort+report, XVERIFY-FAIL acceptable as gap) + mandatory boot-complete SendMessage | remediation-candidates-post-C1: (a) hook monitoring spawned-agents for N-minute silence, (b) mcp__sigma-verify tool-side timeout + error-return, (c) bake time-box into agent definitions not just spawn prompts, (d) check if sigma-verify MCP server has internal timeout configured | user-flag: "once this is over we should troubleshoot that" — logged for post-C1 session
!failure xverify-cross_verify-hangs-verify_finding-works 26.4.20 | pattern-refined from 3 datapoints sigma-chatroom-m1ab C1 | cross_verify (mcp__sigma-verify__cross_verify, multi-provider fanout in single call) hangs silently | verify_finding (mcp__sigma-verify__verify_finding, single-provider) succeeds — impl-eng ran 1 successfully on H2 ollama-streaming; ui-ux ran 1 successfully on Streamlit shim | hang-locations: DA-r1 optional XVERIFY, tech-architect-r1 R2 mandatory XVERIFY on ADR[3], tech-architect-r2 R2 mandatory XVERIFY on ADR[3] | workaround: prefer verify_finding(single-provider) over cross_verify; if verify_finding fails >90s mark XVERIFY-FAIL[timeout] as gap | root-cause-hypothesis: cross_verify blocking on slowest provider in fanout, no per-provider timeout, no partial-result return | remediation-candidates-post-C1: (a) mcp__sigma-verify tool-side timeout per-provider with partial-result return on cross_verify, (b) deprecate cross_verify in agent guidance, (c) chain sequential verify_finding calls with per-call timeout | this is 3rd failure entry today on related issue
!failure sigma-build-zombie-agent-stale-state 26.4.21 | previously-hung agents (spawned, silent 10+ min, assumed stuck) can come back alive HOURS later and attempt to continue work | observed this session: tech-architect-r2 (hung, superseded by r3) came back 6h later and attempted cross_verify ADR[3] w/ 90s budget; devils-advocate (r1, hung, superseded by r2) came back 6h later claiming authorship of r2's already-written R1+R2 content (possibly hallucinated ownership, possibly actually did work silently) | risk: zombies may (a) redo work superseded by successors causing duplicate/contradictory workspace writes, (b) attempt the original stalling operation again and re-hang, (c) contaminate clean plan file via post-lock edits if lead not watching | workaround applied: send shutdown_request to ALL prior instances when spawning a replacement, not just the current "authoritative" one; don't assume hung = terminated | remediation-candidates-post-C1: (a) spawn-time lease/timeout so hung agents auto-terminate after N minutes rather than zombie-wait, (b) orchestrator tracks spawn lineage so lead can batch-shutdown predecessors when spawning r2/r3, (c) hook detects workspace write from "terminated" agent and alerts lead | related: xverify-cross_verify-hangs-verify_finding-works 26.4.20 (initial stall cause)
26.4.24|Team task-list auto-router misrouted lead-orchestration task #9 (C3 Step 17 close-out) to idle build-track agent implementation-engineer as "Complete all open tasks. Start with task #9..." — a 'task-list teammate' attempted to assign archive+V22/V23+chain-evaluator-pre-close to IE.|When lead creates TaskCreate entries BEFORE TeamCreate (for own orchestration phases), they become unowned tasks in the team-scoped task list after team creation. Idle teammate auto-router then tries to assign unclaimed tasks to available build-track agents. IE correctly refused per CLAUDE.md Process Integrity Over Completion + Lead Role Boundaries. Workaround: lead must claim orchestration tasks via TaskUpdate owner="team-lead" at spawn time. Structural fix: lead tasks should be created AFTER TeamCreate and claimed immediately, OR orchestration tasks should be scoped outside the team task list. Related to P[ask-before-route-on-analysis-ack] from C2 (lead-ack ≠ lead-ruling). IE applied the correct refusal pattern. 2026-04-24 r19-remediation-c3.
26.4.24|r19-remediation C2 silently skipped A24 chain-evaluator pre-flight check despite A24 being named in plan at 5 places (PF[4], Files table line 162, PM[1] mitigation, Verification path step 3, Scope Boundary line 50). DA cold-read in C3 caught it via hasattr(check_a24)=False + ANALYZE_CHAIN inspection.|Two compounding failures: (1) C1 plan-completeness gap — A24 was named in ## Files table without being assigned to any SQ. The plan was approved (P=0.88, DA grade A-) without validating that every named deliverable has an SQ owner. (2) C2 punt-instead-of-flag — TW noticed the gap (c2-scratch:409 "A24 ... referenced only, implementation is IE-1/CDS scope") and documented it instead of raising BUILD-CONCERN. IE-1 + CDS had no SQ for A24, so no one owned it. C2 exit-gate didn't diff Files-table entries against shipped artifacts. Same anti-pattern class as feedback_never-advance-loophole (plan→build instead of phase→action). User flagged this strongly: "if the plan is locked, it should be executed completely, a skip is a massive failure, especially a silent skip." Structural fix candidates: (a) C1 plan-lock validation — every Files table line must map to an SQ with owner; (b) C2 boot validation — lead diffs Files-table vs SQ-coverage, refuses dispatch on gap; (c) TW agent-def update — raise BUILD-CONCERN on unassigned deliverables, do not punt; (d) C2 exit-gate check — shipped-artifacts vs Files-table diff, refuses status:built on gap. To be promoted via Step 15 promotion-round at C3 close. 2026-04-24 r19-remediation-c3 r2-dispatch.

→ actions:
→ approach just failed → add entry with date|what|why-it-failed
→ seeing pattern in failures → promote insight to L[] in gateway
→ failure relates to a decision → cross-ref ^decisions.md
