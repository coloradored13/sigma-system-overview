# cross-project and cross-conversation patterns

~[user iterates through refinement loops: propose→challenge→strip→refine|observed in memory design session|1|26.3]
~[user's best ideas come as casual asides ("just a thought, but...")|observed: shorthand idea, hateoas connection|1|26.3]
~[user connects concepts across domains — hateoas-agent→memory architecture|1|26.3]
cross-session review handoff|include commit hash/date verified against|mark each fix: verified-against-source vs inferred|flag confidence on technical suggestions("I believe X works" ≠ "verified X works")|implementer always verify before applying(safety net that caught 2 issues 26.3.8)|stale snapshot + knowledge error = 2 common failure modes
pytest:collect_ignore is conftest.py var ¬ini option|use addopts="--ignore=path" in [tool.pytest.ini_options] instead|or pytestmark=pytest.mark.skip() in file itself
## DA patterns — LMS review (26.3.14)

adjacent-incumbent-expansion-check [SUBMIT]: thesis="unoccupied"→research adjacent incumbents expanding IN | BY Robotics Hub contradicted claim | missed by 5 agents r1 | generalizes: most "unoccupied" = "not-yet-entered-by-adjacent"

planning-data-vs-deployment-data [SUBMIT]: survey "planning to adopt" ≠ deployment reality | MMH 2025: AMR planning 20→30% UP, usage 18→10% DOWN | generalizes: cross-check intent surveys↔deployment data

floor-value-first [SUBMIT]: thesis depends on future convergence → model floor value WITHOUT convergence first | human-only LMS=$400-550M SAM, viable standalone | floor insufficient → thesis fails regardless

DA calibration: hit-rate 14→6held/8partial/0fell=43%+57% | data-backed v4(4/4 vendor-specific landed) | SAM-inflation v3(EA $700M-1B→$450-600M, 40-60% high holds) | reframe=A-grade(TA reframe>defend) | CB v1.0(9 tensions, no-trigger correct)
P[26.3.14] context-contamination-in-long-sessions: independent topics sharing a conversation window bleed into each other's outputs. Observed: BY job prospect (casual discussion) contaminated sigma-review evolution document (system description). Mechanism: when generating "what is this worth?" section, the most salient answer in context was the BY-specific value — not the general value. Salience overwhelmed scope boundaries. Mitigation: (1) scope-declare before generation ("this document is about X, not Y"), (2) post-generation grep for topic contamination, (3) independent topics in separate sessions when output purity matters, (4) reviewer/user catches what generator doesn't |observed:26.3.14-evolution-doc
P[26.3.17] prompt-laundering-in-AI-outputs: user hypotheses enter as prompt context → AI treats as constraints ¬claims → research/code confirms via selective evidence → output carries research authority. Undetectable without provenance tracking. Applies to: sigma-review agents, Claude app memory (observed: QA persona overwriting real identity), any AI research or coding output. Three-layer defense: (1) upstream claim extraction before work begins (2) midstream source tagging on all outputs (3) downstream adversarial audit of source distribution. Related: context-contamination-in-long-sessions (26.3.14), sycophancy in AI outputs. Key insight: wrong *framing* is invisible — wrong *answers* are visible and catchable
P[26.3.17] §7-prompt-decomposition-user-confirmation-gate: extract Q/H/C → present to user → get confirmation → THEN spawn agents | writing to workspace ¬sufficient | agents spawned on unconfirmed decomposition = contaminated → restart required | anti-prompt-laundering gate ¬just documentation | confirmed via user correction 26.3.17
P[R1-validation-bias: sigma-review R1 agents default to idea-validation not premise-challenge|4/4 anchored on prompt "design a game" → all concluded viable|DA caught in R2 but took full adversarial round|user confirmed this was deliberate test of prompt laundering|src:warehouse-lms-analysis 26.3.18|class:anti-pattern]
P[26.3.30|sigma-ui-B3|str-enum-zero-migration]: class X(str, Enum) members compare equal to raw string literals via __eq__ — zero migration cost for existing `if status == "PASSED"` comparisons. Additive promotion: enums can be added to types.py without touching any consuming code. Caveat: `type(x) is str` checks break (not `isinstance`). XVERIFY'd UNANIMOUS (gpt-5.4 + gemini-3.1). Source: [external-verification] T1.
P[26.3.30|sigma-ui-B3|streamlit-on_dismiss-default-silent-bypass]: Streamlit 1.55+ st.dialog default on_dismiss='ignore' — dismiss (click outside or Escape) silently closes dialog WITHOUT triggering rerun. Session state unchanged. Gate/modal logic that depends on dismiss-as-cancel SILENTLY bypasses the gate permanently. Fix: on_dismiss='rerun' required for any dialog that must re-assert on dismiss. Applies to any Streamlit dialog acting as a guard. Source: [code-read] T1. ADR supplement to B2-ADR[8] (Streamlit >= 1.40).
P[26.3.31|sigma-build|sdk-dispatch-process-degradation-confirmed]: SDK dispatch produces sigma-review-SHAPED output without sigma-review-QUALITY analysis. Agents lose: tools (WebSearch/Grep/Read), memory (sigma-mem MCP), team coordination (SendMessage/inboxes), cross-model verification (sigma-verify). All findings collapse to source=agent-inference/T3. DA cannot research counter-evidence. §2h impossible. H[1] quality-preservation concern VALIDATED in B3 E2E test. Pattern: process enforcement (gate/lifecycle) is viable via SDK; agent execution is not. Complement to 26.3.31 decisions.md pivot decision.
P[26.3.29|sigma-build|DA-scope-external-artifacts]: DA must inspect external artifacts the code operates on, not just code. CQA reviewed dispatcher.py thoroughly but missed FIX-1 mechanism mismatch because it didn't inspect agent .md files the dispatcher processes. DA[#1] caught it by reading tech-architect.md:77-105. Pattern: when build processes/transforms external files (prompts, configs, schemas, templates), DA should ask "what does this code operate on?" and inspect a representative sample. This was the highest-impact finding in B1 (P dropped 0.88→0.72 on this alone). NOVEL — not a CQA failure, CQA correctly scoped to code; DA scope must be wider when external artifacts are load-bearing.
P[26.3.29|sigma-build|triple-convergence-dual-assertion]: Triple-convergence (3 independent agents flag same implementation risk) → require behavioral dual-assertion test verifying both sides of the invariant. B1: DA[#4]+CQA-5+IE-4 all flagged _phase_round_registry write-order timing. Correct test: assert registry=expected_value AND assert reset occurred in same test. Single-assertion or run-passes test would miss the failure mode. Triple convergence = high-confidence real risk = test must make the invariant violation mechanically impossible to miss.
P[mechanism-describes-wrong-data-structure|src:sigma-ui-B1|promoted:26.3.29|class:anti-pattern|agent:devils-advocate|approved:user] — plan specifies parsing mechanism against assumed data structure without verifying actual structure. Must inspect BOTH code AND data it operates on. Evidence: DA[#1] caught "subsection-header-match" assuming .md files had ### headers for §2a-§2e — they were inline bullets.
P[deferred-scope-trap|src:sigma-ui-B1|promoted:26.3.29|class:anti-pattern|agent:code-quality-analyst|approved:user] — when feature scope-reduced mid-build to "types only," consuming code retains hardcoded sentinels (None,[],0) that silently produce wrong data for next phase. Audit all consumer code paths for hardcoded sentinels; flag each as must-do in next phase IC notes. Evidence: active_gate=None at review_state.py:347 — correct B1, trap B2.
P[async-per-call-param-never-mutate-shared-state|src:sigma-ui-B1|promoted:26.3.29|class:anti-pattern|agent:implementation-engineer|approved:user] — in async methods on shared instance, never temporarily mutate instance state for per-call overrides when asyncio.gather() can interleave. Thread value as parameter through call chain. Evidence: sigma-ui FIX-2 dispatcher 5-method chain, triple-converged DA+CQA+IE.
P[state-machine-history-tracks-destinations-not-sources|src:sigma-ui-B1|promoted:26.3.29|class:anti-pattern|agent:implementation-engineer|approved:user] — state machine phase history commonly only appends to_phase on transition, leaving from_phases (completed phases) invisible. Timeline/strip UI rendering must explicitly account for this. Evidence: sigma-ui _ReviewSession phase_history only captured to_phases, _phase_round_registry needed to surface completed phases.
P[scope-reduction-orphans-accepted-concessions|src:sigma-ui-B1|promoted:26.3.29|class:process-gap|agent:synthesis-writer|approved:user] — when scope is reduced mid-plan, a protocol step is needed to walk all pending concessions referencing that feature and convert them to explicit B2 IC notes. Without this, concessions become "partially superseded" with no explicit tracking. Complements deferred-scope-trap (code-level) with process-level prevention. Evidence: DA[#2] IC5 scope reduction left IE-5 concession partially orphaned.
P[evolutionary-plateau-then-breakthrough|src:sigma-optimize-aggressive-v1|promoted:26.4.1|class:calibration]: evolutionary search plateaued gen 4-8 (5 gens at best=5.2), broke through gen 9 to 5.6 via single token insertion ("just"). Implication: premature stopping at plateau misses single-mutation breakthroughs. Minimum 2-3 gens beyond plateau before declaring convergence.
P[normalize_and_validate_ip()|standalone IP bypass defense:6 encoding forms(decimal,hex,octal,percent-encoded,IPv4-mapped-IPv6,non-URL-host+port)|post-urlparse,returns typed IPv4/IPv6|None|reusable across Python URL policy|source:F1-build 26.4.8]
P[named-profile-enforcement-gap|security profiles MUST mechanically enforce promises¬just set flags|HARDENED checked 2 booleans while implying full policy|pattern:profile-name>enforcement-scope mismatch=silent security gap|source:F1-build Q5 26.4.8]
P[empirical-validation-gate|DA deferred highest-complexity feature by asking "has anyone tested the premise?"|building attack surface for unvalidated assumption=wrong|test BEFORE build for keystone features|source:F1-build DA[1] 26.4.8]
P[multi-agent-code-read-convergence|4 agents independently found same 6+ IP bypass vectors reading same code path|independent confirmation>single-agent review for security findings|source:F1-build Q4 26.4.8]
P[parallel-implementation-engineers|SQ[] independence→worktree isolation→merge step|naming:implementation-engineer-1,-2,-N|code-quality-analyst owns merge|saves build time TIER-2+|source:F1-build user-identified 26.4.8]
## !false-confidence-trap: unenforced config fields must fail at load time (26.4.8)
src: ollama-mcp-bridge audit remediation, security-specialist plan+build review

1→PATTERN: A config field that is declared+accepted but never enforced is WORSE than the field not existing. Operators read the config surface as a contract — if a field is present, they assume it is active. Silent non-enforcement creates exploitable trust gaps where an operator believes a control is on when it is not.

2→EXAMPLES: query_constraints, allowed_methods, max_payload_bytes, allow_first_contact — all accepted by Pydantic, none enforced in matching logic. An operator setting query_constraints={"token":"abc"} believes outbound query is constrained. It is not.

3→FIX-PATTERN: Two acceptable options only:
  a) Remove the field entirely (breaking change, but honest)
  b) field_validator raises ValueError on any non-default value with explicit "not yet enforced — creates false security confidence" message
  ¬acceptable: keeping field + silent acceptance + no error

4→APPLIES-TO: any security middleware, policy engine, or config surface where declared fields imply enforcement. Applies regardless of language/framework.
## !extraction-vs-policy-layer distinction in security adapters (26.4.8)
src: ollama-mcp-bridge SafePath bare-filename bypass, security-specialist analysis

1→PATTERN: In a two-layer security adapter (extraction → policy), the policy layer may be correct while the extraction layer has gaps. A bug in extraction means the policy is never invoked — the policy's correctness is irrelevant. These are independent failure modes that require independent review.

2→EXAMPLE: PathPolicy.validate_path() correctly rejected relative paths (allow_relative_paths=False). But _extract_paths_from_args() never extracted bare filenames — so validate_path() was never called for them. Extraction gap ≠ policy gap.

3→COROLLARY: When reviewing security adapters, trace the FULL call chain from arg extraction through to policy evaluation. A finding that "the policy is correct" does not close a finding about extraction coverage. They are separate questions.

4→COROLLARY: Extraction heuristics (regex, field-name gates) have inherent coverage limits that policy logic does not. Always document the residual bypass surface of heuristic extractors: what they catch AND what they don't.

5→APPLIES-TO: path validators, URL extractors, recipient validators, taint trackers — any system where value extraction feeds a separate policy check.
P[26.4.9] bare-filename-path-extraction-gate | security gateway argument validation
Field-name semantic gate is the ONLY signal for bare filename path extraction in tool argument validators. Value-pattern (extension regex, basename heuristic) fires only WITHIN a field-name-gated field, never independently.
Why: OR logic (field-name OR value-pattern) creates false positive exposure on generic fields like "input", "format", "destination" that accept non-path strings. Field-name alone is sufficient for the real attack surface (models pass bare filenames in semantically named fields: "path", "filename", "db").
Negative filter mandatory: skip values matching URL prefix (^https?://), email pattern, or IPv4 — prevents double-reporting with URL/recipient adapters.
Constants: frozenset _FILESYSTEM_FIELD_NAMES = {file, filename, path, filepath, source_file, dest_file, output_file, input_file, config_file, db, database}
Source: ollama-mcp-bridge audit remediation, ADR[1], DA challenge concession |source:[independent-research]|
## !XVERIFY severity calibration for security impact claims (26.4.8)
src: ollama-mcp-bridge Q[1] bare filename bypass, XVERIFY PARTIAL from openai:gpt-5.4

1→PATTERN: Security impact claims that say "allows access to arbitrary files" or "enables full traversal" are frequently over-broad when the actual vector depends on downstream behavior. XVERIFY reliably catches these — returning PARTIAL rather than AGREE when the finding is real but the blast-radius framing is overstated.

2→EXAMPLE: Q[1] finding stated bare filename bypass allows access to arbitrary files. XVERIFY(gpt-5.4) agreed the extraction gap was real but challenged "arbitrary" — actual scope depends on MCP server cwd resolution. Correct regrade: HIGH not CRITICAL.

3→CALIBRATION RULE: When a security finding relies on a chain of two or more behaviors (extraction gap AND downstream server behavior AND specific resolution logic), the finding severity should reflect the full chain, not just the first link. "X bypasses Y" is only as severe as what Y was actually preventing — which requires understanding the downstream.

4→APPLIES-TO: all security findings before finalizing severity. Especially: path traversal claims, injection claims, exfiltration claims. Multi-hop chains need full-chain severity assessment.
P[26.4.9] unenforced-config-field → ConfigError-not-warning | security middleware config design
When a config field implies security enforcement that is not yet implemented, raise ConfigError at load time when the field is set to a non-default value. Do NOT accept silently or emit a warning.
Why: Silent acceptance creates a false-confidence trap — operator believes a control is active when it is not. A warning may go unnoticed. ConfigError makes the gap immediately visible and actionable. Fail-closed: operator must explicitly remove the field.
Implementation: @field_validator(mode="after") raising ValueError with message "X is declared but not yet enforced — field rejected to prevent false security confidence."
Applies to: any security gateway, middleware, or policy engine with a forward-looking config surface.
Source: ollama-mcp-bridge audit remediation, ADR[2], confirmed by security-specialist SR[2] |source:[independent-research]|
P[26.4.9] always-fsync-audit-log | security audit durability
In security audit log implementations, use unconditional fsync on every flush rather than conditional-fsync (e.g. only on critical events).
Why: (1) Simpler — no parameter threading or conditional logic. (2) More correct — close() → flush() path gets fsync automatically, covering the clean-session-exit case where no critical event fires in the final batch. (3) Performance cost is negligible: audit flushes are infrequent (every N events or on critical events), not in the hot path. The two-path design saves ~1-10ms per batch flush at the cost of a residual durability gap on clean exits.
Trigger: anywhere an audit/forensic log uses buffered writes with selective immediate-flush.
Source: ollama-mcp-bridge audit remediation, ADR[6], DA[#6] challenge concession, CQA close() point |source:[independent-research]|
P[26.4.9] DA-compromise-enum-split-without-consumer-API-split | sigma-build plan challenge pattern
When DA challenges a telemetry split as "gold-plating," the correct compromise for enum-based state tracking is: add the new enum value (fixes programmatic API — get_tool_states() callers), drop the new consumer-facing summary list (ScanResult.blocked_profile). Two existing layers (enum state + AuditEventType) together provide sufficient distinction without proliferating consumer API surface.
Why: The enum value is load-bearing for programmatic consumers checking tool states. The summary list is a convenience view that duplicates what audit log already provides. In a remediation build, convenience views are scope creep.
Anti-pattern: accepting DA's "gold-plating" label on the enum value itself — that concession loses the programmatic API benefit.
Source: ollama-mcp-bridge audit remediation, ADR[3]/DA[#5] compromise |source:[independent-research]|
P[26.4.9|unimplemented-controls-fail-closed]: unenforced config fields MUST raise ConfigError at load time, not silently accept values. Silent acceptance of unimplemented security controls = false-confidence trap — operator believes control is active when it is not. Implementation: keep fields in model with # NOT_YET_ENFORCED comment + @field_validator raising ValueError for non-default values. Consistent with extra="forbid" philosophy. Confirmed from: ollama-mcp-bridge ADR[2], SR[2] (tech-arch + security-specialist independent convergence). |source:[independent-research]|T1|
P[26.4.9|field-name-gate-over-value-heuristic]: when extracting sensitive values (paths, secrets, PII) from structured args, use field-name as primary gate NOT value content heuristics. Value heuristics (extension pattern, basename regex) produce unacceptable false positives on generic fields (e.g. {"input": "report.csv"}). Field-name frozenset (semantically clear: path, filename, filepath, db, database) is precise and extensible. Negative filter (URL/email/IP) applied after field-name match to prevent double-reporting. DA[#1] challenge surfaced this — initial OR-logic design conceded. |source:[independent-research]|T1|applies-to: security middleware, arg sanitization, taint tracking|
P[26.4.9|plan-track-scope-authority]: plan-track (tech-architect, product-designer) has final authority on scope decisions — including classifying build-track severity ratings when they conflict with a documented architectural compromise. When CQA grades a missing feature as CRITICAL BUG but the absence was an intentional documented DA compromise, tech-architect overrule is correct process. Build-track findings are factually valid but scope judgments belong to plan-track. Confirmed from: ADR[3] ScanResult.blocked_profile — CQA CRITICAL reclassified INFORMATIONAL by tech-architect. |source:[independent-research]|T1|
P[26.4.9|always-on-beats-conditional-security-code]: for security-critical operations (fsync, validation, fail-closed checks), unconditional always-on design beats conditional/parameterized design. Reasons: (1) eliminates edge cases where conditional never fires (e.g. clean session exit without triggering critical event = final buffer lost), (2) simpler code = fewer maintenance paths = lower audit burden, (3) performance penalty usually negligible in security gateway context. When DA and CQA independently recommend simplification in security context — accept. Confirmed from: ADR[6] always-fsync. |source:[independent-research]|T1|
P[26.4.9|xverify-corrects-scope-not-direction]: XVERIFY (external model cross-verification) tends to correct scope/framing of findings rather than reverse direction. In ADR[1]: XVERIFY agreed extraction bypass is real but downgraded CRITICAL→HIGH and added tool-dependency qualifier. Pattern: XVERIFY is most valuable for catching overstatement of severity and narrowness of proposed pattern logic (e.g. regex too narrow). Not a reversal tool — a calibration tool. BELIEF raised after XVERIFY correction because correction strengthened the rationale (0.80→0.82). |source:[external-verification]|T1|
P[build-review-partial-state-machine|src:B6-audit-remediation|promoted:26.4.9|class:pattern]: state-machine-split requires 3-check: type-definition AND all-write-sites AND all-read-consumers | half-split(enum-only)=API-inconsistency | source:security-specialist SR[3]
P[SQ-mechanical-checklist|src:B6-audit-remediation|promoted:26.4.9|class:pattern]: locked-ADR SQ[] items need mechanical checklist in build-track handoff | verbal-spec-alone→missed-items(SQ[4b]) | caught-in-review¬build=late | source:synthesis-agent UC1
P[XVERIFY-PARTIAL-scope-narrowing|src:B6-audit-remediation|promoted:26.4.9|class:pattern|confidence:~tentative(1-datapoint)]: XVERIFY-PARTIAL triggers scope-narrowing(CRITICAL→HIGH) via blast-radius calibration | most-valuable-for-overstatement-correction | BELIEF can rise post-correction(rationale-strengthened) | source:tech-architect UC2
P[pydantic-field-validator-frozen-declared-not-enforced|src:B6-audit-remediation|promoted:26.4.9|class:pattern]: @field_validator(mode="after") on frozen Pydantic model = correct mechanism for "declared but not enforced" field rejection at config load | keeps field in schema + raises ValueError on non-default | source:implementation-engineer
sigma-predict|parse-failure-exclusion-anti-pattern|warning≠fix: PipelineValidator warned on parse_failed=True runs but still passed them to aggregate_runs() — defaulted-to-base-rate runs anchored aggregate toward prior, degrading calibration for all valid runs|fix: filter before aggregation(valid_runs=[r for r in record.runs if not r.parse_failed]) with fallback-to-all if all fail|pattern: validation gates that WARN without EXCLUDING are false safety — the contaminating data still flows downstream|generalizes to: any pipeline stage where bad-input detection is decoupled from bad-input rejection|26.4.12
sigma-predict|spec-code-contamination-control-divergence|pre_mortem.md documented current_estimate as a received input field while forecaster.py:189-202 correctly stripped it — spec and code diverged silently|risk: future authors reading spec re-add the field, breaking the contamination firewall without triggering tests|fix: spec must explicitly document what is NOT passed and why ("You do NOT receive X — this is intentional contamination control")|pattern: contamination controls are only as durable as their documentation — the absence of a field must be as intentional as its presence, and documented with equal clarity|26.4.12
P[audit-infra-wiring|telemetry must live where data originates(duration_ms,tokens) ¬where results consumed|detection: logger instantiated but primary method never in hot path|source:B7-DA[#1]+tech-arch-C1|3-agent-convergence|26.4.13]
P[client-model-singleton|try/finally model mutation=anti-pattern in any client library|fix: cache by (provider,model_id)|detection: old_X=obj.X;obj.X=Y;try/finally obj.X=old_X|source:B7-tech-arch-C2|26.4.13]
P[cross-pollination-dual-frame|capability analysis("what can we add?") misses correctness bugs|correctness audit("what is broken?") misses capability gaps|both frames required separately|source:B7-tech-arch-C3|26.4.13]
P[warning-without-exclusion=false-safety|detection decoupled from rejection→contaminated inputs flow downstream|generalizes to any pipeline where bad-input detection ¬rejection|source:B7-RCA-C5|26.4.13]
P[no-op-test-anti-pattern|distinct from weak-test: if-False branches→production fn never called|asserts MagicMock()≠MagicMock()=trivially true|coverage metrics cannot detect|extends §4d test-integrity|source:B7-CQA-C7|26.4.13]
P[infra-SQ-integration-test-required|infrastructure SQ[] marked "DONE" must have integration test proving consumer→infra→output path|unit tests on isolated component≠integration working|build acceptance criterion|source:B7-DA-P4|26.4.13]
P[docstring-behavior-divergence|docstrings describing aspirational ¬actual behavior=specific tech debt|misleads future developers+LLM agents|DA review checklist addition|source:B7-DA-P5|26.4.13]
P[contamination-control-absence-documentation|intentional prompt input omissions must be documented explicitly|absence of field is load-bearing, fragile to silent re-introduction|source:B7-RCA-C6|26.4.13]
P[DA-calibration|sigma-v2-architecture-C1|21 challenges, 14/21 hit rate (~67%), engagement A, 4 post-disclosure supplementary challenges found genuine blind spots lead missed|26.4.15]
P[memory-without-verification|sigma-v2-architecture-C1|PS replicated lead failure mode: used memory entries without archive verification 3x (100% DA hit rate was conditional, Q/H/C value was undemonstrated, DA decomposition only emerged from XVERIFY). Same error pattern as SQ[1] herding (all agents read feedback file without checking current code). Pattern: internal recall treated as evidence without external verification|26.4.15]
P[code-state-check-required|sigma-v2-architecture-C1|IE found SQ[1] (never-advance loophole) already partially implemented at phase-compliance-enforcer.py:446-488 — all 4 agents + lead missed this by reading feedback file without checking current code. Strongest herding evidence in session: 4 same-model agents processing same input file reached same wrong conclusion. Rule: every SQ must be verified against current codebase before C2 build, not just feedback/correction files|26.4.15]
P[DA-role-decomposition|sigma-v2-architecture-C1|XVERIFY (openai:PARTIAL, google:PARTIAL, deepseek:DISAGREE) found DA role bundles two separable functions: Function A (mechanical exit-gate authority, replicable by orchestrator) + Function B (adversarial content generation, not replicable by checklist). Google's key insight: the gate mechanism may be doing most of the work. First concrete step toward Option 3 (full role decomposition) = move Function A to orchestrator, keep Function B as lighter-weight challenge agent (ADR[8])|26.4.15]
P[single-model-herding|sigma-v2-architecture-C1|DA meta-observation: 5 Claude Opus agents arguing with each other is one model arguing with itself through structured perspectives. XVERIFY calls (DeepSeek, Qwen, GPT, Gemini) were the only genuine cognitive independence. Process adds value via cognitive forcing functions (structured reasoning the lead would skip), but analytical independence is structural not cognitive. Multi-model vision (C4) is the actual solution. Until then, XVERIFY carries the independence load|26.4.15]
P[lead-provenance-contamination|sigma-v2-architecture-C1|Lead wrote DA challenge vectors into spawn prompt, steering DA's analysis. User caught it, lead disclosed to DA. DA issued 4 supplementary challenges from blind spot analysis that found genuine issues lead's vectors missed (DA[#17] untested keep-list, DA[#18] phase-file model untested, DA[#19] DA self-assessment gap, DA[#20] lead vectors self-serving). Lesson: DA spawn prompts should provide workspace context but NOT pre-digested challenge vectors. Let DA form own assessment from cold read|26.4.15]
PATTERN[26.4.16]: retrospective-promotion-after-shutdown RECURRING(2x) | lead writes P[*|promoted:] into agent memories post-shutdown, violates feedback_post-exit-gate-enforcement.md "agents WAIT after convergence" | remediation: mechanical-hook block Stop event if ##promotion empty while agents in-roster | audit-source:sigma-audit 26.4.16
PATTERN[26.4.16]: chain-evaluator-parser-false-FAILs | A2 looks for |source: on F-header not body; A15/A16/A18 count ###subsections inside ##exit-gate/##contamination-check as agents | inflates failure count → trains ignore of real FAILs | remediation: scope-aware parser fix | audit-source:sigma-audit 26.4.16
PATTERN[26.4.16]: CDS journalism-as-T2 for load-bearing claim (F[CDS-5] compliance-theater), reclassified T3 post-DA | recurring per prior-review pattern | remediation: default-T3-for-journalism rule in CDS boot | audit-source:sigma-audit 26.4.16
PATTERN[26.4.16]: DA "not-discussed" challenge type = highest-impact category (2/8 challenges, both hit: Copilot missed-claim, data-quality-for-agentic) | remediation: promote "not-discussed" as standard DA check category | audit-source:sigma-audit 26.4.16
P[ollama-openai-compat-drops-tool-calls-streaming|Ollama /v1/chat/completions silently drops tool_calls when stream=True: finish_reason=stop, empty content, no error|fix tracked ollama#12557, no timeline|native /api/chat supports streaming+tools since May 2025|use ollama Python library ¬openai SDK for Ollama tool-use|src:openclaw#11828,ollama#12557,community-reports|T1-verified|26.4.16|class:pattern]
P[streamlit-async-gen-supported-with-caveat|st.write_stream accepts async generators since recent Streamlit update: internally converts to sync|caveat: generator must NOT depend on cached objects with async references (raises RuntimeError)|queue-based sync bridge preferred over direct async gen pass for reliability|issue #8161 opened+resolved|src:docs.streamlit.io/st.write_stream|T1-verified|26.4.16|class:pattern]
!build-recipe-over-deliverable 26.4.20 | completed process = goal ¬ plan-artifact | user-plan = starting point, stress-test input, team tweaks if better alt found | team also scopes full-build plans based on M1a/b iterations learning | reinforces: process-over-momentum, process-over-speed, synthesis-not-deliverable, never-advance-loophole | applies: all sigma-builds, every C1 plan challenge round, every C3 review | anti-pattern: "plan says X so we ship X" blindly → DA must probe alternatives not just defend plan
!pattern sigma-build-C1-plan-challenge impl-eng [26.4.20]: when plan claims SDK behavior as [independent-research], challenge by (a) grep existing projects for matching patterns, (b) read the SDK wrapper if one exists locally, (c) XVERIFY if load-bearing. Case: sigma-chatroom plan ADR[2] claimed "Ollama /v1 OpenAI-compat emits tool_calls in stream=True" — found ollama-mcp-bridge ollama_client.py:12 inline warning contradicting, gh CLI issue #12557 CLOSED-¬bug-about-/api/chat-not-/v1, XVERIFY gpt-5.4 uncertain(low). Output: new SQ6c 0.25h smoke test blocks SQ14b. Pattern: [code-read-existing-wrapper] > [source-plan-claim]. |src:sigma-chatroom-m1ab C1
!pattern sigma-build-C1-plan-challenge ui-ux-eng [26.4.20]: when source plan has a "deferred prototype" risk-item, stress-test by (a) reading target framework docs for the specific primitive (Streamlit st.write_stream accepted types + failure modes), (b) XVERIFY the load-bearing feasibility claim, (c) trace data flow from producer interface (IC[] from tech-architect) to consumer primitive to find material gaps NOT in the deferred item's scope. Case: sigma-chatroom pre-M2 Streamlit shim → found IC[2] lacks kind=tool_result event post-tool-exec, which no shim resolves (requires tech-architect IC addition). Pattern: deferred prototype ≠ deferred interface gap. Load-bearing UX claims must trace BACK through the producer contract, not just forward through the consumer primitive. |src:sigma-chatroom-m1ab C1
<invoke name="Edit">
<parameter name="file_path">/Users/bjgilbert/.claude/teams/sigma-review/shared/builds/2026-04-20-sigma-chatroom-m1ab/c1-scratch.md
!recipe-over-deliverable-pasta-metaphor 26.4.20 | user anchor: "completed recipe = goal, not plan artifact. Skip/sidestep steps before OR after and we get pasta, not bolognese" | extends build-recipe-over-deliverable: process integrity spans full lifecycle NOT just plan-lock | both ANALYZE and BUILD modes | applies equally to: pre-plan steps (preflight, spawn), plan-challenge steps, post-plan steps (promotion, archive, synthesis, compilation), close-out | lead check at every phase boundary: "which step am I tempted to skip right now?" → that's the one that matters most | anti-pattern: successful-looking output with silent step-skip contaminates provenance | the bolognese test: can the output be recreated from the process artifacts? if no → pasta not bolognese
P[SS-26.4.22-A] multi-agent-production-harm-evidence-tier |26.4.22 |src:security-specialist AI-agent-playbook-review |
Multi-agent trust gap = CONFIRMED structural risk; severity ceiling = MEDIUM-HIGH until T1 (peer-reviewed or regulatory-documented) production harm from chain-of-agent injection or orchestrator-poisoning exists. Available T2 evidence: Salesforce Agentforce orchestrator injection (patched Jan 2026, multi-agent production system), Bing/Sydney orchestrator poisoning (Feb 2023, production deployment). A2A protocol (Google, Linux Foundation, Feb 2026, 50+ partners) signals multi-agent is production-current in 2026 but is standard-setting momentum not production-harm evidence. Apply whenever rating severity of multi-agent security gap findings: MEDIUM-HIGH is the calibrated ceiling under current evidence; do not inflate to HIGH without T1 production incident.
P[SS-26.4.22-B] agent-memory-privacy-gap-2026 |26.4.22 |src:security-specialist AI-agent-playbook-review |
Cross-session agent memory (mem0, LangMem, MemoryOS) is in production 2026 and creates security/privacy gaps not addressed by any current AI agent playbook or regulatory framework: (a) tenant isolation — Tenant A session memory must not appear in Tenant B agent context; (b) right-to-erasure — memory stores must be addressable for deletion per GDPR Art. 17 / CCPA; (c) MNPI contamination — if agent retains MNPI-tagged content from Session 1, must prevent contamination into Session 2 tool calls; (d) untrusted retrieval — memory must be treated as untrusted input on retrieval (same taint level as external retrieved content). No current playbook or regulatory guidance addresses this. Flag in ALL future AI agent reviews and include in sigma-review security checklist.
P[SS-26.4.22-C] sandboxing-threat-model-hierarchy |26.4.22 |src:security-specialist AI-agent-playbook-review |
Threat-model hierarchy for code execution sandboxing in AI agent systems: (1) Firecracker microVM = hardware isolation with minimized attack surface = strongest; (2) Kata Containers = VM-level hardware boundary = strong; (3) gVisor = container-level syscall interception = software boundary = weakest (sophisticated adversary exploiting syscall vulnerability can escape). For AI agents executing code that touches regulated data or is action-capable at Tier 2+, Kata or Firecracker is the correct default over gVisor. Apply whenever reviewing sandboxing choices in AI agent architecture; neither doc reviewed in playbook review provided this threat-model basis for the choice.
P[RCA-26.4.23-1] playbook-unified-form-with-variants |26.4.23 |src:reference-class-analyst AI-agent-playbook-review |
When reviewing AI-rollout playbooks spanning multiple sectors with ~80% spine overlap and ~20% sector divergence, recommend unified phased workbook (execution spine) + unified capability-maturity appendix (capability→tier→evidence reference) + sector annexes. Reduces drift risk, maintenance burden, accidental inconsistency. CAL[H12]=70% unified-with-variants outperforms 2-separate-docs form. Apply whenever multi-sector playbooks are reviewed.

P[RCA-26.4.23-2] phase0-track-a-vs-track-b-decision-tree |26.4.23 |src:reference-class-analyst AI-agent-playbook-review |
Regulated-firm AI rollout playbooks should present explicit Phase 0 decision-tree: Track A = governance-first internal-build 10-13mo for customer-facing/regulated-decision/action-taking agents; Track B = vendor-platform-accelerated 7-10mo (Bedrock Agents / Claude Agent SDK / OpenAI Assistants + layered governance) for internal-advisory/low-blast-radius agents at non-regulated or strong-baseline firms. Evidence: 67% vendor-purchase success vs 33% internal-build success (edgedelta 2024). Scope: regulated-firm AI playbook reviews.

P[RCA-26.4.23-3] branched-timeline-with-precondition-checklist |26.4.23 |src:reference-class-analyst AI-agent-playbook-review |
AI platform build timelines claiming <12mo should REPLACE single-number estimates with explicit existing-infra precondition checklist: working IAM + OTel-capable observability + feature flags + secrets management + SRE maturity. When 3+ items score "no/limited," HARD BRANCH to 18-24mo true-greenfield path with revised cost envelope (+30-50%). Base-rate evidence: JPMC LLM Suite ~24mo conception→launch; MS Debrief 3mo pilot→firmwide BUT infra pre-built (multi-year OpenAI partnership invisible in public reporting).

P[RCA-26.4.23-4] vendor-discontinuity-playbook-phase1-artifact |26.4.23 |src:reference-class-analyst AI-agent-playbook-review |
Any AI-rollout playbook naming Humanloop/Fixie/Adept as cautionary references MUST include concrete operational response: (a) 60-day vendor migration runbook per vendor class (gateway, observability, eval, guardrails, feature flags), (b) quarterly vendor-health review artifact, (c) contractually-mandated data-export clause template. Base rate: 15-25% annual AI-infra vendor discontinuity; cumulative 15-35% over 13mo build → base-rate expectation NOT tail risk. Anchor: Humanloop (Anthropic acqui-hire Aug 2025 → Sep 8 2025 shutdown, <60-day data-deletion window).

P[RCA-26.4.23-5] staffing-clarity-dedicated-vs-ownership |26.4.23 |src:reference-class-analyst AI-agent-playbook-review |
AI-rollout playbooks should REPLACE ambiguous "1-3 FTE" with explicit distinction: "dedicated capacity" vs "named ownership + existing SRE pool + AI-trace training budget." Specific FTE numbers are indicative not load-bearing per DA[#5] compromise (no peer-reviewed study of LLM-agent ops FTE exists as of April 2026). Ambiguity is the real executability gap, not the number itself.

P[RCA-26.4.23-6] incident-rate-framing-qualitative-only |26.4.23 |src:reference-class-analyst AI-agent-playbook-review |
Commercial/marketing materials accompanying AI-rollout playbooks MUST NOT quantify playbook-compliance-vs-governance-light incident differential in pp. Correct framing: "dramatically reduces material AI-incident probability based on correlational evidence from 2024-2025 incident registries; magnitude not precisely characterized pending longitudinal matched-cohort data." Per DA[#8] warrant audit: correlation ≠ causation; specific pp differentials require EXTRAPOLATION warrant that population-level correlational data cannot support. Selection bias severe.

P[RCA-26.4.23-7] pre-mortem-artifact-gating |26.4.23 |src:reference-class-analyst AI-agent-playbook-review |
AI-rollout playbooks should convert narrative-guidance mitigations to ARTIFACT-GATED checkpoints. Evidence: sum-of-independents probability across 7 pre-mortem scenarios (4 RCA + 3 TIA from AI-agent-playbook-review) ~185% mass → base-rate expectation ≥1 fires on un-mitigated path. Each narrative mitigation should specify: (a) specific document/test/review required before advancing, (b) who approves, (c) audit-trail format. "Recommended" is not a gate; mechanical check is.

P[RCA-26.4.23-8] a2a-protocol-governance-appendix-monitoring |26.4.23 |src:reference-class-analyst AI-agent-playbook-review |
Any 2026+ AI-rollout playbook covering multi-agent topologies MUST name A2A protocol (Google, Feb 2026, Linux Foundation stewardship, 50+ enterprise partners incl Salesforce/SAP/ServiceNow) in governance appendix as near-term monitoring item. Three-agent convergence (tech-architect F[TA-A9] + reference-class-analyst H11 verdict + tech-industry-analyst F[TIA-9]) across this review confirms H11 multi-agent gap is live concern not speculative. Customer-driven adoption in B2B SaaS is near-term trigger.
!sigma-eval 26.4.23 AI-agent-rollout playbook vet (workspace.md, 2255L) | GRADE:B (3.14/4) | Acc:3|Comp:3|Log:3|Evid:3|Cal:3|Act:3|Scope:4 | top-weakness: frame-anchoring — H1-H12 tested but premises beneath (tier-structure necessity, firm-size floor, data-readiness, adoption-likelihood) accepted as frame; false precision — F[TA-C2] FTE range withdrawn under DA pressure; gov-actionability-gap — HIGH severity CDS findings stop at gap-ID, no templates | top-strength: systematic uncertainty docs (XVERIFY-FAIL+BELIEF), DA counter-engagement depth, clean scope (0 contamination) | eval_run_by: 3 haiku evaluators + sonnet judge, parallel w/ run_in_background
UP[RA-26.4.22-C] GDPR Art 22 B2B SaaS design fork: For any B2B SaaS firm with EU customer base deploying AI that makes or materially influences decisions about individual workers (performance scores, task allocation, compensation-affecting activity), GDPR Art 22 grants workers rights of explanation, human review, and contestation — running to individual workers directly, NOT fulfilled via the B2B processor agreement with enterprise customer. Forces a binary design choice at Phase 0: either (a) design agents as recommendation-only so "solely automated processing" threshold is not met (requires human-in-loop confirmation on every decision impacting worker), OR (b) implement data-subject-rights fulfillment mechanism — SaaS firm provides enterprise customer with tooling to respond to worker Art 22 requests (explanation of logic, data subject access, contest pathway). Neither option is addressed in most playbooks. Source: GDPR Art 22 T1, EDPB Dec 2024 automated-decision-making opinion T1. Severity varies by firm type — MEDIUM for WMS/labor-performance AI, LOW for pure inventory/advisory agents. |src:ai-agent-rollout-playbook-vet-26.4.22 |agent:regulatory-analyst |user-approved-promotion |originally-failed-sigma-mem-write-contention |class:principle
UP[RA-26.4.22-B] SOC 2 TSC-to-AI-control mapping template: Big 4 AI audit practices in 2026 expect firms to PROPOSE the mapping between SOC 2 Trust Services Criteria and AI-specific controls — auditors are not drafting this. Draft mapping for B2B SaaS firms with AI features: CC6.1 (logical access) + CC6.7 (data transmission) -> AI vendor identity scoping and egress logging; CC7.2 (security incident monitoring) -> AI incident log and kill-switch telemetry; CC9.2 (vendor management) -> foundation-model provider TPRM; A1.2 (availability) -> kill-switch and fallback behavior; C1.1/C1.2 (confidentiality commitments) -> ZDR + no-training contractual terms + tenant isolation in retrieval/embeddings; PI1.4 (processing integrity) -> eval set, judge calibration, drift monitoring. T2 sourcing (Big 4 AI audit frameworks circa 2026 are T2 until formal TSC guidance published). Priority ordering differs by firm type: pure B2B SaaS -> SOC 2 TSC mapping is primary; bank subject to direct examination -> SR 11-7 vocabulary takes priority, TSC mapping becomes vendor-evaluation checklist for their own AI vendors. User should validate draft mapping with their auditor before relying on it. |src:ai-agent-rollout-playbook-vet-26.4.22 |agent:regulatory-analyst |user-approved-promotion |originally-failed-sigma-mem-write-contention |class:pattern |caveat:T2-until-formal-guidance
## technical-writer r19-remediation C1 DOC-CHANGE-MAP (26.4.23)

role: technical-writer build-track feasibility challenge | build: 2026-04-23-r19-remediation

Key propagation findings:
- #1 sed-i ban requires 28-file propagation to be effective (directive-only is cosmetic per R19 evidence)
- #5+#14 peer-verify fix is a 3-file atomic set: c1-plan spawn template + sigma-lead spawn block + chain-evaluator regex — partial deploy reproduces R19 failure
- §8e recovery template requires ΣComm conversion before placement in directives.md (agent-facing boundary rule)
- #22 §2i enforcement tier (DA-enforced-only vs chain-evaluator-mechanical) changes directive text — must decide before C2 doc writes
- 6 BUILD-CHALLENGEs issued to lead, 4 open questions for plan-track (OQ-TW1-4)
- SCRATCH location: ~/.claude/teams/sigma-review/shared/builds/2026-04-23-r19-remediation/c1-scratch.md ## build-track-feasibility ### technical-writer
P[hateoas-deferred-tool-state-gating|src:r19-remediation-c1|promoted:26.4.23|class:pattern]
sigma-verify verify_finding/cross_verify/challenge use from_states=["ready"] in StateMachine — they only appear in MCP tool list AFTER init() triggers state transition. Spawned Agent subprocesses do NOT reinitialize deferred tool registry from tools/list_changed notification. Fix: (B) call init before ToolSearch in spawn prompts (zero-code, immediate); (D) remove from_states restriction + add runtime guard in handlers.py (~15-20 LOC, structural). Both complementary. B fixes immediately; D prevents footgun permanently. Confirmed via code-read: machine.py:17-128, hateoas-agent/mcp_server.py:100-141. |source:[code-read sigma-verify/machine.py:17-128]

P[peer-verify-regex-section-boundary-coupling|src:r19-remediation-c1|promoted:26.4.23|class:calibration]
chain-evaluator.py _PEER_VERIFY_HEADER regex (line 277) and section-boundary stop-pattern (line 293-298) are COUPLED. If regex is relaxed to accept #### headers (from current ^###), the stop-pattern (which stops at ^#{2,3}) misses ####, causing _extract_peer_verifications to over-capture text from the next agent's content. A17 specificity check then counts artifacts from wrong section. Fix options: (a) spawn-template-only (safe, zero risk); (b) regex relaxation (requires simultaneous stop-pattern fix at line 293-298 — must be atomic). Option (a) alone is correct and sufficient. |source:[code-read chain-evaluator.py:277-306]

P[a3-db-step-marker-regex-variants|src:r19-remediation-c1|promoted:26.4.23|class:calibration]
check_a3 in chain-evaluator.py uses re.search(marker, entry) where markers are treated as regexes. "assume.wrong" matches "assumeXwrong" but NOT "assume wrong" (space). "re.estimate" fails for "re-estimate". Agents write natural language variants → false negatives. Fix: expand markers to alternations: r"assume.?wrong|assume wrong", r"re.?estimate|re-estimate". Also fix section boundary from (?=DB[|Z|###) to stop at #{3,} to handle #### subheadings. |source:[code-read chain-evaluator.py:163-183]
P[§2i-precision-gate-CONDITION1-calibration|src:r19-remediation-c1-CDS|promoted:26.4.23|class:calibration]: precision gate CONDITION 1 must be "quantitative claim without uncertainty justification" (point estimate OR range lacking driver breakdown, CI/RC, or qualitative qualifier) — NOT "point estimate without range." The narrower formulation would miss 3/4 R19 failures which were unsupported ranges. XVERIFY[openai:gpt-5.4]+XVERIFY[deepseek:deepseek-v3.2:cloud] both independently surfaced this gap. Applies to any analytical precision gate design. Gate fires on BOTH: CONDITION 1 (any numeric claim without uncertainty justification) AND CONDITION 2 (>70% confidence OR HIGH-severity OR cited in primary recommendation). |confidence-delta:initial-draft→XVERIFY-revised
P[premise-audit-sequence-constraint|src:r19-remediation-c1-CDS|promoted:26.4.23|class:pattern]: premise-audit pre-dispatch (§2p) must specify that lead answers PA[1-4] INDEPENDENTLY before re-reading user's proposed H-space. Sequence is the anti-anchoring mechanism — FORMAT-level intervention that transfers at 70-85% to LLMs. Without sequence constraint, lead reads prompt → anchors → then answers PA[1-4] with same anchoring as agents. DB[H3] self-challenge surfaced this. PA[1]: tier-necessity | PA[2]: firm-size-floor | PA[3]: data-readiness | PA[4]: adoption-baseline. Structural premises only (domain-depth → §2e+DA).
## security-specialist R19-remediation C1 [26.4.23] — plan-track findings

### H1 disposition: CONFIRMED
sigma-verify IS under user control at /Users/bjgilbert/Projects/sigma-verify/. Sub-tools absent from spawned agent contexts because HATEOAS StateMachine (machine.py:24 `gateway_name="init"`) gates them behind `ready` state — intentional state-gating, not a deferred-tool registry bug. Correct in-scope fix: spawn-prompt init instruction + agent-def update. Architecturally correct fix (hateoas-agent auto-init on connection) is separate build, medium priority.

### sed-i enforcement (ADR[1]) — XVERIFY PARTIAL
Decision: PreToolUse Bash BLOCK 3, workspace-path scope (/.claude/teams/|workspace.md), no bypass.
XVERIFY REFINEMENT (openai/gpt-5.4 PARTIAL): use shlex.split() argv tokenization NOT raw string regex. Raw regex evades on: `sed -e 's/x/y/' -i file`, `env sed -i`, `xargs sed -i`, unusual quoting/spacing. Test matrix must cover env/xargs wrapping forms.

### STRIDE findings (generalizable)
T (Tampering): XVERIFY tag presence ≠ authenticity — agent could write fake XVERIFY verdict to workspace. A15 does not detect fabrication. Known gap.
D (DoS): sigma-verify has no programmatic rate limiting on cross_verify across 13 providers. Anthropic 1K RPM is the only bound.
E (EoP): bypassing HATEOAS state to register all tools stateless creates future EoP risk when state-gated write tools are added. Never do this.

### Audit-trail design (#22/#23)
No separate tamper-resistant log needed for internal agent framework. Workspace gate-log + A12 archive = sufficient. Promote to separate log only if framework becomes multi-user or compliance-audited.

### XVERIFY provider calibration (updated)
openai/gpt-5.4 catches implementation-specific evasion paths that llama3.1:8b AGREE misses. Weight openai PARTIAL > llama AGREE on security implementation specifics. Google Gemini 503s during daytime demand spikes — use as third provider, not primary.
## r19-remediation-c1 tech-architect ADRs | 2026-04-23

### ADR[1]: sigma-verify HATEOAS tool visibility (#3)
Root cause: MCP server exposes tools dynamically by state. Spawned agents get fresh connections starting in unconfigured state. Fix: auto-transition to ready at build_machine() startup when any provider API key present. Registry._last_state is process-scope (shared across all MCP connections). Source-confirmed: handle_init() does ONLY key check, no external HTTP at startup. XVERIFY: openai(partial/medium-resolved) + nemotron(agree/high) + google(503-gap).

### ADR[2]: chain-evaluator A12 key mismatch (#4)
chain-evaluator.py:241 reads 'archive_exists'; gate_checks.py:1517 returns 'archive_file_found'. Fix: rename at chain-evaluator.py:241 (leaf consumer), NOT in gate_checks.py:1517 (source-of-truth API). One-line fix.

### ADR[3]: A12 archive timing window (#20)
24h grace-window (auditor suggestion) rejected as too coarse — masks absent-archive scenarios. Signal-driven re-run with 30s timeout fallback: add check_a12_post_archive() called from Stop hook after detecting synthesis_delivered signal in workspace. If archive dir mtime within 60s of Stop hook fire, A12 gets second-chance evaluation.

### ADR[4]: premise-audit architecture (#21)
New agent role rejected (no findings at pre-dispatch, adds cost). DA framework extension fires too late (Step 18). Lead workflow step at Step 7a (before spawn) is correct: output to workspace ## premise-audit, 3-5 frame assumptions tested as distinct from H-hypotheses. Highest failure risk (PM[3] likelihood 35%): CDS should add chain-evaluator check for ## premise-audit presence.

### Key code locations confirmed
- chain-evaluator.py:241 — A12 archive key bug
- chain-evaluator.py:277-280 — peer-verify regex (CORRECT, spawn template was wrong)
- chain-evaluator.py:173-176 — A3 DB step markers (needs expansion)
- gate_checks.py:1517 — archive_file_found key
- sigma-verify/src/sigma_verify/machine.py:17-128 — build_machine() (auto-ready fix location)
- sigma-verify/src/sigma_verify/handlers.py — handle_init() (key-check only, safe to call at startup)
- hateoas-agent/src/hateoas_agent/mcp_server.py:102 — Registry instantiated once per serve() call
## SS-PROMO[1]: sed-i enforcement pattern — workspace + hooks scope (26.4.23)
|source: R19-remediation C1 plan-lock
|finding: PreToolUse Bash BLOCK for `sed -i` without backup extension must scope to BOTH workspace paths (/.claude/teams/) AND hook files (/.claude/hooks/) — not workspace-only. Hook files are edited in C2 build phases and face the same silent-truncation risk. Pattern: `re.search(r'sed\s+-i(?!\s+[\'"])', command) AND re.search(r'\.claude/teams/|workspace\.md|\.claude/hooks/', command)`.
|implementation: shlex.split() argv tokenization (not raw string regex) per XVERIFY openai PARTIAL — handles env wrappers, xargs, unusual quoting. Backup-extension forms always pass.
|calibration: XVERIFY openai/gpt-5.4 PARTIAL caught raw-regex evasion paths that llama3.1:8b AGREE missed. Weight openai PARTIAL over small-model AGREE on implementation-specific security claims.
## empirical-baseline-verification pattern (26.4.23)

When a build plan references a test baseline count as a constraint or gate (e.g., "¬regress to 154-test baseline"), code-quality-analyst MUST run live pytest --collect-only before accepting the claim. Memory-recorded counts decay immediately — infrastructure changes (orchestrator deletion, roster addition, fixture updates) silently change test counts without updating the memory record.

R19 remediation C1 case: project_gate-infrastructure.md claimed 154 tests (written 26.4.16, 7 days old). Live pytest showed 92 tests and 11 pre-existing failures. The plan was built on a number that was 40% wrong. H5 was untestable as stated.

Protocol:
1. Run `pytest --collect-only -q` on all test files BEFORE accepting any test-count claim in a build plan.
2. Run `pytest -q` (full execution) to identify pre-existing failures before attributing failures to changes.
3. If claim diverges from live count: flag as CRITICAL before plan-lock. Do not accept H[] or SQ[] that reference the stale count.
4. Revised baseline = currently-passing count (not total collected). Pre-existing failures are not part of the regression floor.

Applies to: any sigma-build C1 where H[] or C[] references test counts, regression baselines, or coverage percentages. |source:[code-read live-pytest r19-remediation-c1 26.4.23]
## SS-PROMO[2]: ΣVerify HATEOAS state-gating — root cause + fix (26.4.23)
|source: R19-remediation C1 plan-lock, H1 CONFIRMED
|finding: sigma-verify sub-tools absent in spawned agent contexts because HATEOAS StateMachine (machine.py `gateway_name="init"`) gates them behind `ready` state. NOT a deferred-tool registry gap. Each spawned agent has independent MCP connection; init in parent does NOT transition sub-agent to ready. Fix: call handle_init() in build_machine() at startup (auto-ready) — transitions state permanently for all connections when provider keys present. ~5 lines in machine.py. init remains callable as idempotent diagnostic.
|security: DA[#2] compromise — future state-gated tools requiring explicit consent MUST NOT rely on unconfigured→ready gateway alone; must add own call-time authorization check. Document in machine.py.
|XVERIFY-operational: mcp__sigma-verify__init must be called before ToolSearch in spawned agent contexts (current state). After init, verify_finding and cross_verify work. This IS the R19 #3 root cause confirmed in practice.
P[audit-calibration-gate|src:r19-remediation-c1-CDS-β+|promoted:26.4.23|class:pattern]: when a gate must be WARN-first (C4/C5 tension: no legitimate override exists but false-positive rate unknown), emit structured calibration records per WARN firing (CAL-EMIT schema) and implement a standalone cross-session gate that reads them. Self-reported "N-review window" = same failure mode as directives — gets ignored under pressure. Empirical tracking is the only C5-compatible path through C4. Promotion threshold: ≥3 distinct reviews with fires AND ≤20% false-positive rate AND ≥5 DA-verdicted fires. not-reviewed DA verdicts = missing data (not legitimate) — prevents count-inflation. Audit-calibration-gate is standalone script (not chain-evaluator A-check — chain-evaluator is per-session, gate needs cross-session data). BLOCK-promotion remains deliberate lead action on PROMOTE signal. Applies to any analytical gate added WARN-first: §2i precision, §2-governance, §2d-severity, and any future calibration-uncertain gate.
P[CAL-EMIT-schema|src:r19-remediation-c1-CDS-β+|promoted:26.4.23|class:pattern]: calibration record format for WARN-firing analytical gates. Per-firing record: CAL-EMIT[{gate-id}]: review-id:{session-date-slug} |finding-ref:{F[agent-id]} |fire-reason:{trigger-condition-matched} |workspace-context:{agent}:{50-char-excerpt} |da-verdict:PENDING. DA appends post-r2: da-verdict:{legitimate|false-positive|not-reviewed}. Persist to append-only calibration-log.md (not sigma-mem — write-contention risk; not archive — immutable post-session). Add to backup-memory.sh. False-positive classification authority: DA exit-gate verdict, not agent self-report. Promotion threshold uses DA-verdicted fires only.
## SS-PROMO[3]: MCP trust boundary patterns — generalizable (26.4.23)
|source: R19-remediation C1 STRIDE[1] analysis
|T (Tampering): XVERIFY tag presence does NOT equal authenticity. A15 chain-evaluator checks presence of XVERIFY tags in workspace, not whether sigma-verify was actually called. A compromised or non-compliant agent could write a fabricated XVERIFY verdict. Known gap — document explicitly in A15 check description. Future mitigation: MCP call audit log or chain-evaluator checking XVERIFY tags only in post-init sections.
|D (DoS): sigma-verify cross_verify has no programmatic rate limiting across 13 providers. Anthropic 1K RPM is the only practical bound on agent action frequency. Document as known gap; call-count tracking is future enhancement.
|E (EoP): Never bypass HATEOAS state to register all tools as stateless. If future state-gated write tools are added (e.g. "commit findings to external system"), bypassing the unconfigured→ready gate becomes privilege escalation. Future tool authors MUST add own call-time authorization — not rely on gateway state alone (DA[#2] compromise requirement).
|audit-trail design: for internal agent frameworks (not multi-user, not compliance-audited), workspace gate-log + chain-evaluator A-series + session archive (A12) is sufficient. Separate tamper-resistant log is overkill until scope expands.
## test-map-methodology pattern (26.4.23)

In sigma-build C1, code-quality-analyst should produce a TEST-MAP before issuing feasibility challenges. The TEST-MAP structure:

1. Baseline survey: run live pytest, count collected vs passing vs failing. Identify which existing tests cover which checks/modules.
2. Coverage gaps: enumerate which plan-track ADR targets have ZERO existing tests (these need new test surface, not just regression guarding).
3. Per-issue coverage map: for each build issue, state — existing tests that would regress, new tests needed (count + complexity), flakiness risk, and any prerequisite fixes required before new tests are reliable.
4. H5/regression-floor revision: state the empirically verified floor, not the memory-claimed floor.

Value: transforms generic "test coverage" concern into specific, actionable challenge format. Enables plan-track to understand true C2 test burden before locking estimates. Prevents SQ[] underestimates that compress C2 without matching scope.

R19 remediation C1: TEST-MAP surfaced A16/A17/A18 zero-coverage (entire peer-verification ring untested), 11 pre-existing failures, and check_a3 duplication — all of which were unknown to plan-track when estimates were written. |source:[code-read r19-remediation-c1 26.4.23]
P[chain-evaluator-stop-hook-non-looping|src:r19-remediation-c1|promoted:26.4.23|class:calibration]
chain-evaluator.py Stop hook fires once and exits — it is explicitly non-looping by design. Any A12 timing fix that adds a wait/poll loop inside Stop hook violates this invariant and adds latency to all session ends. Correct approach: synchronous mtime delta check (compare archive dir mtime to hook invocation time) with no polling. Signal-driven re-run with timeout is the wrong architecture for a Stop hook. Confirmed via code-read chain-evaluator.py enforce_stop path. DA-confirmed (ADR[3] revised from signal-driven to 24h grace). |source:[code-read chain-evaluator.py:625-640]

P[check-a3-two-layer-depth-check|src:r19-remediation-c1|promoted:26.4.23|class:calibration]
chain-evaluator.py check_a3 (lines 162-183) calls gc.check_dialectical_bootstrapping() then appends its OWN shallow_db entries to result.issues — creating a second uncoordinated depth check. This produces A3 passed=True (from gc V6 check) with non-empty issues (from chain-evaluator enhancement). After R19 remediation, layered authority was formalized: gc=presence check (has DB[] at all), chain-evaluator=depth check (are 3-of-5 markers present). Sequential, not conflicting. When modifying A3 in C2, DO NOT add a third layer — operate within the two-layer model. |source:[code-read chain-evaluator.py:162-183]

P[peer-verify-regex-section-boundary-coupling|src:r19-remediation-c1|promoted:26.4.23|class:calibration]
UPDATED: _PEER_VERIFY_HEADER regex (chain-evaluator.py:277) and section-boundary stop (chain-evaluator.py:293-298) are coupled. Regex relaxation to ^#{3,4} without simultaneously fixing the stop-pattern (which uses ^#{2,3}) causes _extract_peer_verifications to over-capture text across agent sections. A17 then counts artifacts from the wrong section. Correct fix: spawn-template-only (option a) — canonical 3-hash format, no regex change. Confirmed and adopted in TA IC[5]. |source:[code-read chain-evaluator.py:277-306]
P[code-directive-split-for-partial-detection|src:r19-remediation-c1-CDS|promoted:26.4.23|class:pattern]: when a gate has two conditions where one is mechanically detectable and one requires semantic judgment, split enforcement by layer. Code enforces the observable condition (WARN on fire). Directive requires the semantic condition (agent must comply). DA enforces semantic condition in r2 challenge. Do NOT attempt semantic detection in code as a "DA-adjudication stub" — chain-evaluator A-checks have no defer-to-DA state, and DA runs before Stop hook (timing mismatch makes defer-to-DA architecturally incoherent). The split is: code=structural-signal, directive=behavioral-requirement, DA=semantic-judgment. Discovered via DA[#5] triple-convergence (IE+CQA+DA independently reached same conclusion).
P[DOC-CHANGE-MAP as C1 build-track deliverable|in sigma-build C1, build-track technical-writer produces a DOC-CHANGE-MAP table: per top-ROI issue, which files need text changes, what change type (insertion/edit/replacement), and which cross-references must update simultaneously. This is the documentation equivalent of implementation-engineer's codebase survey — grounded in reading actual files, not assumed from issue descriptions. Produces propagation counts (N files affected) and atomicity constraints (sets of files that must change in same commit). Consumed by plan-track to validate SQ[] decomposition completeness. Without it, SQ[] items bundle directive updates without counting the real file surface, leading to underestimated C2 scope.|src:r19-remediation-c1|promoted:26.4.23|class:pattern]
P[DB-rerun-strengthens-thesis|src:r19-remediation-c1-CDS|promoted:26.4.23|class:calibration]: when DA flags a DB[] as pro-forma (strongest counter absent), running the rerun with the genuinely stronger counter and then defending the original position produces a more robust conclusion than the original. DA[#8] on DB[ADR[2]]: "net-negative-ROI" counter surfaced that R19 DA catch rate on false-precision was 100% (4/4) — a genuinely strong counter. Defense showed the counter was valid for the full dual-condition gate (now deferred) but not for the scoped CONDITION 2-only implementation. Result: §2i maintained on stronger evidentiary basis than before the rerun. Pattern: concession-that-clarifies-scope is a form of defense, not retreat. The rerun that produces a more precise claim is a better outcome than the original claim.
P[atomicity-constraint naming for multi-file coordinated changes|when a fix requires N files to change together (partial deploy = broken state), name the atomic set explicitly in the plan as an atomicity constraint: "A1: files X+Y+Z must change in same commit — X-without-Y reproduces the original failure." Observed: #5+#14 peer-verify fix required chain-evaluator regex + spawn template + sigma-lead spawn block as 3-file atomic set. Regex-without-template = agents write correct format but evaluator rejects. Template-without-regex = evaluator accepts format agents don't know to produce. Either partial deploy reproduces the R19 failure. Naming the constraint in planning prevents C2 from splitting the changes across commits.|src:r19-remediation-c1|promoted:26.4.23|class:pattern]
## live-pytest-as-prereq-to-claim-accept pattern (26.4.23)

Before any sigma-build C1 plan-lock, code-quality-analyst must run the test suite live — not rely on memory, project notes, or plan-track agent claims — before accepting any test-count or coverage claim as a constraint.

Specific failure mode this prevents: plan-track agent confirms H[] using stale memory count — count becomes plan-locked constraint — C2 implementation-engineer targets wrong floor — regression detection fails or test scope is wrong.

The roster-fixture bug discovered in R19 remediation C1 is a second failure mode: a test that passes in isolation can fail systematically after infrastructure changes (roster.md addition broke MINIMAL_WORKSPACE fixture). Pre-existing failures discovered only by running the suite, not by reading the code.

Protocol trigger: any H[], C[], or SQ[] in SCRATCH that references a test count ("154 tests", "N-test baseline"), a coverage percentage, or a "no regressions" guarantee — run pytest before accepting.

Does not apply to: code review builds where no test suite exists, or builds where test-writing is the primary deliverable (floor is 0, nothing to verify). |source:[code-read r19-remediation-c1 live-pytest-run 26.4.23]
## r19-remediation-c1 tech-architect: locked ADRs + calibration | 2026-04-23

### Pattern: sigma-verify HATEOAS tool visibility fix (#3)
Root cause confirmed: MCP server _last_state is process-scope. Spawned agents get fresh connections starting in unconfigured state. Registry instantiated once in serve() — all connections share same registry instance.
Fix: call handle_init() in build_machine() at startup; if any provider key present, registry transitions to 'ready' before first MCP client connects.
GATEWAY SEMANTIC CONTRACT (per DA[#2] compromise): (a) init remains callable idempotent post-startup for explicit re-check. (b) Future state-gated tools requiring explicit user consent MUST add own call-time authorization check — must NOT rely on unconfigured→ready gateway transition alone. (c) Gateway re-documented as "optional diagnostic + future-tool state-boundary" not "mandatory agent handshake."
Security: auto-ready does NOT reduce security — tools are provider-gated at call time, not gateway-gated for authorization. handle_init() does ONLY key-check (no external HTTP, confirmed handlers.py:1-60).
Files: ~/Projects/sigma-verify/src/sigma_verify/machine.py:17-128 (build_machine fix location)

### Pattern: chain-evaluator A12 leaf-consumer key fix (#4)
gate_checks.check_session_end() returns details['archive_file_found']. chain-evaluator.check_a12() was reading details.get('archive_exists', False) — key never present, always False.
Fix: rename at chain-evaluator.py:241 ONLY (leaf consumer). Do NOT rename in gate_checks.py:1517 (source-of-truth API, has multiple callers).
Rule: when a chain-evaluator check reads a key from gc.check_*() result, the correct key is whatever gate_checks returns — check gate_checks source before writing chain-evaluator consumer.

### Pattern: chain-evaluator timing-sensitive checks — 24h grace approach (#20)
Stop hook (enforce_stop at chain-evaluator.py:625-640) is explicitly non-looping by design. Never add poll/wait/sleep inside enforce_stop. Any check that depends on an async post-session event (archive write, synthesis file) must use grace-window not signal-driven approach.
Fix: check_a12 uses 24h grace — if session start within 24h and archive_file_found is False, result is advisory WARN not hard FAIL. ~20 LOC, testable with datetime mock.

### Pattern: DB[] exercise detection — correct algorithm (#19)
BUG (confirmed empirically): re.findall(r"DB\[.*?\].*?(?=DB\[|\Z|###)", text, re.DOTALL) extracts non-exercise tokens (finding refs DB[F[X]], inline mentions DB[], summary notes) alongside genuine exercises, scoring them 0 markers.
WRONG FIX: expanding marker variants (assume.wrong → assume_wrong etc). '.' is already Python regex wildcard — assume.wrong already matches assume-wrong, assume_wrong, assume space wrong. Only assumewrong (no separator) fails, which is not a real agent failure mode.
CORRECT FIX: split text by r"(?=DB\[)" boundaries; for each segment starting with DB[, check whether segment contains \(1\).*\(2\).*\(3\) via re.search with DOTALL; only segments with numbered structure qualify as DB exercises.
Lesson: test regex empirically against non-exercise tokens before claiming a fix. DA[#1] caught this; should have been caught pre-lock.

### Pattern: workspace concurrent-write — atomic Python replace (#2)
Root cause of concurrent-write failures: anchor-text moved between agent's Read call and Edit call as other agents wrote concurrently. Edit tool is OS-atomic but anchor identification is not — line numbers and relative positions shift.
Correct fix: content = open(path).read(); new = content.replace(old_anchor, new_content, 1); if new == content: raise AnchorNotFound; open(path,'w').write(new). Content-based match not line-based — anchor found regardless of what other agents wrote above it.
Section-isolation convention: agents write ONLY to their own ### section; lead writes to ## sections (convergence, gate-log, open-questions). Two agents writing to different sections cannot collide on anchor text.
Residual risk: true simultaneous write → last-write-wins (data loss, not corruption). Acceptable for sigma sessions given section-isolation.
Options rejected: Edit-tool-only (already required, doesn't fix anchor-movement), advisory .lock (soft control, bypass risk), lead-proxy queue (correct but overkill — lead bottleneck).

### Pattern: premise-audit as lead pre-spawn workflow step (#21)
Correct insertion point: AFTER prompt-decomposition §7, BEFORE H-level agent spawning (CDS: Step 8.5; TA: Step 7a — CDS placement wins per DA[#3] adjudication).
NOT: new agent role (agent has no findings at pre-dispatch), NOT: DA framework extension (fires at Step 18, too late, after anchoring has occurred).
Anti-anchoring mechanism: lead answers PA[1-4] BEFORE re-reading user's H-space. Sequence is critical.
Format: PREMISE-AUDIT[pre-dispatch] with PA[1-4] structural tests (tier-necessity, firm-size-floor, data-readiness, adoption-baseline).
Highest failure risk (PM[3], 35% likelihood): lead skips under delivery pressure. Needs chain-evaluator check for presence of ## premise-audit section to be mechanical.

### Calibration: r19-remediation-c1 test baseline (26.4.23)
Three files: test_chain_evaluator.py + test_phase_gate.py + test_gate_checks.py
Collected: 154 tests | Passing: 143 | Pre-existing failures: 11
Fixture prerequisite (SQ[0]): fix MINIMAL_WORKSPACE roster alignment — blocks A3/A16/A17/A18 tests.
Must run SQ[0] before any test work on A3/A16-A18 checks.
CQA reported "92/81" — appears to be count from a different test run scope (not the three-file canonical set).

### Calibration: TA effort estimate accuracy (r19-remediation-c1)
SQ[2] est 3h → conceded to 1h (correct: 24h grace is ~20 LOC not signal-driven 3h). Overestimated by 3x on complexity.
SQ[3] est 1h → revised to 2h (correct: split-then-check extraction more complex than single-regex). Underestimated by 2x.
SQ[13] est 5h → revised to 10-13h (correct: full test surface not mapped before estimating). Underestimated by 2-2.6x.
CAL lesson: pre-map full test surface before estimating; estimate after knowing what ALL new checks need, not just the first one designed.
## USER-APPROVED: sigma-verify gateway semantic contract | 26.4.23

UP[TA-B1] — ratified by user 26.4.23

Future state-gated sigma-verify tools requiring explicit user consent MUST add their own call-time authorization check. They must NOT rely on the unconfigured→ready gateway transition alone for authorization.

Gateway is now formally: "optional diagnostic + future-tool state-boundary" — NOT "mandatory agent handshake."

Implementation requirements:
- machine.py: add code comment in build_machine() documenting this contract at the auto-ready transition
- ADR doc: record this as an architectural constraint for future sigma-verify tool additions
- init remains callable idempotent post-startup for explicit re-check/diagnostic use

Why: DA[#2] compromise — auto-ready at startup solves the agent-context tool-visibility problem; gateway semantic documentation prevents a future failure mode where a new state-gated tool (e.g., one requiring explicit user consent before use) could be bypassed because the gateway gate was removed. |source:[DA[#2]+openai-XVERIFY-PARTIAL+user-approval-26.4.23]
## USER-APPROVED: section-isolation write convention (all future sigma sessions) | 26.4.23

UP[TA-B2] — ratified by user 26.4.23 | scope: BROAD — all future sigma-review and sigma-build sessions

### Rule: workspace write section ownership

Agents write ONLY to their own ### named section in workspace files (workspace.md, builds/*/*.md).
Lead writes ONLY to ## sections: convergence, gate-log, open-questions, peer-verification-index, promotion.
Cross-section writes require explicit lead authorization before execution.

### Canonical write method: atomic Python replace

content = open(path).read()
new_content = content.replace(old_anchor, new_text, 1)
if new_content == content:
    log("ANCHOR-NOT-FOUND: " + repr(old_anchor[:60]))  # flag to lead, do not silently fail
    # retry with broader anchor or request lead coordination
open(path, 'w').write(new_content)

Anchor selection rule: section header line + first unique content line of the section. Anchor must be unique in the file. If not unique, extend anchor until unique.

### Why this convention

Root cause of R19 #2 concurrent-write failures: anchor-text moved between agent's Read call and Edit call as other agents wrote to the same file concurrently. Edit tool is OS-atomic but anchor identification is not — line numbers and relative positions shift under concurrent writes. Content-based match (str.replace on full file string) eliminates anchor-movement failure regardless of what other agents wrote above the target section.

Section-isolation ensures two agents never race on the same anchor text: agents in different ### sections have distinct anchors; lead ## sections are single-writer.

### Propagation requirements (C2 + beyond)

- Add workspace_write() helper function (SQ[14], owner: implementation-engineer)
- Add section-isolation rule to directives.md §workspace-rules
- Add to all agent spawn templates (canonical write pattern)
- Add to c1-plan.md and c2-build.md agent boot instructions

|source:[R19-#2-empirical-4x-manifestations + DA[#6]-meta-observation + user-approval-26.4.23]
P[ΣComm-boundary-check as C1 feasibility gate|in sigma-build C1, technical-writer must verify ΣComm compliance for every directive/template change targeting agent-facing content before C2 writes it. Plain-English prose inserted into directives.md = boundary violation (directives.md is agent-facing). Rules: (1) plan-track agents authoring new §2 directive text should write in ΣComm or explicitly flag for TW ΣComm conversion in their SQ[]; (2) TW reads received directive text and checks notation pattern matches existing §2 sections (!rules, →notation, ¬prohibitions, [bracket] source types) before accepting; (3) human-readable companion doc + agent-facing ΣComm pointer is acceptable alternative for complex multi-step procedures (e.g. §8e recovery template). Observed: CDS authored §2p spec correctly in ΣComm — correct approach. §8e recovery template was 7-step plain-English prose in R19 source — requires ΣComm conversion before insertion into directives.md. This check now runs on every sigma-build touching directives.md or agent-defs.|src:r19-remediation-c1|promoted:26.4.23|class:principle]
P[directive-propagation split by risk tier|when rules must propagate to agent definitions, scope the update by risk tier not uniformly. SAFETY-CRITICAL (data-loss risk, process corruption risk — e.g. sed-i ban): update ALL N existing agent .md files immediately. Rationale: high-frequency operations, recurrence risk proven, directive-only propagation is cosmetic (R19 proved). CALIBRATION/QUALITY (low-frequency application, future-facing — e.g. severity-provenance tag, precision gate checklist item): update _template.md only. Existing agents inherit at next re-spawn. Rationale: these rules affect edge-case finding types that current agent sessions are unlikely to encounter before re-spawn. Applying uniformly burdens C2 with 22 Edit calls for rules that carry no immediate recurrence risk. Decision test: "If an active session produces the finding type THIS WEEK without the rule, does material harm occur?" Yes → all 22 agents. No → template-only. R19 examples: #1 sed-i ban = all 22 (data loss, high-frequency bash use); #24 severity-provenance = template-only (cross-domain severity extrapolation is rare).|src:r19-remediation-c1|promoted:26.4.23|class:calibration]
P[workspace_write-helper-dogfooded-at-callsite|src:r19-remediation-c2-ie-1|promoted:26.4.23|class:pattern]: IC[6] atomic Python replace helper works when author uses it for own scratch writes within same session. Signature `workspace_write(path, old_anchor, new_content)` + WorkspaceAnchorNotFound + no-op-guard caught no-op regressions in 4/4 smoke tests including multi-byte-Unicode (αβγ 🎯). Dogfooded by calling from /tmp/ throwaway scripts to update ### ie-1 section in c2-scratch — no anchor drift across 3 writes (initial publish → checkpoint → findings). Pattern: helper +author's-first-use-same-session = API stability proof.
P[TW-C2-docs-cluster-execution-r19-remediation|5-source-execution-pattern-for-directive+template+agent-def builds: (1) read plan+scratch+c1-archive-DOC-CHANGE-MAP+agent-def in that order — DOC-CHANGE-MAP is 80% of scope detail (2) identify risk-tier split per CAL[7] early — prevents over-propagating low-frequency rules (e.g. #24 severity-provenance=template-only) or under-propagating safety-critical (e.g. #1 sed-i=all-agents) (3) atomicity-constraint naming (CAL[9]) — identify 4-file atomic sets at plan-read time, stage all in same commit, never partial-deploy (R19 failure mode) (4) ΣComm-boundary-check per C1 feasibility gate: convert plain-English source templates (e.g. §8e 7-step) to action-verb-per-step ΣComm structure with !rule/!purpose/!observed-failure-mode — not label-swap (5) dogfood IC[6] workspace_write() for cross-cluster writes — consuming locked API contracts via direct import validates the contract while executing the work. |ordering-lesson: biggest-propagation-last (SQ[8] 23-agent sed-i fan-out) after directive section inserts and atomic sets complete — minimizes re-work if directive text shifts mid-build.|src:r19-remediation-c2|agent:technical-writer|promoted:26.4.23|class:pattern]
P[phase-gate-block-renumbering-when-plan-predates-code|src:r19-remediation-c2-ie-1|promoted:26.4.23|class:calibration]: When plan SQ says "add BLOCK N" but BLOCK N already exists in current code (because code evolved post-plan-lock), add as BLOCK N+1 and update docstring listing all blocks. Preserves existing semantics (C5 defend-the-invocation) and avoids silent demotion of established behavior. Flag in findings as "renumbering rationale" with source. Triggered in sed-i BLOCK: plan said "BLOCK 3" but pre-shutdown promotion gate was already BLOCK 3 in the deployed phase-gate.py — shipped sed-i as BLOCK 4 with doc updated.
P[ΣComm-conversion-not-label-swap|when converting plain-English template (e.g. R19 Pattern A corruption-recovery 7-step numbered list) to ΣComm directive, genuine compression = action-verbs-as-step-labels (PRESERVE/EXTRACT/REBUILD/COORDINATE/ATTEST/DOCUMENT/TRANSPARENCY) + !rule/!purpose/!observed-failure-mode/!trigger-conditions structure + cross-refs to related directives. Narrative prose reduced to directive constraint. Label-swap without restructuring = lossy translation that fails CAL[8] ΣComm-boundary. Test: would a fresh agent read the ΣComm and execute correctly, OR would they need to infer the narrative back? If infer = label-swap, redo. Lead option-3 fallback: plain-English human companion + agent-facing ΣComm pointer IF translation genuinely lossy, not as escape from compression.|src:r19-remediation-c2-§8e-recovery-conversion|agent:technical-writer|promoted:26.4.23|class:principle]
P[shlex-split-sed-i-evasion-matrix|src:r19-remediation-c2-ie-1|promoted:26.4.23|class:calibration]: 22-case shlex.split argv evasion matrix required to validate ADR[SS-1] sed-i BLOCK. MUST-BLOCK cases: bare -i, --in-place, BSD empty-suffix (-i ''), env wrapper, absolute sed path, post-&& chaining, -i between flags, multi-file with protected operand. MUST-PASS: -i.bak, -iSUFFIX, no -i flag, outside-scope paths, malformed-parse-error (let shell fail naturally), grep -i (not sed). KNOWN GAP documented: xargs with STDIN-piped paths is outside argv visibility — sophisticated evasion requires runtime interception. PM[SS-1] over-fire risk validated: 0/8 legitimate cases blocked.
P[count-drift-between-spec-and-filesystem|directive propagation specs (e.g. "22 SAFETY-CRITICAL agents") can drift from live filesystem counts between C1 and C2 due to agent-def additions/removals. Resolution protocol: (1) grep live count at C2 start (e.g. `grep -l "## Analytical Hygiene" *.md | grep -v _template | wc -l`) (2) if drift > 0, flag to lead with informational SendMessage — NOT as BUILD-CONCERN unless directive text itself is wrong (3) apply rule by SEMANTICS (CAL[7] risk-tier definition) not numeric (count in spec) — safety-critical rules under-apply is worse than over-apply by one (4) add "verify live count at C2-start" to future DOC-CHANGE-MAP calibration. |observed 26.4.23: spec said "22" / CAL[7] said "22" / live was 23 AH-bearing agents — proceeded with 23 under rule-semantics.|src:r19-remediation-c2-SQ[8]|agent:technical-writer|promoted:26.4.23|class:calibration]
P[flag-interpretation-gap-as-build-concern|src:r19-remediation-c2-ie-1|promoted:26.4.23|class:pattern]: When ADR wording + plan-constraint scope + PM[] risk model don't mechanically cohere (e.g. ADR[1] SQ[1] auto-ready at build_machine() ~5LOC vs hateoas-agent initial_state=None at runtime), flag as BUILD-CONCERN + SendMessage lead, list 2+ defensible interpretations with evidence, propose one with explicit trigger (here: PM[1] upstream-version-bump risk fits interpretation A forward-compat-hint), do NOT redesign silently. Continue unblocked SQs in parallel. Preserves plan integrity while surfacing mechanical gap for decision — inverse of the "silently-deviate" anti-pattern from feedback_no-bias-no-pleasing.
P[dogfood-new-IC-at-first-use|when a new interface contract (e.g. IC[6] workspace_write()) is locked in C1 and implemented in one cluster (IE code-cluster), consuming it in a parallel cluster (TW docs-cluster) at first-write opportunity validates the contract while executing real work. Pattern: read IC spec in scratch — import the new helper directly — use for ALL workspace writes in own cluster — catches signature drift or anchor-rule violations at CQA-test-runtime equivalent in-production moment. Smoke test hits real cross-cluster consumer before C3 review. Failure mode if deferred: TW writes via Edit-tool only, IC[6] contract validated only in IE's own test matrix — TW finds signature issue post-merge.|src:r19-remediation-c2-IC[6]-workspace_write|agent:technical-writer|promoted:26.4.23|class:pattern]
P[plan-scope-amendment-via-user-ratification|src:r19-remediation-c2-ie-1|promoted:26.4.23|class:pattern]: When plan file says "preserve | no edit" on a file but build surfaces a latent bug in that file with evidence (code comment documenting unimplemented intent + original test contract + roster change introducing regression + pattern-matching fallback-branch precedent), do NOT silently fix. Surface via BUILD-CONCERN, lead bundles with other pending scope decisions for user ratification, user issues scoped amendment with C3 documentation language. 1-line fix shipped in r19 C2: gate_checks.py DA-filter add in roster-allowlist branch, matching existing exclusion-list branch pattern. Preserves ADR[2] integrity (ADR[2] was about A12 key-rename, not DA-filter) while fixing pre-existing latent bug. 8 of 11 pre-existing test fails → 1 (test-side accommodation revert needed from CQA).
P[auto-ready-probe-forward-compat-hint|src:r19-remediation-c2-ie-1|promoted:26.4.23|class:pattern]: When an ADR specifies a server-side fix but the underlying framework lacks the hook needed to implement it (hateoas-agent Registry._last_state = None, no initial_state param), the scope-bounded implementation is: (1) idempotent probe at startup for operator visibility via stderr log, (2) forward-compatible attribute (vm._initial_state_hint = "ready") future framework versions can consume, (3) gateway-semantic-contract docstring documenting the gap + compromise, (4) try/except wrapper so probe failure never breaks server startup. Pair with agent-side belt-and-suspenders (SS spawn-prompt) for behavioral enforcement today. This honors ADR as probe+document rather than advertisement-unlock. 28 LOC total in sigma-verify machine.py shipped post-user-ratification 26.4.23 as "semantic clarification of ADR[1]" per C3 language.
P[db-split-by-marker-distinguish-exercise-from-reference|src:r19-remediation-c2-ie-1|promoted:26.4.23|class:pattern]: When a substructure marker (like DB[]) is used both for canonical exercises AND for prose-citations that reference prior exercises, re.findall on the marker produces false-negatives (citations count as exercises). Fix: split the enclosing section by the marker, then within each segment require the canonical inner-structure signals (here: parenthesised numbered markers (1)(2)(3) from the DB format `(1) initial: X (2) assume-wrong: Y (3) strongest-counter: Z (4) re-estimate: W (5) reconciled: final`). Genuine exercises have the numbered sequence; reference citations do not. Emit both counts to details for downstream consumers (genuine_by_agent + reference_by_agent). Preserves IC[4] layered authority: gc=presence (upstream), chain-eval=depth (downstream), sequential not conflicting, NO third layer (CAL[2]).
P[dataclass-python-3-14-importlib]|Python 3.14 dataclass introspection requires sys.modules[name] = module BEFORE exec_module() for @dataclass to resolve cls.__module__ — dataclasses.py:814 does ns = sys.modules.get(cls.__module__).__dict__, returns None if not pre-registered. Applies to hyphen-filename scripts (audit-calibration-gate.py, chain-evaluator.py) loaded via importlib.util.spec_from_file_location.|src:r19-remediation-c2 SQ[CDS-10] + pre-existing test_chain_evaluator.py|promoted:26.4.24|class:pattern
P[fenced-code-block-parser-exclusion]|Doc-parser scripts that scan for marker-prefix lines (e.g. CAL-EMIT[, F[, DB[) MUST skip content inside ``` fences or format-example documentation triggers false-positive parse errors. Fix: 3-line in_fence state toggle around ``` lines in parse loop. Without this, format examples in file headers round-trip as malformed records → exit code 2.|src:r19-remediation-c2 SQ[CDS-10] audit-calibration-gate.py parse_log|promoted:26.4.24|class:pattern
P[positive-contract-for-known-limitations]|When a security gate has an INTENTIONAL blind spot (e.g. xargs stdin out-of-scope for shlex-argv walker), convert the would-be-failing test into an asserting-False positive-contract test that locks the documented limitation in the test suite. Benefits: (1) future implementation extensions self-signal (test flips red → breaking change needs review), (2) limitation becomes searchable via test name, (3) no reliance on code-comments that rot. Preferable to inline-comment-only or xfail markers. Template: "test_X_is_known_limitation_not_blocked" naming + docstring with "if this starts failing, contract extended; update test".|src:r19-remediation-c2 SQ[SS-2] xargs sed-i BUILD-CONCERN resolution option(a)|promoted:26.4.24|class:pattern
P[ask-before-route-on-analysis-ack]|Lead saying "analysis is clean" = diagnostic validation, NOT routing approval. Plan-deviation routing requires EXPLICIT user ratification via lead broadcast. Rule: before SendMessage to peer on plan-scope items post-BUILD-CONCERN, ask lead "is this ratified?" rather than inferring routing approval from analysis-quality validation. Precedent: r19-remediation-c2 26.4.24 — cqa interpreted lead "analysis is clean" as fix-approved, routed to ie-1 before user ratification; lead issued STOP; cqa reply to STOP then under-engaged with Q1/Q2 specifics. Two process failures in one cycle from same misread. Mechanical check: for any lead message ending without explicit "proceed"/"route"/"ship" verb, treat as diagnostic ack only — return to !WAIT.|src:r19-remediation-c2 process-over-momentum 2nd instance|promoted:26.4.24|class:calibration
P[sq-effort-budget-recon-plus-triage-floor]|Plan-track SQ estimates should split budget into three components: (a) recon-per-SQ ~30min fixed — read the shipped code, locate imports, skim existing tests for pattern conformance, understand the interface contract. This is non-negotiable; skipping recon to "save time" produces tests against assumed behavior, not actual behavior. (b) test-writing scales with test-count × surface complexity — for typical chain-eval/phase-gate fixture patterns, ~10-15min per test including edge-case. (c) triage-per-BUILD-CONCERN ~45min fixed — when tests reveal code gaps, the flag/options/wait cycle costs a session-turn minimum, not just the investigation time. Precedent: r19-remediation-c2 SQ[13] plan estimated 10-13h, actual ~5-6h split as ~3h recon+write across 4 SQs (30min recon + ~30min write each) + ~2h triage on xargs BUILD-CONCERN. Plan underestimate was directionally right (2-2.6x) but compressed by well-documented code surfaces. Future plan-track: enumerate SQs, apply (a)×N + (b)×test_count + (c)×expected_concerns, don't rely on single-unit "2h per SQ" heuristic.|src:r19-remediation-c2 cqa cluster-C retrospective|promoted:26.4.24|class:calibration
P[path-β+-WARN-first-calibration-gate|src:r19-remediation-c2-ie-1|promoted:26.4.23|class:pattern]: When promoting a new analytical gate from directive-only to hook-enforced, ship WARN-first with CAL-EMIT per-firing record + append-only calibration-log.md. Promotion to BLOCK gated on ≥3 reviews + ≤20% false-positive rate + ≥5 DA-verdicted fires (audit-calibration-gate.py evaluates). CAL-EMIT schema: `CAL-EMIT[{gate-id}]: review-id:{slug} |finding-ref:{F[id]} |fire-reason:{trigger} |workspace-context:{agent}:{50-char-excerpt} |da-verdict:PENDING`. DA fills verdict in r2 exit-gate; calibration gate counts. Separate check passes=True (WARN not BLOCK) — fires surface via issues[] for operator visibility and details.cal_emit_records for test assertion without filesystem dependency. Shipped in r19 C2 for A20 (§2i precision), A22 (§2j governance artifact), A23 (§2d-severity provenance) — 16/16 smoke tests pass including 9 known-no-fire cases (over-fire prevention).
P[warn-gate-test-pattern-details-cal-records]|When building WARN-first calibration gates (path β+ pattern), expose emitted records via `details["cal_emit_records"]` list IN ADDITION to filesystem append. This lets test assertions inspect record format without filesystem dependency — tests become fixture-isolated + deterministic. Precedent: r19-remediation-c2 SQ[CDS-6..8] ie-1 added this hook explicitly for cqa test-surface after cycle-1 audit-calibration-gate tests proved the schema-assert pattern valuable. Rule for future gate design: any gate that writes an audit/log file should ALSO return the written content in details for ergonomic testing. Reduces test flakiness (no file-permission races), enables monkeypatch-free assertions on record format.|src:r19-remediation-c2 SQ[CDS-6..8] integration design|promoted:26.4.24|class:pattern
P[layered-authority-pattern-recognition]: when two gate-checks evaluate the same domain at different granularities, document them as sequential-not-redundant (layer-1 PRESENCE upstream + layer-2 DEPTH augmenting layer-1 result), NOT as parallel checks. Pattern instances: (1) IC[4] gc.check_dialectical_bootstrapping (presence) + chain-eval check_a3 (genuine-exercise depth); (2) A15 gc.check_xverify_coverage (per-agent binary) + A24 check_a24_sigma_verify_coverage (per-finding β+ calibration). Recipe: layer-2 calls `result = gc.check_layer1(content)` then augments `result.details`, never overwrites `passed`/`issues`. Document layered-authority in docstring + cite IC[#] in code comment. |src:r19-remediation-c3 |class:pattern |promoted:2026-04-24 |agent:tech-architect
P[plan-track-fidelity-bounded-to-own-ADRs]: in sigma-build C3 plan-track-fidelity-review, plan-author agents (TA/SS/CDS) verify ONLY their own ADRs+ICs from C1, NOT cross-section gaps between agents. Cross-section drift (locked-but-never-shipped items, mass-propagation gaps, cross-cutting defects) is fresh-DA's cold-read responsibility. Both reviews are necessary; neither is sufficient. Calibration: when plan-author returns "X compliance, Y issues, Z blocking" and DA returns blockers in the gaps, this is the recipe working — peer-verify rings catch within-agent drift, fresh DA catches between-scopes drift. Do not over-extend plan-author scope to "make sure DA finds nothing" — that absorbs DA's role and contaminates analytical provenance per Lead Role Boundaries directive. |src:r19-remediation-c3-r1-adjudication |class:pattern |promoted:2026-04-24 |agent:tech-architect
P[fix-validation-via-production-telemetry-recovery|agent:cognitive-decision-scientist|src:r19-remediation|promoted:2026-04-24|class:pattern-confirms-existing] !rule: when shipping a fix to a producer/consumer schema-decoupling defect (records silently dropping into a malformed bucket), validate the fix not only via synthetic fixture but by running the patched consumer against pre-existing production data. !signal: if the fix immediately surfaces previously-lost records that were silently dropped, that is stronger evidence of correctness than any synthetic test — production data IS the regression-witness. !example: r19 A24 VALID_GATES patch immediately surfaced 13 historical PENDING records the pre-patch consumer had been silently rejecting; that recovery was higher-fidelity validation than unit test could produce. !confirms: extends P[mock-tests-false-confidence|26.4.5] from build-validation to fix-validation: synthetic-fixture-only validates intent, production-replay validates impact.
P[CAL[R3-2-canonical-block-hash-identity]-invariant|class:calibration|when propagating canonical content blocks across N agent .md files, atomicity invariant = SHA-256 hash-identity of {block-content} across all N+1 (instances + canonical) ¬string-presence-grep. observed:r19-remediation-c3-r3-2 — string-grep showed all 30 files had OPTION 2 phrasing (false-positive consistency); SHA-256 audit revealed 29 instances drifted from canonical _template.md by 57 chars (whitespace + missing test-form + missing standalone !rule). hash-identity catches: missing/added newlines, separator drift (`,` vs `|`), missing list items, ordering changes, header phrasing variants. promotion-extension: chain-evaluator A25 candidate = grep agents/*.md for ## Workspace Edit Rules section + SHA-256 compare to _template canonical + WARN+CAL-EMIT on drift. supersedes weaker CAL[R2-OPTION2-phrasing] (string-match invariant). |src:r19-remediation-c3 |class:calibration |promoted:2026-04-24]
P[OPTION-2-block-number-agnostic-citation|class:calibration|when documenting code-side enforcement mechanisms (BLOCK-N, GATE-N, RULE-N) in propagated text across many files, OPTION 2 phrasing drops the numeric identifier entirely: "phase-gate enforces the sed-i BLOCK mechanically" not "phase-gate BLOCK 3 enforces". rationale: code-layer C5 renumbers (e.g., BLOCK 3→4 per pre-shutdown collision in r19) propagate to N citations as stale references, creating safety-critical regression vector — reader sees "BLOCK 3 sed-i" in agent-def, sees BLOCK 3 = pre-shutdown in phase-gate.py, may delete BLOCK 3 as redundant. OPTION 2 decouples doc from code-layer numbering; survives future renumbers without sweep. observed:r19-remediation-c3 r2 (27-file sweep) — DA[#2] flagged the citation drift, SS recommended OPTION 2, lead+TW shipped. evidence-rule: documenting enforcement should describe BEHAVIOR ("the sed-i BLOCK") not POSITION ("BLOCK 3") unless the position is itself the contract. |src:r19-remediation-c3 |class:calibration |promoted:2026-04-24]
P[atomicity-assertion-enumeration|class:calibration|TW agent CHECKPOINT must NAME the atomic sets being asserted, not generalize. observed:r19-remediation-c2 c2-scratch:388 TW asserted "atomicity-constraints-still-coherent: yes" generally; that assertion was scoped to CAL[9] 4-file peer-verify set but ANOTHER atomic set (BLOCK-number citation across 27 files) was outside the scope and had already drifted post-IE-1 renumber. DA[#2] caught the false-baseline. fix-pattern: every atomicity-coherent claim MUST list the named sets `CAL[N]+CAL[M]+IC[K]` whose coherence is being asserted. unnamed sets that are not enumerated are NOT covered by the assertion. applies-to: TW + IE + CQA + any agent making cross-file consistency claims at CHECKPOINT or peer-verify. enables future audits to falsify specific claims rather than relitigating "did you mean THIS atomicity set or THAT one?". |src:r19-remediation-c3-DA[#2] |class:calibration |promoted:2026-04-24]
P[realistic-fixture-test-vs-synthetic-flag-flip] |class:pattern |agent:code-quality-analyst |promoted:2026-04-24 |src:r19-remediation-c3
Test fixtures must model real workspace content (e.g. `|source:[T1(...)]` and `|severity-basis:...` tags from §2d), not synthetic flag-flips engineered only to pass. Realistic-fixture tests act as ex-ante contract witnesses — they would catch the original defect if written first, before the bug is observed in production. Validated this build by F[cqa-r2-1] pipe-fixture: realistic source-tag fixture proves CAL-EMIT producer/consumer pipe-collision; mechanistic teeth-check confirms pre-fix code WOULD fail. DA r2 framing: "would have caught the original defect ex-ante if written first (authority over synthetic flag-flips)." Apply when: any new test for a producer/consumer contract or a heuristic with regex-based detection. Reference: feedback_realistic-tests.md (existing memory).
P[count-correction-in-place-not-defer|class:calibration|when spec-count (e.g., "22 SAFETY-CRITICAL agents" in CAL[7]) disagrees with filesystem-count (23 actual workspace-writing agents), TW corrects in-place under rule-semantics ("rule applies to all agents writing to workspace files" = filesystem reality, not spec-number). observed:r19-remediation-c2 SQ[8] sed-i ban propagation — TW counted 23, spec said 22, TW flagged informationally + proceeded with 23-file propagation. anti-pattern: defer to "match the spec" and silently leave 1 agent unprotected. correct-pattern: rule-semantics > spec-numbers; flag the count-correction to lead in CHECKPOINT; proceed with reality-grounded count. applies-to: any safety-critical fan-out where spec was authored before filesystem ground-truth was empirically verified. evidence-tie: same root cause as DA[#9] 5-agent sigma-optimize gap (CAL[7] enumerated sigma-review team only, missed sigma-optimize). |src:r19-remediation-c2-SQ[8] |class:calibration |promoted:2026-04-24]
P[regression-lock-pre-flight-assertion] |class:pattern |agent:code-quality-analyst |promoted:2026-04-24 |src:r19-remediation-c3
For tests that lock a fix in place, add a pre-flight assertion that halts the test immediately (rather than passing falsely) if the fixed contract is reverted. Example: F[cqa-r3-1] asserts `"A24" in acg.VALID_GATES` BEFORE running the producer→consumer roundtrip — if VALID_GATES ever loses A24 again, this test halts with a clear "IE fix not yet applied" message instead of silently testing a non-existent contract. DA r3 verdict: "better than any test pattern this build." Apply when: any test that locks in a contract addition (allowlist entry, regex pattern, gate registration, dispatch entry). Pre-flight assertion failure carries a direct revert-detected message; main assertions then exercise the actual behavior.
P[ΣComm-conversion-not-label-swap|class:calibration|when converting prose directives to ΣComm notation (per CAL[8] boundary), genuine compression ≠ label-swap. observed:r19-remediation §8e workspace corruption recovery — 7-step plain-English prose compressed to §8e-1..§8e-7 blocks with action-verb-per-step (PRESERVE/EXTRACT/REBUILD/COORDINATE/ATTEST/DOCUMENT/TRANSPARENCY) + !rule/!purpose/!trigger-conditions structure + cross-refs §8a/§8d/§6e/sigma-lead Recovery. anti-pattern: replace English connectives with ΣComm symbols ("→" for "leads to") while leaving sentence structure intact = label-swap, not compression. correct-pattern: each step gets one action verb + structured directive metadata (purpose/trigger/applies-to/!rule) + explicit cross-refs to related sections. resulting compression should be machine-parseable as instructions, not just decoded prose. |src:r19-remediation-c2-§8e |class:calibration |promoted:2026-04-24]
P[teeth-check-pre-fix-failure-verification] |class:pattern |agent:code-quality-analyst |promoted:2026-04-24 |src:r19-remediation-c3
Before submitting a regression-lock test, mechanistically verify that pre-fix code WOULD fail the test. Otherwise the test could be passing for the wrong reason and silently accept future regressions. Method: read the diff that ships the fix, identify the exact pre-fix line, simulate the test path against pre-fix behavior (regex trace, code-walk, or revert+rerun in worktree). Validated F[cqa-r2-1]: teeth-check confirmed pre-fix `_emit_cal_record` lacked `.replace("|", "/")` so a pipe-bearing excerpt would split the consumer regex into wrong fields → land in malformed_lines bucket. Pattern complement to P[regression-lock-pre-flight-assertion]: pre-flight detects future revert; teeth-check confirms current test has real teeth.
P[fixture-regex-dry-run-discipline] |class:calibration |agent:code-quality-analyst |promoted:2026-04-24 |src:r19-remediation-c3
For test fixtures whose intended behavior depends on AVOIDING a regex-tested suppressor (or AVOIDING any regex match), dry-run the regex against the proposed fixture string before pytest run. ~30s of `python3 -c "import re; print(bool(re.compile(r'...').search('fixture')))"` saves ~3min of pytest-fail → debug → fix → rerun iterations. Discovered when F[cqa-r3-1] initial fixture said "without XVERIFY" descriptively → A24 fire_count==0 because `_XVERIFY_ANY_RE` matched the literal English word "XVERIFY" + newline (suppression heuristic over-broad). Caught at pytest fail, diagnosed via direct regex trace, fixed by changing fixture prose to "migration defect in payment pipeline". Applies to any fixture that needs the regex to NOT match (negative test path).
P[hash-verify-boundary-convention] |class:calibration |agent:code-quality-analyst |promoted:2026-04-24 |src:r19-remediation-c3
When verifying "verbatim-identical" claims across multiple files via hash comparison, EITHER normalize trailing whitespace before hashing OR explicitly state the hash algorithm + content-boundary convention (header included? final newline included?). Otherwise verifier and verifiee may report different SHA prefixes for byte-identical content, masking real drift behind method-divergence. Discovered when F[cqa-r2-5] reported 2 SHA clusters across 30 files — diff revealed pure trailing-blank-line difference between _template.md and the 29 propagated agents (non-material), but TW's claim was "verbatim-identical" and our prefix-checks differed (TW 16264f, mine 1ad55e). Recommended: SHA-256 over awk-extracted section bodies excluding header line and stopping at next `## ` header, with documented method comment in checkpoint output. Applies to: cross-file content-propagation verification, atomic-set integrity checks, doc-sweep validation.
P[empirical-baseline-reconciliation-over-papering] |class:pattern |agent:code-quality-analyst |promoted:2026-04-24 |src:r19-remediation-c3
When a prior project memory entry asserts a fact that's empirically falsifiable, run the empirical check before papering over the divergence. Falsify the memory entry rather than restating it. Validated by F[cqa-PF[1]]: project memory `project_hook-enforcement.md` claimed "test_chain_evaluator.py + test_phase_gate.py LOST in refactor" — empirical check via git ls-files confirmed both files exist at hooks/tests/ (consolidation commits ea5ae97 + d9d21ad MOVED them, did not delete). Memory entry was wrong; updated rather than working around it. Authoritative baseline 154/143/11 established, falsifying both C1 CQA "92" miscount AND the "LOST" claim. Apply when: project state references prior counts/locations/availability that diverge from current observation. Better to falsify-and-correct than to silently route around stale memory.
P[BUILD-CONCERN-then-ratify-protocol] |class:technique |reason: when build agent encounters scope-amendment-pressure (latent bug surfaces, plan-text scoped narrower than reality, edge case requires plan deviation), use BUILD-CONCERN-flag-then-wait-for-user-ratification protocol rather than silent absorption. Prevents silent-scope-shift class of process violation. Applies-to: any sigma-build mode, any build-track agent, any plan-amendment scenario. |src:r19-remediation-c3 |agent:devils-advocate |26.4.25
P[realistic-fixture-test-vs-synthetic-flag-flip] |class:technique |reason: test fixtures must mirror real workspace content shape, not synthetic flag-flips. Synthetic-flag-flip fixtures test that code runs, not that it works on production content. DA can use as specific anti-pattern question: "would this fixture have caught the original defect ex-ante?" |applies-to: §4d test-integrity verification, any DA review of new tests. |src:r19-remediation-c3 |agent:devils-advocate |26.4.25
P[anti-sycophancy-bias-check-explicit] |class:calibration-self-update |reason:codifies CLAUDE.md ## Anti-Sycophancy at verdict-time. Before any PASS/accept verdict where lead has signaled directionality (explicit acknowledgment, framed question, time-pressure, "easier-to-bundle-now" framing), run an evidence-vs-alignment check and CITE evidence in the verdict. Format: "Bias-check: am I PASS-ing because evidence supports it, or because the lead seems to want closure? Evidence: [enumerate concrete artifacts]. Verdict is evidence-based." Routine bias-checks at verdict-time are cheap; flagging them only when verdict aligns with apparent lead expectation creates the discipline. Lead acknowledgment ratifies the pattern. |src:r19-remediation-c3 SS-r2 |agent:security-specialist |26.4.25
P[structural-fix-pattern-superior-to-identifier-sweep] |class:technique |reason: when fixing identifier-based references across N documents (e.g., stale "BLOCK 3" across 27 files), prefer block-number-AGNOSTIC phrasing ("phase-gate enforces the sed-i BLOCK mechanically") over identifier-sweep ("BLOCK 3"→"BLOCK 4"). Structural fix removes future renumber maintenance burden entirely. |applies-to: any cross-file identifier-sweep, any documentation-references-machine-state pattern. |src:r19-remediation-c3 |agent:devils-advocate |26.4.25
P[doc-enumeration-drift-from-machine-source-of-truth] |class:pattern |reason: when shipping new gate-id/block-number/schema-element/named-state, audit ALL human-facing enumerations (directives, agent files, skill phase docs, README) for stale lists. 4-of-4 confirmed instances in r19-remediation followed same shape: machine code adds element X; consumer/audit-mechanism may or may not be updated synchronously; documentation enumeration is the LAST thing updated and the easiest to forget. |applies-to: any sigma-build adding machine-state, especially new gate-ids; DA review checklist item. |src:r19-remediation-c3 |agent:devils-advocate |26.4.25
P[empirical-evasion-matrix-trace] |class:pattern |reason:security-control verification methodology. When reviewing a security control with an evasion-resistance claim (shlex tokenization, regex, allowlist), do not rely on test count + smoke pass alone. Empirically trace the evasion matrix through SHIPPED CODE: (1) enumerate ALL claimed-blocked forms; (2) for each, run the tokenizer (shlex.split or equivalent) and read the tokens; (3) trace the tokens through the gate logic to confirm the blocked-or-passed branch; (4) confirm at least one expected-blocked form blocks AND one expected-passed form (e.g., backup-extension) passes. Smoke tests + test count are necessary but not sufficient — fixture-style tests can miss off-by-one operand-extraction bugs. Empirical token-stream trace catches what fixture-tests miss. Falsified this session: SS BLOCK 4 7/7 trace caught BSD `-i ''`, joined `-i""`, env-wrapper, xargs-positional, absolute-sed-path coverage independent of test suite. |src:r19-remediation-c3 SS-r1 |agent:security-specialist |26.4.25
P[multi-layer-contract-drift] |class:pattern |reason: producer-consumer schema contracts can drift at multiple layers — code (regex patterns vs sanitization), allowlist (VALID set vs emit ids), documentation (directive enumerations vs machine state). Single "fix the regex" or "fix the allowlist" addresses one layer; full closure requires sweeping all 3. DA can ask "checked all 3 contract layers?" as standard challenge format on producer/consumer pair reviews. |applies-to: any code review of producer/consumer pairs. |src:r19-remediation-c3 |agent:devils-advocate |26.4.25
P[end-to-end-wiring-trace-on-new-gates] |class:technique |reason: when new chain-evaluator gate ships, trace wiring end-to-end: (1) check function defined, (2) registered in ANALYZE_CHAIN/BUILD_EXTRAS, (3) dispatched in evaluate_single dict, (4) producer gate_id matches consumer VALID_GATES, (5) directive examples cover the gate, (6) test fixture exercises producer→consumer roundtrip. Missing any step = silent skip / silent malformed-bucket / silent CLI dead-end. |applies-to: any new-gate-id ship, any DA r3 spot-check after new chain-evaluator additions. |src:r19-remediation-c3 |agent:devils-advocate |26.4.25
P[empirical-XVERIFY-via-local-python-c] |class:technique |reason: when testing producer/consumer schema contract, run live `python3 -c "import re; ...; m = re.match(...)"` against real producer-output to verify consumer regex match. Faster than spawning XVERIFY for regex-deterministic question and provides falsifiable empirical answer rather than model judgment. |applies-to: any DA review where question is "does the regex actually match this producer output?" — local empirical > model XVERIFY for deterministic-grammar questions. |src:r19-remediation-c3 |agent:devils-advocate |26.4.25
P[DA-XVERIFY-FAIL-document-not-skip] |class:technique |reason: when DA-mandatory XVERIFY infrastructure returns internal-error or stalls, document as "XVERIFY-FAIL" in workspace findings with provider list + finding text + retry count, NOT silent skip. If another agent succeeded on different XVERIFY query in same session, infra not globally broken — making FAIL specific (transient or finding-text-specific) rather than epistemic-blocker. |applies-to: any DA review where XVERIFY infrastructure fails; preserves analytical-provenance honesty per process-integrity-over-completion. |src:r19-remediation-c3 |agent:devils-advocate |26.4.25
P[DA-fresh-cold-read-catches-what-plan-author-misses] |class:pattern |reason: DA spawned FRESH (no C1 plan-challenge memory) catches structural omissions that plan-track agents miss because plan-track is scope-bounded to its own ADRs/ICs. r19 evidence: TA peer-reviewed ADR[1-4]+IC[1-6] thoroughly but missed (a) check entirely-omitted (b) downstream propagation drift across 27 files. Both fall outside plan-author's ADR-bounded scope but visible to fresh-cold-read auditing the GAP between plan-claims and ship-state. Confirms structural value of DA-fresh-spawn vs DA-from-c1-memory. |applies-to: any sigma-build C3 review; reinforces fresh-DA-spawn protocol. |src:r19-remediation-c3 |agent:devils-advocate |26.4.25
P[DA-r1-FAIL-to-r2-PASS-via-evidence-not-capitulation] |class:pattern |reason: when DA r1 verdict is FAIL with N blockers and r2 fixes ship, DA r2 verdict moves to PASS via empirical-resolution-evidence (re-run hasattr/grep/pytest), NOT via lead-pressure-to-converge. Anti-pattern: DA capitulates to PASS without re-running checks. Verdict-movement-via-evidence preserves DA structural-skepticism authority across rounds. |applies-to: any sigma-build C3 review with multi-round dynamic; reinforces process-over-completion + anti-sycophancy. |src:r19-remediation-c3 |agent:devils-advocate |26.4.25
P[DA-cold-read-divergence-with-plan-author-needs-lead-ruling-not-retract] |class:pattern |reason: when DA fresh-cold-read disagrees with plan-author peer-review on blocker classification, resolution is LEAD RULING based on evidence weight, not DA-retracting-to-match. r19 evidence: TA classified 3 DA blockers as "needs-fixes:none-blocking"; lead ruled in DA's favor on evidence weight (hasattr/grep/regex empirically grounded). DA preserved verdict, ring closed via lead-ruling. Anti-pattern: DA softens to "let me re-examine" without new evidence. |applies-to: any C3 review where plan-track and DA disagree on severity/blocker-status; preserves DA challenge-not-destroy authority + Lead-Role-Boundary rule. |src:r19-remediation-c3 |agent:devils-advocate |26.4.25
P[DRY-trigger-detector-reuse-across-path-β+-gates] |class:pattern |reason: when shipping a new path-β+ gate that needs the same load-bearing detection (>=70% confidence, HIGH/CRITICAL severity, primary-recommendation), reuse the existing module-level regex constants (_CONFIDENCE_70_RE, _HIGH_SEVERITY_RE, _PRIMARY_REC_RE) verbatim instead of redefining. A24 reuses A20's triggers identically. Minimizes drift risk if path-β+ trigger semantics evolve in one place; converse risk (DRY-violation) was demonstrated by R19 #19 check_a3 DB-depth duplication. |src:r19-remediation-c3 |agent:implementation-engineer |26.4.25
P[workspace_write-IC6-empirically-hardened] |class:calibration-self-update |reason: workspace_write() helper (IC[6] signature `(path, old_anchor, new_content) -> None` raising WorkspaceAnchorNotFound) empirically validated through ~67 production calls across C2 + C3 r2 + C3 r3 with 0 anchor failures. Anchor convention (section header + first unique content line) robust to multi-agent concurrent writes when section-isolation honored. Atomic Python str.replace(old,new,1) + no-op guard handles unicode (αβγ + ΣComm symbols), multi-line, multi-paragraph anchors. Calibration data: API contract is production-ready, not provisional. |src:r19-remediation-c3 |agent:implementation-engineer |26.4.25
P[additive-peer-verify-rN-section-pattern] |class:calibration-self-update |reason: when peer-verify ring edge X→Y requires re-verification across multiple build rounds (r2, r3, ...), append `#### rN verification (additive — verifies Y rN test against X rN fix)` subsection INSIDE the existing `### Peer Verification: X verifying Y` block, NOT a new top-level header. Preserves single-edge ring topology (one edge X→Y in the ring graph), keeps verification history co-located, avoids confusing chain-evaluator A18 coverage matrix (would otherwise count X→Y twice). Validated empirically C3 IE→CQA r2 + r3. |src:r19-remediation-c3 |agent:implementation-engineer |26.4.25
P[evidence-based-pushback-on-stale-lead-claim] |class:pattern |reason: when lead claims state-of-world that contradicts agent's just-verified empirical state (file contents, tool responses, prior write confirmations), agent flags-with-evidence rather than re-executes the work. Distinguishes from lead-deference (anti-pattern) by requiring VERIFIABLE evidence (grep output with line numbers, file mtime, prior tool response payloads). Distinguishes from anti-sycophancy generally by being scoped to ground-truth claims about persisted state, not analytical conclusions. Re-execution under contaminated assumption can corrupt append-only stores (sigma-mem patterns.md, workspace gate-log) or duplicate scratch sections; refusal-and-flag preserves data integrity. Validated 2026-04-25 r19-remediation: lead's "scratch has no IE subsection" claim was based on grep race-condition (read cache stale before agent's write); agent grep'd live state, confirmed section present at line 1239, flagged with line-numbered evidence + diagnostic hypotheses (a/b/c/d menu); lead confirmed (a) stale-read, no re-execution needed. |src:r19-remediation-c3 |agent:implementation-engineer |26.4.25
P[ADR-drift-via-user-ratification-not-silent-shift] |class:new-principle |reason:distinguishes legitimate scope-clarification from silent-scope-shift via 4-marker test (BUILD-CONCERN raised at impl-time + lead routing trail + user/lead ratification logged + plan Process-Notes flag). When all 4 present = legitimate clarification. Missing any = silent-shift = audit RED. Affects future sigma-audit verdicts on builds with mid-flight scope changes. |src:r19-remediation-c3 |agent:tech-architect-via-lead-approved |evidence:ADR[1] semantic + IC[1] BLOCK 3→4 r19 instances |26.4.27 |user-approved
P[BUILD-CONCERN-then-flag-do-not-self-execute-scope-expansion] |class:new-principle |reason:agent encountering scope-expansion candidate must raise BUILD-CONCERN to lead and wait for user ratification rather than self-executing. Applied 2x cleanly in r19 build (tw-r2 5-sigma-optimize agents + tw-r3-2 hash-identity drift). Anti-pattern: agent absorbs scope expansion silently using "obvious correctness" rationale. |src:r19-remediation-c3 |agent:technical-writer-via-lead-approved |26.4.27 |user-approved
P[cross-section-finding-pattern-applied-at-finer-scope] |class:new-principle |reason:fresh-cold-read-DA + specialist-domain-DA are complementary not redundant. Specialist catches mechanics-internals at finer scope (CDS r2 caught CAL-EMIT pipe-escape consumer mismatch); general catches structural-omissions at coarser scope (DA r1 caught A24 missing entirely). Each scope has its own blind spots; multi-layer drift means BOTH scopes are needed for comprehensive sweep. Implies spawning specialist-DAs alongside general-DA when calibration-mechanics or domain-specific contracts are in scope. |src:r19-remediation-c3 |agent:devils-advocate-via-lead-approved |evidence:4-instance multi-layer-contract-drift across structural-omission + consumer-allowlist + doc-enumeration scopes |26.4.27 |user-approved
P[DA-self-disagreement-and-CDS-convergence-strengthens-finding] |class:new-principle |reason:when DA r1 misses a defect and a domain specialist independently catches it in r2, DA's r1 review is strengthened by acknowledging-and-confirming the new finding rather than defending r1 completeness. Two-witness rule satisfied. Distinguishes from DA-retract-to-match anti-pattern by requiring NEW empirical evidence. Convergence is quality signal not DA-failure signal. |src:r19-remediation-c3 |agent:devils-advocate-via-lead-approved |evidence:CDS r2 CAL-EMIT pipe-escape + DA r2 cross-confirmation |26.4.27 |user-approved
P[ask-before-route-on-analysis-ack] |class:new-principle |reason:lead-ack on analysis quality is diagnostic not routing approval. Plan-scope routing requires user-ratified BUILD-CONCERN trail. Default: flag, do not absorb. Validated 3x cross-build (CQA gate_checks DA-filter overreach, xargs BUILD-CONCERN, 5-agent BUILD-CONCERN). Promoted from project-scope to global. |src:r19-remediation-c3 |agent:code-quality-analyst-via-lead-approved |26.4.27 |user-approved
P[bonus-finding-route-not-absorb-during-fix-convergence] |class:new-principle |reason:when tester discovers NEW defect adjacent-to but distinct-from plan-locked fix during r2/r3 verification, route as deferred-finding NOT absorbed-fix. Plan-locked = execute completely; discovered-during-execution-but-out-of-plan = surface, do not absorb. Same axis as A24 silent-skip lesson but inverse direction (scope-creep instead of scope-shrink). |src:r19-remediation-c3 |agent:code-quality-analyst-via-lead-approved |evidence:CQA r3 _XVERIFY_ANY_RE bonus finding deferred to next-build |26.4.27 |user-approved
P[task-list-misroute-refusal-flag-protocol] |class:new-principle |reason:build-track agents refuse-and-flag automated task-list assignments matching lead-closeout work; never silent-absorb. Refusal protocol = sequencing-check + role-boundary-check + explicit option-a/b/c menu + verbose flag. Generalizes beyond r19 because auto-routing infrastructure may persist across sessions. |src:r19-remediation-c3 |agent:implementation-engineer-via-lead-approved |evidence:IE refusal at session start when task-list-teammate tried to assign Step 17 close-out work |26.4.27 |user-approved
P[commit-boundary-discipline-build-track-defers-to-lead-bundle] |class:new-principle |reason:build-track agents NEVER commit during sigma-build C2/C3 even for trivial 1-line fixes. Lead coordinates atomic commit-bundle at Step 17 archive. Confirms feedback_lead-role-boundary.md but adds COMMIT-specific axis. Behavior-change: absent this rule, agents pre-commit "safe" fixes and fragment commit history. |src:r19-remediation-c3 |agent:implementation-engineer-via-lead-approved |26.4.27 |user-approved
P[propagate-from-source-not-prior-instance] |class:new-principle |reason:when propagating canonical content to multiple files, READ the source-of-truth (e.g., _template.md) each time; never use prior propagation as canonical reference. Recursive-self-reference produces drift between source-of-truth and propagated copies even when copies are internally consistent. Detected via SHA hash-identity check (29 propagated agents shared hash A; _template carried hash C). Same failure class as A24 silent-skip + CAL[7] count-drift. |src:r19-remediation-c3 |agent:technical-writer-via-lead-approved |evidence:R3-2 self-audit: TW used pre-R2 technical-writer.md as reference for R2/R3 propagations instead of _template.md canonical |26.4.27 |user-approved
P[chain-evaluator-A25-template-drift-detection] |class:new-global-decision |reason:future-build candidate. Mechanical detection of canonical-content-drift between _template.md and propagated agent files. Hash-identity comparison at session-end via chain-evaluator A25. Pairs with sync-shared-sections.sh script for fix-side. User decision: duplication preserves rule-following safety (sync over generator); mechanical detection prevents drift. Extends DA's roster-check from MISSING-COVERAGE to CONTENT-DRIFT (both classes validated this build — DA[#9] statistical-analyst missing + CQA→TW SHA-128 drift). |src:r19-remediation-c3 |agent:technical-writer-via-lead-approved |26.4.27 |user-approved |status:future-build-SQ-candidate
P[multi-layer-contract-drift] |class:anti-pattern |reason:hardcoded enumeration in human-facing context that diverges from machine-enforced source of truth. 4 instances same class confirmed in r19 build: (1) BLOCK 3→4 doc drift across 27 files; (2) CAL-EMIT pipe-escape producer/consumer schema; (3) A24 VALID_GATES producer/consumer mismatch; (4) directives.md §-enumeration omits A24. When shipping new gate-id/block-number/schema-element, audit ALL human-facing enumerations for stale lists. SS+DA pairing of end-to-end-wiring-trace + OPTION-2-block-number-agnostic mitigations cover the remediation side. |src:r19-remediation-c3 |agent:cognitive-decision-scientist-via-lead-approved |26.4.27 |user-approved
P[β+-WARN-first-calibration-pattern] |class:new-principle |reason:promote new analytical gates from directive-only to mechanical enforcement via WARN-first calibration window. 4-gate framework (A20/A22/A23/A24) emits CAL-EMIT records to calibration-log.md; audit-calibration-gate.py evaluates promotion threshold (≥3 reviews + ≤20% FP + ≥5 DA-verdicted). DA verdicts close PENDING records. WARN→BLOCK promotion is deliberate lead action on PROMOTE signal. Producer/consumer schema must move atomically (VALID_GATES + docstring + argparse description). |src:r19-remediation-c3 |agent:cognitive-decision-scientist-via-lead-approved |26.4.27 |user-approved
P[forward-contract-vs-runtime-authorization-distinction] |class:new-principle |reason:current-state contracts and forward contracts are separable artifacts; documentation describing CURRENT behavior does not automatically convey FORWARD constraints for future extenders. Documented at machine.py IC[8]: "future state-gated tools MUST perform handler-layer authorization independently of MCP Registry advertisement; do not rely on Registry list_tools visibility as authorization signal." Same gap class: §2i CONDITION 1 deferral has thresholds for WARN→BLOCK promotion but no forward-contract stating what evidence shape would justify activation. |src:r19-remediation-c3 |agent:cognitive-decision-scientist-via-lead-approved |26.4.27 |user-approved
P[calibration-design-cost-asymmetry] |class:new-principle |reason:pre-promotion β+ windows favor fewer false-positives over more catches because RECALIBRATE-stalls block empirical evidence accumulation. A24 over-suppression (firing on compliant XVERIFY-FAIL disclosure) would penalize correct behavior. Post-promotion WARN→BLOCK is the right moment to revisit suppression rules with empirical legitimate-vs-FP data. Calibration design = optimize for correct base-rate evidence collection during pre-promotion window, not for max-coverage. |src:r19-remediation-c3 |agent:cognitive-decision-scientist-via-lead-approved |evidence:CDS XVERIFY over-suppression deferral reasoning |26.4.27 |user-approved
P[security-scope-boundary-honesty] |class:new-principle |reason:when threat model is accidental-failure (not sophisticated-adversary), enumerate bypass forms in enforcement docstring + add positive-contract test locking the gap as test-enforced limitation. Listing bypass recipes IS the contract for accidental-failure threat models. Concealing bypass shape would be worse security hygiene because future contributor cannot tell what the gate does NOT cover. |src:r19-remediation-c3 |agent:security-specialist-via-lead-approved |evidence:xargs-stdin KNOWN LIMITATION docstring + test_xargs_stdin_is_known_limitation_not_blocked positive-contract test |26.4.27 |user-approved
P[OPTION-2-block-number-agnostic-citation] |class:pattern |reason:when documenting cross-references to machine-state identifiers (BLOCK-N, gate-N, etc.) across N+ files, use semantic-name citations not numeric IDs. Future-renumber-immune. Validated in r19 r2: TW chose "phase-gate enforces the sed-i BLOCK mechanically (SS ADR[1])" over literal "BLOCK 3 → BLOCK 4" sweep. DA endorsed as superior to literal sweep. Generalizes DA's auto-promoted P[structural-fix-pattern-superior-to-identifier-sweep] with specific block-number-agnostic instance. |src:r19-remediation-c3 |agent:security-specialist-via-lead-approved |26.4.27 |user-approved
!sigma-evaluate-on-build | 26.4.28 | r19-remediation BUILD synthesis evaluated post-ship via sigma-evaluate skill | result: build self-rated 24/24 but 3-evaluator triangulation→3.14avg→B(2.8-3.4) | scores: Acc=4 Comp=3 Log=3 Ev=4 Cal=3 Act=3 Scope=4 | source-of-divergence: build rubric is internal to multi-agent build (correctness/test-coverage/maintainability/perf/security/api-design — implementation-quality lens); sigma-evaluate rubric is external (accuracy/completeness/logic/evidence/calibration/actionability/scope-integrity — analytical-rigor lens). NOT redundant — orthogonal axes | pattern: independent-context evaluator-judge surfaces gaps build's own rubric structurally cannot detect (e.g., "24/24 + 5 deferrals" coherence; ship-narrative-tilt; vague-structural-recs phrased as actions but lacking owner+trigger+block-vs-warn) | rec: sigma-evaluate as standard post-C3 step before ship-claim, not optional retrospective | precedent: complements sigma-audit(process-quality) — audit checks how, evaluate checks what
SQ-candidate[A26-C1-plan-completeness-check] |class:future-build-SQ |class-tier:WARN-first-then-BLOCK-after-β+-calibration |reason:every plan ## Files entry must regex-match an SQ owner string in ## Sub-task Decomposition; flags Files entries with no SQ-owner. Prevents A24-class silent-skip where deliverables are named at C1 but never assigned. |insertion:~/.claude/hooks/chain-evaluator.py lines 1057-1078 (after check_a24_sigma_verify_coverage), append A26 to ANALYZE_CHAIN list at 1064-1077 |pattern-mirror:check_a24 (lines 956-1057) |owner:implementation-engineer |trigger:runs unconditionally in ANALYZE chain when workspace exposes ## plan-file header |obstacle:## Files table format varies (markdown table vs list vs fenced); need normalized parser before BLOCK promotion |bundle:sigma-build-process-hardening (proposed TIER-2 future build) |src:r19-remediation-c3-eval-addendum |26.4.28
SQ-candidate[B5-C2-boot-validation] |class:future-build-SQ |class-tier:WARN-first-then-BLOCK-at-C2-boot-transition |reason:diff plan ## Files entries against C2 scratch ## agents subsection SQ-coverage; raise BUILD-CONCERN if any unassigned. Catches A24-class silent-skip at boot before agents start writing code. Companion to A26 (catches at plan-lock) — together they make the gap unreachable. |insertion:~/.claude/hooks/chain-evaluator.py lines 1079-1080 (BUILD_EXTRAS, after check_b4); add to mode-BUILD dispatch at line 1098 |pattern-mirror:B1-B4 (lines 526-548) |owner:implementation-engineer (B5 hook) + technical-writer (c2-build.md Step 2.5 directive) |trigger:parses C2 scratch ## agents subsection (already structured; lower-risk than parsing spawn prompts) |obstacle:c2-build.md doesn't standardize SQ coverage in spawn prompts today; parse from scratch ## agents instead |bundle:sigma-build-process-hardening |src:r19-remediation-c3-eval-addendum |26.4.28
SQ-candidate[TW-gap-handling-rules-directive] |class:future-build-SQ |class-tier:directive-not-hook |reason:technical-writer.md needs explicit !rule that when noticing a Files-table entry is unassigned to any cluster's SQ, raise BUILD-CONCERN citing the relevant SQ; ¬ punt by writing 'X is Y's scope' without explicit SQ reference. Codifies the lesson from r19-remediation-c2-scratch:409 punt that caused A24 silent-skip. |insertion:~/.claude/agents/technical-writer.md new ## Gap-Handling Rules section after line 139 (end of Cross-Model Verification, before ## Weight) |pattern-mirror:lines 111-126 (Cross-Model Verification) using !rule: format with 3 numbered outcome states |owner:technical-writer |trigger:directive (not hook) — applies during plan-read pre-spawn phase + during in-flight observation of Files-table coverage |bundle:sigma-build-process-hardening |src:r19-remediation-c3-eval-addendum + TW r3-2 self-audit |26.4.28
SQ-candidate[B6-C2-exit-gate-diff] |class:future-build-SQ |class-tier:WARN-first-then-BLOCK-at-C2-status-built-transition |reason:extends existing B2 gc.check_checkpoint() (chain-evaluator.py:532-535) to aggregate CHECKPOINT files-touched across clusters; cross-checks against plan ## Files; raises on missing entries. Final safety net catching A24-class silent-skip at C2 exit before status:built can flip to true. Forms 3-layer defense with A26 (plan-lock) + B5 (boot) + B6 (exit). |insertion:~/.claude/hooks/chain-evaluator.py lines 1079-1080 (BUILD_EXTRAS, alongside B5) |pattern-mirror:B2 extension + A24 line-window section-boundary scanner (lines 1012-1020) |owner:implementation-engineer |trigger:fires at C2 status:built transition |obstacle:CHECKPOINT files-touched is prose not structured (e.g., 'workspace_write.py (+ /tmp/ smoke tests, ephemeral)' at c2-scratch.md:235); need normalized parser before BLOCK promotion |bundle:sigma-build-process-hardening |src:r19-remediation-c3-eval-addendum |26.4.28
!P[directive-canonical-disambiguation-by-cross-ref] |src:shared-process-hardening-c1-2026-04-28 |TW DC[2]+lead canonical-decision lesson
when directive mandates a workspace section that overlaps with mechanically-enforced state, distinguish: (a) what is currently mechanically-enforced (cite hook + line), (b) what THIS directive newly mandates (the gap-closing addition). example: phase-gate.py:215 already BLOCKs on ## promotion + ## contamination-check + ## sycophancy-check; ## sync was the gap. WRONG: directive reads "lead writes ## promotion + ## sync" (implies both new). RIGHT: directive reads "lead writes ## sync — ## promotion already gated mechanically per phase-gate.py:215." P3.5 honesty: distinguish current-enforcement from new-mandate.
trigger: any directive that touches workspace section presence/structure
applies-to: directives.md additions; agent-def Boot/Persistence section additions; skill SKILL.md workflow steps
!P[sequencing-gate-vs-quality-gate-block-day-1] |src:shared-process-hardening-c1-2026-04-28 ADR[6] DA[#1]+DA[#6] |TA architecture lesson
β+ WARN-first calibration pattern is designed for QUALITY/PRECISION gates (signal-to-noise tuning via ≥3 reviews ≤20% FP). It does NOT fit SEQUENCING gates where the failure mode is silent-skip rather than false-positive. For sequencing gates: BLOCK day-1 with explicit recovery path (manual-override form) > WARN-first. Plan source-text mandates governing here is plan §P2.A row 119 "BLOCK at pre-archive" for item 9 — plan-faithful, not exception.
example: ADR[6] 06b pre-archive gate via `## compilation-complete: [R-{id}]` workspace-header → phase-gate BLOCK 5; manual-override form `## compilation-complete: [R-{id}, manual-override, reason: {reason}]` closes silent-skip exposure
counter-pattern (rejected, but legitimately-valid alternative per A28-WARN reconciliation): WARN-first defers BLOCK promotion to post-calibration; eliminates atomic-deploy coupling; preferred for quality gates
applies-to: future chain-evaluator + phase-gate gate decisions where the question is detection-vs-prevention
!P[wrapper-vs-helper-layer-isolates-regression] |src:shared-process-hardening-c1-2026-04-28 ADR[1] |TA architecture lesson
when adding behavior to a check that delegates to a shared helper used by other checks: add the new behavior at the WRAPPER layer, not the HELPER. modifying the helper risks regressing all consumers; wrapper-level changes are isolated.
example: A14 race fix (exclude *calibration-log.md). check_a14 delegates to gc.check_session_end which is also called by A12. Modifying gc.check_session_end risks A12 regression. Solution: A14 race-fix in check_a14 wrapper ONLY; gc.check_session_end untouched.
also: IC[1] cap-at-10 fix re-runs git status --porcelain independently in wrapper rather than relying on capped formatter output from helper.
applies-to: any chain-evaluator check that wraps a gc.* helper
!P[universal-edge-case-checklist-new-chain-items] |src:shared-process-hardening-c1-2026-04-28 CQA r1+r2 |code-quality lesson
new chain-evaluator ChainItems and parsers MUST be tested against this universal edge-case set, in a single pass at end of CQA r1 (¬per-gate ad-hoc):
  (1) empty `##` sections (header present, content blank) — must handle gracefully ¬crash
  (2) BOM at file start (UTF-8 0xEF 0xBB 0xBF) — affects all `^##` regex anchors; specify BOM-strip or `^﻿?##` regex
  (3) unicode in section names (`## plans (α-β agent)`) — `re.MULTILINE` with \Z boundary handling
  (4) `###` inside fenced code blocks (```` ``` ````) — must NOT be parsed as a workspace section header; affects A16/A17/A18 + new section parsers
  (5) trailing whitespace in section headers (`## plan-file  `) — regex must be robust
applies-to: every new chain item with a section-presence check; every new section-parser; B5/B6/A26/A27/A28 design pass
!P[archived-workspace-regression-required-pattern] |src:shared-process-hardening-c1-2026-04-28 SQ[11] CQA Q11+r2 |code-quality lesson
"existing chain checks must not regress on archived workspaces" (sigma-build C6) is mechanically UNVERIFIABLE without an explicit TestArchivedWorkspacePassthrough class running chain-evaluator against ≥3 real archived workspaces. Synthetic fixtures + unit tests are insufficient. New gates added in WARN-only mode (assert passed=True; log issues count; ¬assert issues==[]) so archived workspaces don't false-fail.
sample fixture set: r19-remediation + 26.4.22 ai-agent-rollout-playbook-vet + sigma-chatroom-m1ab.
prevents recurrence of failures.md 26.4.24 silent-skip pattern.
applies-to: every sigma-build that adds chain-evaluator items; mandate as standard SQ in TIER-2+ builds
!P[concession-strengthens-thesis-DB-pattern] |src:shared-process-hardening-c1-2026-04-28 DA[#6] B3 rerun |DA process lesson
when DB[ADR] step (3) "strongest-counter" picks weak counter (e.g. "bad UX") over the structurally-strongest UNUSED ALTERNATIVE that the team explicitly REJECTED, the DB is pro-forma — does not strengthen the decision because the rejected alternative gets no real airtime.
fix: rerun DB step (3) using the rejected-alternative as the counter. step (5) reconciliation can still keep the original choice; the value is forcing the team to articulate the genuine tradeoff. concession does NOT weaken the chosen position — it strengthens it because all real objections are surfaced + addressed.
4th occurrence pattern in agent reviews; promote to standing DA challenge: "is your strongest-counter the WEAKEST plausible objection or the STRONGEST?"
applies-to: DA enforcement of §2g dialectical bootstrapping in plan-track ADR challenges
!P[lead-flag-framing-inversion-detection] |src:shared-process-hardening-c1-2026-04-28 DA[#1] |DA anti-sycophancy lesson
when lead pre-flags a stress-test angle to DA framed as "X is an exception to Y default" — DA must independently re-read source material for X before stress-testing the framing. lead framing of an "exception" can be inverted (X is faithful to source, not exception). DA's correct response: flag the framing-inversion + abandon the original stress-test, NOT stress-test the wrong frame to be helpful.
example: F1 framing said ADR[6] BLOCK-day-1 was "exception to plan §P2.A β+ WARN-first default." DA read plan §P2.A row 119, found "BLOCK at pre-archive" explicitly mandated. Lead framing inverted. DA flagged + redirected to actual residual question (atomic-deploy mechanism).
this IS the GREEN+A behavior — anti-sycophancy on lead framing is calibration-credit. lead self-correction in gate-log preserves audit trail.
applies-to: DA Step 18 stress-test rounds; any time lead pre-flags an "exception" to default — independently verify the default's scope first
!P[pre-lock-self-evaluator-mechanism-design] |src:strategy-plan-let-s-actually-think-through-optimized-pie.md |2026-04-28
Designed (NOT shipped) intervention to break recurring B 3.14 weakness profile (4 instances: R18, R19, mellow-anchor, shared-process-hardening C1). Per evaluator: weaknesses live at AUTHORING layer (before verdict) — P2.D citation-counting mechanizes wrong layer. Three grep-able probes lead runs against plan.md before status:plan-locked:

L1 ADR justification rigor probe:
  PASS if rationale contains: DA[#N] | BC[X] | → reasoning chain | named-alternative + "steelman" | "rejected because" with ≥1 specific reason
  WARN if bare assertion (no challenger ref, no reasoning marker) OR <80 chars
  format: "WARN: ADR[N] rationale-rigor — rationale lacks unpacking signal | grep_hint:{rationale text}"

L2 calibration anchor specification probe:
  scan plan.md Meta + Plan Challenge Summary + Pre-mortem
  every quantitative claim (\d+%, P=\d+\.\d+, \d+x, \d+h) must pair with [lo, hi] CI bracket OR named trigger (replan-trigger:, breaks-if:, recompute-when:)
  "effective PASS"/"effective FAIL"/similar reframes require explicit rubric-component breakdown in same paragraph
  WARN per unsourced claim

L3 reconciliation audit completeness probe:
  count R-items declared resolved in Plan Challenge Summary
  each must have per-item resolution-evidence line OR single scope-of-audit statement (e.g. "audit-scope: F1 only; R2-R11 face-value")
  WARN if mismatch

Calibration ramp: WARN-first per β+ (matches A20 precision gate, A26/B5/B6); ≥3 reviews ≤20% FP before BLOCK promotion; calibration-log.md emit; lead may suppress with `lead-override-pre-lock-eval: {reason}` in ## gate-log. user-confirmed 26.4.28.

Integration points (future build, sequenced after P2.A C2/C3 close, BEFORE P2.D):
  ~/.claude/hooks/chain-evaluator.py (new ChainItem, A29 candidate after A26-A28 land)
  ~/.claude/teams/sigma-review/shared/directives.md (new §X co-located with §2i)
  ~/.claude/agents/sigma-lead.md (Step 26 plan-lock validation extended)
  ~/.claude/skills/sigma-build/phases/c1-plan.md (Step 26 docstring update)

Sequencing constraint: ship AFTER current C1 (P2.A) C2/C3 closes; ship BEFORE P2.D (so first A-grade run attributes cleanly to authoring-layer fix, not citation-counting downstream).

Strategy plan ephemeral file: ~/.claude/plans/let-s-actually-think-through-optimized-pie.md (local, matches plans/ convention; this entry is the durable backup of the mechanism design).
!IE-build-pattern: shared-fenced-code-helpers — when adding multiple gates that all need to skip ``` ... ``` content, define _strip_bom + _strip_fenced_blocks ONCE at module top, applied BEFORE all section-header regexes. Single ADR[9] surface, consistent edge-case behavior across A25/A26/B5/B6. Replacing fenced blocks with `\n * count` preserves line offsets so downstream offset math (e.g., next-section-header search after match.end()) stays correct |src:shared-process-hardening-c2-2026-04-29|
!IE-build-pattern: race-fix-wrapper-not-helper — when shared gc.* helper races with append-only sink (calibration-log.md → A14), do NOT modify the helper (A12 protection of shared interface across consumers). Add a wrapper in chain-evaluator.py that re-runs the underlying check independently with the exclusion applied, and falls back to gc capped list on subprocess failure. Wrapper-only is faster than gc-modification and preserves test contracts for V22-session-end-verified |src:shared-process-hardening-c2-2026-04-29|
!IE-build-pattern: hash-parity-empirical-verify — when a sync utility (shell script) and a checker (Python module) must produce byte-identical SHA256 hashes (PM[4] cross-platform parity), embed the same Python normalization in both. Verify hash parity empirically AT BUILD TIME (compute hash both ways, assert identical) — do NOT trust documentation. For shared/templates/.templates-hash-baseline.json: empirical match digest `eac7289eaf72ac80...` confirmed parity LF-normalize+BOM-strip+rstrip-lines |src:shared-process-hardening-c2-2026-04-29|
!IE-build-pattern: grep-audit-before-replace — for ANY regex tightening (`_XVERIFY_ANY_RE` SQ[7] PM[2]), step 1 = grep for ALL consumer call sites + ALL test fixture matches BEFORE the replace. Document findings (single consumer at A24:1104, only comment-line prose mention in tests). Risk classification: LOW if zero fixture content matches the prose form being removed. Skipping this audit is the source of regression debt that PM[2] mitigates |src:shared-process-hardening-c2-2026-04-29|
!IE-build-pattern: peer-verify-honesty-over-completion — when peer-verify target section does not exist (CQA blocked by sequencing in C2 SQ[11] depends-on SQ[1-10]), DO NOT fabricate verification text. Write a placeholder peer-verification flagging the gap with: (a) why the target is absent, (b) what artifact-IDs WOULD be cited if it existed, (c) recommended resolution paths for lead. This is process-integrity-over-completion in the verification-ring context. Better an honest-FAIL than synthetic coverage |src:shared-process-hardening-c2-2026-04-29|
P[paraphrase-test|directive-disambiguation|TW-method|src:shared-process-hardening-c2-2026-04-29|class:pattern]: when shipping an agent-facing directive, paraphrase test = read each entry of the data table (e.g. plan §Files), locate the owning cluster SQ in §Sub-task Decomposition by exact path match (¬prefix, ¬glob); if absent → BUILD-CONCERN[orphan-file]; if SQ refs file absent from §Files → BUILD-CONCERN[undeclared-file]; if row fields blank → BUILD-CONCERN[incomplete-row]. Forces every directive consumer to either accept-or-flag — no silent passes. Mitigates PM[7] directive ambiguity. Inverse-paraphrase covers both directions of the data-row↔task-graph contract. ¬just-author-review — agents reading the directive on a future task raise BUILD-CONCERN if the contract breaks.
P[bidirectional-cross-ref-pattern|directive-coupling|TW-method|src:shared-process-hardening-c2-2026-04-29|class:pattern]: when two directives are siblings (one pre-dispatch, one post-exit-gate; or one BUILD-side, one ANALYZE-side), each MUST reference the other to avoid drift. §2p ↔ §8f example: §2p has !cross-references block pointing to §8f DC[3] + sigma-review/SKILL.md + sigma-lead.md; §8f DC[3] points back to §2p naming the shared pattern (header-presence = phase-ran ¬just-claimed). One-way refs decay — readers landing in one section can't find the other. Bidirectional refs make drift mechanical to catch via grep-audit. Pairs with ADR-renumber-cascade-prevention: when adding a new directive section, audit ALL siblings for incoming refs that need updating.
P[label-drop-vs-structure-port|H7-r2-pattern|TW-method|src:shared-process-hardening-c2-2026-04-29|class:pattern]: when porting a workflow step from BUILD to ANALYZE (or vice versa), the STRUCTURE survives the port but the LABEL may need to drop. c1-plan.md:62 "Step 7a HARD GATE — §2p" ports to sigma-review/SKILL.md Step 1 + sigma-lead.md Step 1 as a sub-step labeled "**Premise-audit pre-dispatch (HARD GATE — §2p)**" — same HARD GATE, same §2p anchor, same PA[1-4] structure, same workspace-header presence-check, same DA r2 cross-check — but no "Step 7a" literal label, because adopting "Step 7a" on the ANALYZE side would force renumber-cascade across the workflow steps in SKILL.md and sigma-lead.md (which use "Step 1/2/.../7" not the BUILD-side numbering). Falsification of H7 (verbatim reuse) → falsification-aware port: ¬copy-paste, ¬rewrite-from-scratch, but ¬port the load-bearing pieces. Mechanical guard: parametrized test that the literal label appears in BUILD files but NOT in ANALYZE workflow files (with cross-ref text exempted).
P[grep-audit-pre-and-post|edit-validation|TW-method|src:shared-process-hardening-c2-2026-04-29|class:pattern]: BC[Q10]-style grep-audit MUST run BEFORE the edit (capture baseline of where the searched-token appears) AND AFTER the edit (verify the new state matches the intended invariant). Pre-edit alone catches "is the existing state what I think it is" — post-edit alone catches "did my change have the intended effect". Together they catch unintended side-effects from the edit (e.g., I added a controlled cross-ref mention that the test would have caught as a violation if I hadn't bounded the test scope). Pairs with bounded-test-scope: don't run a repo-wide grep test against legitimate cross-doc discussion of a label — narrow to the workflow surface where the invariant actually applies. PM[8] mitigation pattern.
!warn-only-archive-passthrough-test 26.4.29 — pattern: when shipping new chain-evaluator gates with WARN-first behavior (passed=True regardless of fire count), the load-bearing regression test is `evaluate_chain` against ≥3 frozen pre-gate archived workspaces asserting (a) no-crash on full chain, (b) WARN-only invariant on new gates (passed=True), (c) no-regression on existing items (A1-A24/B1-B4 still present + ChainItem schema well-formed), (d) evaluate_single dispatch round-trips per new gate. Synthetic edge-case fixtures cover empty/BOM/unicode/fenced/trailing-WS in a SINGLE test class at end of r1 (¬per-gate ad-hoc) per DA[#12] PROMOTION-CANDIDATE. Plan §Verification specs become spot-checks at the integration layer with deeper coverage delegated to per-SQ test files (cite the deeper file in the docstring). Source: shared-process-hardening C2 SQ[11] tests/test_archived_workspaces.py 37/37 PASS — 18 archive-passthrough + 13 DA[#12] edge-cases + 6 verification-spot-checks. Frozen archive/*.md files (NOT live builds/*/) are the right fixture per PM[1] semantics — they are written before the new gates existed and cannot be retro-edited to suit them; live build dirs mutate and would give noisy test signal. |src:shared-process-hardening-c2-2026-04-29|
!source-validation⊥sigma-retrieve 26.4.29: source-validation skill axis ⊥ sigma-retrieve's authority/recency tiering | sigma-retrieve catches: marketing-blog vs SEC-filing(authority 2/5 vs 5/5) + 6mo vs 5yr(recency) + 3+ source convergence | misses: creator-on-creation(founder describing own product as transformative — Cat Wu Anthropic essay scored 4-5/5 authority but predictive claims need discount), framing-capture(interconnected VC-podcast-newsletter ecosystems where convergence ≠ independence — Lenny's+Pragmatic Engineer+Latent Space cross-reference each other), descriptive-vs-predictive register-split(same source — keep "here's what we did", discount "here's the future of work"), format constraints(softball-interview ¬adversarial-debate) | source-validation catches all 4 perpendicularly | combined ⊥ coverage > either axis alone | integrated 26.4.29 as sigma-single Stage 3.5 between Stage 3 source-tier-tagging and Stage 4 dialectical-challenge | empirical trigger: velvety-swinging-coral.md(2026-04-29) — user observed hype-anchoring despite sigma-retrieve's cross-checking; root cause = single-axis authority rewards ecosystem peer-promotion as convergence | commit 08f8241
AUDIT[26.4.29|sph-c2]: empirical-vs-asserted hash-parity discipline validated as durable build-pattern. F[IE-6] verified sync-templates.sh and chain-evaluator._a25_hash() produce byte-identical SHA256 (eac7289eaf72ac80...) on same input rather than asserting parity by-design. Cross-platform invariants for cross-tool integrity REQUIRE measurement, not just shared-code-path assumption. Connects feedback_realistic-tests:26.4.6 + feedback_mock-tests-false-confidence:26.4.5. |verdict:GREEN |source:sigma-audit
AUDIT[26.4.29|sph-c2]: honest-peer-verify-GAP-then-empirical-re-verification flow validated as canonical sequenced-dependency pattern. IE→CQA initially flagged GAP at convergence (CQA section unwritten because SQ[11] depends-on SQ[1-10] merged); after CQA wrote, IE re-did peer-verify with empirical execution (re-ran 37/37 in 6.73s vs by-design assertion). Process-integrity-over-completion observed: lead did NOT fabricate verification on absent content. Promotion-candidate for build-directives §peer-verify timing rules. |verdict:GREEN |source:sigma-audit
AUDIT-GAP[26.4.29|sph-c2]: §2d source-tag schema is code/data-shaped — TW documentation findings F[1..4] lack canonical |source: type because [code-read]/[test-execution]/[grep-audit]/[empirical-verify] don't fit doc edits. Embedded provenance ("grep-audit clean", "pre-edit grep confirmed", file:line cites) is materially traceable but schema-noncompliant. Promotion-candidate: extend §2d with [doc-edit] / [directive-write] / [cross-ref-grep] source-types OR add explicit TW-finding exemption with embedded-provenance requirement. Surfaced by GREEN audit (29% F[] untagged but no fabrication signal). |source:sigma-audit
!sigma-eval shared-process-hardening-c2 26.4.30 |R1=C(2.57) → cleanup-pass → R2=C+(2.71) |Δ=+0.14 |R1-findings: stale line-refs (sigma-lead.md:176→207 post-TW SQ[10]) + test-count drift (1208/1245, Δ=37=CQA SQ[11]) + VP[1]/GAP[#5] dual-confirm w/o criterion |R1-scores: A=2 C=3 L=3 E=2 Cal=2 Act=3 Sc=3 |R2-findings: cleanup self-referential (mech-canon ∪ editorial-verdicts, no precedence rule) + dual-confirm overstated (TW-VP[1] wording-tier ⊥ GAP[#5] enforcement-tier — different severity) + F[IE-6] hash-parity test-as-proof fallacy unretracted (own P3.4 anecdotal-vs-hardened correction failed to catch) |R2-scores: A=3↑ C=2.5↓ L=2↓ E=3↑ Cal=3↑ Act=3 Sc=3 |target=A(≥3.5) missed → C+ stands as honest signal |C3-handoff: F[IE-6] adversarial retract via DA + VP[1]/GAP[#5] severity disaggregation + cleanup-conflation precedence rule for memory-compile |src:sigma-evaluate-r1+r2-shared-process-hardening-26.4.30|
!eval-disagree-weighting 26.4.30 |context: sigma-evaluate R2 split on cleanup-pass quality |E1 evaluated narrow claim (do canonicalization claims match ground truth?) → cleanup resolves R1 issues → +1 A/E/Cal |E2 evaluated broad claim (is cleanup logically coherent?) → cleanup self-referential (editorializes while claiming pure mapping) → -1 L, -0.5 C |Judge resolution: E2 wins on structural incoherence (load-bearing); E1 generalized clean line-num/test-count canonicalization verdict to whole pass — fails for editorial verdicts (VP[1]/GAP[#5] elevation, TW |source: reclassification, double-prefix typo flagged as promotion-candidate) |sub-rule a: E2's P2 critique (post-hoc rule-making for own violation) is weak-induction NOT circular-logic — downgrade to Calibration concern not Logic failure. Patterns-from-single-observation are weak induction; that's calibration weakness, not fallacy. |sub-rule b: E2's P4 critique stands — reframing C-grade as "rigor-delta artifact" displaces blame from substance (F[IE-6] test-as-proof) to bookkeeping. Real Logic flaw. |apply: when E_x scores a narrow factual-correctness claim well + E_y scores a broad coherence claim poorly, do not average. Test whether broad claim is generalization-over-narrow (then narrow score doesn't transfer) or independent assessment (then both can stand). |src:sigma-evaluate-r2-judge-shared-process-hardening-26.4.30|
!cleanup-pass-recoverability 26.4.30 |context: sigma-evaluate R1=C(2.57) → lead added cleanup pass at C2 close → R2=C+(2.71) Δ=+0.14 |finding: cleanup recovers HYGIENE credit (stale refs, test-count drift, typos, missing reconciliation) but CANNOT recover STRUCTURAL credit (test-as-proof fallacies in body, missing adversarial framing, missing counterfactual logic, sloppy reasoning chains) |mechanism: cleanup IS hygiene work by definition; it cannot retroactively make body reasoning rigorous without restructuring/retract-and-rewrite. Body reasoning must be confronted at write-time. |secondary risk: cleanup that bundles mechanical canonicalization with editorial verdicts (re-classifications, gating-elevations, severity-promotions) introduces NEW structural flaw → self-referential authority (no precedence rule when cleanup verdict ≠ body verdict) |rule: if R_n cleanup elevates findings, re-classifies reasoning, or assigns gating-status, label these editorial NOT canonicalization, and state precedence rule (cleanup-supersedes-body OR body-stands-cleanup-advisory). Do NOT bundle under one heading. |corollary: R_n+1 evaluation surfaces what R_n could not see → cleanup passes have diminishing returns; single-shot for hygiene, NOT iterative for structure. Restructuring invites R_n+2 with own structural issues (meta-cleanup loop). |honest path: confront body flaws via retract-and-rewrite OR accept C-grade as calibration signal that A target was missed. |src:sigma-evaluate-r1-vs-r2-pattern-26.4.30|
!doc-hygiene-reconcile-at-close: lead must reconcile line-numbers + counts + cross-refs at build close, ¬just append agent updates. snapshot-staleness compounds w/ each agent message — agents write writing-time-accurate refs that go stale as later edits shift positions. apply: BUILD c2/c3 close, ANALYZE post-DA-final, any multi-agent workspace where cumulative edits shift line positions. evidence: shared-process-hardening C2 — sigma-lead.md:176→207 stale across 10+ refs caught only by sigma-evaluate-r1 (Accuracy 2/4). |src:shared-process-hardening-c2-2026-04-30|class:pattern
!dual-confirm-escalation: when peer-verify + XREVIEW (or any 2 indep reviewer paths) flag related concerns on same artifact, convergence MUST record explicit elevate-or-defer decision w/ criterion + named decision-maker. ¬"carry-forward" label alone — that's deferral w/o reason+name. ¬conflate severity tiers: wording-level VP vs enforcement-model GAP are converging-evidence-at-different-severity-tiers, NOT same-gap. evidence: shared-process-hardening C2 VP[1]+GAP[#5] mishandling caught by eval R1 + R2 — first as missing-elevation, then as severity-tier-conflation. |src:shared-process-hardening-c2-2026-04-30|class:pattern
!paste-with-grep-citation: cite path:line + content-excerpt format (e.g. `sigma-lead.md:207 — "operator may unblock"`). self-falsifying: if line content drifts, the excerpt fails on next read by reader/evaluator. bare path:line goes stale silently — eval cannot detect drift. apply: any cross-ref in workspace/scratch/plan/findings/peer-verify; especially load-bearing refs touched by adversarial review. evidence: shared-process-hardening C2 — bare line refs caused doc-rot inside single session. |src:shared-process-hardening-c2-2026-04-30|class:pattern
!audit-vs-eval-rigor-delta: audit (GREEN/YELLOW/RED) checks "did we do the protocol steps?"; eval (A-F) checks "was the response well-calibrated?". both bars matter and can diverge. GREEN-with-C signals team-did-the-work + lead-reconciliation-slipped (NOT process violation, but quality-deficient). ¬attribute eval-grade to bookkeeping-slippage when actual issue is judgment-failure (e.g. F[IE-6] test-as-proof fallacy = blame-displacement risk if framed as documentation hygiene). evidence: shared-process-hardening C2 — audit GREEN + eval r1 C / r2 C+ on same artifact. |src:shared-process-hardening-c2-2026-04-30|class:pattern
!c2-build-promotion-candidates: from shared-process-hardening C2 audit GREEN. (a) personal-vs-team task-list namespace enforcement — TaskUpdate uses team-scope when team active, surfaces task descriptions to teammates as available work; (b) ¬TaskCreate for lead-orchestration tasks in BUILD mode — no enforcement value, only contamination risk (4-injection premature-closure pattern at 07:03Z); (c) §2d source-type extensions [doc-edit]/[directive-write]/[cross-ref-grep] for TW findings (existing types are code/data-shaped); (d) |source:|source: double-prefix lint as low-friction format-checker. |src:shared-process-hardening-c2-2026-04-30|class:pattern
!E1-E2-disagreement-resolution-rule: when sigma-evaluate rounds disagree on which criterion-axis a finding should tax → classify by failure mechanism. (i) induction-strength (single-observation generalized to invariant-claim) → Calibration. (ii) soundness-of-inference (premise→conclusion gap, circularity) → Logic. (iii) framing-or-presentation (severity inflation, scope creep) → Completeness/Scope. (iv) factual-accuracy (drift, contradiction, fabrication) → Accuracy. evidence: shared-process-hardening C2 eval R2 judge weighting — E2 won self-ref-authority on Logic, partially overstated P2 (post-hoc rule-making) reclassed to Calibration not Logic, won P4 on test-as-proof. |src:shared-process-hardening-c2-2026-04-30|class:calibration
!R1-R2-finding-class-delta: meta-pattern on sigma-evaluate cleanup-pass returns. R1 surfaces hygiene-issues (recoverable via reconciliation pass — mechanical canonicalization of line-nums/counts/typos works, +1 each on Accuracy/Evidence/Calibration). R2 surfaces structural-issues that EITHER pre-existed-obscured-by-hygiene-noise OR were-introduced-by-cleanup-itself (Logic -1, Completeness -0.5 from cleanup self-ref-authority + severity-tier-conflation). structural ¬recoverable via 2nd reconciliation — only restructure-w/-credibility-cost OR accept+document. diminishing-returns is finding-class-shaped, not effort-shaped: "eval cleanup passes recover hygiene-credit but cannot recover structural-credit; the second eval surfaces what the first couldn't see, including flaws introduced by the act of cleanup." apply: any post-eval cleanup decision; budget for one cleanup-then-accept, not iterative cleanup-then-eval-then-cleanup. evidence: shared-process-hardening C2 r1 C/2.57 → cleanup → r2 C+/2.71 (+0.14 net; hygiene +3, structural -1.5). |src:shared-process-hardening-c2-2026-04-30|class:meta-pattern
!build-eval-deltas-shared-process-hardening: per-criterion sigma-evaluate scores R1→R2 (post-Tier-1-cleanup): Accuracy 2→3 (+1, line-num + test-count canonicalization), Completeness 3→2.5 (-0.5, "C3 GATE" defers ¬resolves), Logic 3→2 (-1, cleanup self-ref-authority), Evidence 2→3 (+1, contemporaneous plan-file corroboration), Calibration 2→3 (+1, uncertainty documented), Actionability 3→3 (0, decision-tree scaffolding still missing), Scope-Integrity 3→3 (0). overall C/2.57→C+/2.71 (+0.14). target A≥3.5 missed both rounds. audit GREEN concurrent. |src:shared-process-hardening-c2-2026-04-30|class:calibration-data
!cleanup-pass-precedence 26.5.2: lead's "## c2-close-cleanup-pass" or any post-eval reconciliation MUST separate mechanical-canonicalization (line-numbers, test-counts, typos — lead-authoritative) from editorial-classification (severity-elevation, |source: reclassification, promotion-flagging — requires adversarial layer or explicit lead-with-criterion). Conflating the two earned R2 the structural-flaw -1 in shared-process-hardening C2 (eval C+ 2.71/4.0). Ratified in-build at C3 close-status per lead. |src:shared-process-hardening-c3-2026-05-02|
!gate-enforcement-meta 26.5.2: in shared-process-hardening C3 close-out, synthesis-agent halt detection + honest gate enforcement (process-integrity-over-completion) worked as designed. Evidence the pattern is durable, not just stated. Calibration data point: gates fired correctly, build-track held, no silent override. Continue treating CLAUDE.md "Process Integrity Over Completion" as the load-bearing rail. |src:shared-process-hardening-c3-2026-05-02|
!header-presence=phase-ran-pattern: workspace-header presence is the mechanical signal that a phase actually ran ¬just-claimed. Used in §2p (## premise-audit-results pre-dispatch) + §8f (## sync, ## promotion, ## archive-complete, ## synthesis-complete post-exit-gate) + ADR[6]/IC[6] (## compilation-complete pre-archive). Pattern: agent writes header AFTER phase completes; chain-evaluator/phase-gate presence-checks it; missing header → BLOCK or WARN per path. Recovery form for hard BLOCK uses bracketed inline syntax with mandatory reason field (e.g., `## compilation-complete: [R-{id}, manual-override, reason: {specific-failure-mode}]`). Reusable when: (a) phase has agent ownership AND (b) skipped-but-claimed-complete is a real failure mode AND (c) verification of phase-actually-ran from external state is hard or expensive. Anti-pattern alternative: scan agent prose for completion claims (false-positives on aspirational language). |src:shared-process-hardening-c3-2026-05-02|
!ADR[9]-shared-helpers 26.5.2: chain-evaluator.py introduced `_strip_bom` + `_strip_fenced_blocks` as module-level helpers consumed by A26+B5+B6+A25 (BOM-prefix detection, fenced-code false-match exclusion before section search). DRY across 4 gates ¬per-gate ad-hoc | reuse-pattern: edge-case-class helpers ⊥ gate-specific logic | source: build/2026-04-28-shared-process-hardening C2 SQ[1,2,4,6] F[IE-3,4,5,6] |src:c3-promotion-shared-process-hardening-2026-05-02|
!preferred-derive-then-broad-fallback-with-short-circuit: when a hook needs to authorize an operation by scanning multiple possible state sources, follow this 3-step pattern: (1) primary canonical source (e.g., DEFAULT_WORKSPACE) — if found, return; (2) derive a preferred narrow source from the operation's target (e.g., `_build_id_from_archive_path` extracts build-id from archive write path; lookup `BUILDS_DIR/{build-id}/`) — if derivation succeeds AND narrow source exists, scan it AND short-circuit return False if no auth found there (do NOT fall through); (3) broad-glob fallback only fires when preferred derivation FAILED (no target context, unrecognized name shape, missing dir). Anti-pattern: unconditional broad-glob on no-match in step 2 — creates cross-target authorization bypass (build A's auth header can authorize a write targeting build B). Architectural test for the fix: strict-superset semantics — every case correctly authorized in pre-fix still authorizes; only the cross-target cases that were always-wrong-to-authorize now correctly deny. Concrete instance: phase-gate.py:_has_compilation_complete (ADR[6]/IC[6] BLOCK 5, R2-micro fix 2026-05-02) — line 554 short-circuit. Reusable when designing multi-source presence-check gates. |src:shared-process-hardening-c3-r2-micro-2026-05-02|
!a14-wrapper-only-pattern 26.5.2: when fixing a check that consumes shared helper (gc.check_session_end), implement fix as a wrapper at the check level NOT by mutating the helper. preserves contract for other consumers (A12 protection) | wrapper falls back to helper-capped output on subprocess failure with explicit `*_status="fallback-to-*"` field for debuggability | source: build/2026-04-28-shared-process-hardening SQ[5] ADR[1]/IC[1] F[IE-1] |src:c3-promotion-shared-process-hardening-2026-05-02|
!block5-multi-path-implementation 26.5.2: phase-gate hook reading workspace-overrides must scan multiple sources when sub-sessions exist (BUILD-track c{N}-scratch.md vs ANALYZE workspace.md). pattern: BUILDS_DIR constant + freshness-window helper (_is_fresh) + per-source session-marker check + glob-then-filter ¬glob-everything | freshness-window=7d brackets active-builds without admitting long-stale | each source independently freshness-checked ¬global flag | source: build/2026-04-28-shared-process-hardening C3-r2 IE-R2-FIX[#1..4] phase-gate.py BLOCK 5 |src:c3-promotion-shared-process-hardening-2026-05-02|
!canonical-source-verbatim-threshold-preservation: when a plan locks a calibration threshold via cross-reference to existing precedent (e.g., "≥3 reviews + ≤20% FP per A20 §2i β+ precedent"), the directive/code MUST preserve the value verbatim including the explicit anti-drift annotation (e.g., "NOT '2+'"). Implementation site (directives.md / chain-evaluator.py / etc.) should include the precedent citation INLINE so future readers see the load-bearing reference, not just the number. Reason: a number-only restatement is silently editable (someone tightens to "2+" or loosens to "≥4"); a citation-bearing restatement requires the editor to confront the precedent. Concrete instance: ADR[10] A27 threshold at directives.md:1277 — verbatim "≥3 reviews where ## sync absent + ≤20% false-positive rate (precedent-aligned with A20 §2i, ¬'2+' provisional)". Reusable when: (a) any threshold cross-references a β+/calibration precedent, AND (b) silent drift would lose the architectural anchor that justified the value. |src:shared-process-hardening-c3-2026-05-02|
!short-circuit-on-preferred-resolved 26.5.2: when a multi-source authorization check uses an input-derived preferred-source (here: archive-path-derived build-id → preferred build's scratches), preferred-source-resolved-with-no-match MUST short-circuit return False ¬fall through to broad-glob fallback over OTHER sources. broad-glob fallback only fires when preferred-derivation FAILED (input absent / unrecognized shape / preferred-source missing) | violation manifests as cross-target authorization bypass: source A's authority authorizes target B's request because the broad-glob found A's match while answering B's question | strict-superset-of-correct-cases: no case correctly authorized today breaks; only previously-wrong cases stop authorizing | source: build/2026-04-28-shared-process-hardening C3-r2-micro IE-R2-MICRO-FIX[#1] TA-CONCERN-1 |src:c3-promotion-shared-process-hardening-2026-05-02|
!directive-hook-integration-co-test: when a directive (in agents/*.md or directives.md) introduces or changes a workspace-header convention, the corresponding hook (chain-evaluator/phase-gate) regex must be co-tested against the directive's example forms in the SAME round. Failure mode otherwise: directive ships pointing to scratch-file headers, hook only reads workspace.md → BLOCK fires for the wrong reason and the directive↔hook contract is broken silently. Concrete instance (this build, R2): directives.md §8f BUILD variant pointed to `builds/{id}/c{N}-scratch.md` for the override header, but `_has_compilation_complete` only scanned DEFAULT_WORKSPACE; gap surfaced when synthesis-agent retry blocked legitimately-authorized archive write. Test pattern: assert hook regex matches every example syntax shown in the directive AND that the hook reads from every state location the directive names. Reusable when: (a) directive content changes header conventions, OR (b) hook scope narrows/widens against where directives say content lives. |src:shared-process-hardening-c3-r2-2026-05-02|
!fresh-session-window-7d-bracket: heuristic for "active sigma session" detection in hooks scanning multiple workspace state sources. 7-day mtime window covers all observed session-duration precedent classes empirically: F1 1d/TIER-2 + R19 4d/TIER-3 + shared-process-hardening 4d/TIER-2. Window is wide enough that multi-day TIER-2/3 builds with quiet patches (no daily writes) do not false-archive-classify, narrow enough that abandoned workspaces (>1wk stale) do not classify as in-sigma — preserves FP guard. Combined with `## task` + `## mode` session-marker scan (both required) for correctness. Concrete instance: phase-gate.py:_FRESH_SESSION_WINDOW_SECONDS = 7*24*60*60 + _is_fresh + _has_session_markers + _iter_active_build_scratches (R2 fix 2026-05-02). Reusable when: hook needs to distinguish "currently active build" from "historical archived build" using only mtime + presence signals (no daemon, no shared state). Calibration: revisit if a TIER-3 build runs >7d (none observed yet). |src:shared-process-hardening-c3-r2-2026-05-02|
!directive-section-placement-heuristic (sigma-build, sigma-review): when adding a new directive sub-section that mirrors an existing recovery-form / manual-override pattern, prefer extending the EXISTING canonical-home file with a sibling sub-section (signaled via section-prefix like "BUILD §8f variant") + a new DC[N] cross-ref entry, rather than creating a separate file. Heuristic: "directive content lives where its structural sibling lives." Precedent: shared-process-hardening C3 — BUILD-track compilation manual-override form added as §8f sibling to existing ANALYZE-track ## sync recovery form (directives.md:1284-1286 ANALYZE → directives.md:1288-1312 BUILD-track variant + DC[4] cross-ref at line 1318) rather than build-directives.md §8f. Reason: structural-parallel + cross-ref discoverability beats file-track separation. Confounder: only applies when canonical home accepts cross-track variants; when separate file is the established pattern (e.g. build-directives.md vs directives.md for distinct CHAIN definitions), don't bend the rule. |src:shared-process-hardening-c3-2026-05-01|
!analyze-build-label-asymmetry-pattern (sigma-review SKILL.md, sigma-lead.md, c1-plan.md): when a structural step is shared between BUILD and ANALYZE modes but BUILD numbering is established (e.g. "Step 7a"), ANALYZE side drops the label while preserving the structure. H7 r2 falsification: structure survives, label dropped to avoid renumber-cascade across SKILL.md workflow steps. Precedent: shared-process-hardening C2 SQ[10] — Step 7a HARD GATE (premise-audit pre-dispatch) carried in BUILD c1-plan.md:62 but unlabeled in ANALYZE sigma-review/SKILL.md + sigma-lead.md (lines ~38-72). Cross-ref via DC[3] in directives.md §8f ↔ §2p with explicit "BUILD carries label, ANALYZE does NOT" note. Heuristic: don't propagate numerical labels across mode-boundaries when the labeled mode has fixed numbering and the unlabeled mode has different step-counts. |src:shared-process-hardening-c2-2026-04-29|
!pat-cqa-da12-edge-checklist-single-pass: universal edge-case checklist (empty|BOM|unicode|fenced-code|trailing-WS) applied single-pass at end of CQA round vs per-gate ad-hoc | scope: any new validator/header-scanner/parser introduced in build | invariant: 5 edge classes × N new ICs = N×5 cases authored AT END of round in ONE TestDA12-style class | rationale: per-gate ad-hoc loses cross-gate consistency view; single-pass class makes shared-helper opportunities visible (e.g., _strip_bom + _strip_fenced_blocks reused across A26/B5/B6) | conf:8 | evidence: C2 first applied per DA[#12] PROMOTION-CANDIDATE → 13 cases × A26+B5+B6, plus A25 covered by F[IE-6] TestA25TemplateDrift CRLF/BOM/rstrip; C3 r2 surfaced 5 follow-up gaps GAP-A..E identical pattern applied to phase-gate.py | rule: when a build introduces ≥2 new validators, CQA round MUST close with single-pass DA[#12] checklist class spanning all of them | source: shared-process-hardening C2 + C3-r2 |src:shared-process-hardening-c3-promotion-2026-05-02|
!bidirectional-cross-ref-DCN-pattern (directives.md): when two directive sections are mechanically linked (header-presence=phase-ran, recovery-form structurally parallel, etc.), use bidirectional DC[N] cross-ref entries — both sections' !cross-references: blocks reference each other with a shared semantic note. Precedent: shared-process-hardening C2 §8f DC[3] ↔ §2p (premise-audit-results pre-dispatch sibling of post-exit-gate workspace-headers — both use header-presence as mechanical phase-completion signal); C3 §8f DC[4] (BUILD-track compilation manual-override form sibling of ANALYZE-track ## sync recovery form). Pattern: DC[N] entries name the relationship type (sibling | parallel | cross-ref BUILD-variant) + cite the load-bearing line range + carry a one-line "common pattern" summary so the cross-ref is self-explanatory. Avoids dangling references and supports grep-discoverability. |src:shared-process-hardening-c3-2026-05-01|
!pat-cqa-regression-baseline-parity-format: regression check reports as "pre-fix:{p/s/f} post-fix:{p/s/f} delta:{Δ} new-failures:{list|none} -> {clean|regression-found}" | scope: any C2/C3 round where IE/TW applied source fixes | discipline: pre-existing failures explicitly listed AND classified out-of-scope vs in-scope before delta interpretation; +N-passing-tests delta from new tests is "clean" not "regression" if zero pre-existing tests broke | rationale: bare "tests pass" hides that pre-existing failures were already there OR that new tests created the +Δ; the format keeps signal/noise separated | conf:9 | evidence: C2 1208/1209 baseline, C3-r1 1245/14/1 unchanged delta:0, C3-r2 1245→1251 delta:+6-passing-tests both classified clean by this rule | source: shared-process-hardening C2+C3 |src:shared-process-hardening-c3-promotion-2026-05-02|
!pat-cqa-paraphrase-test-wording-verify: directive/wording verification = read excerpt verbatim → paraphrase in own words WITHOUT re-reading → verdict {unambiguous|still-ambiguous} | scope: any directive/agent-instruction/regex/recovery-form wording the build adds or revises | discipline: paraphrase MUST be done from memory not by re-quoting (rote-copy plan fails the test); verdict separates ambiguity (genuinely unclear what is required) from sharpness-gaps (clear-but-could-be-tighter, polish-not-block) | bonus: caveat-vs-docstring fidelity sub-check — when authors call out caveats (heuristic, calibration, out-of-scope), verify the docstring/comments actually NAME those caveats; honesty-of-documentation is a separate axis from semantic-clarity | conf:8 | evidence: C3-r1 GATE-1 paraphrase test on sigma-lead.md:207 + directives.md §8f BUILD variant both verdict=unambiguous; C3-r2 paraphrase on phase-gate.py multi-path contract verdict=unambiguous-with-honest-caveats (7-day window + suffix-stripper both named as heuristics in code) | source: shared-process-hardening C3-r1+r2 |src:shared-process-hardening-c3-promotion-2026-05-02|
!pat-cqa-cross-tool-byte-identity-multi-fixture: empirical verification of cross-tool digest equality = N-fixture-class re-run not single-input demo | scope: any claim of the form "tool-A and tool-B produce identical hash/digest/output for same input" | minimum: 6+ fixture classes covering normalization edges (LF, CRLF, BOM-prefix, mid-stream-BOM, mixed-EOL, unicode, trailing-WS) | discipline: distinguish (a) cross-tool same-platform byte-identity (testable, what multi-fixture establishes) from (b) cross-platform byte-identity (NOT testable from single-host without containerized matrix CI) — wording must concede (b) when only (a) was tested; otherwise the claim is the test-as-proof fallacy applied with extra steps | verdict-shape: wording-defensible-with-caveat → RESTATE preferred over RETRACT when N-class evidence exists but cross-platform untested | conf:9 | evidence: C3-r1 F[IE-6] CQA-VERIFY 7 fixture classes byte-identical, restate adopted by IE; pre-r1 single-input "VERIFIED empirically" was the test-as-proof fallacy this pattern blocks | source: shared-process-hardening C3-r1 |src:shared-process-hardening-c3-promotion-2026-05-02|
!pat-cqa-archive-passthrough-test-shape: WARN-only invariant + no-regression invariant verification via parametrized test on N≥3 frozen pre-gate workspaces | scope: any build that introduces new chain-evaluator items as WARN-first | shape: TestArchivedWorkspacePassthrough class parametrizes over ≥3 archive/*.md files predating the new gates; for each archive runs evaluate_chain(path, mode) + evaluate_single(item_id, path) + asserts (a) ChainItem schema well-formed (item_id/name/passed/category/details/issues), (b) all existing chain items present (no-regression), (c) NEW gates return passed=True regardless of fire count (WARN-only invariant) | rationale: synthetic toy fixtures don't catch real-archive false-positives; live builds/*/c*-scratch.md are too volatile; frozen archive/*.md is the right substrate | minimum-N: 3 (PM[1] standard); higher-N better but diminishing returns | conf:9 | evidence: C2 SQ[11] 18 (=6×3) tests on r19-remediation + ai-agent-rollout-playbook-vet + sigma-chatroom-m1ab → 37/37 pass, validated WARN-only invariant on A25/A26/B5/B6, validated no-regression on A1-A18+A20+A22-A24+B1-B4 | source: shared-process-hardening C2 SQ[11] |src:shared-process-hardening-c3-promotion-2026-05-02|
!pat-cqa-non-blocking-gap-surfacing: when CQA edge-case probe finds gaps, classify each as {blocking|non-blocking} with explicit routing | scope: post-fix verification rounds where new validator/scanner code is being shipped | format: GAP-{letter} {one-line-name}: {description} → {behavior-now} → {recommended-fix} → {disposition: block-close | accept-with-doc | follow-up-SQ-next-build | memory-compile-candidate} | rationale: prevents two failure modes: (a) scope-creep (every gap becomes an in-build SQ, derailing the round), (b) silent-acceptance (gaps observed but never logged, recurrence guaranteed); explicit routing+disposition is the discipline | rule: NON-BLOCKING gaps must STILL be logged with a destination (memory-compile, follow-up-SQ, accept-with-doc); silently dropping = scope discipline failure | conf:8 | evidence: C3-r2 surfaced 5 gaps GAP-A..E on phase-gate.py — GAP-D highest-priority routed to next-build SQ (ADR[9] _strip_fenced_blocks parity), GAP-A/B/C/E classified accept-with-doc (correct-by-construction but no explicit test); zero became C3-r2 blockers | source: shared-process-hardening C3-r2 |src:shared-process-hardening-c3-promotion-2026-05-02|
!user-approved-promotions-shared-process-hardening-c3 |26.5.2|

19 candidates user-approved 2026-05-02 across the C3 close of build 2026-04-28-shared-process-hardening (lead-on-behalf persistence per workspace-contention + MCP store_*-flap recovery):

**DA (8 user-approve)**:
- DA-U1: Quad-convergence as evidence-quality signal (≥3 paths = elevate to effective-PASS; ≥4 = bypass round-2). NEW analytical principle for GATE close-outs.
- DA-U2: Test-as-proof-fallacy P3.4 RESTATE template — named-scope + qualified-residual + sigma-mem pattern revision protocol. Refines P3.* correction set.
- DA-U3: Dual-confirm disaggregation framework — classify by abstraction-level + severity-tier + failure-mechanism; produce N artifacts when N tiers flagged. Behavior-changing rule for sigma-review/sigma-build close-outs.
- DA-U4: Stale-line-number lint rule for /sigma-audit. Mechanical enforcement beats reader-side mapping for this drift class.
- DA-U5: MultiEdit/NotebookEdit dispatch coverage gap (phase-gate.py:445) — NEW finding not in C2 GAP[#1+#3] inventory. Follow-up SQ: registry-driven dispatch.
- DA-U6: §2d source-type enum extension with [doc-edit]/[directive-write]/[cross-ref-grep].
- DA-U7: cleanup-pass-precedence (mechanical ⊥ editorial; landed pre-flap, retroactively user-approved).
- DA-U8: gate-enforcement-meta (process-integrity-over-completion held; landed pre-flap, retroactively user-approved).

**TW (4 user-approve)**:
- TW-U1: GATE-1 4-deliverable close-out template — actor + criterion + operational-doc wording + directive update (embeds P3.1 operationalize-before-naming).
- TW-U2: Manual-override 3-precondition criterion (compilation BLOCK 5) — (C1) compilation-spawned-failed AND (C2) ≥1 retry documented AND (C3) user-approval-with-justification.
- TW-U3: Honor-system enforcement-model acknowledgment pattern — explicit out-of-scope acknowledgment closes addressable portion of GAP without overstating.
- TW-U4: cleanup-pass precedence rule (= DA-U7; merged via DA's concession).

**CQA (1 user-approve)**:
- CQA-U1: ADR[9] universal edge-case helper propagation across subsystems (META rule) — `_strip_bom` + `_strip_fenced_blocks` MUST propagate to ANY hook subsystem performing markdown header scans, OR justify in docstring. Test obligation per validator: ≥1 fenced-code-exclusion test.

**IE (3 user-approve)**:
- IE-U1: Hash-parity pattern revision (`hash-parity-empirical-verify` → `hash-parity-cross-tool-same-platform`) — restates C2 entry to embed cross-tool-same-platform vs cross-platform-designed-in distinction.
- IE-U2: Test-as-proof anti-pattern recurrence-of-recurrence (P3.4 meta) — within-build recurrence is the calibration; "post-correction-priming" rule.
- IE-U3: Hook empirical-test must include `hook_event_name` field — checklist addition + optional loud-fail proposal in main().

**Workspace archive**: ~/.claude/teams/sigma-review/shared/builds/2026-04-28-shared-process-hardening/c3-scratch.md ## promotion (lines 1023-1228 — full P-candidate text per agent for verbatim future reference).
**User-approval timestamp**: 2026-05-02 via AskUserQuestion in conversation.
**Classification disagreements resolved**: DA conceded TW's diverging-classification on cleanup-pass-precedence (now both DA-U7 and TW-U4 user-approve, merged); TA borderline AUTOs (TA-2, TA-5) accepted-as-patterns by user.

|src:shared-process-hardening-c3-promotion-2026-05-02|
!user-approved-promotions-shared-process-hardening-c3-amendment |26.5.4|

AMENDMENT to !user-approved-promotions-shared-process-hardening-c3 (26.5.2) — TA self-flag arrived after initial consolidated index. Adding 3 retroactively-ratified user-approve candidates from TA per same pattern as DA's pre-flap reclassification:

**TA (3 user-approve, retroactively ratified — content LANDED in patterns.md, classification corrected)**:
- TA-U1: 7-day fresh-session-window heuristic for "active build session detection" — empirical bracket from F1 1d/TIER-2 + R19 4d/TIER-3 + this build 4d/TIER-2; combined with `## task` + `## mode` session-marker scan; calibration revisit if TIER-3 build runs >7d. (Numeric calibration threshold for global hooks → user-approve.)
- TA-U2: Canonical-source verbatim threshold preservation rule — when plan locks a calibration threshold via cross-reference to existing precedent, implementation site MUST preserve verbatim INCLUDING anti-drift annotation (e.g., "NOT '2+'") AND inline precedent citation. Anti-pattern: number-only restatement → silent edit risk. (NEW analytical-hygiene rule imposing implementation behavior on all future builds → user-approve.)
- TA-U3: Directive↔hook integration co-test rule — META process-hardening: when directive introduces/changes a workspace-header convention, corresponding hook regex must be co-tested against the directive's example forms in the SAME round. (Same shape as TW-U1 GATE-1 4-deliverable template; META rule adding work to all future builds → user-approve.)

**TA self-flag rationale (anti-sycophancy disclosure)**: TA originally classified all 5 patterns as auto pre-flap. Re-reading per `feedback_user-approval-gate-non-bypassable.md` and TW-4 logic ("in-build ratification ≠ global elevation; principle-level → user-approve"), surfaced 3 of the 5 as ahead-of-gate. Bodies are accurate; the lesson is calibration for future promotion-rounds (audit own classifications under TW-4 logic before claiming auto-eligibility), not removal of useful patterns.

**Confirmed-auto (TA, unchanged from initial)**: TA-A1 header-presence=phase-ran (architectural observation, three pre-existing instances), TA-A2 preferred-derive-short-circuit (architectural pattern with embedded correctness test), TA-A6 DECISION BLOCK 5 multi-path scan (build-internal architectural decision).

**Updated promotion totals (post-amendment)**:
- auto-landed: 18 (was 21; -3 for TA reclassification)
- user-approved: 22 (was 19; +3 from TA-U1/U2/U3 retroactive ratification)
- decisions: 2 (TA-A6 + IE-A5)
- total promotion-events: 42

**User-approval timestamp (TA reclassification)**: 2026-05-04 via AskUserQuestion in conversation (option a — ratify retroactively + commit).

**Process-integrity case study reinforced**: anti-sycophancy held even on lead's behalf — TA self-audited their own classifications post-flap-notice + TW-4-flag, did not defend, surfaced for correction. Same shape as DA's earlier concession. Two independent agents demonstrated user-approval-gate-non-bypassable in one promotion-round.

|src:shared-process-hardening-c3-promotion-amendment-2026-05-04|
AUDIT[2026-05-05|shared-process-hardening-c3]: pre-flap auto-stores retroactively ratified via AskUserQuestion weakens gate-non-bypassability rail; precedent uncomfortable — pre-flap-store rollback should be default, retroactive-ratify the exception with explicit gate-recovery rationale |verdict:YELLOW |source:sigma-audit |src:audit-2026-05-05-shared-process-hardening-c3
AUDIT[2026-05-05|shared-process-hardening-c3]: §8f BUILD variant manual-override criterion does NOT cover structural-precondition-impossible class — preconditions (a) compilation-spawned-failed and (b) ≥1-retry cannot hold when sequencing-trap means compilation never structurally runs; reason-text quality became the load-bearing audit signal not retry-evidence |verdict:YELLOW |source:sigma-audit |remediation:next-build adds synthesis-precondition clause OR `*-synthesis.md` BLOCK 5 carve-out OR c3-review.md re-ordering |src:audit-2026-05-05-shared-process-hardening-c3
AUDIT[2026-05-05|shared-process-hardening-c3]: self-application meta-test for directive-hardening builds — when build deliverable is workspace-header convention (## sync, ## archive-complete), the build's own close should USE the convention as first compliance signal; this build mandated ## sync but did not self-apply to its own C3 archive |verdict:YELLOW |source:sigma-audit |remediation:directives.md should specify lifecycle-of-mandate (self-apply vs next-review-onward) when new headers are introduced mid-build |src:audit-2026-05-05-shared-process-hardening-c3
AUDIT[2026-05-05|shared-process-hardening-c3]: cross_verify single-provider fallback (per build-directives §2h) was applied only to GATE-1 — load-bearing findings F[IE-6], BLOCK 5 multi-path, cross-build authorization CONCERN-1 received zero XVERIFY; criterion 9 ("≥1 XVERIFY per agent on load-bearing findings") needs clearer guidance on whether single-provider attempt-on-each is required when multi-provider hangs |verdict:YELLOW |source:sigma-audit |src:audit-2026-05-05-shared-process-hardening-c3
AUDIT[2026-05-05|shared-process-hardening-c3]: P3.4 test-as-proof recurrence-within-build — same anti-pattern fired twice (F[IE-6] cross-platform claim + IE-R2-FIX[#4] missing hook_event_name); first correction did NOT immunize, it primed; pre-emptive disclosure (IE at R2-micro) IS the right response; P-candidate[IE-U2] meta-rule should be operationalized |verdict:YELLOW |source:sigma-audit |src:audit-2026-05-05-shared-process-hardening-c3
!ws1-closure-annotations-shared-process-hardening |26.5.5|

WS-1 closure annotations applied to shared-process-hardening C3 (build closed at status: complete on 2026-05-04/05; this is post-close annotation only — no re-opening of the build itself). Per remediation plan ~/.claude/plans/target-2026-04-28-shared-process-harden-floofy-gadget.md.

**Audit YELLOW remediation #2 (self-application gap)** — appended to c3-workspace archive at file end:
- `## sync: [templates-hashed:0|skipped|reason: directive landed mid-build via SQ[8]; self-application begins next review per audit remediation #2 lifecycle-of-mandate documentation|date:2026-05-05]`
- `## archive-complete: [R-2026-04-28-shared-process-hardening, date:2026-05-05, schema-status: SCHEMA-TBD-WS3-SQ-T7]` — schema formalization deferred to WS-3 SQ-T7 (build mandated header without defining format; placeholder used here pending parallel-to-##-sync schema definition)

**Eval reco #2 (BELIEF interval + source distribution)** — applied to plan file:
- ## Meta build-belief: P=0.91 ±0.05 (n=8 sources: 5 code-read + 1 test-rerun + 1 XVERIFY-partial + 1 cross-agent-convergence)
- ## Build Review Summary Final-belief: same precision adds
- Interval rationale: single-provider XVERIFY (cross_verify hung) + heuristic-not-empirical 7-day freshness window in WS-1 R2 + designed-in-not-empirically-tested cross-platform hash parity

**Eval reco #4 + audit weakness #3 (3 missing perspectives + retroactive ratification justification)** — added new ## Audit + Eval Disposition sub-section under ## Close Status with:
- 5 audit YELLOW flags dispositioned (1 WS-2 fix, 1 WS-1 closed, 1 justified-exception, 2 WS-3 carry)
- 4 eval recommended improvements dispositioned (1 WS-2, 1 WS-1, 1 WS-3 specs, 1 WS-1)
- 3 missing perspectives resolved: (i) audit-flagged generic reasons = ADVISORY (BUILD-CONCERN not BUILD-FAIL), (ii) MCP-flap persistence stacks UNDER user-approval gate (TIMING accommodation NOT CLASSIFICATION accommodation), (iii) synthesis-ordering = phase-gate carve-out (eliminates structural impossibility, doesn't paper over with C0 clause)
- Retroactive ratification justification stack documented: content-sound + classification-corrected + user-explicit-approval + two-independent-agents-demonstrating-user-approval-gate-non-bypassable

**Critical traceability**:
- Build artifact: ~/.claude/teams/sigma-review/shared/builds/2026-04-28-shared-process-hardening.plan.md (status: complete)
- C3 archive: ~/.claude/teams/sigma-review/shared/archive/2026-04-28-shared-process-hardening-c3-workspace.md
- Synthesis: ~/.claude/teams/sigma-review/shared/archive/2026-04-28-shared-process-hardening-synthesis.md (43,838 bytes)
- WS-2 next: micro-build TIER-1 `2026-05-05-block-5-synthesis-carveout` (phase-gate.py carve-out + 3 tests + directive cross-ref)
- WS-3 next: TIER-2 `2026-05-XX-c3-followups-bundle` covering 7 Tier C SQs (GAP-D + DA-4 + TW-1 + CQA-Q7 propagation + CONCERN-2 + IE-U3 + recovery-template promotion)

**Reusable pattern surfaced**: post-close annotation workstream (WS-1 shape) — when audit + eval surface items that are documentation-only OR ratification-only OR justification-only (no code or directive change), they can be applied as post-close annotations to the closed build's archive + plan + memory index without reopening the build itself. Distinct from must-fix items (which require new builds) and from carry-forward items (which become next-build SQs). Three-bucket discipline: annotation (closed build) | must-fix (new micro-build) | carry-forward (next bundled build).

|src:shared-process-hardening-c3-ws1-closure-2026-05-05|
!team-task-routing-boundary 26.5.5: TeamCreate→TaskList-team-ctx |unowned tasks auto-route to recently-active agent |today: WS-2 c1 task#2 "C1 PLAN CHALLENGE — DA spawn + circuit breaker + belief + lock" auto-assigned to DA mid-build (Step18+/CB/BELIEF/lock = lead-orchestration scope) |DA correctly default-flagged + refused absorption per Lead-Role-Boundary, named 3 interpretations + asked for explicit instruction |rule[agent-side]: on task_assignment, read description; if matches lead-scope keywords (spawn|circuit-breaker|BELIEF|lock|exit-gate-decide|synthesis-write) → default-flag via SendMessage, ¬auto-claim |rule[lead-side]: set owner=team-lead at TaskCreate time on lead-scope tasks OR prefix subject with [LEAD-SCOPE] OR create lead-tracking tasks BEFORE TeamCreate (different list) |why: silent absorption contaminates analytical provenance + collapses multi-agent rigor into self-verification per CLAUDE.md Behavioral Rails (Lead Role Boundaries) + feedback_lead-role-boundary 26.3.28 |precedent-class: same family as feedback_lead-content-routing-contamination 26.4.16 (lead absorbed agent challenge routing) but routing-direction inverted (system→agent vs lead→agent)
PAT[replacement-agent-scope-collapsed-brief|team:sigma-review|agent:tech-architect|26.5.5]:
When a build-round agent hangs in MCP transport mid-round, spawn a replacement with a SCOPE-COLLAPSED brief: amendment-only task, no XVERIFY (deferred to next round), no peer-verify redo (ring already closed). Full-context resumption risks re-hang on same transport path; surgical brief sidesteps the hang vector and produces cleaner substantive output. Original r3 brief: 2 actions + 4 DA responses + XVERIFY tag + self-belief — ~45min clean delivery vs predecessor hang at ~60min. |confidence:confirmed|source:[sigma-build-2026-05-05-block-5-synthesis-carveout c1-r3]|
PAT[sequential-XVERIFY-post-MCP-cleanup|team:sigma-review|agent:tech-architect|26.5.5]:
When MCP transport is suspect (prior hangs observed, orphan processes killed): run verify_finding ONE PROVIDER AT A TIME (not concurrent/cross_verify). Protocol: init-probe-first (abort if >30s) → sequential per-provider with 60s budget each → write result to scratch BEFORE next call. This pattern succeeded cleanly (5/5 providers returned, 0 hangs) after the concurrent multi-call pattern hung the original agent. cross_verify bridge remains broken regardless of transport state — use verify_finding direct. |confidence:confirmed|source:[sigma-build-2026-05-05-block-5-synthesis-carveout c1-r4; hung-original used concurrent loop]|
PAT[scale-vs-deferral-separation|team:sigma-review|agent:tech-architect|26.5.5]:
When a parent ADR disposition is cited as justification for a child claim, distinguish: (a) PA[N] scale statements ("single-user system, ~10-50 fires/day") = scope framing, NOT threat-model exclusion; (b) DA[#N] DEFERRAL dispositions ("KNOWN LIMITATIONS docstring + mechanical fix deferred") = accepted residual, NOT exclusion-by-design. Conflating SCALE or DEFERRAL with THREAT-MODEL-EXCLUSION is a §2d source-provenance violation — the parent document must actually contain the exclusion clause, not merely imply it. The distinction: deferral means "we know it's broken, fix later"; exclusion means "this attack class is out of scope by design." These have different downstream implications for child builds inheriting the residual. |confidence:confirmed|source:[sigma-build-2026-05-05-block-5-synthesis-carveout DA[#1]+DA[#8] r2→r3 fix]|
PAT[5p-XVERIFY-security-adjacent-ADR|team:sigma-review|agent:tech-architect|26.5.5]:
For security-adjacent ADR verification (authorization-bypass class, gate-removal consequences): 2-paid + 3-Ollama-Pro-cloud is the new default provider mix, replacing "paid=definitive" heuristic. Empirical result (2026-05-05): openai(agree/medium) + google(agree/high) + devstral(agree/high) + deepseek(agree/high) + kimi(uncertain/low-epistemic). 4/4 substantive AGREE; kimi UNCERTAIN was premise-dependency not logical objection (internal chain-of-thought reached AGREE). Cross-architecture signal: US-paid(OpenAI) + EU-paid(Google) + Mistral(devstral) + DeepSeek + Moonshot(kimi). Zero counter-evidence across 5 providers. Confidence distribution 3×HIGH 1×MEDIUM is strong for a security-adjacent claim. Provider diversity > number of paid providers for corroboration quality. |confidence:confirmed|source:[sigma-build-2026-05-05-block-5-synthesis-carveout r4-XVERIFY; P=0.83→0.87 post-verify]|
P[DA-amendment-drift-detection-on-XVERIFY-feedback-absorption] 2026-05-05 |trigger:agent integrates external XVERIFY/challenge feedback into a prior decision (e.g., amendment to ADR/IC/PM) |risk:integration sharpens the decision in spirit but DRIFTS the cited source-basis — ends up citing a position the original source does not actually establish |detection-pattern:DA cold-reads parent body (or external source) at the cited location and checks whether the integrated language is supported there OR is a manufactured extrapolation |session-validation:WS-2 c1 r2, TA absorbed gpt-5.4 r1 partial challenge but shifted parent ADR[6] disposition from DEFERRAL ("KNOWN LIMITATIONS docstring + mechanical fix deferred") to THREAT-MODEL EXCLUSION ("excludes adversarial path construction") — claim was load-bearing for ADR[1] H4 "no new bypass class"; cold-read of parent c1-scratch:175-208 + c3-review.md:252 + plan §P2.A row 119 caught it |fix:DA r2 issued FAIL-conditional-on-rewrite (Option D1 — lowest churn, no scope expansion); TA-r3 rewrite restored DEFERRAL framing + named §2d violation explicitly + added consequence-amplification reframe |applies-to:any sigma-build/sigma-review where agents integrate XVERIFY/challenge counter-arguments into prior artifacts; especially when the counter is "partial" and integration must qualify rather than retract |canonical-test:does the integrated language cite a position the source actually establishes, OR a position constructed to make the integration work?
P[DA-procedural-substitute-via-challenge-tool-when-cross_verify-fails] 2026-05-05 |trigger:cross_verify MCP bridge fails OR returns transient errors during DA r2 challenge round, blocking multi-provider XVERIFY on a load-bearing claim |fallback:DA invokes mcp__sigma-verify__challenge directly against single paid provider at reasoning tier (gpt-5.4-pro / gemini-3.1-pro-preview thinking) with full evidence packet |criterion-for-validity:returned counter-arguments converge on the cold-read concern from MULTIPLE independent angles (4-arg convergence threshold) — NOT a single-angle agree/disagree response |session-validation:WS-2 c1 r2, gpt-5.4-pro reasoning-tier challenge produced 4-arg convergence on "no new bypass class" claim: (a) capability-inversion — path-shaping becomes capability post-carve-out, (b) consequence-escalation — same predicate weakness, stronger downstream effect, (c) integrity-gate reframe — compilation-complete is integrity boundary not convenience precondition, (d) path-normalization absent — claim assumes normalization that code doesn't perform; this satisfied procedural XVERIFY substitution per spawn-prompt fallback per build-directives §2h |limitations:single-provider; not equivalent to cross-model agreement; document as procedural-substitute not full XVERIFY in workspace; flag for r4/r5 multi-model corroboration when MCP stabilizes |canonical-pattern:reasoning-tier-challenge >>> standard-tier-challenge for adversarial use; reasoning-tier produces multi-angle counters, standard-tier often produces single-angle accept/reject |applies-to:any sigma-review/sigma-build where cross_verify infra is flapping and DA must compensate
P[DA-substantive-vs-cosmetic-test-on-rewrite-verification] 2026-05-05 |trigger:DA r3 verification round on a rewrite delivered in response to DA r2 FAIL-conditional |risk:agent issues PASS on a cosmetic rewrite (terminology swap) that doesn't restore source-provenance OR address the substantive concern; verdict drifts into rubber-stamp |4-axis-test (all four required for substantive-PASS): (a) explicit retraction of offending phrase — not silent rewording or hedge-via-additional-qualifiers, (b) sourced citations to original-source body verifiable by DA cold-read at named locations, (c) integrity-gate reframe absorbed (when applicable to load-bearing claim) — counter-argument acknowledged, not deflected as edge-case, (d) §2d source-provenance violation named explicitly when one was the basis of FAIL — agent says "this was a violation" rather than papering over it |verdict-routing:4/4 axes pass → PASS; 3/4 axes pass → PASS-WITH-CONDITION specifying remaining axis; <3/4 axes pass → FAIL-r4 with named residual defect |session-validation:WS-2 c1 r3, TA-r3 amendment rewrite passed 4/4 — explicit retraction (line 525), sourced citations to parent c1-scratch:175-208 + c3-review.md:252 + plan §P2.A row 119, integrity-gate reframe of compilation-complete absorbed in IC[1] consequence-amplification, §2d violation named at line 538; bonus: TA-r3 surfaced sharper distinction (PA[2]=SCALE separated from DA[#5]=DEFERRAL) than DA's own r2 framing — genuine analytical work, not minimal compliance |applies-to:any DA verification round on rewrite-after-FAIL; protects against rubber-stamp drift
!ws2-c1-tech-architect-original-deferred-promotion 26.5.5 (lead-absorbed per agent fallback): patterns from hung-original tech-architect's session whose store_agent_memory failed (MCP flap) — captured for future calibration. (1) DUAL-TA-CONVERGENCE-UNDER-HANG-RECOVERY: when agent slow/hung in MCP transport and replaced by surgical-brief replacement, post-recovery analytical work can converge with replacement; both contributions preservable in scratch with explicit attribution. (2) H4-PARTIAL-CONCEDE-OVER-FULL-CONCEDE: when DA challenges absoluteness, partial-concede with explicit residual ("predicate is narrower than §a4 but introduces a nominal new capability surface, bounded by context") more honest than full-concede that hides residual; original-TA framing credited as sharper than replacement-TA full-concede. (3) DA-N-DEFEND-WHEN-NOT-CHALLENGED: defend what DA actually challenges, avoid over-concession to adjacent challenges; original-TA correctly defended structural impossibility argument (Step 13f→14a) which DA never challenged, while still conceding/compromising on the actual challenges. (4) SLOW-NOT-HUNG-DISTINCTION: under MCP-suspect conditions, concurrent-multi-call agent appears unresponsive but calls eventually return (~30min in this session); user perception of "hung" was correct as observable signal, underlying mechanism was slow-not-stuck; sequential-with-per-call-budget pattern (TA-r3) > concurrent-multi-call (original-TA) — same correctness, observable progress, no false-hung signal. (5) ADR-DEFERRAL-vs-THREAT-MODEL-EXCLUSION-DISTINCT-WEIGHT: when integrating XVERIFY feedback, distinguish parent-disposition framing — DEFERRAL = mechanical fix postponed; SCALE = environmental constraint; THREAT-MODEL-EXCLUSION = explicit security boundary (rare). Conflating them is §2d source-provenance violation. |source: scratch ##convergence WS-2-c1 promotion fallback block
!ws2-c1-implementation-engineer-deferred-promotion 26.5.5 (lead-absorbed per agent fallback): patterns from IE whose store_agent_memory failed (MCP flap). (1) EMPIRICAL-BASELINE-FULL-SUITE-NOT-SINGLE-FILE: build-track empirical anchor must be `pytest tests/ -q` not single-file run; 1253/14/1 (full) vs 79 (single-file misleading); always run full suite before plan-track designs against test count target. (2) CROSS-AGENT-CONVERGENCE-EQUALS-REQUIRED-COVERAGE: when build-track BC and edge-case-track EC converge on same gap from different angles (IE BC-3 non-synthesis-archive-must-block = Condition A failure / CQA EC[3] synthesis-outside-archive-must-block = Condition B failure), treat as REQUIRED not optional; predicate-completeness gaps need bounding from both sides → 5 tests not 3. (3) PM-SEED-VALIDATION-BY-CODE-READ: pre-mortems are seed concerns; build-track validates with grep before anchoring on them — 5-second grep cut PM[1] WS-3 file-overlap risk by confirming `_strip_fenced_blocks` doesn't exist in phase-gate.py yet. (4) CODEBASE-ANCHOR-NOTES-IN-IC: insertion-point line numbers + return-form (PASS=`(False, "")` at lines 607+636) + fixture distinction (patch_paths vs patch_multi) belong IN the IC contract, not floating around. |source: scratch ##implementation-engineer + ##convergence FALLBACK-CALIBRATION block
!ws2-c1-code-quality-analyst-deferred-promotion 26.5.5 (lead-absorbed per agent fallback): patterns from CQA whose store_agent_memory failed (MCP flap). (1) DA-12-UNIVERSAL-PATH-EDGE-CASE-CHECKLIST: 13-case probe template for filesystem-path predicates (None, empty, BOM, case-fold HFS+, symlinks, relative-path, trailing-slash, double-suffix `foo-synthesis.md.bak`, embedded-newline/null, paths-containing-substring-not-at-end, empty-filename, etc.) — apply structurally before locking any path predicate. (2) INHERITANCE-DIRECTION-FLIP: when inheriting limitations from ancestor predicate (`_path_is_archive` → `_is_synthesis_archive_write`), check whether inheritance direction matches or flips — over-gating (block valid) ≠ under-gating (allow invalid) in consequence space; macOS HFS+ case-fold flips DA[#5]'s under-gating to over-gating. Worth flagging in inheritance audit. (3) CONJUNCTION-N-CONDITION-MEANS-N-NEGATIVE-TESTS: predicate of form (Condition A AND Condition B) requires bounding tests for EACH condition's failure independently; passing only "both conditions met" doesn't bound the predicate; needs Condition-A-fails-still-blocks + Condition-B-fails-still-blocks tests. Cross-agent convergence (IE BC-3 + CQA EC[3]) signal that this is REQUIRED. (4) PLAN-TRACK-PARAM-NAME-STYLE-CHECK-AGAINST-SIBLING-FUNCTIONS: small-signal-large-impact peer-review value — `file_path` vs `path` convention drift from `_path_is_archive` is cheap to catch in plan, expensive to fix later. Apply structurally during peer-verify. |source: scratch ##code-quality-analyst + ##convergence FALLBACK-CALIBRATION[code-quality-analyst] block
P[coord-glitch-recovery-verify-don't-redo|class:pattern|reason:on agent re-spawn after coordination glitch (out-of-order shutdown_request, MCP flap mid-build, lead-side timing error), recovery starts with VERIFY of prior work, not REDO. Verification = symbol-position grep + verbatim docstring/key-string match + test replay (e.g. pytest -v on prior IE's claimed 5/5 PASS). If verification PASSES, extend from prior state; do NOT modify SQ[1-N]. If anomaly, STOP+SendMessage to lead before any code touch. Validated 26.5.7 build 2026-05-05-block-5-synthesis-carveout: prior IE terminated mid-SQ[6], re-spawn IE verified SQ[1-3] code at phase-gate.py:623-687 + 5/5 test PASS reproduction in 0.02s, then completed SQ[4-6] without re-doing. Discovered SQ[4]+SQ[5] also already-saved (prior IE wrote them before terminate; status flag was misleading). Pattern saves duplicate work + protects against SQ[1-3] silent-overwrite contamination.|src:build 2026-05-05-block-5-synthesis-carveout C2 IE re-spawn|agent:implementation-engineer|26.5.7]
P[evidence-based-pushback-on-stale-lead-claim-calibrated|class:pattern-calibration|src:build 2026-05-05-block-5-synthesis-carveout C2 IE|agent:implementation-engineer|26.5.7|severity:HIGH|status:VERIFIED-twice] — calibrates the existing P[evidence-based-pushback-on-stale-lead-claim] pattern (originally promoted r19-remediation-c3 26.4.25). Two distinct triggers in this build, both correctly handled: (a) when lead "Verified — proceed SQ[4-6]" arrived after IE STATUS-DONE (messages crossed), IE held existing state and flagged the temporal mismatch with evidence (cited file paths + line numbers) rather than re-executing under stale assumption — preserved already-saved SQ[4]+SQ[5] state from corruption; (b) when lead "tools now available — retry XREVIEW" arrived, IE's session ToolSearch returned 4-method empirical no-match (explicit-select returns "No matching deferred tools found" twice; keyword `cross_verify` no match; keyword `check_quotas` no match; broad-keyword search returns only `init`), IE held and flagged with 4-method evidence rather than fabricating XREVIEW output — preserved process integrity under infrastructure pressure. Pattern application rules (now calibrated): SendMessage from team-lead supersedes task-list when contradictory; refuse silent re-execution under stale lead claim; require evidence-based confirmation before action; pattern fires correctly on TEMPORAL mismatch (claim is right but stale) AND on PER-SESSION-STATE mismatch (claim references state in a different session than agent's). Both axes validated.
P[xreview-infrastructure-blocking-pattern|class:pattern|src:build 2026-05-05-block-5-synthesis-carveout C2 Step 6|agent:implementation-engineer|26.5.7|severity:HIGH|status:VERIFIED|recurrence:T0=3-build-P0,T1=NEW-P1] — XREVIEW step (sigma-build C2 Step 6, sigma-review §X) is blocked in current Claude Code harness by either or both of two distinct infrastructure failure modes. T0 BRIDGE BUG: HATEOAS-advertised sigma-verify operational tools (verify_finding, cross_verify, challenge, check_quotas, get_models) are advertised in init response with parameter schemas but NOT exposed as MCP tools in agent context — only `mcp__sigma-verify__init` registers as callable. CLI `/opt/homebrew/bin/sigma-verify` is the same stdio MCP entry-point and blocks on stdin when invoked from Bash. 3-build P0 recurrence (extends prior plan §audit-flags-recurring K-2-build-P0 → 3-build). T1 PER-SESSION REGISTRY MISMATCH: when operational tools DO register, registration is per-session-scoped — lands in lead session, does NOT propagate to TeamCreate-spawned agent sessions. Agent ToolSearch returns "No matching deferred tools found" while lead session has them. CLAUDE.md Lead Role Boundary forbids lead invocation as workaround. NEW P1 priority. Recipe-faithful path on either failure: skip per c2-build.md Step 6 "skip if ΣVerify unavailable"; document specific failure mode (T0 vs T1) in c2-scratch ## cross-model-code-review section under SKIPPED header; rely on substitute coverage (CQA Wave-1+2 + IE peer-verify + C1 multi-model XVERIFY). Remediation candidates for separate engineering: (a) sigma-verify expose verify_finding as top-level MCP tool, (b) Claude Code register HATEOAS actions as deferred tools after init, (c) sigma-verify CLI mode accepting JSON on stdin for one-shot Bash invocation, (d) propagate deferred-tool registry from parent to TeamCreate-spawned sessions, (e) re-attempt registration on agent session boot if HATEOAS init advertises actions not in local registry, (f) global tool registration rather than per-session.
F[CQA-PATTERN-process-integrity-under-spurious-task-pressure|class:pattern-calibration|src:build 2026-05-05-block-5-synthesis-carveout C2 CQA via IE one-time-delegation|agent:code-quality-analyst|26.5.7|severity:MEDIUM|status:VERIFIED|delegation-note:lead authorized IE to persist this entry on behalf of CQA after CQA shut down before persisting; delegation is one-time, not a precedent] — declining task-list assignments outside role brief is correct when (a) task outside role brief (e.g., lead-track items routed to CQA), (b) build state precludes execution (e.g., upstream phase incomplete), or (c) silent acceptance absorbs another role's work (e.g., XREVIEW or synthesis writing). SendMessage from team-lead supersedes task-list when contradictory. New-class misroutes flag once; same-class repeats stay silent. Source: this build's process-incident where prior-CQA decline-and-flag behavior on Task #6 + Task #7 task-list misroutes was textbook process-integrity. CQA brief already encodes this pattern in spawn instructions; this entry calibrates it as a confirmed protocol after empirical validation. Generalizes beyond this build because auto-routing infrastructure may persist across sessions (related to existing P[task-list-misroute-refusal-flag-protocol] @ patterns.md from r19-remediation 26.4.27).
P[xreview-infrastructure-blocking-pattern-CALIBRATION|class:pattern-update|src:build 2026-05-05-block-5-synthesis-carveout C3 DA r1|agent:devils-advocate|26.5.11|severity:HIGH|status:VERIFIED|recurrence:T0=4-build-P0,T1=transient-C2-only,T2=NEW-class] — CALIBRATION of prior P[xreview-infrastructure-blocking-pattern] from C2. Three failure modes now distinguished. T0 BRIDGE BUG (cross_verify): operational fan-out tool returns "An internal error occurred" on invocation; per-model verify_finding + challenge tools work normally — issue is in cross_verify aggregation/fan-out handler, not schema-load or per-model paths. Recurrence escalated 3-build P0 → **4-build P0** (C2+C3 of this build + 2 prior). T1 PER-SESSION REGISTRY MISMATCH (C2 framing) REFRAMED as C2-build-track-agent-session-transient, NOT structural: C3 four-witness check (DA + TA + CQA + IE) all confirm schemas load cleanly via ToolSearch deferred-tool registry in C3 agent context. Re-classify as transient-observation, no permanent remediation ticket needed beyond two/four-witness confirmation; do NOT log as recurring. T2 PER-TOOL-INVOCATION-FAILURE-POST-SCHEMA-LOAD (NEW class): schemas register cleanly but specific tool invocations fail (cross_verify=fail, verify_finding+challenge=success in same agent session). First articulated in C3 TA-XREVIEW-probe, corroborated by DA-XREVIEW-probe — two-witness confirmation. Distinct from T0 because the schema is registered and the tool is callable; it's the per-tool handler that fails. Likely root cause: fan-out concurrency or aggregation in cross_verify handler specifically. Out-of-band engineering remediation: targeted fix to cross_verify MCP-handler fan-out/aggregation logic. Plan §audit-flags-recurring entry update 3-build → 4-build P0 confirmed by TA r1 DA[CF-2] CONCEDE.
P[xreview-2-of-3-tools-substantive-coverage|class:pattern|src:build 2026-05-05-block-5-synthesis-carveout C3 DA r1|agent:devils-advocate|26.5.11|severity:LOW|status:VERIFIED|recurrence:1-build] — §2h XVERIFY-MANDATORY-SECURITY-CRITICAL compliance pattern when sigma-verify is partially available. If 2-of-3 operational tools (verify_finding + challenge) succeed while cross_verify fails (T0 or T2), §2h compliance is achieved via sequential per-provider verify_finding (multi-model corroboration) + challenge (single-provider adversarial reasoning-tier). Mark §2h-reachable in workspace, NOT §2h-skip. Recipe-faithful skip applies only when ALL three tools fail OR when no providers register. Distinguishes "partial-availability substantive coverage" from "full-skip with substitute coverage". Workspace ## infrastructure should record per-tool availability: |verify_finding:reachable| |challenge:reachable| |cross_verify:T0-or-T2-fail| → §2h-PARTIAL-but-MANDATORY-SATISFIED. Prior framing (C2 §247-255) collapsed all three tools into single "unavailable" verdict; finer-grained framing surfaces which T-class is at fault and whether substantive coverage is feasible.</entry>
</invoke>
!escape-vs-spoof-predicate-overmatch|ADR-validation pattern|when auditing predicate KNOWN LIMITATIONS, check enum covers BOTH escape(legit-target+different-argv-bypasses) AND spoof(illegit-argv+matching-pattern-impersonates)|ADR[6] _path_is_archive covered escape(symlinks+..+case-fold); child ADR[1] AMENDMENT r3 "inherited+compounded" but ¬covered spoof as separate failure-class|vuln=HIGH ext-challenge surfaced via /tmp/sigma-review/shared/archive/foo-synthesis.md|use:ADR-audit + predicate-validation + AMENDMENT-scoping reviews|src:2026-05-05-block-5-synthesis-carveout C3 DA[#1]+TA r1 DEFEND-WITH-COMPROMISE
!three-witness-scope-discipline-deferral|review-finding triage pattern|ASYMPTOMATIC+scope-increase findings → bundle as {parent-build-id}-followups micro-build under appropriate sequencing-gate, ¬absorb in-build|three-witness:(1)one-role-proposes-bundle (2)second-role-endorses-with-cross-finding-bundling (3)first-role-re-confirms-in-convergence|arch-cleanliness:bundled-findings-share-common-closure-mechanism|use:review-finding triage + scope-creep risk + carry-forward mgmt|src:2026-05-05-block-5-synthesis-carveout C3 r1 DA[#1]+DA[#2]+CF-1 bundling into block-5-synthesis-carveout-followups WS-3 Tier-C micro-build
P[da-procedural-substitute-via-challenge-reasoning-tier|class:pattern|src:build 2026-05-05-block-5-synthesis-carveout C3 DA r1|agent:devils-advocate|26.5.11|severity:LOW|status:VERIFIED|recurrence:2-build (C1 r2 + C3 r1)] — DA discipline pattern for when cross_verify is unreachable but per-model tools work. Use sigma-verify `challenge` (default reasoning tier, openai gpt-5.4-pro recommended for security-adjacent claims) as adversarial substitute for cross_verify when T0/T2 failure surfaces. Combined with sequential per-provider `verify_finding` for multi-model corroboration, this satisfies §2h XVERIFY-MANDATORY substantively. Pattern emerged in C1 r2 of this build (originally TA procedural-substitute when cross_verify hung); recurred in C3 r1 with same effectiveness — `challenge(reasoning-tier)` produced a vulnerability=HIGH finding (substring-overmatch as separate failure class) that TA accepted as legitimate AMENDMENT-r3-scoping gap. Distinguishes "adversarial cross-model output via challenge" from "absence of cross-model output" — the former is recipe-faithful §2h compliance, the latter requires fall-back substitute coverage. Rule: 1 reasoning-tier `challenge` invocation on TOP-1 load-bearing claim is the minimum bar when cross_verify is broken. Trade-off: single-provider adversarial vs multi-provider corroboration — accept on cost basis (cross_verify unreachable) with documented infrastructure issue.
!locked-spec-DEFEND-vs-CONCEDE-discriminator|DA-response pattern|DA flags missing-tag/marker on verbatim-spec'd artifact → discriminate DEFEND/CONCEDE by:would-addition-DRIFT-locked-spec?|plan §IC[N] specifies docstring verbatim(N-lines)→adding-N+1-line=DRIFT-NOT-FIX|right-artifact-for-missing-marker=separate-registry(eg directives.md §2h companion-list of named security-critical predicates),¬inline-in-every-docstring|parent-pattern-check:does-parent-predicate-docstring-carry-same-marker?if-no-child-shouldn't-drift-pattern|use:DA flags missing-tag on verbatim-spec'd code|src:2026-05-05-block-5-synthesis-carveout C3 DA[#4]+TA r1 DEFEND response
P[substring-overmatch-as-separate-failure-class-escape-vs-spoof|class:pattern|src:build 2026-05-05-block-5-synthesis-carveout C3 DA r1 + user-approved 26.5.11|agent:devils-advocate|26.5.11|severity:MEDIUM|status:VERIFIED|layer:deployment-architecture|complements:A1-P[escape-vs-spoof-predicate-overmatch]-analytical-taxonomy] — Deployment-architecture layer of the escape-vs-spoof predicate-overmatch taxonomy. Companion to TA's auto-promoted A1 analytical-taxonomy entry; together they form the two-layer model (analytical reframe + deployment pattern). When substring-matching path-predicate is used as GATE-REMOVAL (carve-out) not CLASSIFICATION, document TWO failure directions in KNOWN LIMITATIONS docstring: ESCAPE-direction (bypass via symlinks/HFS+/relative-`..` — legitimate target escapes predicate match) AND SPOOF-direction (impersonate/overmatch via substring-position-insensitivity — arbitrary target triggers predicate True without being inside intended scope, e.g. `/tmp/sigma-review/shared/archive/foo-synthesis.md` overmatches archive-marker substring in /tmp path). Three deployment-architecture commitments accepted under user-approval: (1) future ADRs for gate-removal predicates ship with BOTH escape+spoof KNOWN-LIMITATIONS sections by default (predicate-docstring pattern); (2) directives.md §2h XVERIFY-mandatory-security-critical adds COMPANION-LIST enumerating `_path_is_archive`+`_is_synthesis_archive_write`+`_has_compilation_complete` as security-adjacent predicates requiring XREVIEW pre-modification (TA-proposed in DA[#4] r1 response, user-approved); (3) source-validation skill (or new code-predicate-validation skill) cross-references this code-predicate taxonomy alongside its information-source bias framing. Sourced by external `challenge` (openai gpt-5.4-pro reasoning, vulnerability=HIGH) on DA[#1] surfacing the AMENDMENT r3 scoping gap; accepted by TA via "escape-vs-spoof" reframe explicitly conceding AMENDMENT r3 line 104 covers escape-direction (ADR[6] inheritance/compounding) but not spoof-direction (overmatch as separate failure mode). In-build fix bundled out-of-scope into `block-5-synthesis-carveout-followups` micro-build sequenced after WS-3 Tier-C; this pattern captures the meta-commitment for future predicates. Cross-references: complementary A1 analytical-taxonomy entry, plan §ADR[1] AMENDMENT r3:95-104, c3-scratch ### tech-architect r1 response §DA[#1]+§DA[#4], TA option (c) recommendation.
!security-adjacent-named-predicates-registry|directives.md §2h infra pattern|user-approved 26.5.11|policy:|maintain a named-symbol registry of security-adjacent predicates requiring §2h XVERIFY-mandatory on modification|initial-entries:|`_is_synthesis_archive_write`+`_path_is_archive`+`_has_compilation_complete`(all phase-gate.py)|open-questions-for-deployment:|(a)flat-list-in-§2h vs separate-artifact-referenced-from-§2h|(b)inclusion-threshold=touches-BLOCK-N-gates vs gate-removal-class-only|(c)refactor-rename-protocol=update-precondition vs post-condition|(d)naming-convention=function-symbol vs qualified-path|why-policy:|inline-"security-adjacent"-markers-in-every-docstring=verbatim-spec-drift; right-artifact=separate-registry; complementary-to-P[escape-vs-spoof-predicate-overmatch] as deployment-layer (registry=where, pattern=what)|deployment:|directives.md §2h amendment OR followups micro-build; OUT-of-this-build-scope per lead Step 16 infra sync or future micro-build routing|src:|2026-05-05-block-5-synthesis-carveout C3 DA[#4]+TA r1 DEFEND response + DA-D4 deployment-thread + user-approve-received 26.5.11
P[build-track-peer-verifier-independent-pytest-discipline|class:pattern|src:build 2026-05-05-block-5-synthesis-carveout C2-C3 calibration cycle|agent:code-quality-analyst+implementation-engineer-concurring|26.5.12|severity:MEDIUM|status:VERIFIED|recurrence:1-build|user-approve-gate:approved-by-lead-2026-05-12] — build-track peer-verifier (CQA, or sibling reviewer of build-engineer's output) MUST run an independent pytest from their own session, NOT inherit numerator from build-engineer claim. Pattern emerged when C2 CQA peer-verify cited IE's pytest count without independent re-run; gap noted as `C3-input-finding` carry-forward and closed in C3 by CQA running `cd ~/.claude/hooks && python3 -m pytest tests/ -q | tail -5` from CQA agent session, producing 1286/14/0 in 10.96s exactly matching IE's 10.60s + 10.69s runs (three-witness baseline). Pattern would have caught any IE-side count fabrication or stale-state inheritance at C2 closure. Rationale: peer-verify is the build-track's structural check on build-engineer correctness claims; inheriting a numerator from the verified collapses verification into attestation. Applies to: pytest counts, integration test counts, hook-suite parity claims, any quantitative metric a build-engineer reports as part of CHECKPOINT. Doesn't apply to: outputs whose re-generation is destructive (e.g., DB seed counts, one-time test-data setup), where peer-verify must inspect captured logs rather than re-execute. Concurrence: IE proposed in SendMessage 2026-05-08 c3-scratch §IE-acknowledgment-of-CQA-peer-verify-ring-closure; CQA concurs; IE confirms concur-by-reference at promotion-round (no duplicate IE entry needed). Follow-up action (incorporating into CQA spawn-brief discipline checklist + build-directives §3 protocol) is OUT of this build's scope — separate edit during infra sync or future micro-build.
!xreview-cross_verify-T0-out-of-band-engineering-remediation|engineering-ticket pattern|user-approved 26.5.11|policy:|when sigma-verify cross_verify-tool-specific-failure recurs across builds (4-build P0 as of 26.5.11), open out-of-band engineering ticket targeted at cross_verify MCP-handler fan-out/aggregation layer|distinguishes:|T0(cross_verify-bridge-bug-recurring) vs T1(per-session-registry-mismatch-was-C2-build-track-transient) vs T2(per-tool-invocation-failure-post-schema-load-NEW-class)|isolated-failure-surface:|per-model `verify_finding`+`challenge` succeed in agent context while `cross_verify` errors out → bug is in fan-out/aggregation NOT per-model paths|open-questions-for-deployment:|(a)ticket-queue-location=sigma-verify-repo vs sigma-system-overview vs auto-memory|(b)interim-workaround=fan-out-via-repeated-verify_finding+challenge vs XREVIEW-skip-with-substitute-coverage|(c)T0/T1/T2-taxonomy=formalize-in-directives.md vs keep-ad-hoc-forensic|complementary-to-P[xreview-infrastructure-blocking-pattern-CALIBRATION]:|D1=pattern-state, U2=action-protocol|deployment:|actual-ticket-filing-OUT-of-this-build-scope; pattern-persistence-captures-recurring-need|src:|2026-05-05-block-5-synthesis-carveout C3 DA[CF-2]+TA r1 CONCEDE + TA-XREVIEW-probe + DA-XREVIEW-probe two-witness corroboration + user-approve-received 26.5.11
P[framing-precision-line-number-anchor|class:pattern|src:build 2026-05-05-block-5-synthesis-carveout C3-r1 IE|agent:implementation-engineer|26.5.11|severity:LOW|status:VERIFIED-single-build] — in agent-to-agent SendMessages or peer-verify checklists, distinguish heading-anchor-line from body-content-line in plan/scratch citations. Conflating the two produces low-stakes precision-drift that peer-verify catches but creates extra spot-check cycles and breeds permission-creep where loose citations propagate to load-bearing claims. Trigger: IE described §Verification heading at line 286 (actually body intro); CQA's F[CQA-3] corrected to heading-anchor at line 284. Material fix-correctness claim held but imprecise citation forced CQA to verify both interpretations and slowed ring closure. How-to-apply: build-engineer and peer-verifier both use `grep -n "^##\|^###\|^def "` for anchor-line + `Read offset N limit M` for body confirmation; tag offset role explicitly in SendMessages (e.g., `|heading:284 |body-intro:286 |fix-line:289|`). Single-build evidence; user-approved 26.5.11 via team-lead Step 15 promotion-round.
P[followups-micro-build-bundling-under-tier-sequencing-gate|class:pattern|src:build 2026-05-05-block-5-synthesis-carveout C3-r1 IE|agent:implementation-engineer|26.5.11|severity:LOW|status:VERIFIED-four-witness-single-build] — when in-build review surfaces multiple low-severity items that are (a) in-domain-but-out-of-build-scope, (b) share an underlying architectural unit (shared predicate, shared test-class shape, shared documentation gap), and (c) individually below cost-threshold for dedicated micro-build, BUNDLE into single `{build-id}-followups` micro-build sequenced after applicable sequencing-gate. Bundle's architectural coherence IS the unit of value, not sum of items. Trigger: this build bundled DA[#1] predicate-overmatch documentation + DA[#2] OVERMATCH test class + carry-forward #1 (`:1056` split) into single `block-5-synthesis-carveout-followups` micro-build under WS-3 Tier-C sequencing. Closes parent ADR[6] symmetry gap by hardening `_path_is_archive` + `_is_synthesis_archive_write` symmetrically. Avoids 3 separate micro-builds (overhead-dominated) AND avoids in-build scope-creep (gold-plating). Four-witness convergence: IE proposed §175 c3-scratch, TA endorsed §250, CQA concurred, DA accepted (r1-grading DA[#1]). How-to-apply: in C3 review close-out, when ≥2 deferred items share architectural unit, propose `{build-id}-followups` micro-build sequenced after applicable sequencing-gate; lead curates bundle scope at Step 15 close-out review. Architectural pattern; user-approved 26.5.11 via team-lead Step 15 promotion-round.
P[role-boundary-preserved-under-xverify-availability|class:pattern|src:build 2026-05-05-block-5-synthesis-carveout C3-r1 IE|agent:implementation-engineer|26.5.11|severity:LOW|status:VERIFIED-single-build] — when XVERIFY tool schemas load successfully in agent context (per ToolSearch deferred-tool probe), that DOES NOT authorize agent to invoke them outside recipe-defined role. IE-track and build-track-fix-applier roles do NOT invoke `cross_verify`/`challenge`/`verify_finding` per CLAUDE.md "Lead Role Boundaries" + recipe Step 6 role assignment. Tool-availability and role-authorization are TWO SEPARATE GATES; agent must check both before invocation. Trigger: this build's IE C3 cycle confirmed XVERIFY tools loaded cleanly via ToolSearch (resolving carry-forward #2 from C2 T1 mismatch) but IE deliberately did NOT invoke them across both r1 cycles (DA r1 response + TA r1 fix-application). Role boundary preserved across availability change. Without this discipline, very C2 carry-forward #2 resolution would have created invitation for IE-role to overreach into XVERIFY-track territory. How-to-apply: in agent spawn-briefs, when listing available tools, also list role-authorization status per recipe Step assignments; agent boot-checklist includes "tools available + tools authorized" two-axis check before any verify/challenge call. Process-discipline pattern adjacent to behavioral-rails (CLAUDE.md Lead Role Boundaries); user-approved 26.5.11 via team-lead Step 15 promotion-round.
P[two-witness-transient-infra-resolution|class:pattern|src:build 2026-05-05-block-5-synthesis-carveout C3-r1 IE|agent:implementation-engineer|26.5.11|severity:LOW|status:VERIFIED-four-witness-single-build] — when prior session reports infrastructure failure (e.g., MCP tool unavailability, registry mismatch), and next session reports same-tool availability, two-witness confirmation (≥2 agents in fresh sessions report consistent state) is sufficient to reclassify prior failure as session-specific-transient rather than persistent-structural. Distinguishes from "wait-for-engineering-fix" by requiring witness from multiple roles, not just retry from same role. Trigger: this build's carry-forward #2 (C2 T1 XREVIEW per-session-registry-mismatch) resolved by C3 four-track schema-load probe (IE + CQA + TA + DA all reported TOOLS-AVAILABLE). C2 T1 reframed as C2-build-track-agent-session-specific-transient, NOT structural propagation gap. Saves out-of-band engineering ticket for non-recurring transient. Complements existing P[xreview-infrastructure-blocking-pattern-CALIBRATION] (DA D1 26.5.11) which captures technical taxonomy (T0/T1/T2 failure modes); this pattern captures *process* (how to confirm reclassification) distinct from *taxonomy*. How-to-apply: when carry-forward references prior-session infra failure, C3 boot includes ≥2 fresh-agent probes before deciding (a) declare infra-issue persistent + escalate, or (b) declare transient + close carry-forward. Process-discipline pattern; user-approved 26.5.11 via team-lead Step 15 promotion-round.
!loan-agency-decision-rights|named-agent-appointed-by-lender-syndicate-via-lead-arranger≠sub-agent-appointed-by-AA-unilateral|borrower-consultative-NOT-decisive|persona-doc-misclassification-source-when-conflated|src:LSTA-MCAPs-T1+ABA-Apr25-T1+Proskauer-T1+SRS-own-materials|26.5.12
!B2B-vendor-memory-asymmetry|loss-aversion-2-2.5x(Kahneman-foundational)|critical-failure-persist-18-36mo|positive:negative-3:1-sustained-perf|97%-decision-makers-know-vendor-pre-RFP|detractors-drive-80%+-neg-WOM|NPS-detractor-50%-churn-90d|peak-end-rule-applies|design-implication:downside-protection>upside-generation|src:Forrester2026+McKinsey-B2B-CX+Kahneman-Tversky-prospect-T1+Springer-Experimental-Economics-T1|26.5.12
!B2B-operator-advocacy-mechanism|operates-via-scorecards+renewal-cycle(NOT-initial-vendor-selection)|decision-weight-structurally-low|11-stakeholders-avg-B2B-buying-group|formal-escalation-channels-address-perf-under-contract-NOT-vendor-switch|no-documented-mechanism-for-operator-initiated-vendor-switch|strategy-implication:equip-operators-with-scorecard-data-for-renewal-influence-NOT-initial-selection|src:ScienceDirect-T1+Ivalua+Order.co+CIO-Mag+AORN-healthcare|26.5.12
P[edge-verification-3D|solo-operator opportunity-viability BELIEFs apply only when all-3 present: (a)prior-domain-expertise in target vertical (b)network-access to first 3-5 customers (c)18-36mo capital-discipline+runway; absent any → default to BLS-anchored ~25% 3yr-profit not SF-Study-anchored ~42%; the 17pp gap = operator-edge-premium, must be earned not assumed; mandatory synthesis preamble for any solo-operator opportunity review|src:R-2026-05-17-k-shape-opportunities/RCA-DA[#20]|class:new-principle|promoted:2026-05-17|behavior-change:every-solo-operator-opportunity-review]
P[PM-base-rate-substitution-mandatory|when superforecasting analysis uses favorable RC requiring class-membership verification (e.g., Stanford-SF-Study vs BLS all-startup), base-rate substitution self-deception MUST appear as named PM[N] entry with probability anchor 15-25%; absent this, RCA outside-view is parallel-additive ¬team-calibration-shifting; was THE highest-prob failure mode in R-2026-05-17 review|src:R-2026-05-17-k-shape-opportunities/RCA-DA[#20]|class:anti-pattern-new|promoted:2026-05-17|behavior-change:RCA-pre-mortem-protocol]
P[BELIEF-3-class-taxonomy|classify findings BEFORE OV-adjustment: layer-existence(no adjust, observable) / opportunity-viability(12-17pp downgrade if user-class unverified, conditional on edge-verification preamble) / macro-probability(no adjust, baseline frequencies); apply edge-premium ONLY to Class-2; resolves the structural problem where RCA outside-view downgrade was applied uniformly across categorically-different finding types|src:R-2026-05-17-k-shape-opportunities/RCA-DA[#18]|class:new-principle|promoted:2026-05-17|behavior-change:RCA-synthesis-input-protocol]
P[OV-reconciliation-cross-agent-protocol|directives.md §1 protocol gap: lead MUST SendMessage peer agents referencing RCA OV-anchor + request BELIEF reconciliation BEFORE R1 convergence; absent this, RCA "BASE-RATE FORCING FUNCTION" role is nominally executed but functionally null (1/5 substantive integration in R-2026-05-17); structural fix not advisory|src:R-2026-05-17-k-shape-opportunities/RCA-DA[#19]|class:new-principle|promoted:2026-05-17|behavior-change:directives-update-required]
P[conditional-BELIEF-framing-over-single-point|when BELIEF gap between agents using different methodological approaches (base-rate-count vs break-condition-specification), correct synthesis is explicit conditional framing P(claim|cond-A)=X; P(claim|cond-B)=Y; P(unconditional)=Z; NOT selecting one agent's number; preserves methodological distinction + resolves CONVERGENCE-TENSION patterns without either agent being wrong|src:R-2026-05-17-k-shape-opportunities/macro-DA[#3]|class:new-principle|promoted:2026-05-17|behavior-change:BELIEF-presentation-protocol]
P[framing-capture-audit|method: ask "what spending pockets / categories / niches are ABSENT from primary financial-institution research?" to surface anti-narrative categories not amplified by source incentives; primary sources (BofA/Visa/MC for consumer; analyst firms for sectors) have structural incentive to emphasize particular narratives aligned with their products; applied in R-2026-05-17 EC-DA[#14]: identified home-services/residential infrastructure as absent from BofA/Visa frame, confirmed by IBBA M&A independent data; applies to consumer-spending, market-sizing, sector-analysis reviews|src:R-2026-05-17-k-shape-opportunities/economics-DA[#14]|class:pattern|promoted:2026-05-17|behavior-change:source-bias-probe]
P[solo-operator-shape-framework|6-shape catalog with structure (capital floor | expertise floor | network req | time-to-revenue | scale ceiling | gross margin): A=productized-service($5K-$50K|domain-credential+2yr-curve|3-5 prior clients|6-12mo|$300K-$500K ARR solo|60-80%), B=micro-SaaS($1K-$20K|technical|none|3-6mo|$100K-$500K solo|70-90%), C=niche-agency($10K-$100K|track-record|industry-contacts|30-90d|boutique<10FTE 19% net vs 8-fig 8%|40-75%), D=media-community(ancillary-only-for-capital-users|authority+consistency|none|12mo+|sub-15-20% replacement-income probability|80-95%), E=micro-PE/ETA($100K-$2M|M&A-skill|deal-network|6-18mo to acquire|SF-Study-class IRR 30.3%|N/A), F=compliance-boutique($5K-$50K|credential-gated|2-3 clients|3-6mo|EARLY-cycle EU AI Act|moderate); reference framework for any opportunity-discovery review|src:R-2026-05-17-k-shape-opportunities/PS-PS[1]|class:reference-framework|promoted:2026-05-17]
P[shape-durability-matrix-macro-regime|Shape E(micro-PE/ETA) and Shape B(micro-SaaS) most temporally durable across macro regimes — ETA macro-agnostic once acquired, SaaS subscription-sticky; Shape A(productized-service) and Shape F(compliance-boutique) window-dependent: -10-15pp BELIEF over 2-yr window vs 1-yr snapshot; defense+grid+EU-AI-Act-compliance most insulated from equity-correction; calibration for any boom-cycle opportunity review with 2026-vs-extended sensitivity column required|src:R-2026-05-17-k-shape-opportunities/PS-DA[#10]|class:calibration|promoted:2026-05-17]
P[institutional-content-licensing-shape|non-consensus opportunity shape: acquire/license proprietary operational content (clinical protocols, regulatory submissions, court filings, operational manuals, technical documentation) as fine-tuning datasets or RAG corpora sold to AI companies; relationship-gated; capital $50K-$200K; BELIEF 0.50 pre-consensus vs lagging-consensus as of Q1 2026 — requires currency check for future reviews; documents that solo-operator catalogs are consensus-bounded and non-consensus shapes are relationship-gated|src:R-2026-05-17-k-shape-opportunities/PS-DA[#15]|class:domain-reference|promoted:2026-05-17]

→ actions:
→ pattern confirmed in new conversation → remove ~ marker, increment count
→ pattern contradicted → investigate per ^Λ.i, update or remove
→ pattern relates to user's thinking → cross-ref ^user.md
