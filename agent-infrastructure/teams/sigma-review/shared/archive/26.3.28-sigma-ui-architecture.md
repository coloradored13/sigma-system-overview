# workspace — sigma-ui architecture analysis
## status: active
## mode: ANALYZE

## task
Analyze the architecture for sigma-ui — a local web application that replaces the chat-based sigma-review interface with a proper orchestration UI. Core problem: control inversion (agents call orchestrator, can skip/game). Target: orchestrator as outer loop that dispatches agents, computes belief state from workspace observables, enforces user gates, writes immutable audit logs.

Existing components: hateoas-agent (Python state machine), sigma-mem (MCP memory), sigma-verify (cross-model verification), orchestrator-config.py (workflow definition), 20+ agent definitions with battle-tested spawn prompts.

## scope-boundary
This review analyzes: sigma-ui architecture — agent dispatch mechanism, UI framework selection, belief state computation, user gate design, inter-agent communication pattern, and architectural risks
This review does NOT cover: implementation code, timeline estimates, pricing/cost analysis, deployment to cloud, changes to existing sigma-review analytical process quality, other projects discussed in session
temporal-boundary: none
Lead: before writing synthesis or documents, re-read this boundary.

## prompt-decomposition

### Q[] — Questions (6)
Q1: Agent dispatch — Anthropic Python SDK vs Agent SDK vs Claude Code CLI shell-out? Tradeoffs (cost, control, latency, tool access, maintainability)?
Q2: UI framework — Streamlit (speed) vs React/Next.js (control) for local-only, clone-and-run tool?
Q3: Belief state computation — what workspace observables are reliable? How to compute from observables instead of agent self-report?
Q4: Minimal viable user gate set — which gates add real enforcement without becoming bottlenecks?
Q5: Inter-agent communication — workspace-as-shared-state sufficient when agents are API calls, or need message bus?
Q6: Architecture risks — what could go wrong? Failure modes?

### H[] — Hypotheses (6)
H1: Current system has control inversion (agents call orchestrator, can skip/game it)
H2: Orchestrator-as-outer-loop fixes the control inversion problem
H3: Existing components (hateoas-agent, sigma-mem, sigma-verify, orchestrator-config.py, agent definitions) can be reused
H4: Analytical quality of current sigma-review can be preserved while fixing control inversion
H5: Local-only, single-user, clone-and-run is viable for this tool
H6: Workspace observables are more reliable than agent self-report for belief state

### C[] — Constraints (6)
C1: Local-only (no cloud deployment)
C2: Clone-and-run from GitHub repo
C3: API keys in environment variables
C4: Single user
C5: Must preserve analytical quality of current sigma-review process
C6: Build on existing components

## infrastructure
ΣVerify: openai (gpt-5.4) + google (gemini-3.1-pro-preview) available
Team: flickering-drifting-penguin
Mode: ANALYZE TIER-2
Agents: tech-architect, product-designer, ux-researcher, implementation-engineer, reference-class-analyst + DA (r2)

## findings
### tech-architect

#### Q1: Agent dispatch — SDK selection
F1[PRIMARY]: Anthropic Python SDK (anthropic.Anthropic + messages.create) is correct dispatch mechanism. ¬Agent SDK (v0.0.x, no stable API contract 26.3). ¬Claude Code CLI shell-out (subprocess overhead + no structured return + shell injection surface + no streaming control). |source:[independent-research]|T2

§2a positioning: Python SDK is clear incumbent for production multi-agent orchestration. Agent SDK 0.x premature. CHECK CONFIRMS.

Tradeoff matrix:
- Python SDK: full control + structured response objects + streaming + error handling + batch. Cold-start per API call. |T1
- Agent SDK: intended for agent↔agent but 0.x, limited docs, no production adoption evidence. |T3
- CLI shell-out (CURRENT): agents get full Claude Code tools (Bash,Read,Write,Glob,Grep,MCP). ¬structured return, shell injection risk, no streaming. This is WHY current agents can do file I/O. |[agent-inference]

!CRITICAL tool-access gap: CLI shell-out preserves Claude Code tool access agents currently depend on. Python SDK API calls do NOT get these tools by default — only tools=[] passed explicitly. Sigma-review agents use file I/O extensively. Four mitigation options (cross-agent analysis from impl-engineer, ranked C>D>B>>A):
  (a) pass file I/O + MCP tool definitions → !SECURITY BLOCKER: Bash tool = arbitrary code exec on orchestrator host. ¬acceptable. Degrading to Read/Write/Glob/Grep subset collapses to option (b). |[cross-agent]
  (b) orchestrator pre-loads all file content + injects as context → MEDIUM complexity (~150 LOC). R2+ rounds require re-read+reinject per round. Prompt grows ~8-12K tokens/call by R3 — within 200K context but monitor. |[cross-agent]
  (c) redesign agents for structured output → HIGH upfront / LOW ongoing (~200 LOC orchestrator file-I/O module + 20+ agent .md rewrites). Agents already write ΣComm structured output; file I/O is orchestrator-owned in new model. Solves concurrent-write contention (review-7) as side effect. Risk: agent prompt rewrites could degrade analytical quality. Mitigate: A/B test one agent first. |[cross-agent]
  (d) hybrid B+C (strangler fig) → MEDIUM complexity. Implement B first as bridge, retire file I/O instructions per agent with evidence ¬assumption-driven big-bang rewrite. Recommended if risk-averse on agent quality degradation. |[cross-agent]
REVISED RECOMMENDATION: D (hybrid B+C strangler fig) > C (cleanest end-state) > B (viable bridge) >> A (security blocker). §2c gap: CLOSED via cross-agent. |source:[cross-agent:implementation-engineer]|T2

!ARCHITECTURE IMPLICATION — orchestrator owns FULL agent memory lifecycle in option C/D (cross-agent, IE):
  pre-spawn: call handle_get_agent_memory() from sigma-mem handlers.py → inject as "## Your Memory" context block. Handler exists ¬new code. ~50 LOC orchestrator addition.
  post-response: call handle_store_agent_memory() → persist session findings to agent memory.
  !both ends required: pre-spawn-only = agents read past calibration but session findings never persist. post-response-only = agents write to unloaded memory. Either half alone = broken memory lifecycle.
  context-builder LOC revised: ~200 (was ~150). ¬material to complexity ranking. |source:[cross-agent:implementation-engineer]|T2

#### Q3: Belief state computation from observables — H2/H6
F2[PRIMARY]: XVERIFY[openai:gpt-5.4]: H2 assessment=PARTIAL (high confidence). Outer-loop dispatch is material improvement but NOT complete control-inversion fix. Residual bypass: if guards read agent-produced workspace artifacts, agents can game those artifacts. |source:[external-verification]|T1

Reliable observables (structural, hard to fake): convergence-declaration count (✓ syntax in convergence section), section-population (non-empty findings written), XVERIFY tag presence, source-provenance tag distribution, §2e outcome-3 gap-count.
Unreliable (agent-asserted, gameable): agent-declared belief scores in prose, self-assessed confidence %, context_true("pre_synthesis_validated") keys written by agents.

gate_checks.py already has run_compute_belief() + run_validation() + BUNDLES dict. cmd_compute_belief() CLI is the right seam to extend for hard-observable computation. |source:[independent-research]|T1

§2e H6 premise-viability: observable reliability CONFIRMED with structural-parsing condition. Orchestrator must parse structure ¬trust prose content.

DB[H2: orchestrator-as-outer-loop fixes control inversion]:
(1) initial: outer loop = complete fix; agents can't advance phases they don't control
(2) assume-wrong: agents still write workspace → guards read workspace → agents write well-formed convergence declarations without doing the work
(3) strongest counter: if exit_gate_passed() evaluates context_true("pre_synthesis_validated") from workspace parse of agent-written content, agent writes the right string without earning gate passage
(4) re-estimate: outer-loop fixes WHO calls advance(); does NOT fix WHAT guards read
(5) reconciled: H2 = PARTIALLY CONFIRMED. Dispatch control inversion is fixed. Gaming residual requires hard-observable guards.
→ REVISED: outer-loop dispatch + hard-observable guards together fix the problem. Neither alone sufficient.

#### Q5: Inter-agent communication
F3[MEDIUM]: Workspace-as-shared-state IS sufficient for API-dispatch model WITH structural change: parallel phase agents write to private output buffers, orchestrator merges post-phase. Current concurrent-write contention (review-7: 5+ agents simultaneous Edit failures) disappears with sequential-merge pattern. ¬message bus needed. |source:[independent-research]|T2

§2a flag: concurrent-write contention is documented. Sequential dispatch resolves it; parallel dispatch requires buffer-merge. Both simpler than a message bus. Outcome 2 — CONFIRMS with acknowledged risk.

#### Q6 + H3: Architecture risks + component reuse
F4[HIGH]: Three hard risks requiring explicit mitigation:

RISK-1[HIGH]: Guard gaming residual — guards reading agent-written observables preserve bypass path despite outer-loop dispatch. Mitigation: structural parsing (✓ declaration count, section presence, tag syntax regex) over agent-asserted values. References F2/DB above. |source:[agent-inference]

RISK-2[HIGH]: API-dispatch tool-access loss — agents dispatched via Python SDK get only explicitly-passed tools. Current agents rely on file I/O + sigma-mem MCP + sigma-verify MCP. Three mitigation options with complexity analysis in F1 above. HIGHEST implementation complexity risk. |source:[independent-research]|T2

RISK-3[MEDIUM→HIGH in sigma-ui context]: AsyncRunner private-attr bypass (review-8 memory F1, async_runner.py:97-99+117-128) — AsyncRunner duplicates Orchestrator sync execution via private attr mutation; may bypass guard evaluation on retry. §2b check: MEDIUM in isolation but ELEVATED to HIGH if AsyncRunner used as gate-enforcement trust anchor in sigma-ui. CHECK CHANGES ANALYSIS: use direct Orchestrator.start()/advance() cycle (orchestrator-config.py CLI pattern) ¬AsyncRunner until review-8 F1 resolved. |source:[independent-research]|T1

H3 COMPONENT REUSE MAP:
- hateoas-agent Orchestrator: REUSE AS-IS (workflow already in orchestrator-config.py)
- gate_checks.py: REUSE + EXTEND (cmd_compute_belief + BUNDLES = belief-state engine seam)
- sigma-mem: REUSE AS-IS (MCP server; orchestrator calls it; agents call it if dispatch passes MCP tool defs)
- sigma-verify: REUSE AS-IS (same MCP pattern as sigma-mem)
- Agent .md definitions: REUSE as system prompts for API-dispatched agents
- orchestrator-config.py: REUSE AS-IS; sigma-ui wraps it with event loop
¬reuse: AsyncRunner (risk elevated) | CLI shell-out dispatch pattern
H3: CONFIRMED-PARTIAL. All components reusable with caveats above. |source:[independent-research]|T1

#### DA R2 responses

DA[#1] GUARD-GAMING — CONCEDE PARTIALLY, REVISE
Concede: "hard to fake" framing was wrong. Structural parsing (✓ count, section presence, tag regex) raises cost ¬eliminates gaming. Agent knowing the format can produce syntactically valid compliance artifacts without genuine work.
Defend+revise: ONE observable is genuinely gaming-resistant — XVERIFY where orchestrator holds API key and executes the call. Agent cannot write XVERIFY[openai:gpt-5.4] into existence; orchestrator either ran it or it didn't.
REVISED observable tier model:
  TIER-A (gaming-resistant): orchestrator-produced — XVERIFY results, round counts via phase definition, mandatory section counts enforced by spec
  TIER-B (cost-raising, ¬gaming-resistant): structural-parse — ✓ syntax, section presence, tag format
  TIER-C (gameable): agent-asserted — prose confidence %, "PASS" strings, context_true() keys
RISK-1 MAINTAINED HIGH. Mitigation narrowed: gate_checks.py must prioritize TIER-A observables. TIER-B is defense-in-depth ¬primary enforcement. |source:[agent-inference]→REVISED-DA[#1]

DA[#2] AGENT SDK DISMISSAL — COMPROMISE
Research confirmed (26.3.28): Agent SDK v0.1.48, hooks (SubagentStart, tool-call intercept, block/transform), sessions+ResultMessage, MCP write-lock for parallel subagents. "0.x premature" characterization was stale. |source:[independent-research]|T2
Defend core ranking: Python SDK still preferred for sigma-ui because (1) hooks fire at tool-call level ¬phase-transition level — wrong abstraction for guard evaluation; (2) Agent SDK assumes iterative coding workflow ¬parallel-dispatch+belief-state+exit-gate model; (3) outer-loop control (when to spawn, which agents, whether guards pass) is ¬hook-level concern.
REVISED rank: Python SDK (primary) > Agent SDK (viable alternative, lower fit for phase-gate model — worth prototyping) > CLI shell-out (security blocker). §2a CHANGES. |source:[independent-research]|T2→REVISED-DA[#2]

DA[#3] H2 WEAKER THAN PRESENTED — CONCEDE
Concede: DA correct. Two ANDed conditions for H2. DA[#1] revision weakens condition-2 (hard-observable guards). Only TIER-A observables are genuinely gaming-resistant; structural-parse tier is gameable. P(H2 fully works) ≈ 0.55-0.65 (outer-loop=HIGH × TIER-A implementation discipline=MEDIUM).
REVISED: H2 = WEAKLY-TO-PARTIALLY CONFIRMED (lowered from PARTIALLY CONFIRMED). Thesis holds IF TIER-A observables implemented; degrades toward unguaranteed on TIER-B-only. |source:[agent-inference]→REVISED-DA[#3]

DA[#4] TOOL-ACCESS RISK FRAMING — COMPROMISE
Defend: "MEDIUM complexity" correct for implementation effort — option D path is clear, handlers exist.
Concede: undersells RISK dimension. Capability regression from "full Claude Code toolset + live file reads" to "pre-loaded context snapshots" is genuine H4 risk. Shallower agent outputs possible at full-review scale; A/B testing one agent ¬sufficient to detect.
REVISED RISK-2: HIGH implementation complexity AND HIGH risk to H4 — two separate dimensions. H4 genuinely uncertain pending option D prototype. |source:[cross-agent:IE]→REVISED-DA[#4]

DA[#5] SOURCE PROVENANCE F3 — CONCEDE
Concede: DA correct. "Private output buffer + orchestrator merge" = architectural reasoning from review-7 contention evidence + producer-consumer first principles. ¬external literature. T2 label wrong.
RECLASSIFIED: F3 |source:[agent-inference] (review-7 documented contention + standard pattern). Finding stands; tag corrected. →RECLASSIFIED-DA[#5]

#### Analytical hygiene (post-DA)
§2a: Agent SDK revised "premature" → "viable alternative, lower fit for phase-gate pattern" — CHANGES #1
§2b: H2 confidence lowered to WEAK-PARTIAL | AsyncRunner maintained — CHANGES #2
§2c: RISK-2 = HIGH-complexity AND HIGH-H4-risk (two dimensions) — CHANGES #3
§2e: H6 revised CONFIRMED-WITH-CONDITION → PARTIALLY-CONFIRMED (TIER-A only gaming-resistant) — CHANGES #4
§2d: F3 reclassified T2[independent-research] → [agent-inference] — CHANGES #5

### product-designer

#### Q2: UI Framework — Streamlit vs React/Next.js

**F1: Framework choice is conditional on visualization centrality, not framework capability**
XVERIFY[openai:gpt-5.4]: partial — "Streamlit correct for status-dashboard-first MVP; React/Next.js if tension/convergence visualization is central product requirement rather than secondary dashboard feature"
DB[Streamlit-vs-React]: revised from "React wins on control" → conditional split based on primary use pattern

Streamlit(correct-when):
- primary-use=monitoring+approval-gates(status cards, phase progress, findings feed)
- visualization=secondary(sparklines, badge indicators, simple bar progress)
- team=python-only, setup-cost=material, ¬npm/node dependency chain
- clone-and-run constraint IS real: npm install + Next.js config adds ~5-10 min setup vs `pip install -r requirements.txt; streamlit run app.py`
|source:independent-research(WebSearch):T2(docs.streamlit.io,fast.io)|

React/Next.js(correct-when):
- tension/convergence visualization=central+interactive(graph layout, node-edge belief state, D3 custom renders)
- multi-agent concurrent state updates=high-frequency(>2/sec per card — Streamlit rerun semantics break down)
- component isolation requirements=strict(20 agent cards must update independently without any cross-contamination)
|source:independent-research(WebSearch):T2(logrocket.com)|

**VERDICT for sigma-ui:**
sigma-ui visualization requirements (tension/convergence) appear to be *monitoring* not *interactive exploration* → Streamlit-viable
BUT: 20 agent cards simultaneously active = high concurrent update load — this is the load-bearing risk
§2b flag: if belief-state computation triggers card updates at >1Hz aggregate, Streamlit rerun semantics will produce visible jank even with st.fragment
→ architectural requirement: fragment-per-agent-card is mandatory, NOT optional, if Streamlit chosen
→ each fragment uses run_every=polling-interval (recommended: 2-5s for agent status, not sub-second)
— §2b maintained because evidence shows Streamlit handles this at polling frequencies, ¬high-frequency push |source:independent-research(WebSearch):T2(docs.streamlit.io/develop/concepts/architecture/fragments)|

**REVISED FINDING F1 (CONFIRMED HIGH-CONFIDENCE — 26.3.28):** Streamlit with mandatory fragment-per-agent-card architecture is correct for sigma-ui. Event rate confirmed by tech-architect: belief-state recomputes on convergence events ¬continuous, 1-3 events/agent/phase at 2-5s intervals → aggregate rate well below inversion threshold. §2b flag RESOLVED. Confidence: HIGH. Remaining inversion condition (single): tension/convergence visualization redesigned to require interactive node-edge graph. |source:[cross-agent]:T1(tech-architect-event-rate-confirmation)|

**F2: Information Architecture — Status Dashboard**

Hierarchy (primary→detail):
```
L1: Phase strip (UNDERSTAND→PLAN→BUILD→CONVERGE) — always visible, current phase highlighted
L2: Agent status grid — 20 cards, 4-5 per row, badge+name+last-activity
L3: Active agent detail — expanded card on click: current task, tool calls, source tags, confidence
L4: Findings feed — append-only log, agent-tagged, collapsible by agent
L5: Tension/convergence panel — sidebar or drawer, DA challenges + response mapping
```

Key IA decisions:
- Phase strip = persistent nav metaphor (matches sigma-review mental model of sequential phases)
- Agent cards = primary real-time surface (status badge: IDLE/ACTIVE/CONVERGED/BLOCKED)
- Findings feed = chronological ¬per-agent-grouped (promotes cross-agent pattern detection)
- Tension panel = deliberate friction (requires click to open — DA challenges should feel weighted, not ambient noise)
|source:independent-research(orchestration-UI-patterns:LangSmith+AutoGen+CrewAI):T2|

Anti-patterns observed in LangSmith/AutoGen Studio:
¬flat-trace-list (loses phase context) | ¬token-metrics-primary (irrelevant for sigma-review) | ¬auto-expand-all (cognitive overload at 20 agents) | ¬live-scroll (findings feed should paginate/freeze while reading)
|source:independent-research(WebSearch):T2(langchain.com/langsmith)|

**F3: Component Architecture**

Core components + relationships:
```
PhaseStrip
  └─ PhaseNode (×N: UNDERSTAND/PLAN/BUILD/CONVERGE)
       └─ GateIndicator (locked/open/passed)

AgentGrid
  └─ AgentCard (×20)
       ├─ StatusBadge (IDLE/ACTIVE/CONVERGED/BLOCKED/ERROR)
       ├─ AgentName + RoleTag
       ├─ ActivityIndicator (spinner/last-event timestamp)
       └─ [expanded] AgentDetail
            ├─ CurrentTask text
            ├─ ToolCallLog (append-only list)
            └─ SourceProvenanceBar (T1/T2/T3 distribution)

FindingsFeed
  └─ FindingCard (×N, append-only)
       ├─ AgentBadge + timestamp
       ├─ FindingBody (ΣComm compressed, expandable to full)
       ├─ SourceTag (independent-research/prompt-claim/cross-agent/agent-inference/external-verification)
       └─ XVerifyBadge (XVERIFY/XVERIFY-FAIL/none)

TensionPanel (drawer/sidebar)
  └─ TensionThread (×N)
       ├─ DAChallenge
       └─ AgentResponse (×N, threaded)

UserGate (full-page overlay or modal — see F4)
  ├─ GateTitle + PhaseTransitionLabel
  ├─ SynthesisSummary (read-only)
  ├─ ConfirmButton (primary) + RejectButton (destructive)
  └─ GateLog (audit trail)
```

Streamlit mapping: each AgentCard = @st.fragment(run_every=Ns) | FindingsFeed = st.session_state append + st.fragment | TensionPanel = st.sidebar or st.expander | UserGate = st.dialog (full-screen) or page navigation
|source:agent-inference + independent-research(docs.streamlit.io):T2| ← corrected from T1 (DA[#5])

**F4: User Gate UX — Q4 contribution**

Tiered gate model (matched to action reversibility):
```
TIER-1 (irreversible phase transition): full-page state takeover
  triggers: ANALYZE→BUILD convergence approval, synthesis promotion approval
  pattern: page navigates to dedicated gate screen — NO background visible
  why: irreversible = user must context-switch intentionally. Background visibility creates distraction+false urgency
  Streamlit impl: st.switch_page() or st.session_state gate_mode=True → renders GatePage ¬main dashboard

TIER-2 (soft confirmation): modal overlay
  triggers: round-advance approval, single-agent finding challenge
  pattern: st.dialog() — blocks interaction with backdrop dimming
  why: confirmation needed but action is round-scoped ¬session-scoped

TIER-3 (informational): inline banner
  triggers: agent blocked/waiting, XVERIFY-FAIL notification, research-gap flag
  pattern: st.warning() / st.info() in findings feed — ¬interrupts workflow
  why: awareness without forced stop
```

DB[gate-modality]: confirmed — CI/CD precedent (GitHub Environments required reviewers = full-page gate) supports TIER-1 full-page. Gate heaviness matches action reversibility. Full-page ¬abandoned when gate IS the interface state ¬interruption to another state.
|source:independent-research(WebSearch):T2(smashingmagazine.com,eleken.co) + agent-inference|

Cross-agent refinement (ux-researcher): DB assume-wrong check updated. Original concern: abandonment risk. ux-researcher §2a: PRIMARY risk for local dev tool is NOT abandonment — it is RUBBER-STAMPING (clicking through without reading). Full-page BETTER than modal for this risk because it removes all background context competing for attention. Local tool behavior ≈ IDE modal dialogs (IntelliJ refactor preview, git merge conflict) — users engage ¬dismiss. DB[gate-modality] outcome 2 STRENGTHENED: original reasoning holds AND rubber-stamping risk reinforces full-page choice.
|source:[cross-agent]:T2(ux-researcher-Q2-gate-validation)|

Agentic UI principle (Smashing Magazine 2026): "Pre-Action uses Intent Preview" — gate should show WHAT will happen (synthesis summary, agents promoted, decisions locked) BEFORE confirm button is active. Not just "Are you sure?" → show consequences. [CONFIRMED by ux-researcher as mandatory condition for full-page gate to remain valid]
XVERIFY ¬performed on F4 (non-load-bearing — F1 is load-bearing finding, XVERIFY allocated there)

**F5: Tension/Convergence Visualization**

§2a check: does sigma-ui need to SHOW tension or just LOG it?
Outcome 1 — CHANGES ANALYSIS: "tension visualization" in scope implies monitoring-grade display suffices (DA challenge → agent response threaded view), NOT interactive graph. This keeps Streamlit viable (F1 not inverted).

Pattern: TensionThread (threaded conversation metaphor — DA challenge as "opening post", agent responses as "replies")
- Convergence indicator: per-agent badge progressing ACTIVE→CONVERGED, aggregate convergence bar (N/total converged)
- Belief state: NOT a node-edge graph at MVP — confidence % per finding with trend arrow (↑↓→) is sufficient and Streamlit-compatible
- DA activity: distinct visual treatment (red/amber badge, challenge count counter) — must be visually separable from standard agent activity
|source:agent-inference + cross-agent(LangSmith-pattern-research):T2|

#### Analytical Hygiene Checks

positioning-check(§2a): Streamlit-for-agent-UI is increasingly validated pattern (2025-2026 ecosystem) — ¬contrarian finding, but sigma-ui's concurrent-update load is a real gap. Outcome 2 — confirmed with acknowledged risk: fragment architecture discipline is mandatory mitigation.

calibration-check(§2b): XVERIFY partial confirms conditional split. Confidence held at medium (¬high-conviction until tech-architect confirms backend event rate). §2b flag preserved in F1.

cost/complexity-check(§2c): Streamlit path = lower complexity (Python-only, ~500 LOC MVP). React path = higher complexity (~1500+ LOC, node toolchain, state management library needed). For local developer tool, complexity cost is real. Outcome 2 — confirms Streamlit direction.

premise-viability(§2e): H5 (local-only, clone-and-run viable) — confirmed. Both Streamlit and React support this but Streamlit setup is materially simpler. H5 CONFIRMED for Streamlit path.

#### Cross-agent addendum (after reading tech-architect R1 findings)

|source:[cross-agent]| tech-architect F1+RISK-2: tool-access loss on API dispatch is MATERIAL to component design

Impact on F3 (component architecture):
- AgentDetail>ToolCallLog: display differs by dispatch mode. CLI shell-out → rich tool stream (Bash,Read,Write,MCP). API dispatch → only explicitly-passed tools. Component must handle both + show "dispatch-mode:limited" indicator.
- AgentDetail>CurrentTask: if agents receive context via prompt injection (option b) ¬live file I/O, "current task" may reflect injected context ¬live workspace reads — UI must distinguish.
- UserGate TIER-1: active dispatch mode must be surfaced on gate screen — operator needs to know what tools agents had during review being promoted.

§2a outcome 1 — CHANGES component architecture: DispatchModeBadge (tech-architect) + STALE badge (ux-researcher) added to AgentCard:
```
AgentCard (×20)
  ├─ StatusBadge (IDLE/ACTIVE/CONVERGED/BLOCKED/ERROR/STALE) ← STALE added via ux-researcher cross-agent
  ├─ AgentName + RoleTag
  ├─ DispatchModeBadge (CLI | API | API+tools) ← ADDED via tech-architect cross-agent
  ├─ ActivityIndicator (spinner / last-event timestamp — timestamp critical for STALE detection)
  └─ [expanded] AgentDetail
```
STALE rationale (ux-researcher F-UX4): sync-client freeze risk can silence agent without signal. Amber STALE badge + last-event timestamp distinguishes valid silence (agent computing) from frozen silence. STALE threshold = 2× expected polling interval.
|source:[cross-agent]:T2(tech-architect-F1+RISK-2) + [cross-agent]:T1(ux-researcher-F-UX4-STALE)|

### ux-researcher

#### F-UX1: Minimal viable gate set = 3 default + 1 conditional [Q4 primary answer]
XVERIFY[openai:gpt-5.4]:partial — "3 gates strong default baseline ¬universally sufficient; conditional gate for mid-round scope drift needed" |XVERIFY[google:gemini-3.1-pro-preview]:agree(high-conf) |source:[external-verification] |T2
§2a:OUTCOME-2 — 3-gate pattern validated by both models; gpt-5.4 partial adds conditional gate refinement, accepted

G1(pre-spawn,hard-block): prompt-decomposition-confirmation — user confirms Q/H/C before agents spawn; wrong Q/H/C contaminates entire review; 30s pause worth cost; ¬skip
G2(mid-review,hard-block): DA-exit-gate verdict with proceed/stop decision — FAIL requires explicit user choice [Proceed to R3] [Stop Review]; frame as DECISION ¬notification; UI shows (a)DA verdict summary (b)unresolved challenges list (c)two explicit action buttons; ¬"OK" button ¬auto-retry
G3(post-review,semi-auto): promotion-candidates approval — auto-promote calibration+pattern-confirms-existing; surface only user-approve candidates; ¬require review of auto items
G4(conditional,scope-drift): fires ONLY IF R1 findings materially contradict Q/H/C (e.g., H2 falsified in R1); orchestrator detects via workspace delta; ¬fires-every-round

All other transitions: observable ¬blocking (round advancement, agent convergence, XVERIFY results, zero-dissent circuit-breaker)
§2b:OUTCOME-2 — known gate-enforcement-failure (26.3.23 audit: CB hard-gate skipped) confirms hard-blocking required ¬advisory |source:[cross-agent]
§2c:OUTCOME-2 — 4 blocking UI components; minimal implementation cost
DB[F-UX1]: assume-G2-automates → counter(FAIL=user-must-decide-proceed-or-stop) → reconciled:G2=decision-gate ¬notification |outcome-2-confirmed

IA VALIDATION (product-designer F4): G1-G3 map to PD tiered-gate model — G1=TIER-2(modal), G2=TIER-1(full-page), G3=TIER-2(modal), G4=TIER-2(conditional). CROSS-AGENT CONVERGENCE |source:[cross-agent]

---

#### F-UX2: Mental model shift — control-room-operator NOT conversation-participant [H4 + Q4 context]
§2a:OUTCOME-2 — dashboard-as-control-room confirmed; Smashing Magazine 2025; DesignRush 2026 |source:[independent-research] |T2

Chat: sequential+linear+user-tracks-state-by-reading+user-in-loop-implicitly
Dashboard: concurrent+spatial+system-tracks-state+user-monitors+intervenes-at-gates-only

Critical implication: SILENCE IS VALID — agents working without surfacing output is correct behavior; opposite of chat where silence=nothing-happening; gate bottleneck risk comes from users resisting silence and forcing interruptions
¬this is NOT a mental-model-onboarding problem (user is system creator; ¬conceptual explanation needed)
THIS IS a first-run-confidence problem: user needs to SEE system is wired correctly before first real review

DB[F-UX2]: assume-wrong(standard onboarding needed) → counter(user is creator ¬needs explanation) → revised: first-run=pre-flight confidence check ONLY ¬conceptual walkthrough |outcome-1-CHANGED-ANALYSIS
First-run experience: API connectivity check + agent spawn test BEFORE first real review; show component status (Anthropic API, sigma-mem, sigma-verify, agent definitions count); failures = specific actionable errors ¬generic "setup failed"
§2b:OUTCOME-2 — startup-validation = highest-DX-impact from prior reviews (review-7 setup.sh lifted DX A-→A); applies directly |source:[cross-agent]

---

#### F-UX3: Clone-and-run DX — Streamlit achieves ≤3-command target [H5 answer, Q2 contribution]
§2a:OUTCOME-2 — Streamlit-for-local-Python-tools confirmed by comparative evidence |source:[independent-research] |T2
§2c:OUTCOME-2 — Streamlit: pip install + 1 command; React: Node second stack + build step; complexity cost real for C2 constraint

Target DX: git clone → cp .env.example .env → python run.py (3 commands) — achievable with Streamlit ¬achievable with React without significant scaffolding
DB[F-UX3]: assume-wrong(Streamlit polling jank ruins concurrent UX) → counter(20 agents + 2-5s polling = visible refresh artifacts) → re-estimate(Streamlit correct at ≤10 concurrent; sigma-ui typical=5-8) → reconciled:Streamlit correct v1; re-evaluate-at->10-concurrent |outcome-2-confirmed-with-acknowledged-risk
§2b-RESOLVED via cross-agent: tech-architect confirms belief-state recomputes on convergence events ¬continuous; 1-3 events/agent/phase at 2-5s intervals → aggregate rate well below jank threshold → §2b risk CLOSED for v1 |source:[cross-agent]:T1

Clone-and-run DX checklist:
- .env.example: all required keys labeled+documented inline
- setup.sh OR make setup: idempotent+prereq-checks+colored-output (mirrors review-7 setup.sh DX-lever pattern)
- Port-conflict detection: specific error + suggested alt port ¬generic bind error
- Startup prereq check: Python version, API key presence, agent definitions exist
- ¬Node/npm dependency in v1

CROSS-AGENT CONVERGENCE: F-UX3 Streamlit direction confirms product-designer F1 (independent paths, same verdict) |source:[cross-agent]

---

#### F-UX4: Progressive disclosure for sigma-ui [Nielsen N1, N3, N6, N9]
§2a:OUTCOME-2 — five-element limit per view (Smashing Magazine 2025), collapsible sections confirmed |source:[independent-research] |T2

Always visible (primary — ≤5 elements, N1 visibility of system status):
1. Current task title (truncated) + round indicator (R1/R2/R3)
2. Agent status tiles — one per active agent (spawned/working/converged/failed)
3. Active gate if firing — labeled with required action; ¬hidden in menu (N9 recognition over recall)
4. Review health indicator (belief state: round-health or P(quality) bar, trend arrow)
5. Elapsed time

On-demand (user expands — N3 user control):
- Individual agent findings | Full workspace markdown | DA challenges + threaded responses (product-designer TensionPanel) | XVERIFY results per finding | Round-by-round history

¬surfaced unless explicitly requested:
- Raw spawn prompts | Memory read/write operations | Infrastructure pre-flight detail (only failures surface) | Token counts / API costs

§2c:OUTCOME-2 — collapsible-tile: low cost, high cognitive-load-reduction; cognitive-load-reduction>feature-completeness confirmed primary DX metric |source:[cross-agent]

---

#### H4 contribution: audit log as quality-preservation mechanism
Control-inversion fix preserves analytical quality IF orchestrator faithfully executes spawn protocols. NEW quality risk in dashboard architecture: orchestrator bugs (wrong sequencing, missing spawns) invisible without immutable audit log.
!Audit log = quality-preservation mechanism ¬nice-to-have; H4 confirmation is provisional without it |source:[agent-inference] |T3
§2a:OUTCOME-3 gap — ¬external evidence for this specific risk pattern; flagged for DA challenge

#### Cross-agent addendum (IE findings)

IE-F4 SSE event taxonomy (agent_start|agent_converge|phase_transition|gate_check|error) maps directly to F-UX4 always-visible elements: agent_start→spawned | agent_converge→converged | phase_transition→round-indicator | gate_check→active-gate-fires | error→FAILED-tile+TIER-3-banner |source:[cross-agent]:T1(IE-F4)

!NEW UX RISK from IE-F2 sync-client blocker: stalled event loop = SSE stream freezes = tiles show last-known state WITH NO STALENESS SIGNAL — Nielsen N1 failure (user ¬distinguish "working silently" from "frozen silently")
Mitigation: last-event timestamp on each agent tile; if no update >30s during active phase → STALE badge (amber); positive signal for valid silence, negative for stall |source:[cross-agent]:T1(IE-F2)+[independent-research]:T2(Smashing-Magazine-2025-data-freshness)
§2a:OUTCOME-1-ADDENDUM — CHANGES F-UX4: STALE added to tile status set (idle/spawned/working/converged/failed/STALE) + last-event timestamp always visible

#### DA R2 responses (ux-researcher)

DA[#1] COMPROMISE — G2 adaptive tier: TIER-2(PASS) / TIER-1(FAIL)
DA correct: "proceed to R3" is round-scoped; blanket TIER-1 violated my own tiering logic.
Revision: G2=TIER-2(modal) when PASS; G2=TIER-1(full-page) when FAIL ("Stop Review" = session-terminating).
PASS modal: [Proceed to R3] primary action. FAIL escalates: full-page with unresolved challenges + [Stop Review] destructive. Rubber-stamping on FAIL still mitigated via full-page; PASS gets correctly weighted modal.
REVISED F-UX1 G2: adaptive-tier |outcome-1-CHANGED-ANALYSIS |source:[DA-audit]

DA[#2] CONCEDE — H4 audit log finding → open question
Cannot produce domain-specific external evidence. T3 agent-inference on load-bearing H4(P=52%) is insufficient.
REVISED: downgraded to OQ-UX1 (implementation phase question, ¬architectural requirement from this review)
OQ-UX1 (plain): Does immutable audit logging specifically improve analytical quality in multi-agent orchestration, or does hard-observable guard enforcement provide equivalent quality assurance? |source:[DA-audit]

DA[#3] COMPROMISE — semantic silence: scope boundary clarified, orchestrator requirement flagged
DA correct: STALE handles structural event silence ¬semantic failures (malformed output, DA-challenges-ignored).
Compromise: UI surfaces guard-check outcomes (§2e outcome counts per expanded tile; belief-state bar = aggregate). Semantic failures defeating guards WILL surface as gate non-passage or low belief score. UI makes guard outcomes visible ¬performs semantic validation.
!gap: guard semantic coverage = orchestrator design requirement; UI cannot close independently |source:[DA-audit]+[agent-inference]
MAINTAINED with scope boundary: semantic-validation=orchestrator ¬UI |outcome-2-confirmed-with-scope

DA[#4] CONCEDE — pre-flight hard boundary added
IN: (1)Anthropic-API-key-valid+reachable (2)sigma-mem-MCP-responds (3)sigma-verify-MCP-responds+≥1-provider (4)agent-defs-dir-exists+≥1-.md (5)workspace-dir-writable
OUT: agent-def-content-validation | individual-agent-spawn-test(costs-tokens) | MCP-schema-validation | disk-space | network-speed | package-version-pinning | any-check-requiring-real-spawn
Principle: pre-flight checks ONLY what blocks G1 |outcome-1-CHANGED-ANALYSIS |source:[DA-audit]

DA[#5] COMPROMISE — two DX targets: first-run(≤4 commands) + per-review(1 command)
DA correct: dependency list exceeds 3 commands first-time.
FIRST-RUN: git-clone → cp-.env.example → ./setup.sh(pip+MCP+prereq) → python-run.py (#4 commands; still better than React ~7+Node)
PER-REVIEW (MCP already running): python run.py (#1 command)
REVISED F-UX3: "≤4 first-run; 1 per-review" replaces "3 from git-clone" |outcome-1-CHANGED-ANALYSIS |source:[DA-audit]

#### Hygiene summary (post-DA)
§2a:4/4 ✓ (+IE-addendum+DA-revisions) | §2b:4/4 ✓ | §2c:4/4 ✓ | §2e:H5-checked-VIABLE ✓ | §2d:all-findings-tagged ✓
XVERIFY: F-UX1 cross-verified → "3+1-conditional" ✓
cross-agent: F-UX3+PD-F1(Streamlit) ✓ | G1-G3+PD-F4 ✓ | IE-F4→F-UX4 ✓ | IE-F2→F-UX4(STALE) ✓
DA-R2: #1-COMPROMISE(G2-adaptive-tier) | #2-CONCEDE(H4→OQ-UX1) | #3-COMPROMISE(scope-boundary) | #4-CONCEDE(pre-flight-5-IN) | #5-COMPROMISE(first-run≤4,per-review=1)

### implementation-engineer

#### IE-F1 — Orchestrator reuse: HIGH (85% verbatim); AsyncRunner private-attr = avoid as trust anchor
|source:[independent-research]|T1 — confirms tech-architect H3, extends RISK-3

Code-read confirmed: `set_executor(fn)` is the sole integration point — executor becomes `async def call_anthropic_api(agent, task) -> output`. All phases/transitions/guards/checkpoint carry over verbatim.

Verbatim (~85%): all phase defs | all guards (exit_gate_passed, belief_above, round_limit, context_true, all_converged) | post-exit-gate phases | AgentSlot roster | checkpoint save/restore.
Adaptation (~15%): A1 — CLI → FastAPI routes (~100 LOC). A2 — gate_checks CLI → library functions (~20 LOC). A3 — context population: manually `advance --context '{"converged":true}'` → workspace-parser feed + user gate HTTP endpoint. A3 = substantive work.

§2b: `AsyncRunner._async_start()/_async_advance()` access private attrs directly (code-read, lines 97-99, 117-128). Confirms tech-architect RISK-3. Mitigation: `asyncio.to_thread(orch.start)` / `asyncio.to_thread(orch.advance)` — public API. ~2 LOC fix.
§2e: H3 CONFIRMED for orchestrator layer.

---

#### IE-F2 — sigma-mem/sigma-verify MCP: auto-injection LOST in API dispatch; sync-client BLOCKER confirmed
DB[IE-F2: MCP tools are unavailable to API-dispatched agents unless explicitly wired]:
(1) initial: `messages.create()` ¬auto-inject MCP tools; agents lose sigma-mem/sigma-verify unless orchestrator wires them
(2) assume-wrong: what if Anthropic SDK added implicit MCP support that I missed in code-read?
(3) strongest counter: SDK docs + mcp_server.py code-read both confirm MCP runs as separate stdio subprocess — no path for automatic injection into raw messages.create() calls; would require Anthropic to ship a fundamentally different integration layer
(4) reconciled: finding CONFIRMED — no revision. Sync-client blocker (lines 231-234, 365-376) is a separate independently-confirmed gap. Both claims survive assume-wrong.
XVERIFY[openai:gpt-5.4]: partial(high) — core confirmed (MCP NOT auto-injected); two-tier is valid but not uniquely required |source:[external-verification]|T1

Core: Claude Code auto-injects MCP schemas from ~/.claude/settings.json. `messages.create()` does NOT — only explicit `tools=[]` appear.

P1 — Orchestrator-mediated: call sigma-mem/sigma-verify handler functions via Python import. Results → context text in prompts. Simplest. Agents lose autonomous memory access mid-analysis.
  P1 scope (clarified via cross-agent): includes ALL pre-spawn context injection — agent memory boot-read (handle_get_agent_memory per agent → "## Your Memory" context block), workspace state, decisions, peer findings per round. Also includes post-response writes (handle_store_agent_memory after agent response — otherwise session findings ¬persist). handler already exists in sigma-mem handlers.py, ¬new code.
P2 — Tool-forwarding: `Registry.get_current_tool_schemas()` returns {name, description, input_schema} (code-read confirmed). Anthropic ToolParam needs exactly these 3 keys — key alignment is exact. ~20 LOC conversion. Intercept tool_use blocks in executor, route to handlers.
P3 — Hybrid (recommended for C5): P1 for session-level (agent memory boot-read, load decisions, store findings pre/post), P2 for in-analysis (search_memory, store_agent_memory mid-analysis, verify_finding). Best process fidelity.

!CONCRETE BLOCKER: sigma-verify `clients.py` uses SYNC `openai.OpenAI()` + `google.genai.Client()` (code-read: lines 231-234, 365-376). Blocking calls in async FastAPI event loop. Fix: `asyncio.to_thread()` per method OR migrate to `AsyncOpenAI()`. ~15 LOC/method. Required regardless of integration pattern.

LOC estimate revised (cross-agent): context-builder ~200 LOC (revised from ~150 — adds agent-memory injection + workspace-round-N injection per agent). ¬material change to complexity ranking.

§2b: P3 adds ~150 LOC to executor. Maintained: agent-level memory ops required for C5.

---

#### IE-F3 — SDK: Direct Anthropic Python SDK correct; Agent SDK + CLI shell-out DISQUALIFIED
Confirms tech-architect F1 with impl-specific evidence. Q1 answered. |source:[independent-research]|T1

Option A — `anthropic.messages.create()`: agent .md spawn prompts → `system=` param directly (code-read: already Anthropic-format markdown, zero translation). `AsyncAnthropic()` for async-native. Tool access gap mitigated by IE-F2 P2/P3. CORRECT CHOICE.
Option B — Agent SDK: HATEOAS dynamic tool list incompatible with static registration; outer loop conflicts with orchestrator-as-outer-loop (reintroduces control inversion, violates H2). DISQUALIFIED.
Option C — CLI shell-out: new OS process per agent, no asyncio composition, stdout parsing brittle, no tool_use access. DISQUALIFIED.

§2a: Agent SDK initially plausible — ruled out on tool model incompatibility (outcome 1, changes analysis).

---

#### IE-F4 — FastAPI + SSE: feasible; blocking-call discipline = primary implementation risk
|source:[independent-research]|T1

Concurrency: `asyncio.gather()` + `asyncio.Semaphore(5)`. Per-agent timeout already in `Orchestrator.run_agents_parallel(timeout=)`. Push SSE events: agent_start | agent_converge | phase_transition | gate_check | error.

!MANDATORY event-loop discipline: ALL sigma-verify sync client methods + file I/O MUST use `asyncio.to_thread()`. One blocking call stalls ALL concurrent dispatches (single-threaded event loop). Convention-only enforcement — no type-system guard. Missing one = undetectable stall under load.

§2b: uvicorn single-worker sufficient for C1+C4. Risk: enforcement is convention-only.
§2e: H5 CONFIRMED — FastAPI + uvicorn + requirements.txt = clone-and-run.

---

#### IE-F5 — Migration: strangler fig, 4 phases, zero current-system disruption
|source:[independent-research]|T1

Phase 1 — Library: import orchestrator-config.py as module (already works as library). sigma-ui + current system coexist.
Phase 2 — HTTP wrapper: FastAPI routes wrap cmd_* logic. CLI still works.
Phase 3 — UI + async executor: wire frontend to SSE + gate endpoints; sigma-mem P3 hybrid.
Phase 4 — Decommission: remove CLI entry points.

orchestrator-config.py IS the strangler fig target — wrapped not replaced. Current system never broken.

!ORDERED DEPENDENCY in option D (cross-agent flag, source:[cross-agent]|T1):
  write-side of memory lifecycle MUST ship before any agent completes a full review under option D.
  risk: if context-injection (read side) ships first ¬yet write side (handle_store_agent_memory post-response), agents accumulate findings in session context but ¬persist to memory files. Calibration degradation is SILENT — agents appear to work correctly but lose session learnings. No error raised.
  implementation rule: read-side (pre-spawn injection) + write-side (post-response store) are a single atomic unit — ¬ship one without the other. Phase 3 must implement both in same deployment.

#### IE — Analytical hygiene (pre-DA)
□ positioning |□ calibration |□ cost |□ premise |□ provenance — see DA responses below for revisions

#### IE — DA R2 responses

DA[#1] CONCEDE — IE-F4 blocking-client RECLASSIFIED OUTCOME-1 |source:[independent-research]|T1
Convention-only + silent total stall = design defect ¬acknowledged risk. Architectural fix: thin async adapter class wrapping all sigma-verify client methods (to_thread() explicit at boundary ¬scattered per-call). Enforcement: pytest-asyncio integration test with asyncio.wait_for(timeout=2s) on every sigma-verify path + ruff ASYNC100/ASYNC101 at lint time.

DA[#2] PARTIAL CONCEDE — "zero disruption" overstated for phase 3 |source:[independent-research]|T1
Phase 3 failure modes are bounded and recoverable: (a) crash → entry missing ¬corrupted (handlers.py appends ¬overwrites); (b) timeout → ERROR, write ¬attempted, consistent with current behavior; (c) store failure → WARN ¬FAIL, logged, analysis ¬blocked. Checkpoint save/restore covers crash recovery. Amended: phase 3 has bounded recoverable failure modes.

DA[#3] COMPROMISE — P1 for v1; P2 at evidence-based trigger ¬upfront |source:[cross-agent:RCA]|T1
DA correct that P3 conflicts with P(scope-creep)=30-35%. P3 IS necessary for C5 long-term. Resolution: P1 for v1 (~200 LOC), P2 upgrade staged at specific trigger (review shows quality gap attributable to P1 limitation). REVISED recommendation: P1 v1, P2 evidence-triggered.

DA[#4] CONCEDE — "85% reuse" framing misleads; risk concentrates in the 15% |source:[agent-inference]|T1
LOC reuse ≠ risk reuse. REVISED: orchestrator structure reuse HIGH (phases/guards/checkpoint verbatim); integration surface risk concentrates in the 15% novel work (context population, executor wiring, async adapter, MCP schema conversion). Risk map > reuse %.

DA[#5] OUTCOME-3 GAP — Agent SDK v0.1.48 ¬verified; disqualification provisional |source:[agent-inference]|gap→lead
Agent SDK not installed locally (confirmed). Disqualification was based on version I could analyze. DA claims v0.1.48 adds hooks, sessions, MCP integration — cannot confirm from code-read alone. §2a outcome 3 gap. Reclassified: not-confirmed-disqualified pending v0.1.48 API review. →lead for resolution.

#### IE — Analytical hygiene (revised post-DA)
□ positioning: A confirmed, B provisional-gap (DA[#5]), C disqualified |outcome:1+3(gap)
□ calibration: blocking-client OUTCOME-1 (revised from 2) per DA[#1] |outcome:1
□ cost: async adapter ~30 LOC added; P3 deferred to v2 per DA[#3] |outcome:2
□ premise: H3 confirmed-with-condition, H5 confirmed |outcome:2
□ provenance: all [independent-research] + code reads + T1; XVERIFY on IE-F2; DA[#5] gap flagged

### reference-class-analyst

#### DECOMPOSITION
SQ[1]: base-rate-local-agent-orchestration-UIs? |estimable:yes |method:analogue+base-rate |→ RCA
SQ[2]: framework-choice-success-correlation? |estimable:yes |method:analogue |→ RCA+PD
SQ[3]: wrap-vs-rewrite-success-rate? |estimable:yes |method:base-rate |→ RCA
SQ[4]: SDK-approach-precedent? |estimable:partially |method:analogue(limited-N) |→ RCA+TA
SQ[5]: failure-modes? |estimable:yes |method:pre-mortem+analogue |→ RCA

#### REFERENCE CLASSES

RC[agent-orchestration-UI]: N~8-12 |src:[independent-research] |confidence:M
  AutoGen Studio: MS, "research prototype ¬production", maint mode, "conversational chaos" |PARTIAL-FAIL
  CrewAI Studio: 44.3K stars, UI=secondary |ACTIVE-UI-SECONDARY
  LangSmith: observability ¬orchestration, 400 cos production |SUCCESS-DIFF-SCOPE
  OpenClaw Mission Control: OSS dashboard, 300-400K users |EARLY-TRACTION
  Laminar: local localhost:3000 |NICHE-SUCCESS
  Langfuse: OSS observability MIT |SUCCESS(monitoring¬orchestration)
  !pattern: MONITORING UIs succeed > ORCHESTRATION UIs |T2
  !base-rate: ~35-45%(monitoring) | ~20-30%(orchestration) | sigma-ui=orchestration

RC[software-rewrite-vs-wrap]: 30%-success(70%-fail) |N=large |T2(AmazingCTO+surveys) |confidence:H
  sigma-ui ¬full-rewrite: wrapping existing into UI
  REVISED: wrap → ~40-55% |src:[agent-inference]

RC[local-dev-tool]: local-single-user-Python |src:[independent-research] |confidence:M
  Gradio:SUCCESS | Streamlit:SUCCESS | Jupyter:SUCCESS
  !pattern: succeed when REAL pain + low barrier

RC[agentic-AI-deployment]: 40%-cancelled-by-2027 |T2(Deloitte) |confidence:H
  XVERIFY[openai:gpt-5.4]: PARTIAL — too broad. Enterprise ¬applicable to local. Ceiling ¬point-estimate. |src:[external-verification]

#### ANALOGUES

ANA[1]: AutoGen Studio |PARTIAL-FAIL — prototype, fragmentation, maint mode |sim:HIGH |diff:sigma-ui=single-user+existing SM |src:[independent-research]
ANA[2]: Gradio/Streamlit |SUCCESS — Python, low barrier |sim:MOD(local ¬orchestration) |src:[independent-research]
ANA[3]: LangSmith/Langfuse |SUCCESS — monitoring¬orchestration |sim:MOD(dashboard) |src:[independent-research]
ANA[4]: orchestrator-config.py |WORKS — phases+transitions+guards+belief+validation CLI |sim:V-HIGH |src:[prompt-claim+code-read]
ANA[5]: hateoas-agent Orchestrator |WORKS — v0.2 full stack |sim:V-HIGH |src:[prompt-claim+code-read]
ANA-PATTERN: 2/5=internal working(strongest) | 1/5=failure(AutoGen) | 2/5=monitoring>orchestration

#### CALIBRATED ESTIMATES

CAL[H3-reuse]: P=58%(DB-reconciled from 65%) |80%CI=[42%,72%] |90%CI=[32%,80%] |breaks-if:SDK dispatch ≠ CLI state mgmt |src:[agent-inference+code-read]
  §2b OUTCOME-2: components verified working+tested. FastAPI wrap=standard. Risk=dispatch integration.
  cross-agent: TA H3=CONFIRMED-PARTIAL aligns (¬100%, caveats real)

CAL[H4-quality]: P=52%(DB-reconciled from 55%) |80%CI=[35%,67%] |90%CI=[25%,75%] |breaks-if:API agents < CLI quality |src:[agent-inference]
  §2c OUTCOME-2: HARDEST hypothesis. Mechanical enforcement=upside offsetting infra loss.
  cross-agent: TA RISK-2(tool-access-loss) STRENGTHENS concern — quality depends on tool access

CAL[H5-clone-run]: P=75% |80%CI=[60%,87%] |90%CI=[50%,92%] |breaks-if:deps beyond Python |src:[agent-inference+analogue]
  §2b OUTCOME-2: Python local tools=HIGH clone-and-run. Risk=MCP dep hell.
  cross-agent: PD+UXR confirm Streamlit < React setup (CONVERGE)

CAL[overall]: P(core goals)=50% |80%CI=[32%,66%] |90%CI=[22%,75%]

#### DIALECTICAL BOOTSTRAPPING

DB[H3]: 65%→58% | assume-wrong:SDK dispatch ≠ CLI model, context ownership changes | strongest-counter:orchestrator-config.py CLI→FastAPI changes who-has-context/workspace/inboxes | reconciled:integration thicker than surface

DB[H4]: 55%→52% | assume-wrong:quality=Claude Code infra(TeamCreate/SendMessage) | strongest-counter:quality from PROTOCOL ¬infra, mechanical enforcement=improvement | reconciled:central risk, enforcement offsets

#### R1 DISCONFIRMATION DUTY

DISCONFIRM[approach]: FastAPI+SDK+custom-UI=MOST COMPLEX option. Simpler: (1) rich terminal UI, (2) Streamlit over orchestrator-CLI(days¬weeks), (3) Jupyter cells-as-phases |src:[independent-research+agent-inference] |severity:M

DISCONFIRM[alternative]: Streamlit wrapper around orchestrator-config.py | 80% goals, 20% time, Python-only |src:[independent-research]

DISCONFIRM[comparison]: proposed=max control BUT weeks+complexity | alt(Streamlit)=80% goals+days BUT ceiling | recommendation=flag-for-debate
  CONVERGENCE: PD F1 + UXR F-UX3 independently → Streamlit. Three agents, different methods, same conclusion. |src:[cross-agent]

#### PRE-MORTEM

PM[1]: SCOPE CREEP |P=30-35% |mitigation:hard scope freeze, MVP=status+gates+dispatch |src:[agent-inference+analogue(AutoGen)]
PM[2]: DISPATCH INTEGRATION |P=20-25% |mitigation:spike test single agent |src:[agent-inference]
  cross-agent: TA RISK-2(tool-access)+RISK-3(AsyncRunner) corroborate
PM[3]: QUALITY REGRESSION |P=15-20% |mitigation:parallel reviews first 2-3 |src:[agent-inference]
PM[4]: ABANDONED |P=10-15% |mitigation:UI FASTER than chat |src:[analogue]
PM[5]: DEPENDENCY CHURN |P=5-10% |mitigation:pin+abstract |src:[agent-inference]
PM-SUM: P(≥1)=55-65% | P(project-killing)=30-40%

#### OV-RECONCILIATION

inside-view: P~65-70% | outside-view: weighted P~40-50% | gap:15-25%
→ OUTSIDE MORE TRUSTWORTHY. Inside overweights "components exist", underweights integration+scope. NEW code >50% total.
XVERIFY[openai:gpt-5.4]: PARTIAL — agrees better odds than generic platform. ACCEPTED+refined.
RECONCILED: P(core goals)=50% |80%CI=[32%,66%]

#### HYGIENE
§2a:OUTCOME-2 — pain confirmed, alternatives in DISCONFIRM | §2b:OUTCOME-2 — grounded in RCs, limited N flagged | §2c:OUTCOME-1(REVISED) — integration >50% effort | §2e:OUTCOME-2 — H4=weakest

#### CROSS-AGENT ALIGNMENT
TA: H3=CONFIRMED-PARTIAL(aligns my 58%) | RISK-2+RISK-3(corroborate PM[2])
PD: F1 Streamlit(converges DISCONFIRM[alt]) | F4 tiered-gates(converges PM[1] scope control)
UXR: F-UX1 3+1 gates(aligns Q4) | F-UX3 Streamlit(three-way convergence) | F-UX2 audit-log-quality-mechanism(strengthens H4 concern)
H3:P=58% | H4:P=52% | H5:P=75%

#### DA R2 RESPONSES

DA[#1] OV GAP — REFERENCE CLASS WEIGHTING: CONCEDE(material)
  DA is correct: I did not show explicit weighting. The 50% is a judgment call dressed as calibration. Making weights explicit:
  RC[orchestration-UI] 20-30%: most analogous to sigma-ui's GOAL (control agents via UI). Weight: 35%
  RC[wrap-not-rewrite] 40-55%: most analogous to sigma-ui's METHOD (wrapping existing components). Weight: 30%
  RC[local-dev-tool] 55-65%: most analogous to sigma-ui's DEPLOYMENT (single-user, local, Python). Weight: 25%
  RC[agentic-AI-deployment] 40%: ceiling only, per XVERIFY. Weight: 10%
  Weighted: 0.35×25% + 0.30×47% + 0.25×60% + 0.10×40% = 8.75 + 14.1 + 15.0 + 4.0 = 41.9%
  REVISED: P(core goals) = 42% |80%CI=[28%,58%] — DOWN from 50%. DA is right that orchestration-UI (most analogous to goal) should get highest weight, and it has the lowest base rate.
  The inside-view gap is now 65-70% vs 42% = 23-28% gap — LARGER than I initially stated. This strengthens the outside-view-more-trustworthy conclusion. |source:[agent-inference+DA-challenge]

DA[#2] PM[1] SCOPE CREEP — TOO LOW: COMPROMISE
  DA's argument: 2/5 analogues show scope creep as primary failure + industry base rate >50%.
  My defense: sigma-ui has a NARROWER scope than AutoGen Studio (single-user ¬platform, no multi-tenant, no marketplace) and a DEFINED existing backend (orchestrator-config.py). AutoGen Studio scope-crept because it tried to become a platform. Sigma-ui's scope is bounded by "wrap what exists."
  HOWEVER: TA+IE both independently identified strangler-fig partial-stall (20 agents × per-agent migration = 20 scope decisions) as concrete mechanism. This is a SPECIFIC scope-creep vector that my 30-35% underweighted.
  REVISED: PM[1] P = 35-42% (up from 30-35%). The partial-migration stall mechanism adds ~5-7pp to generic scope-creep base rate because it's a CONCRETE additional vector, not just generic drift.
  Implication: PM-SUM P(project-killing) revised from 30-40% → 35-45%. |source:[agent-inference+cross-agent(TA+IE)]

DA[#3] DISCONFIRM[alternative] — 20% GAP UNSPECIFIED: CONCEDE(critical)
  DA is completely right. "80% of goals in 20% of time" is a marketing-quality claim without specifying the 20% gap. This is my most important finding and I left it vague. Specifying now:

  What Streamlit-wrapper ACHIEVES (the 80%):
  - Orchestrator outer loop (orchestrator-config.py called as library) ✓
  - Phase visualization (st.progress + phase labels) ✓
  - Agent status display (st.columns with status badges) ✓
  - User gates at transitions (st.dialog or page switch) ✓
  - Workspace display (st.markdown reading workspace.md) ✓
  - Belief state display (calling compute-belief CLI) ✓

  What Streamlit-wrapper MISSES (the 20%):
  - Agent dispatch via Anthropic SDK (Streamlit would still shell out to CLI/TeamCreate — control inversion NOT fully fixed)
  - Real-time SSE push updates (Streamlit polls ¬pushes — latency, not capability gap)
  - Independent agent-card fragment updates under high concurrency (PD flagged this, manageable at 2-5s poll)
  - Immutable audit log with externally-written provenance (possible but requires additional backend)

  CRITICAL FINDING: the FIRST item — agent dispatch — is a MUST-HAVE. If Streamlit wrapper still shells out to CLI/TeamCreate, control inversion is NOT fixed, which is the CORE PROBLEM sigma-ui solves. This means the 80/20 split is wrong: the missing 20% contains the #1 requirement.

  REVISED DISCONFIRM[alternative]: Streamlit-wrapper achieves 80% of UI goals but 0% of the control-inversion fix unless it ALSO implements SDK-based agent dispatch. Streamlit + SDK dispatch = viable v1 that achieves ~90% of goals. Streamlit + CLI shell-out = dashboard-only (monitoring ¬orchestration), ¬solving the core problem.

  This CHANGES the recommendation: the question is NOT "Streamlit vs FastAPI" but "Streamlit+SDK vs FastAPI+SDK" — both need the SDK dispatch layer. Framework choice is about UI ¬orchestration. |source:[agent-inference+DA-challenge]

DA[#4] H4 P=52% — COIN FLIP IMPLICATIONS: COMPROMISE
  DA's argument is logically valid: coin-flip odds on quality for a quality-focused system is concerning. But the framing "don't build" oversimplifies the decision.

  Defense: P(H4)=52% means quality preservation is uncertain, ¬that quality WILL degrade. The 80%CI=[35%,67%] means there's also a meaningful chance quality IMPROVES (mechanical enforcement > human compliance). The current system has its own quality risks (lead agent can skip gates, agents can game self-report, no immutable audit).

  HOWEVER: DA is right that P=52% DOES favor the simpler alternative IF the simpler alternative has fewer quality-degradation vectors. Per DA[#3], Streamlit+SDK has the same dispatch layer (same quality risk) but simpler UI (fewer integration points for bugs). So P(H4|Streamlit+SDK) > P(H4|FastAPI+custom-UI) because fewer moving parts.

  REVISED: H4 P=52% applies to the FULL proposed architecture. For Streamlit+SDK: H4 P ~57-62% (fewer integration-induced quality leaks). This is a genuine argument for the simpler path.
  Net: DA[#4] strengthens the case for Streamlit+SDK ¬"don't build." |source:[agent-inference+DA-challenge]

DA[#5] CAL[overall] FORMULA: CONCEDE(process)
  DA is correct: CAL[overall]=50% was a judgment call, not a derived estimate. H3×H4×H5 under independence = 0.58×0.52×0.75 = 22.6%. These are NOT independent (H3 failing makes H4 harder, as DA notes), so joint probability is even harder to compute cleanly.

  The 50% was anchored to OV-RECONCILIATION (weighted average of reference classes), not to H3×H4×H5. But I presented it alongside the per-hypothesis estimates without making the derivation clear. That's a process violation.

  REVISED approach: CAL[overall] should be stated as TWO numbers:
  (1) Reference-class-weighted: P = 42% (from DA[#1] revised weighting)
  (2) Hypothesis-conditional: P(H3∧H4∧H5) = ~25-30% (accounting for positive correlation — if H3 succeeds, H4 becomes easier)

  These bracket the estimate: 25-42%. Midpoint ~33%.

  REVISED: P(core goals) = 35% |80%CI=[22%,52%] — DOWN from 50%. This is a MATERIAL revision. The original 50% was overconfident due to imprecise methodology.

  Implication: at P=35%, the full architecture is a BELOW-AVERAGE bet. This further strengthens the case for Streamlit+SDK as v1 (higher P due to fewer moving parts) with option to graduate to FastAPI+custom-UI if v1 succeeds. |source:[agent-inference+DA-challenge]


### devils-advocate

#### R2 challenges delivered (25 total: 5 per agent)

XVERIFY-CHALLENGE[openai:gpt-5.4-pro] on Streamlit consensus: vulnerability=MEDIUM
  counter: "optimizes for prototype speed ¬operational correctness under concurrency"
  gaps: burst rate untested, race conditions between polls+user actions, no correctness benchmark
  evidence-needed: prototype under worst-case burst conditions |source:[external-verification]|T1

XVERIFY-CHALLENGE[openai:gpt-5.4-pro] on Agent SDK dismissal: vulnerability=MEDIUM
  counter: "disqualification premature unless SDK cannot be host-contained — hooks/sessions may enable outer-loop control"
  gaps: version data stale (0.x cited, actual=v0.1.48), no spike test, no API review of hooks/sessions
  evidence-needed: integration spike showing host-controlled execution feasibility |source:[external-verification]|T1

challenge-map:
  tech-architect: #1(guard-gaming-structural-is-gameable) #2(Agent-SDK-dismissal-outdated) #3(H2-weaker-than-presented) #4(tool-access=capability-regression-¬complexity) #5(F3-source-tag-miscategorized)
  product-designer: #1(Streamlit-burst-rate+GitHub-bugs) #2(rubber-stamping-no-empirical-evidence) #3(component-tree-process-coupled) #4(TensionPanel-130-items-overflow) #5(T1-tier-tag-wrong)
  ux-researcher: #1(G2-should-be-TIER-2-¬TIER-1) #2(H4-audit-log-T3-load-bearing-claim) #3(silence-semantic-failures) #4(pre-flight-scope-creep-vector) #5(3-command-DX-aspirational)
  implementation-engineer: #1(blocking-client=design-defect-¬acknowledged-risk) #2(option-D-atomic-failure-modes) #3(P3-hybrid-contradicts-scope-discipline) #4(85%-reuse-masks-risk-concentration) #5(Agent-SDK-stale-evidence)
  reference-class-analyst: #1(OV-reference-class-mixing-false-precision) #2(PM[1]-scope-creep-below-base-rate) #3(DISCONFIRM[alt]-80/20-gap-unquantified) #4(H4=52%-argues-against-building) #5(CAL[overall]-formula-opaque)

#### PROMPT AUDIT (§7d)
PROMPT-AUDIT: echo-count:2 |unverified-claims:2 |missed-claims:3 |methodology:confirmatory-with-partial-investigative

echo-detail:
  E1: "control inversion" — prompt framing adopted verbatim by all 5 agents. H1 marked ASSUMED with zero independent verification. No agent asked: does current system actually exhibit control inversion failures? How often? Concrete cost?
  E2: "orchestrator as outer loop" — prompt solution adopted as design direction by all agents. TA/IE investigated HOW to implement, ¬WHETHER it is right solution.

unverified-claims:
  U1: H1 (control inversion exists) — ASSUMED ¬tested. No agent verified whether agents actually DO skip/game, or how often.
  U2: "20+ agent definitions" need concurrent management — typical sigma-review uses 5-8 (per infrastructure: 5+DA=6). No agent questioned.

missed-implicit-claims:
  M1: Prompt implies current chat-based system is FAILING — no agent asked failure rate or concrete cost of status quo.
  M2: Prompt implies quality WILL be preserved (H4) — H4 at P=52% says otherwise.
  M3: Components listed as reusable (H3) — agents investigated from confirming stance. Only RCA DISCONFIRM questioned fundamental approach.

methodology: CONFIRMATORY with partial investigative elements. 4/5 agents researched HOW to build ¬WHETHER to build. Only RCA DISCONFIRM[alternative]+outside-view = genuine investigative signal. 3-agent Streamlit convergence may reflect shared framing bias (Python-only+clone-and-run constraints → Streamlit as obvious answer).
  ¬confirmatory-flag (below 30% threshold): unverified prompt-derived limited to H1+H4. Most research IS independent.

#### agent response evaluations (R2 complete — all 5 responded)

engagement-grades:
  tech-architect: A- |5/5 addressed |3 concessions, 2 genuine revisions (TIER-A/B/C observable model, Agent SDK upgrade) |no defensiveness
  product-designer: A- |5/5 addressed |4 compromises, 1 concede |burst mitigation + pagination = genuine structural revisions
  ux-researcher: A |5/5 addressed |2 outcome-1 changes (G2 adaptive tier, pre-flight IN/OUT boundary) |cleanest concessions
  implementation-engineer: A- |5/5 addressed |2 strong concedes with specific fixes (async adapter + risk-map framing) |honest OUTCOME-3 gap on Agent SDK
  reference-class-analyst: A |5/5 addressed |DA[#3] = highest-quality single response of R2 — quantifying the 80/20 gap revealed control-inversion fix is in the "missing 20%", reframing entire architecture question |P revised 50%→35% via explicit methodology

team-grade: A- (consistent with my calibration — DA pressure produces genuine revisions, no performative concessions detected)

challenge-hit-rate: 22/25 produced genuine engagement (concede or compromise with revision). 3 partial: TA[#2] defend-with-update, PD[#3] acknowledge-without-material-change, IE[#2] bounded-but-acknowledged. Hit rate = 88% — HIGH. Indicates R1 had genuine analytical gaps (confirmed by P revision from 50%→35% and framework reframing).

key-R2-analytical-shifts:
  1. RCA DA[#3]: "Streamlit+SDK vs FastAPI+SDK" reframing (both need dispatch layer — framework choice is about UI ¬orchestration). LOAD-BEARING.
  2. RCA DA[#1]+[#5]: P(core goals) = 35% (down from 50%). MATERIAL revision via explicit weighting methodology.
  3. TA DA[#1]: TIER-A/B/C observable model (gaming-resistant vs cost-raising vs gameable). STRUCTURAL improvement.
  4. TA DA[#3]: H2 downgraded to WEAKLY-TO-PARTIALLY CONFIRMED.
  5. IE DA[#1]: blocking-client reclassified from acknowledged-risk to design-defect. OUTCOME-1.
  6. IE DA[#3]: P3→P1 for v1 (scope discipline). ALIGNS with RCA.
  7. PD DA[#1]: Streamlit §2b PARTIALLY-REOPENED (burst rate). Stagger requirement added.
  8. UXR DA[#1]: G2 adaptive tier (PASS=modal, FAIL=full-page). STRUCTURAL.

untested-new-consensus:
  1. "Streamlit+SDK vs FastAPI+SDK" reframing — logically derived from specifying unspecified claim, ¬formed under herding pressure. Sound.
  2. P=35% — derived from explicit methodology correction, ¬team consensus. Sound.
  3. TIER-A observable model — refinement by single agent, ¬consensus. Does not require stress-test.
  → no untested NEW consensus formed under DA pressure. All R2 revisions are individual agent corrections, not team herding to new position.

unresolved-items:
  1. Agent SDK v0.1.48 status: IE flagged OUTCOME-3 gap (can't verify locally). TA upgraded to "viable alternative." →lead for resolution (spike test needed, not R2-blocking).
  2. H1 ASSUMED: control inversion premise was never independently verified. Prompt audit flags this. ¬R2-blocking (user is system creator, firsthand knowledge), but should be noted in synthesis.
  3. Burst-rate stagger feasibility: PD conditional on TA confirming orchestrator can enforce stagger. →lead for cross-agent resolution (not a disagreement, an unresolved dependency).

#### EXIT-GATE VERDICT

exit-gate: PASS |engagement:A-(team) |unresolved:[Agent-SDK-spike-test→lead,H1-assumed→synthesis-note,burst-stagger→cross-agent-resolution] |untested-consensus:none(all-R2-revisions=individual-corrections) |hygiene:pass |prompt-contamination:pass(below-30%-threshold,methodology-confirmatory-with-investigative-elements,¬echo-cluster) |cqot:pass(TA-falsifiability-on-H2-conditions,RCA-steelman-on-H4,confidence-gaps-explicit) |xverify:pass(2-XVERIFY-CHALLENGE-on-load-bearing-consensus+all-agents-have-XVERIFY-on-R1-findings)

rationale:
  - engagement ≥ B across all agents: YES (all A- or A)
  - no material disagreements unresolved: YES (Agent SDK is gap ¬disagreement; H1 is assumption ¬disagreement; burst-stagger is dependency ¬disagreement)
  - no untested new consensus: YES (R2 revisions are individual corrections)
  - hygiene substantive: YES (all 5 agents produced outcome-1 or outcome-2 with evidence on DA challenges)
  - prompt contamination within tolerance: YES (2 echoes, 2 unverified claims, 3 missed — below 30% threshold, no echo cluster)
  - CQoT: YES (TA provided falsifiability conditions on H2, RCA steelmanned H4 in DA[#4], confidence gaps explicit across all calibrated estimates)
  - XVERIFY: YES (2 DA challenge() calls on load-bearing consensus; all agents had R1 XVERIFY on at least 1 finding)

!NOTE-FOR-SYNTHESIS: P(core goals) revised DOWN to 35% (from 50%). This is a below-average bet. The R2 reframing (Streamlit+SDK as v1 with graduation path) is the key strategic insight. Synthesis should lead with this conditional architecture ¬the originally-proposed full architecture.

#### product-designer DA responses (R2)

**DA[#1] COMPROMISE — burst rate reopens §2b partially**
DA is correct that §2b was marked RESOLVED on average rate ¬burst rate. Phase-transition burst (5 agents converging within 2s = 5 events/2s = 2.5/sec aggregate) DOES exceed the >2/sec threshold I identified. The GitHub issues (session_state leak #12506, fragment-not-found #11660) are documented T2 evidence I did not account for.

REVISED F1: §2b flag status changed from RESOLVED → PARTIALLY-RESOLVED with explicit burst mitigation required.
- Average rate (0.5-2 events/min across active phase): RESOLVED — well below threshold
- Phase-transition burst (up to 20 events/30s ≈ 0.67/sec aggregate, peak 5 agents/2s = 2.5/sec): NOT RESOLVED — requires explicit mitigation

Burst mitigation (architectural requirement, not optional): orchestrator must stagger agent convergence writes with minimum 500ms interval between workspace state updates during phase transitions. This prevents simultaneous fragment poll collisions. If orchestrator cannot enforce staggering (parallel dispatch), React becomes correct.
¬full inversion to React: staggered convergence write is achievable at orchestrator level (tech-architect to confirm feasibility). If infeasible → React.

Fragment bug mitigation: st.fragment key= parameter must be stable across reruns (agent_id ¬positional index). Prevents fragment-not-found on page reload.

UPDATED F1 confidence: HIGH(conditional) → HIGH with two explicit burst+fragment-stability requirements.
|source:[cross-agent]:T2(DA-challenge) + [independent-research]:T2(GitHub-11660,GitHub-12506)|

**DA[#2] COMPROMISE — rubber-stamping claim weakened; gate design revised**
DA is correct that rubber-stamping concern rests on [agent-inference] ¬empirical evidence. The CI/CD analogy breaks on single-user/no-social-pressure axis. IDE modal dismissal IS a documented pattern — IntelliJ "Don't show again" checkboxes exist precisely because users dismiss routine modals.

I cannot defend the rubber-stamping claim at T1/T2 — it is agent-inference. The UXR cross-convergence does not add independent evidence (same inference, different agent).

REVISED F4: Full-page gate for TIER-1 is still correct, but the justification shifts:
- Original justification: full-page prevents rubber-stamping — WEAKENED (inference only)
- Surviving justification: full-page removes competing background context → forces intentional context-switch → correct for IRREVERSIBLE actions regardless of rubber-stamping risk
- Routine dismissal risk: addressed via gate content design, not modality. Mitigation: gate must show NEW information unavailable elsewhere (agents promoted, decisions locked, specific findings surfaced). If gate shows no new information → it WILL be rubber-stamped regardless of modality. Intent Preview (Smashing Magazine 2026) is therefore not just a design principle — it is the ONLY mechanism preventing routine dismissal.

UPDATED F4: Full-page TIER-1 justified on irreversibility grounds ¬rubber-stamping grounds. Intent Preview mandatory condition becomes load-bearing ¬advisory.
|source:[agent-inference] — rubber-stamping claim flagged as T3, justification shifted to stronger ground|

**DA[#3] COMPROMISE — extensibility gap acknowledged, MVP scope boundary hardens**
DA is correct that F3 component tree is process-coupled to current sigma-review shape. Dynamic agent count (20→23 mid-review), sigma-build mode differences, new gate types — none designed for.

This is a real architectural gap. However: the correct response is NOT to over-engineer extensibility into MVP, but to make the MVP scope boundary explicit so extensibility debt is visible.

REVISED F3 scope annotation:
- AgentGrid: agent count must be data-driven (¬hardcoded 20) — this is minimal: `st.session_state.agents` as list, grid renders len(agents) cards. Dynamic count costs ~0 extra complexity.
- sigma-build mode: different component set. Correct approach: page routing (sigma_review.py / sigma_build.py) with shared PhaseStrip + UserGate components. ¬try to make one component tree handle both modes.
- New gate types: UserGate renders from gate_config dict — tier, title, content, buttons. ¬hardcoded gate content. This is also ~0 extra complexity vs hardcoded.
- TensionPanel: no extensibility needed — threaded metaphor scales to any DA challenge content.

UPDATED F3: three low-cost extensibility fixes (data-driven agent count, page routing per mode, config-driven gate content) + explicit MVP boundary: ¬attempt cross-mode component unification in v1.
§2c: extensibility fixes add ~20 LOC ¬material complexity increase. Outcome 2 — CONFIRMS Streamlit direction with acknowledged extensibility debt for cross-mode unification.
|source:[agent-inference]|

**DA[#4] COMPROMISE — TensionPanel overflow is real; pagination required**
DA is correct: 26 DA challenges × 5 agent responses = 130+ items. This is a verified rendering concern — Streamlit expanders at 100+ items produce visible render lag (documented community reports T3, consistent with Streamlit's Python rerun model).

REVISED F5: TensionPanel threaded metaphor is correct but requires pagination/virtualization:
- Default view: show CURRENT round DA challenges only (¬all rounds). Round selector to navigate history.
- Per-round max display: 30 items (challenge + direct responses) before "show more" expansion.
- Collapsed by default: each DA challenge = collapsed expander, opens on click. 26 collapsed expanders = lightweight; 26 expanded = problematic.
- ¬attempt to render full 130-item tree in single Streamlit rerun — this WOULD cause jank.

This is also relevant to F1: TensionPanel with 130 items is a Streamlit performance risk independent of fragment polling. Mitigated by round-scoped display + collapsed default.

UPDATED F5: threaded metaphor valid + round-scoped pagination + collapsed-default required. ¬static full render.
|source:[agent-inference + independent-research]:T3(Streamlit-community-expander-performance)|

**DA[#5] CONCEDE — T1 tag corrected immediately**
DA is correct. Streamlit docs = T2 (company documentation) ¬T1. Process violation acknowledged.

Correction applied to F3 source tag below. The finding itself is unchanged — Streamlit docs are valid T2 evidence for implementation patterns. Only the tier tag was wrong.
|source:agent-inference + independent-research(docs.streamlit.io):T2| ← corrected from T1

## hypothesis-matrix
| H | Statement | TA | PD | UXR | IE | RCA | Status |
|---|---|---|---|---|---|---|---|
| H1 | Control inversion exists | premise | — | — | — | — | ASSUMED |
| H2 | Outer-loop fixes inversion | PARTIAL(+guards) | — | — | — | — | PARTIAL |
| H3 | Components reusable | CONFIRMED-PARTIAL | — | — | confirmed-w/condition | P=58% | PARTIAL |
| H4 | Quality preserved | RISK-2 threatens | — | provisional(audit-log,T3) | — | P=52% | UNCERTAIN |
| H5 | Clone-and-run viable | — | Streamlit confirms | CONFIRMED(≤3 cmds) | confirmed | P=75% | CONFIRMED |
| H6 | Observables > self-report | CONFIRMED(structural-parse) | — | — | — | — | CONFIRMED-COND |

## convergence
ux-researcher: ✓ R2-complete |DA-revisions:5(#1-COMPROMISE-G2-adaptive-tier,#2-CONCEDE-H4→OQ-UX1,#3-COMPROMISE-scope-boundary,#4-CONCEDE-pre-flight-5-IN,#5-COMPROMISE-first-run≤4-per-review=1) |key-changes: G2=TIER-2(PASS)/TIER-1(FAIL)|pre-flight-hard-boundary(5-IN,explicit-OUT)|DX-target-split(first-run≤4,per-review=1)|H4-downgraded-to-OQ-UX1|semantic-silence=orchestrator-not-UI |H5:CONFIRMED-VIABLE |→ exit-gate-assessment |#5-DA-responses
product-designer: ✓ R2-DA-complete |F1:Streamlit-HIGH-CONDITIONAL(§2b-PARTIALLY-REOPENED:burst-mitigation+fragment-stability-required,stagger-convergence-writes,key=agent_id) |F2:5-level-IA(CONFIRMED) |F3:component-tree+3-extensibility-fixes+T2-tag-corrected(DA[#5]) |F4:gate-justification-shifted(irreversibility¬rubber-stamping,Intent-Preview=load-bearing) |F5:TensionPanel+round-scoped-pagination+collapsed-default |DA[#1]:COMPROMISE(burst-reopens-§2b) |DA[#2]:COMPROMISE(justification-shifted) |DA[#3]:COMPROMISE(extensibility-fixes+scope-boundary) |DA[#4]:COMPROMISE(pagination-required) |DA[#5]:CONCEDE(T1→T2-corrected) |→ promotion-round #5
tech-architect: ✓ R1-complete |F1:Python-SDK>Agent-SDK>CLI-shell-out(!tool-access-gap) |F2:H2=PARTIALLY-CONFIRMED(outer-loop-dispatch+hard-observable-guards-together=fix,neither-alone)|XVERIFY[openai:gpt-5.4]:partial(confirms-residual-guard-gaming-risk) |F3:workspace-sufficient+buffer-merge-for-parallel-phases |F4:RISK-1(guard-gaming),RISK-2(tool-access-loss),RISK-3(AsyncRunner-elevated-to-HIGH) |H3:CONFIRMED-PARTIAL(all-components-reusable+caveats) |→ DA-challenge-F2-guard-gaming-residual+F4-RISK-2-tool-access-loss, impl-engineer-tool-access-migration #4
implementation-engineer: ✓ R2-DA-complete |DA[#1]:CONCEDE(blocking-client=OUTCOME-1-design-defect,fix=async-adapter-class+ruff-ASYNC100+pytest-asyncio-timeout) |DA[#2]:PARTIAL-CONCEDE(phase-3-failure-modes=bounded-recoverable,¬zero-disruption) |DA[#3]:COMPROMISE(P3-deferred,P1-for-v1,P2-evidence-triggered) |DA[#4]:CONCEDE(85%-reuse-framing-misleads,risk-concentrates-in-15%-integration-surface) |DA[#5]:OUTCOME-3-GAP(Agent-SDK-v0.1.48-¬verified,disqualification-provisional→lead) |key-changes:IE-F4-OUTCOME-1,P3→P1-v1,Agent-SDK-provisional,async-adapter-architectural-fix |H3:confirmed-with-condition |H5:confirmed |→ exit-gate-assessment #5

reference-class-analyst: ✓ R2-complete |DA-responses:5/5(2-concede,2-compromise,1-concede-critical) |REVISED:P(core-goals)=35%(down-from-50%,DA[#1]+DA[#5]-forced-explicit-weighting+methodology-correction) |H3=58%,H4=52%(57-62%-for-Streamlit+SDK),H5=75% |PM[1]=35-42%(up,strangler-stall-mechanism) |CRITICAL-REVISION(DA[#3]):Streamlit+CLI=monitoring-only(¬fixes-control-inversion),Streamlit+SDK=viable-v1(~90%-goals) |framework-question=Streamlit+SDK-vs-FastAPI+SDK(both-need-dispatch) |P=35%-full-arch-favors-Streamlit+SDK-v1-with-graduation |→ synthesis #7

devils-advocate: ✓ R2-complete |25-challenges-delivered(5×5-agents) |2-XVERIFY-CHALLENGE(Streamlit-consensus:MEDIUM,Agent-SDK-dismissal:MEDIUM) |prompt-audit:echo-2,unverified-2,missed-3,confirmatory-with-investigative |engagement:A-(team,TA:A-,PD:A-,UXR:A,IE:A-,RCA:A) |hit-rate:88%(22/25) |key-shifts:framework-reframing(Streamlit+SDK-vs-FastAPI+SDK),P=35%(down-from-50%),TIER-A/B/C-observables,H2-downgraded |exit-gate:PASS |→ promotion-round #10

## pre-synthesis-checks

CONTAMINATION-CHECK: session-topics-outside-scope: user career strategy, kaggle competition, other sigma-system projects, gate infrastructure project, dream consolidation | scan-result: clean (no out-of-scope topics found in agent workspace findings)

SOURCE-PROVENANCE[audit]: source distribution — [independent-research]=primary(all agents) | [prompt-claim]=2(H1,H3-component-list) | [cross-agent]=extensive-post-R1 | [external-verification]=XVERIFY-on-5-agents+2-DA-challenge() | [agent-inference]=RCA-calibrated-estimates,some-PD/UXR. T1/T2 coverage on load-bearing confirmed. §2d PASS — no tier violations, prompt-claim within tolerance.

SYCOPHANCY-CHECK: softened: none detected — DA drove P(core goals) from 50%→35%, H2 downgraded, multiple concessions | selective-emphasis: none — RCA DISCONFIRM[alternative] preserved and strengthened through R2 | dissent-reframed: none — framework reframing (Streamlit+SDK vs FastAPI+SDK) is genuinely new analytical insight ¬softened disagreement | process-issues: H1 assumed ¬tested (DA flagged, noted for synthesis)

## open-questions
OQ-1: Agent SDK v0.1.48 spike test needed — IE couldn't verify locally, TA upgraded to "viable alternative"
OQ-2: H1 (control inversion) was ASSUMED by all agents — synthesis should note this was not independently verified
OQ-3: Burst-rate stagger feasibility — PD F1 conditional on orchestrator convergence-write staggering

## promotion

### ux-researcher — promotion (26.3.28)

auto-stored (3): P[gate-heaviness-matches-reversibility|confirms-silent-failure-risk-pattern-extended-to-gate-design] + P[setup.sh-DX-lever-second-domain|confirms-review-7-calibration] + P[startup-validation-highest-DX-impact-second-domain|confirms-existing-C[]] → written to agents/ux-researcher/memory.md

P-candidate[orchestration-gate-adaptive-tier|class:new-principle|agent:ux-researcher|reason:generalizable-to-any-multi-agent-orchestration-UI-with-asymmetric-decision-branches]
Gate tier (modal vs full-page) should be adaptive based on the WORST-CASE branch of an asymmetric decision. A gate where one branch is round-scoped (TIER-2) and another is session-terminating (TIER-1) must escalate only on the irreversible branch. Fixed TIER-1 for all paths → gate fatigue → rubber-stamping on low-stakes paths. DA exit-gate: PASS=modal, FAIL=full-page (escalates). |src:sigma-ui|DA[#1]-COMPROMISE

P-candidate[silence-is-valid-plus-STALE-badge|class:new-principle|agent:ux-researcher|reason:generalizable-to-any-multi-agent-dashboard-where-agents-run-asynchronously]
In orchestration dashboards, productive silence is the correct steady state — opposite of chat where silence=nothing-happening. UI must: (1) make silence visually neutral ¬alarming, (2) distinguish valid silence from infrastructure failure via STALE badge (no event from active agent >2× polling interval → amber STALE + last-event timestamp). Without STALE, Nielsen N1 fails silently when event loop stalls — user cannot distinguish working-quietly from frozen. |src:sigma-ui|IE-cross-agent(N1-risk)

P-candidate[pre-flight-scope-discipline|class:anti-pattern-new|agent:ux-researcher|reason:generalizable-to-any-local-developer-tool-with-first-run-setup]
Pre-flight checks are additive by nature and scope-creep into slow/expensive operations without a hard boundary. Rule: pre-flight checks ONLY what blocks the first user action. Explicit OUT list is as important as IN list. Principle: if the check requires running real work → belongs in the workflow ¬pre-flight. sigma-ui IN=(API-key,MCP-responds,agent-defs-exist,workspace-writable); OUT=(agent-spawn-test,content-validation,disk-space,network-speed). |src:sigma-ui|DA[#4]-CONCEDE

### reference-class-analyst — promotion (26.3.28)

auto-stored (4): P[monitoring-UI>orchestration-UI-base-rate|new-pattern] + P[explicit-RC-weighting-mandatory|calibration-DA-forced] + P[strangler-fig-partial-stall|new-pattern-3-agent-convergence] + P[DISCONFIRM-percentage-claims-must-specify-gap|calibration-DA-forced] + C[CAL-overall-must-be-derived¬judgment|correction-DA-caught] → written to agents/reference-class-analyst/memory.md

P-candidate[wrap-not-rewrite-base-rate-distinction|class:new-principle|agent:reference-class-analyst|reason:generalizable-to-any-project-decision-between-building-new-vs-wrapping-existing-components]
Software rewrite base rate (30% success) does NOT apply when wrapping existing working components into new UI. Wrap-not-rewrite shifts base rate to ~40-55%. Key differentiator: wrapping preserves existing tested behavior and adds a layer; rewriting replaces tested behavior. The distinction matters for project feasibility assessment. Reference class selection must match the METHOD not just the GOAL. |src:sigma-ui-R1|XVERIFY:gpt-5.4-partial

P-candidate[three-agent-independent-convergence-as-corroboration-signal|class:new-principle|agent:reference-class-analyst|reason:generalizable-to-sigma-review-process-as-convergence-quality-metric]
When 3+ agents converge on the same conclusion via demonstrably different analytical methods (base-rate-analogue vs framework-analysis vs DX-constraint), this is stronger corroboration than N agents reaching same conclusion via similar methods. Method-diversity in convergence > agent-count in convergence. In sigma-ui review: PD(framework-eval) + UXR(DX-constraint) + RCA(base-rate-analogue) all independently → Streamlit. This is a quality signal for synthesis weighting. |src:sigma-ui-R1|cross-agent

P-candidate[chat-to-dashboard-mental-model-shift|class:new-principle|agent:ux-researcher|reason:generalizable-to-any-project-replacing-chat-with-orchestration-dashboard]
Chat→orchestration dashboard is a fundamental mental model shift: conversation-participant (sequential, user tracks state by reading) → control-room-operator (concurrent, system tracks state, user intervenes at gates). First-run should NOT explain this conceptually (system creators already know it) but should DEMONSTRATE wiring is correct via pre-flight confidence check. Onboarding-for-concept=waste; onboarding-for-wiring-confidence=value. |src:sigma-ui|DB[F-UX2]

### product-designer — promotion candidates (26.3.28)

auto-stored (3) → written to agents/product-designer/memory.md:
P[gate-UX:heaviness-matches-reversibility|irreversible→full-page|soft→modal|info→inline|DA-tested:confirmed-with-justification-shift|src:sigma-ui|26.3.28|class:pattern]
P[agentic-UI:framework-splits-on-viz-centrality|monitoring-grade→Streamlit|interactive-graph→React|DA-tested:confirmed-conditional|src:sigma-ui|26.3.28|class:pattern]
P[concurrent-agent-status:fragment-per-card-mandatory|polling-2-5s|burst-mitigation-also-required|src:sigma-ui|26.3.28|class:calibration]

P-candidate[average-rate-not-sufficient-for-Streamlit-viability|class:new-principle|agent:product-designer|reason:generalizable-to-any-multi-agent-Streamlit-dashboard]
Streamlit viability under concurrent agent updates requires analysis of BURST rate not just average rate. Phase-transition bursts (multiple agents converging simultaneously) can exceed fragment tolerance even when average rate is within threshold. Mitigation: orchestrator stagger convergence writes (500ms min) + st.fragment key=stable_id not positional. Average-rate-only analysis is a systematic framework-selection gap. |src:sigma-ui-R2(DA[#1])|T2

P-candidate[Intent-Preview-load-bearing-not-advisory|class:new-principle|agent:product-designer|reason:applies-to-any-agentic-UI-with-irreversible-gates]
For irreversible phase-transition gates, showing consequences before confirm button is active is the ONLY mechanism preventing routine dismissal by habituated users. Full-page modality removes competing context; Intent Preview removes the content vacuum that enables rubber-stamping. Both required; neither alone sufficient. Gate without Intent Preview degrades to confirmation theater. |src:sigma-ui-R2(DA[#2]+Smashing-Magazine-2026)|T2

P-candidate[tension-panel-volume-requires-pagination|class:anti-pattern-new|agent:product-designer|reason:any-adversarial-review-UI-faces-same-volume-issue]
Threaded adversarial challenge visualization overflows Streamlit at realistic volumes (26 challenges x 5 responses = 130 items). Required pattern: round-scoped display + collapsed-default expanders + show-more threshold at ~30 items. Full-render of all rounds in single Streamlit rerun is a performance anti-pattern regardless of fragment usage. |src:sigma-ui-R2(DA[#4])|T3(community-reports)

P-candidate[multi-agent-convergence-is-herding-signal|class:new-principle|agent:product-designer|reason:applies-to-any-multi-agent-analytical-review]
3+ agents independently reaching the same conclusion on a contested question is a herding signal requiring DA challenge, not confirmation. Convergence via different methods on same conclusion strengthens the finding; convergence via shared framing or shared source does not. The convergence itself is the reason to challenge. |src:sigma-ui-R2(DA[#1]-herding-challenge)|T2

### implementation-engineer — promotion candidates (26.3.28)

auto-stored (3): P[AsyncRunner-private-attr-coupling], P[Agent-SDK-version-check-before-dismissal], P[convention-only-async=design-defect] → written to agents/implementation-engineer/memory.md

P-candidate[MCP-auto-injection-lost-in-API-dispatch|class:anti-pattern-new|agent:implementation-engineer|reason:generalizable-to-any-project-using-Claude-Code-MCP-tools-then-migrating-to-direct-API-dispatch]
Claude Code auto-injects MCP tool schemas from ~/.claude/settings.json; direct anthropic.messages.create() does NOT. Any project relying on sigma-mem/sigma-verify MCP tools in Claude Code context that migrates to SDK-based agent dispatch silently loses all MCP tool access. Mitigation: P1(orchestrator-mediated Python import), P2(tool-forwarding via Registry.get_current_tool_schemas()), P3(hybrid). Registry key alignment exact match to Anthropic ToolParam. |src:sigma-ui(code-read)|T1

P-candidate[memory-lifecycle-ordered-dependency|class:new-principle|agent:implementation-engineer|reason:applies-to-any-system-with-two-phase-read-write-memory-lifecycle]
When orchestrator owns agent memory lifecycle (pre-spawn read + post-response write), both ends must ship as atomic unit. Read-side (pre-spawn inject) shipping without write-side (post-response store) = agents appear to work correctly but session findings never persist. Silent calibration degradation, no error raised. Pattern: treat read-side + write-side of any two-phase memory lifecycle as a single deployment unit. |src:sigma-ui(cross-agent)|T1

### synthesis-agent — promotion classification (26.3.28)

#### auto-promote (calibration confirms + pattern confirms)

AP[1|pattern-confirm]: monitoring-UIs-succeed>orchestration-UIs — RC[agent-orchestration-UI] base rates: ~35-45%(monitoring) vs ~20-30%(orchestration). Confirms existing pattern from LangSmith/Langfuse/AutoGen analogues. ¬novel.

AP[2|pattern-confirm]: DA-pressure-produces-material-revisions — 88% hit rate (22/25), P revised 50%→35%, framework question reframed. Confirms existing team pattern (2-agent teams herd faster, dual DA rounds catch different issues).

AP[3|pattern-confirm]: outside-view-more-trustworthy-than-inside-view — inside-view 65-70% vs outside-view 42%, gap=23-28%. Inside overweights "components exist," underweights integration+scope. Confirms established RCA calibration pattern.

AP[4|calibration-confirm]: confirmatory-methodology-detection-via-prompt-audit — 4/5 agents researched HOW ¬WHETHER. Only RCA DISCONFIRM provided investigative signal. DA prompt audit correctly identified. Confirms §7d audit value.

AP[5|calibration-confirm]: H4-quality-preservation-is-hardest-hypothesis — P=52% (coin flip) on the most important property. Consistent with cognitive-enhancement-meta review finding that enhanced-single-instance can outperform multi-agent when quality is central.

#### user-approve candidates (novel findings for global memory)

P-candidate[TIER-A/B/C-observable-model|class:new-framework|agent:tech-architect+DA|reason:generalizable-guard-design-pattern-for-any-multi-agent-system-where-agents-write-to-shared-state]
Three-tier model for observable reliability in multi-agent orchestration: TIER-A (gaming-resistant) = orchestrator-produced observables (XVERIFY results, round counts, mandatory section counts enforced by spec) — agent cannot forge. TIER-B (cost-raising) = structural parsing (syntax regex, section presence, tag format) — raises gaming cost ¬eliminates it. TIER-C (gameable) = agent-asserted values (prose confidence %, "PASS" strings, context_true() keys). Gate enforcement MUST prioritize TIER-A; TIER-B is defense-in-depth only. |src:sigma-ui(TA-DA[#1]-revision)|T2

P-candidate[framework-choice-is-UI-not-orchestration|class:reframing|agent:RCA+DA|reason:applies-to-any-agent-UI-project-conflating-frontend-choice-with-backend-architecture]
When building an agent orchestration UI, the dispatch mechanism (SDK, CLI, Agent SDK) is independent of the UI framework (Streamlit, React, FastAPI). Both paths require the same backend dispatch layer. Framework choice determines UI capability ceiling ¬orchestration capability. Conflating "Streamlit vs React" with "monitoring vs orchestration" is a category error — SDK dispatch can sit behind either. |src:sigma-ui(RCA-DA[#3]-reframing)|T2

P-candidate[85%-reuse-framing-misleads|class:anti-pattern-new|agent:DA+IE|reason:generalizable-to-any-wrap-not-rewrite-project-citing-high-reuse-percentages]
High reuse percentage (e.g., "85% verbatim reuse") misleads when risk concentrates in the remaining integration surface. LOC reuse ≠ risk reuse. The 15% novel work (context population, executor wiring, async adapters, schema conversion) carries >80% of implementation risk. Correct framing: reuse map (what carries over) + risk map (where integration complexity concentrates). The risk map determines project difficulty, not the reuse percentage. |src:sigma-ui(DA[#4]+IE-response)|T1

P-candidate[H2-requires-two-ANDed-conditions|class:new-principle|agent:tech-architect+DA|reason:generalizable-to-any-system-claiming-outer-loop-fixes-agent-control]
Outer-loop dispatch alone does NOT fix agent control inversion. Two ANDed conditions required: (1) outer loop controls WHO calls advance (dispatch control), AND (2) guards evaluate orchestrator-produced observables ¬agent-written content (evaluation control). Condition 1 without condition 2 = agents write well-formed compliance artifacts without doing the work. Neither condition alone sufficient. |src:sigma-ui(TA-DB[H2]+DA[#3])|T2

P-candidate[scope-creep-strangler-fig-vector|class:new-risk-pattern|agent:RCA+DA|reason:generalizable-to-any-strangler-fig-migration-with-per-component-migration-decisions]
Strangler-fig migrations with N components create N individual scope decisions (migrate now vs later per component). Each decision is a scope-creep entry point. With 20 agents × per-agent migration, the partial-stall mechanism adds ~5-7pp to generic scope-creep base rate. This is a CONCRETE vector beyond generic drift — the migration structure itself generates scope pressure. Mitigation: hard scope freeze on v1 component set, defer per-component decisions to evidence-triggered v2. |src:sigma-ui(RCA-DA[#2]+TA+IE-cross-agent)|T2

### devils-advocate — promotion candidates (26.3.28)

#### auto-promote (calibration + pattern-confirms — 4 items)

C[architecture-review-88%-hit-rate|25 challenges, 22 genuine engagement=88%|R1 had real analytical gaps: P revised 50%→35%, framework reframed|confirms: 80%+ hit rate = R1 quality signal|src:sigma-ui-architecture|26.3.28]

C[XVERIFY-CHALLENGE-on-multi-agent-convergence|challenge() on 3-agent Streamlit convergence=MEDIUM vulnerability|challenge() on Agent SDK dismissal=MEDIUM|extends technique: run challenge() specifically on multi-agent convergence — herding most dangerous when it looks like independent convergence|src:sigma-ui-architecture|26.3.28]

C[forced-gap-specification-produces-inversions|RCA "80% goals" → DA forced gap enumeration → #1 requirement was IN the gap → reframed entire architecture question|technique: when agent claims X% coverage, force enumeration of gap — load-bearing requirements hide in unspecified remainders|src:sigma-ui-architecture|26.3.28]

review-history: | 26.3.28 | sigma-ui-architecture | 25 | 88%(A-team) | PASS | framework-reframing(Streamlit+SDK), P=35%(down-from-50%), TIER-A/B/C-observables, H2-downgraded, forced-gap-specification |

#### user-approve candidates (2 novel findings)

P-candidate[80/20-vague-claim-detection|class:new-principle|agent:devils-advocate|reason:generalizable-detection-for-any-analysis-claiming-partial-coverage-without-specifying-gap]
When an agent claims "X achieves 80% of goals" without specifying the remaining 20%, the unspecified gap often contains load-bearing requirements. In sigma-ui: "20% gap" contained #1 requirement (control-inversion fix via SDK dispatch). Pattern generalizes: vague coverage claims hide hardest problems in unspecified remainder. Mandate: any partial-coverage claim must enumerate what is excluded, or it is analytically incomplete. |src:sigma-ui(DA[#3]→RCA-critical-concession)|T2

P-candidate[convergence-in-constrained-design-space=expected-¬evidence|class:new-principle|agent:devils-advocate|reason:generalizable-to-reviews-where-constraints-narrow-solution-space]
When prompt constraints narrow solution space (Python-only + clone-and-run + local → Streamlit obvious), multi-agent convergence on that answer is EXPECTED ¬evidence of analytical quality. Detection: "given these constraints, would ANY agent have recommended differently?" If constraints make convergence inevitable, convergence carries zero informational value about correctness. The constraint set itself should be challenged. |src:sigma-ui(prompt-audit+3-agent-Streamlit-convergence)|T2


### tech-architect — promotion candidates (26.3.28)

auto-stored (3) — written to local memory file:
  C[outer-loop-dispatch-necessary-¬sufficient-for-control-inversion-fix|1|26.3.28]
  C[AsyncRunner-review-8-F1-severity-MEDIUM→HIGH-when-gate-enforcement-trust-anchor|1|26.3.28]
  C[CLI-shell-out→API-dispatch-tool-access-gap-primary-impl-complexity-driver|1|26.3.28]

¬duplicate (already captured in workspace with TA attribution):
  TIER-A/B/C observable model (tech-architect+DA) ✓
  H2-two-ANDed-conditions (tech-architect+DA) ✓
  scope-creep-strangler-fig-vector (RCA+DA+TA) ✓

P-candidate[agent-sdk-hook-abstraction-mismatch|class:anti-pattern-new|agent:tech-architect|reason:generalizable-to-any-orchestration-system-choosing-between-sdk-hooks-and-direct-dispatch]
Agent SDK hooks (v0.1.48) fire at tool-call level. Phase-gated orchestration systems need control at phase-transition level (guard evaluation, which agents run, advance() logic). These are different abstraction levels — hooks are correct for tool-call audit/transform/block; wrong for enforcing phase-gate semantics. Conflating them produces a system where individual tool calls are controlled but phase transitions remain unguarded. Direct API dispatch with explicit guard code (Orchestrator.advance()) is the correct model for phase-gated orchestration. DA-tested: DA[#2] confirmed Agent SDK matured but hook-level vs phase-transition-level distinction held. |src:sigma-ui(TA-DA[#2])|T2
